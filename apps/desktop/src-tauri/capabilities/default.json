{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window baseline. start-dragging is NOT part of core:default — without it every data-tauri-drag-region (overlay titlebar strips) is silently denied and the window cannot be moved. set-webview-zoom backs the Ctrl/Cmd +/- zoom hotkeys (zoomHotkeysEnabled) — without it the injected zoom polyfill is silently denied.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:webview:allow-set-webview-zoom",
    "clipboard-manager:allow-write-text"
  ]
}
