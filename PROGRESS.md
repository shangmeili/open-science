# Progress

2026-08-01 · fix(permission): completed the product-owner-confirmed A1 contract for “始终允许” without changing HEOR formulas, prompts, research schemas, provider protocols or research files. The pinned OpenCode sidecar now persists only the current project, action and exact requested resources in local SQLite, reloads them after a full app restart, preserves explicit configured deny/allow precedence, migrates permission rows when a project ID changes, and exposes current-project rules for visible revocation in Settings > Privacy. The desktop now initializes each task Git repository synchronously before switching OpenCode into it and keeps a restored task composer disabled until the session and runtime workspace agree, eliminating the parent-repository identity race found by native E2E. Test-first evidence covers same-project restart reuse, different resource/project isolation, revoke-and-reprompt, project-ID migration, one-time allow and rejection. Verification passed 112/112 pinned OpenCode tests plus typecheck, 114 frontend files/775 tests, 377 Rust tests with one established network ignore plus 2/2 resource-staging tests, 188/188 deterministic HEOR tests, patch 4/4, desktop E2E contract 11/11, packaged fixture 6/6, macOS distribution contract 16/16, typecheck, lint, rustfmt, production build, the 41-source/445-file preflight, and the real macOS Tauri/WKWebView restart/revoke flow. No installer was generated.

2026-07-31 · test(desktop): extended the test-only macOS native Tauri/WKWebView path to cover one real model-backed standalone task after creating a project, without changing product behavior, UI, scientific computation, provider adapters, research data or installed applications. A hermetic loopback fixture and isolated app-private XDG profile provide the local model without network access or real credentials; when a first directory instance still has no default model, the driver uses the public Choose model flow, selects the fixture on the Models page and returns to the app. It creates a project through the UI, returns to global New task, relocates and verifies the current controlled composer before sending, receives the deterministic reply, and proves the task workspace is a direct `session` child of the base folder, the named project is a distinct `heor` scope, and the sidebar task row is outside every project container. Red runs identified missing local-provider setup, an over-strict assertion that counted title/auxiliary calls as duplicate main replies, first-instance model configuration timing, and a WebView element/input-state race; a root-command rebuild reproduced the timing issue even after an earlier 3/3, so it was not accepted until explicit UI model selection and current-node verification were in place. Focused contracts pass 10/10, direct native execution passes 3/3 consecutively, and the root `pnpm test:e2e:desktop` rebuild passes end to end. Full verification passes 770/770 frontend, 188/188 HEOR, 377 Rust with one established ignore plus 2/2 resource-staging tests, 390/390 development contracts, 47/47 release contracts, typecheck, lint, Rust formatting, diff checks and the 41-source/445-file preflight. Six stale temporary roots from earlier failed runs were removed after confirming no owning process; stabilized runs leave none. This remains debug-feature evidence, not installed-DMG evidence; no installer was generated.

2026-07-31 · fix(test): closed P1-TEST-002 by making Tauri's admitted Open Science Skill staging deterministic without changing product behavior, UI, model calls, scientific computation, research data or installed applications. Reproduction proved Tauri's resource copier overwrote source files but retained an ignored `__pycache__/*.pyc` in a reused debug profile; the runtime correctly rejected the mismatched tree, while the native E2E incorrectly reported success because it did not inspect deployment logs. The build path now signals one bounded Cargo rebuild, removes only the validated profile's regenerable `skills-admitted-ai4s` staging tree and lets the existing Tauri copy restore it; the native E2E fails closed on any admitted-asset deployment error. A first implementation that watched its own output directory was rejected after consecutive verbose Cargo runs showed a perpetual Dirty rebuild; the final trigger remains Fresh on the second ordinary Cargo run. The polluted native E2E passed after automatic cleanup, source and staging hashes matched exactly at `db4d137dd69ec7295aa6517238ae1f6817abc051395d0708d5283c687a1d5bb4`, and no Python cache remained. Full verification passed 770/770 frontend tests, 188/188 deterministic HEOR tests, 377 Rust tests with one established ignored test plus 2/2 staging integration tests, 388/388 development contracts, 47/47 release contracts, typecheck, lint, Rust formatting and the 41-source/445-file resource preflight. No installer was generated.

2026-07-31 · test(desktop): extended the approved test-only macOS native WebDriver path to cover passive HTML preview in a real Tauri/WKWebView without changing product behavior, UI, model calls, scientific computation or research data. Red contracts first proved the native driver lacked an HTML fixture, stable file-element handling and a no-egress request observer. The isolated driver now opens Task files, waits for the actual DOM node to stabilize, clicks the local HTML file once, verifies the real iframe has an empty sandbox, checks the loopback response for the passive-document CSP and no-referrer policy, observes an actual iframe load and fails if the untrusted document requests its external test script. Direct native execution passed three consecutive runs and `pnpm test:e2e:desktop` passed end to end. Full verification passed 770/770 frontend tests, 377 Rust tests with one established ignored test, 188/188 HEOR tests, 386/386 development contracts, 46/46 release contracts, typecheck, lint, Rust formatting, the 41-source/445-file resource preflight and focused 10/10 frontend plus 7/7 Rust preview tests. A separate P1 was recorded after a reused debug resource copy retained generated Python bytecode and failed its exact tree hash; source, installed app and current x64 release resources were correct, the generated cache was moved out to restore this test run, and a proper build-cleanliness/log gate remains a separate loop. No installer was generated.

2026-07-31 · audit(ai): completed the no-code P1-AI-001b2 feasibility investigation without claiming an approximate context hash as exact. OpenCode 1.17.13 exposes the final post-plugin system blocks but not the assistant message or provider-call id in the same hook; title, summary and main calls can overlap within one session, while post-completion file hashing cannot recover remote instructions or bytes changed during the call. Queue, timestamp, latest-message and post-hoc manifest schemes are therefore explicitly rejected as exact audit evidence. The recommended implementation is a reviewed minimal patch to the pinned OpenCode sidecar that persists a content-free SHA-256 of the final system blocks directly on the matching assistant message before the provider request, followed by app ledger and release-provenance integration. Because this changes the bundled OpenCode binary/schema and stores a local derivative of project instructions, implementation remains pending explicit product-owner compatibility and privacy approval. No business code, binary, ledger schema, research data or installer changed; the repository remains on baseline `ai4heor-baseline-2026-07-31`.

2026-07-31 · fix(harness): closed P0-AI-002 so an imported or user-authored project `AGENTS.md` can no longer make the app-owned AI4HEOR product Harness disappear from the OpenCode instruction set. Existing project files remain byte-identical and compatible project instructions still load; on every sidecar start the app now fail-closed validates the packaged Harness tree, exact path-and-content-bound SHA-256 and machine policy, preserves any valid inherited ephemeral OpenCode config and custom instructions, de-duplicates and appends the verified absolute product `AGENTS.md` path, and passes the merged config only to that process without modifying persistent config. Invalid inherited JSON/object/instruction contracts, missing or drifted Harness resources and links fail visibly. Tests were red first with five Rust compile errors and five Python contract failures; a subsequent coverage review added a red non-policy-content tamper test before the complete tree hash. Focused verification now passes 9/9 OpenCode-config, 7/7 native Harness and 16/16 cross-resource Harness tests. Full verification passes all 768 frontend tests, 376 Rust tests with one established public-network test ignored, 376/376 development contracts, 188/188 deterministic HEOR tests, 44/44 release contracts, typecheck, lint, Rust formatting, production build and the 40-source/441-file resource preflight. No HEOR formula, research artifact, model request/provider, user project instruction, persistent setting or installer changed. Recording the exact combined instruction set used by each model call remains P1-AI-001b2; no installer was generated.

2026-07-31 · fix(audit): bound new HEOR model-call ledger entries to the exact app-owned workbench preamble revision without recording researcher text, full prompts, prompt hashes derived from researcher content, responses, credentials or URLs. The runtime now records the fixed template id, the exact preamble SHA-256 and one of the seven supported response languages; live completion events and history replay recover the same context, while commands and ordinary non-HEOR text remain unlabelled. The Rust ledger accepts only complete bounded optional context, rejects partial or uppercase hashes, preserves old entries and never rewrites append-only history to retroactively enrich an older record. Tests were red before implementation at the prompt parser, TypeScript bridge and strict Rust schema boundaries. Verification now passes 33/33 focused prompt/ledger tests, 87/87 runtime-store tests, 4/4 focused Rust ledger tests, all 768 frontend tests across 113 files, 372 Rust tests with one established public-network test ignored, 188/188 deterministic HEOR tests, all 44 release tests, typecheck, lint, Rust formatting, production build and the 40-source/441-file resource preflight. Actual workspace Harness fingerprinting, call-to-artifact links and a researcher-facing audit view remain separate P1 work. No installer was generated.

2026-07-31 · fix(audit): retained completed OpenCode model-call usage metadata that AI4HEOR previously discarded, without recording prompt/response content or changing model requests and scientific computation. The SDK now emits and reloads only complete, valid provider/model, parent-message, agent, timing, token/cache-token, finish and runtime-reported-cost fields. The desktop writes the same explicit whitelist from live SSE and task-history replay into an idempotent, append-only, size-bounded SHA-256 hash-chain ledger at `.openscience/model-calls.jsonl`; conflicting duplicates, negative/non-finite cost, invalid timing, corrupt history and symlinked stores fail closed. Targeted tests were red before implementation and now pass 20/20 frontend plus 3/3 Rust; full verification passed 764/764 frontend tests across 113 files, 371 Rust tests with one established public-network test ignored, all 44 release tests, typecheck, lint, Rust formatting, production build and the 40-source/441-file resource preflight. Prompt/Harness versioning, explicit model-call-to-artifact links and a researcher-facing ledger view remain separate P1 work. No installer was generated.

2026-07-31 · fix(release): added a fail-closed installed-frontend bootstrap gate without changing application business logic, model calls, scientific computation or research data. The previous native package checks proved the app process, authenticated OpenCode HTTP readiness and workspace creation, but could still pass if the WebView never mounted AppShell or completed the Tauri runtime IPC call. macOS and Windows verifiers now require fresh app-private diagnostic records for bootstrap start and a valid loopback runtime endpoint, while release evidence stores only three booleans (`app_shell_mounted`, `javascript_executed`, `tauri_runtime_command_returned`) and never stores log text, ports or local paths. Four focused tests first failed with three assertions and one missing-helper error, then passed after the minimal change; all 44 release tests, all 762 frontend tests across 112 files, TypeScript typecheck, ESLint, production build and the 40-source/441-file resource preflight pass. A current DMG was not built, and PowerShell is unavailable locally, so native macOS package proof and Windows runner execution remain packaging-stage gates. This closes only the frontend-bootstrap sub-gap; visible task, permission, preview and import/export E2E remain open.

2026-07-31 · fix(legal): repaired the packaged npm license inventory drift left by the prior same-major `brace-expansion` security update. The production inventory had remained bound to the superseded lockfile hash and listed 1.1.15/2.1.1 even though the committed lockfile resolves 1.1.16/2.1.2. The repository generator rebuilt the path-free inventory from current locked local state; the Cargo component set and hash remained unchanged apart from the common generation date. Verification passed all 10 Tauri HEOR resource contracts, all 3 JavaScript security policy tests, the 40-source/441-file release preflight and diff checks. No business code, dependency lockfile, research data or installer changed.

2026-07-31 · feat(heor): implemented the product-owner-confirmed first deterministic short-horizon decision-tree kernel without changing the existing Markov, partitioned-survival or uncertainty calculations. The independent schema accepts 2–16 explicitly ordered strategies, finite acyclic probability trees, a horizon no longer than one year, zero discounting and no half-cycle correction; every probability, terminal cost and terminal QALY must identify a source or declared proposed assumption, and unsupported recurrence, time dependence, intermediate rewards and unknown fields fail closed. The hand-checkable fixture reproduces comparator cost/QALY 1800/0.68, intervention 2900/0.7375, incremental cost/QALY 1100/0.0575, ICER 19130.434782608696 and INMB 1775 at a 50000 threshold. Results include node-level probabilities, expected contributions and concrete provenance ids; CLI replay binds the original input SHA-256 and reuses the established incremental frontier logic. Verification passed 10 focused tests, all 188 HEOR tests, all 762 frontend tests, 368 Rust tests with one established ignored test, typecheck, lint, Rust formatting, production build, the package resource contract and the 40-source/441-file preflight. Desktop review, reporting, reproducibility packaging and decision-tree DSA/PSA remain separate future loops; no installer was generated.

2026-07-31 · fix(release): completed the implementation and contract portion of P1-TEST-001a without changing application startup, research workflows, model calls, scientific computation or stored data. The macOS and Windows installed-package verifiers no longer accept a merely existing OpenCode process as ready: they parse the packaged process's ephemeral loopback port, issue an unauthenticated `GET /global/health`, and require `401 Unauthorized`, proving both HTTP readiness and enforcement of the app-owned password boundary. Release evidence now requires the paired `opencode-authenticated-http` check and records only the fixed path/status/authentication booleans, never the port, password or response body. The defect was first reproduced by four failing release-contract assertions; after the minimal change, all 43 release tests, 762 frontend tests across 112 files, 368 Rust tests with one established ignored test, TypeScript typecheck, ESLint, Rust formatting, production build and the 39-source/440-file resource preflight passed. A current installed runtime independently returned 401 to the unauthenticated health request. The superseded 1.0.0 DMG was not accepted as current evidence because byte comparison correctly detected its older `heor-workbench/SKILL.md` before first launch. No installer was generated; native proof for a newly built DMG and Windows runner execution remain explicit packaging-stage gates.

2026-07-31 · fix(security): closed P1-SEC-002a without changing business or scientific behavior. The ExcelJS transitive dependency tree now pins the maintainer-patched `brace-expansion` releases `1.1.16` and `2.1.2` within their existing major versions, removing both `GHSA-3jxr-9vmj-r5cp` / CVE-2026-13149 CPU-denial-of-service findings. A fail-closed Python contract checks both the root overrides and resolved lock entries and now runs in the release workflow. Before the change, both contract tests failed; after the change, the security contract passed 2/2, focused XLSX tests 3/3, all 762 frontend tests across 112 files, 368 Rust tests with one established ignored test, TypeScript typecheck, ESLint, Rust formatting, production build, frozen offline install and the 39-source/440-file resource preflight. The Tauri frontend bundle contains none of the affected Node glob/archive code. `GHSA-mh99-v99m-4gvg` remains separately open because its only published patched version is a cross-major change; it was not hidden or misreported as fixed. No installer was generated.

2026-07-31 · fix(security): closed P0-SEC-001 after explicit product-owner approval. Imported HTML is now a passive in-app document: both preview paths remove script permission, browser/gateway `srcdoc` receives a fail-closed CSP before untrusted markup, and the desktop loopback preview server applies `script-src 'none'`, `connect-src 'none'`, no frames/forms/plugins and no referrer to HTML only while retaining same-server static styles, figures, fonts and media. Source view and explicit external open remain available; PDF and other preview responses are unchanged. The defect was first reproduced by failing frontend and Rust tests, then verified by 10 focused inspector tests, 7 preview-server tests, all 762 frontend tests across 112 files, 368 Rust tests with one established ignored test, TypeScript typecheck, ESLint, Rust formatting, production build, the 39-source/440-file resource preflight, and a rendered research-workbench interaction with no error overlay or console error. The architecture, risk register, task priorities, validation matrix and loop evidence are recorded in `docs/audits/AI4HEOR_ENGINEERING_BASELINE_AND_LOOP_2026-07-31.md`. No installer was generated.

2026-07-29 21:00 · fix(ai4heor): added a no-degradation delivery floor to the seeded Harness, live HEOR prompt, and first-party workbench Skill. A failed URL, unavailable PDF, missing single source, connector error, or incomplete first search must now trigger reasonable authorized alternate-source retrieval rather than a narrative, tutorial, researcher-operated checklist, provisional hypothesis, or exploratory substitute; evidence gaps block only dependent claims and an exploratory assumption is available only when the researcher explicitly requested exploratory, teaching, or sensitivity-analysis work. End-to-end work remains incomplete instead of silently changing its requested outcome when a genuine blocker survives exhausted retrieval. Regression verification passed 15 Harness contracts, 29 focused prompt tests, TypeScript typecheck, ESLint, diff checks, production build, and the 39-source/440-file release resource preflight. No installer was generated.

2026-07-28 21:52 · build(ai4heor): generated and independently verified a fresh Intel macOS AI4HEOR 1.0.0 internal-test package after correcting the teaching entry and the named-medicine retrieve-then-confirm flow. Public medicine, price and citation candidates now remain hidden unless each option carries a traceable HTTP(S) source and visible retrieval date; named-medicine work must search authoritative public information before presenting a compact form. Release gates passed 760 frontend tests across 112 files, 177 deterministic HEOR tests, 372 development contracts, 42 release contracts, 367 Rust tests with one established ignored test, TypeScript typecheck, ESLint, Rust formatting, diff checks, production build and the 39-source/440-file resource preflight. Package verification confirmed a valid 96,296,061-byte DMG with SHA-256 `74d9f40e7185e5237799cd4cbe6908e8a6b004e3e9a5312e3853c6611e7f4edf`, version 1.0.0, identifier `com.ai4s.ai4heor`, x86-64 main executable, OpenCode 1.17.13, uv 0.11.26, all 440 controlled resources byte-identical, all 177 packaged HEOR tests, isolated temporary installation, first launch, AI4HEOR workspace creation, Open Science coexistence preservation and complete cleanup. The existing installed application was not replaced. No Developer ID identity is installed, so Gatekeeper correctly rejects this unsigned and unnotarized package; it is suitable only for local Intel Mac testing and is not a trusted public release.

2026-07-27 12:08 · build(ai4heor): completed a fresh Intel macOS AI4HEOR 1.0.0 internal-test package after the reserved-artifact correction and one final delivery audit. The audit found and repaired only a Rust formatting drift; no additional functional regression was reproduced. Release gates passed 758 frontend tests across 112 files, 177 deterministic HEOR tests, 372 development contracts, 42 release contracts, 367 Rust tests with one established ignored test, TypeScript typecheck, ESLint, Rust formatting, diff checks, production frontend build and the 39-source/440-file resource preflight. Independent read-only DMG verification confirmed a valid 96,298,571-byte image with SHA-256 `92c1174c4b58ec123926e7b79424d12ab878f9bb91858ecf373e98452432aee1`, version 1.0.0, identifier `com.ai4s.ai4heor`, an x86-64 main executable, OpenCode 1.17.13, uv 0.11.26, all 440 controlled resources byte-identical, all 177 packaged HEOR tests, temporary installation, first launch, AI4HEOR workspace creation, Open Science coexistence preservation and complete cleanup. The existing installed application was not replaced. No Developer ID identity is installed, so Gatekeeper correctly rejects this unsigned and unnotarized package; it is suitable only for local Intel Mac testing and is not a trusted public release.

2026-07-27 00:37 · fix(ai4heor): closed the reserved-artifact path that allowed an exploratory event-loss draft to masquerade as a review-panel contract. The live prompt, seeded Harness, HEOR workbench, event-disutility, cost-normalization and utility-input Skills now require the admitted analysis schema before creating those reserved JSON paths, require matching validators before review, and require every calculated JSON value to be serialized as a finite number rather than an expression, formula string, non-finite constant or placeholder binding. A strict regression scans every bundled first-party Skill JSON resource. In the explicitly designated `甘精胰岛素` test project, the invalid ad-hoc `heor/event-disutilities.json` was removed, its useful historical calculations were deterministically evaluated and preserved as a clearly labelled exploratory Markdown draft, live references were corrected, and the two remaining historical audit attachments now bind the exact current analysis-plan SHA-256 instead of approval-dependent placeholders. Verification passed 15 Harness tests, 29 focused HEOR frontend tests, strict parsing of all 2,028 project JSON files, and diff checks. No installer was generated.

2026-07-26 22:54 · fix(ai4heor): repaired legacy decision-problem compatibility without rewriting hash-bound research artifacts. The review parser now strictly validates all decision-problem scalar fields while normalizing historical non-empty intervention/comparator arrays for display, the native approval and reference-case auditors accept the same bounded legacy representation, and all three first-party analysis-plan templates now share the scalar decision contract; the previously divergent partitioned-survival template was corrected and is covered by a packaged-template regression test. Verification passed all 758 frontend tests across 112 files, TypeScript typecheck, production build, all 367 Rust tests with one established ignored test, 12 Harness tests, the 39-source/440-file resource preflight, targeted Rust formatting, diff checks and an in-app-browser research-workbench interaction with no application error. A separate pre-existing invalid-JSON expression remains in the current `event-disutilities.json`; it was not rewritten because it is not part of this compatibility defect and is bound research data.

2026-07-26 22:13 · build(ai4heor): generated and independently verified the Intel macOS AI4HEOR 1.0.0 local-test DMG after the record-level provenance, conversation, task-management, language, Harness, Open Science foundation and reference-management corrections. Release gates passed 756 frontend tests, 177 deterministic HEOR tests, 369 development contracts, 42 release contracts, 366 Rust tests with one established ignored test, typecheck, lint, targeted Rust formatting, diff checks and the 39-source/440-file resource preflight. Read-only package verification confirmed a valid 96,261,486-byte DMG with SHA-256 `087d966d267f7b7f0e321a3f279ebaafd473b29f1174409a294a385ecb6521be`, version 1.0.0, identifier `com.ai4s.ai4heor`, x86-64 main/OpenCode/uv binaries, OpenCode 1.17.13, uv 0.11.26, all 440 controlled resources byte-identical, all 177 packaged HEOR tests, isolated temporary installation, first launch, AI4HEOR workspace creation, Open Science coexistence preservation and complete cleanup. No tracked or packaged source contains the provided credential prefix. The running `/Applications/AI4HEOR.app` was not replaced. No Developer ID identity is installed, so Gatekeeper correctly rejects this unsigned and unnotarized internal test package; it is not a formally trusted public distribution.

2026-07-25 · feat(ai4heor): upgraded Research & Analysis provenance from artifact-level pointers to the concrete records represented by each enumerable summary. “Documents” now expands to the exact locally stored files with path, extraction/index status, page and byte counts and SHA-256; evidence-search and synthesis counts expand to their database runs, titles, locators, screening records and extraction records; methods-watchlist, conceptual-model, input-provenance, survival-review and reference-case counts likewise expose their contributing records. Pure scalar calculations retain the exact governed file and field location instead of fabricating a record list. Native auditors now return the real library manifest documents, synthesis searches/records/extractions and methods-watchlist sources/changes, and desktop rows open an actual local file when one exists. Verification passed all 756 frontend tests across 112 files, typecheck, lint, production build, diff checks, targeted Rust formatting, and 20 focused native tests. No installer was generated.

2026-07-25 · fix(ai4heor): removed performative end-of-turn and implementation-sequencing prompts from the live research flow. Session idle no longer creates a synthetic completion message, active work uses the neutral “continuing” status without exposing the last internal command, and the Research & Analysis pane becomes read-only while the assistant is updating project files so panel actions cannot race the conversation turn. The HEOR Harness and workbench prompt now continue researcher-selected work through retrieval, provenance, deterministic derivation, validation and packaging without asking whether to continue, while still reopening genuinely outcome-changing scientific conflicts. Reserved review-pane artifacts must be produced from the exact first-party template and pass their matching validator before being written. Verification passed all 755 frontend tests across 112 files, 192 focused native HEOR tests with the fixed-public-network test ignored, 12 Harness contract tests, typecheck, production build, diff checks, and an in-app-browser open/close interaction with no application error. Read-only validation of the current `甘精胰岛素` project found input provenance and uncertainty structurally valid, but its evidence-synthesis artifact still violates the reserved schema and its reference-case assessment remains draft, hash-mismatched and missing the `model-inputs` requirement; those research artifacts were not rewritten or misrepresented as decision-ready. No installer was generated.

2026-07-24 14:54 · build(ai4heor): generated the Intel macOS 1.0.0 local test DMG from the product-owner-reviewed working tree without producing a Windows package. Release gates passed 740 frontend tests across 112 files, the Open Science foundation, Skill/connector/resource admission contracts, researcher-led HEOR harness, prompt-residue audit, packaged teaching case and bounded HEOR method suites. Independent DMG verification passed all 177 packaged tests, confirmed 440 resources, x86-64 OpenCode 1.17.13 and uv 0.11.26, temporary installation, first launch, AI4HEOR workspace creation, Open Science coexistence isolation and cleanup; `hdiutil verify` confirmed a valid disk image and SHA-256 `3899c39254cc967c35406623ef2db7f39786a15883f7b0e793900052ea39857f`. No Developer ID identity is installed, so the app is unsigned and unnotarized; the source-bound release-evidence recorder correctly refused the dirty working tree, and this artifact remains a local testing candidate rather than a formal release.

2026-07-24 11:19 · fix(ai4heor): corrected two final installed-app regressions before replacement packaging. Project and task row management buttons now open the existing application-owned context menu at the clicked control instead of relying on a dropdown popper that lost its position in the macOS WebView and appeared at the window origin; real right-click behavior and the established project/task actions are unchanged. Model-authored question-option descriptions now retain their raw runtime event for audit but hide obviously corrupted repeated-punctuation output and show a localized explanation instead of presenting it as meaningful researcher input. Settings now explains each connected scientific MCP by purpose and status, admitted Open Science Skills expose their localized capability description, and built-in assistants use localized descriptions in all seven shipped languages. The supplied refined light/dark wordmark assets remain byte-identical and were not redesigned. Verification passed 730 frontend tests across 108 files, typecheck, lint, production build, all Open Science foundation/resource/release contracts, and all governed HEOR method suites. Replacement installers remain gated on the fresh cross-platform workflow.

2026-07-24 04:03 · fix(ai4heor): removed the provider-specific MiniMax China Token Plan shortcut so a custom model endpoint opens as a blank provider-neutral form; repaired scientific MCP lifecycle handling by restarting the local service after configuration changes and boundedly refreshing asynchronous startup status instead of retaining a false all-failed snapshot; suppressed the host webview's native Back/Reload/Open Link context menu while preserving application-owned project and task menus; and added visible new/manage controls for project and task sections and rows. Replaced both theme wordmarks byte-for-byte with the product-owner-supplied refined SVG files without changing the application icon or neighboring HEOR card artwork. Verification passed 725 frontend tests across 108 files, 366 Rust tests with one established ignored test, 177 deterministic HEOR tests, all Open Science foundation and HEOR method gates, typecheck, lint, production build, the 39-source/439-file resource preflight, release evidence contracts, direct connected-state checks for all seven installed scientific MCP servers, and rendered light/dark UI review with no application console error. The prior 1.0.0 package remains invalid for delivery; replacement macOS and Windows packages are now the release gate.

2026-07-24 02:29 · fix(ai4heor): renamed the researcher-facing Runtime settings section to Local service across all seven shipped languages, made theme and language selectors share the same pressed-state semantics and neutral selected treatment, and moved provider management into its own card row while preserving Connect as the primary service-state action. Regenerated the macOS and Windows application icon resources from the product-owner-supplied AI4HEOR Logo without changing its geometry, proportions or colors; source inspection also confirmed that the existing release bundle still embeds the superseded Open Science cloud icon and therefore must not be reused. Focused UI tests passed 12/12, followed by typecheck, lint, production frontend build, diff checks and in-app-browser visual and semantic verification. No installer was generated.

2026-07-24 · fix(ai4heor): classified the previous omission of Open Science's 7 MIT general-research Skills and 7 scientific connectors from the distributable AI4HEOR runtime as a serious product regression, not a product trade-off. The 1.0.0 release contract is now additive and fail-closed: exactly 52 first-party Skills plus the 7 pinned, licensed and tree-hash-locked Open Science Skills must be packaged and exposed in all 7 shipped interface languages; all 7 on-demand scientific connectors must remain connected from the Settings catalog through the Tauri bridge to the isolated installer; and `packages/shared` domain types and chart palettes must remain consumed without drift. The build workflow now fetches the fixed Skill revision and runs the Open Science foundation gate before platform packaging, so a missing Skill, connector, license, hash, resource mapping, localization entry or shared consumer blocks macOS, Windows and Linux artifacts. Verification passed all 7 foundation blockers, the 39-source/439-file release resource preflight, 720 frontend tests, 366 Rust library tests with one established ignored test, 177 deterministic HEOR tests, lint, typecheck, production build and diff checks. No installer was generated during this correction.

2026-07-24 · fix(ai4heor): replaced the performative early-stage approval flow with reviewable research decisions and explicit accountability boundaries. Decision-problem, conceptual-model and analysis-plan checkpoints now show the current scientific content, provide a direct return-to-conversation revision path, and record only an in-app confirmation marker without asking for a reviewer identity or generic rationale; independent validation and local release remain separately identified statements because they carry real accountability, while the interface states that AI4HEOR does not authenticate identity, provide an electronic signature or confer institutional, regulatory, reimbursement or scientific approval. The analysis plan is visible from its checkpoint and can be opened from the desktop app; raw identity and hash plumbing is collapsed under technical records. The visual system now follows a Codex-like neutral desktop palette with native system typography, charcoal selection and primary actions, restrained blue links and purple limited to the AI4HEOR brand mark. Verification passed 706 frontend tests across 105 files, lint, typecheck, production build, focused accessibility/HITL regressions and in-app-browser interaction and visual review. The browser reported only the established React Router future-flag warning; no application error or framework overlay was present. No installer was generated during this correction.

2026-07-23 · fix(ai4heor): replaced the superseded black-symbol application artwork with the product-owner-supplied final white evidence-stack Logo from `AI4HEOR logo.png`. The exact local source was normalized to the frontend icon and regenerated through Tauri into the complete macOS ICNS, Windows ICO, PNG, Windows Store, iOS and Android icon families; the in-app sidebar and new-task surface already resolve the same normalized asset. Brand provenance now records the new source and normalized SHA-256 values. Focused project/task and sidebar tests passed 14/14, all 9 packaged-resource and brand-provenance contracts passed, and the generated 128 px icon was visually inspected for the white mark, gradient, transparent corners and small-size legibility. The previously completed cross-platform workflow run used the superseded icon and is therefore not a delivery candidate; replacement installers still require a fresh source-bound build and verification.

2026-07-23 · fix(ai4heor): replaced the remaining in-app Open Science cloud mark with the product-owner-supplied AI4HEOR application icon, moved the primary desktop surfaces to a calmer Codex-like neutral palette and system sans-serif hierarchy, and corrected the Research files scope so researchers distinguish a durable named project from its current task instead of seeing a raw workspace folder or the internal `heor` artifact directory as the scope name. A project remains the shared local research workspace, its tasks remain separately named sessions associated with that workspace, and `heor` remains only the governed professional-output directory inside the project. Research files and the task file pane now show the project name with the active task below it; standalone tasks show their own task name. Verification passed 703 frontend tests across 105 files, typecheck, lint, production build, the 38-source/363-file release-resource preflight, targeted project/task hierarchy regressions and visual review of the new icon, typography and colors. Existing React test, dependency deprecation, `eval` and bundle-size warnings remain non-failing and unchanged. No installer was generated during this correction.

2026-07-23 · fix(ai4heor): corrected the HEOR workflow boundary that had exposed internal artifact plumbing as work the researcher must perform. The Harness, `heor-workbench` router and every live HEOR prompt now treat evidence retrieval, local import, extraction ledgers, provenance mapping, deterministic execution, validation commands and report packaging as assistant or desktop work; the ordinary response and dated research notes contain completed work, evidence status, assumptions, limitations and only substantive scientific judgments that can change the result. Paths, schemas, commands, hashes, environment variables, Skill identifiers, validators, panel mechanics and internal review states are retained only in Technical details or Run records. Chinese and English task starters now ask only for material unresolved decisions, and evidence retrieval follows the current permission mode without repeated confirmation in full-access or test environments. The regression was derived from the real glargine-evaluation task log that incorrectly listed an A–J internal pipeline as “researcher must do.” Verification passed 702 frontend tests across 105 files, 177 deterministic HEOR tests, 363 Rust tests with 1 established ignored test, 36 Harness/core-Skill/product-document contracts, lint, typecheck, production build and the 38-source/363-file release-resource preflight. Existing non-failing React test, jsdom canvas, dependency deprecation and bundle warnings remain unchanged. No installer was generated during this correction.

2026-07-23 · fix(ai4heor): aligned task and project context menus with the supported Codex interaction model, renamed only the task-side “Analysis history” pane to “Run history,” clarified the Human-in-the-loop decision-problem checkpoint, and replaced the application icon from the product-owner-supplied source. Task menus now offer inline rename and confirmed deletion; project menus offer new task, rename, reveal in the file manager, and confirmed removal, so the host WebView menu no longer appears. The decision-problem dialog now presents the actual population, intervention, comparator, outcome, perspective, time horizon and jurisdiction before recording researcher confirmation; the content hash is explicitly secondary integrity metadata rather than the review subject. Standard Tauri icon generation produced the macOS ICNS, Windows ICO and supporting PNG assets, with matching web favicons and updated local provenance. Verification passed 702 frontend tests across 105 files, 9 packaged-resource contracts, lint, typecheck, production build, ICNS extraction and ICO inspection. Existing React `act(...)`, dependency `eval` and bundle-size warnings remain non-failing and unchanged. No installer was generated during these corrections.

2026-07-23 · feat(ai4heor): expanded the local cost–utility teaching example from a three-number calculation into a complete synthetic teaching workflow. The case now contains a reviewable decision problem, a 27-row assumptions register, evidence-gap log, conceptual model, base case, eight-parameter one-way sensitivity analysis, three structural scenarios, a fixed-seed 1,000-draw probabilistic analysis, six mechanical checks, deterministic draft report, validation notes and a researcher review checklist. The explicit desktop action verifies every governed case asset against the bundle, writes the complete JSON result and draft report plus the two compatibility sensitivity outputs, records their hashes and local run, and exposes actual analysis counts and the positive-incremental-NMB proportion while keeping file paths and hashes collapsed. The interface states that mechanical checks passed but scientific interpretation and independent validation remain awaiting researcher review; no cost-effectiveness, pricing, reimbursement or policy conclusion is created. The Chinese About positioning text now uses the requested compact Chinese-English spacing. Verification passed 699 frontend tests across 105 files, 363/364 Rust tests with the fixed public-network test ignored, 32 teaching/resource/product contracts, lint, typecheck, production build, deterministic result/report repetition and a Simplified-Chinese in-app-browser visual check. Existing React `act(...)`, dependency `eval` and bundle-size warnings remain non-failing and unchanged. No installer was generated during this correction.

2026-07-23 · fix(ai4heor): corrected the learning and local teaching-case journeys that exposed harness instructions instead of serving researchers. “学习药物经济学基础” now prepares the bundled knowledge library automatically, asks one natural question about topic, prior knowledge and available time, requires readable source attribution, and reports corpus gaps instead of offering uncited model knowledge or manual file-import choices. The cost–utility teaching case now explains its research question, three-state structure and synthetic assumptions in the interface; the researcher runs the deterministic local calculation directly from an explicit action, while the assistant is reserved for explanation. File paths, commands, checksums and run IDs are hidden under an optional run-details disclosure rather than appearing in the primary task. The same learning/teaching boundaries are enforced in the AI4HEOR harness and relevant Skills. Verification passed 699 frontend tests across 105 files, lint, typecheck, production build, 14 HEOR harness/teaching tests, and an in-app-browser interaction check in Simplified Chinese; neither learning nor teaching prompts leaked Skill names, commands, JSON paths or SHA values. Existing React test `act(...)`, jsdom canvas, dependency `eval`, and bundle-size warnings remain non-failing and unchanged. No installer was generated during this correction.

2026-07-21 16:06 · fix(ai4heor): completed the AI4HEOR/Open Science isolation and stalled-task recovery and rebuilt the user-supplied About/contact design into the installed Intel-macOS 1.0.0 candidate. AI4HEOR now owns `com.ai4s.ai4heor` and a separate app-private runtime; first launch imports only provider configuration and authentication from the former shared domain, never Open Science sessions, task pointers, databases, caches or managed environments. Live runtime state now follows OpenCode session status, presents waiting/retry/tool/response progress, surfaces persisted provider errors, and unlocks a blank stranded turn after confirmed inactivity. The research review pane no longer treats every unstarted HEOR method as a failed check, and stale packaged `.gitkeep` placeholders cannot block task initialization or enter a research workspace. A real MiniMax-M3 turn completed with `OK`, and concurrent installed AI4HEOR/Open Science inspection proved distinct bundle identifiers, data stores, process trees and ports. The About contact region now follows the supplied unified-card reference and bundles the exact uploaded 1000×1000 Xiaohongshu PNG without the redundant caption; same-frame visual QA and the final installed Chinese screen passed. Verification passed 639 frontend tests across 98 files, typecheck, lint, production build, 335 Rust tests with one established ignored test, 13 macOS distribution tests, the 36-source/351-file resource preflight, 14 AI4HEOR product-document tests, all 177 packaged HEOR-core tests and isolated first launch. The installed and copied 91,904,434-byte `AI4HEOR_1.0.0_x64.dmg` has SHA-256 `86880f81d85dc595b943d3f1ae1db71d004d09018f055b9eb0791d4be7b3b46e`. It remains unsigned and unnotarized for internal Intel Mac testing; Windows was not regenerated or verified in this fix.

2026-07-21 15:00 · fix(ai4heor): refined the new-task, active-task, and About surfaces without producing another installer. The four HEOR shortcuts and composer now form one compact starting area instead of being separated by viewport-sized whitespace; user messages are right-aligned and constrained for readable scanning; the private `$heor-workbench` harness preamble and selected-Skill routing syntax remain in provider requests but are removed from both live echoes and restored history; OpenCode's `New session - <timestamp>` placeholder is replaced by a concise title derived from the first visible research request; and the About section now gives product positioning full width while balancing a compact contact card with the Xiaohongshu card. Browser review at 1280×720 confirmed the Chinese new-task and About layouts. Verification passed all 636 frontend tests across 98 files, lint, typecheck, production build, and diff checks; existing React test `act(...)`, jsdom canvas, dependency `eval`, and bundle-size warnings remain non-failing and unchanged.

2026-07-21 13:51 · release(ai4heor): finalized the tracked AI4HEOR 1.0.0 industrial-test source candidate around the HEOR product contract: researcher-led, natural-language-first work; deterministic and auditable analysis; local-first data handling; provider-independent model configuration; governed Skills and Human review. The user-facing product now includes the corrected Skill-use flow, HEOR-specific workbench and task prompts, neutral input focus treatment, managed scientific-computing setup actions, a packaged 25-document Chinese learning library, and a user-facing About/contact surface without development logs. Source verification passed 631 frontend tests across 98 files, lint, typecheck, production build, 333 Rust tests with one established ignored public-network test, all 177 deterministic HEOR-core tests, 337 development contracts, all 40 release contracts, and the 36-source/351-file resource preflight. The exact-source Intel-macOS and Windows-x64 1.0.0 packages are the remaining delivery gates.

2026-07-21 04:49 · fix(ai4heor): removed the Windows-only duplicate-resource collision by packaging the three reference-case profiles exactly once inside the governed `heor-reference-case` Skill and resolving both native HEOR consumers from that canonical tree; the frozen production frontend was also rechecked under Tauri's production custom protocol and proved a complete rendered root containing 18,936 characters of markup with no startup exception, showing that the apparent macOS white window was a screenshot of an inactive desktop-space composition layer rather than an application failure. Final-source verification passed 627 frontend tests across 98 files, lint, typecheck, production build, the 36-source/351-file resource preflight, 333 Rust tests with one established ignored public-network test, all 177 deterministic HEOR-core tests, 337 development contracts with three environment-dependent PDF tests skipped, and all 40 release contracts; a replacement `0.2.2` Intel-macOS and Windows-x64 build from the corrected tracked commit is still required before handoff.

2026-07-20 · feat(ai4heor): prepared the `0.1.56` Intel-macOS and Windows-x64 internal test delivery around the actual HEOR task flow. “New task” is now an unrestricted natural-language composer with four pharmacoeconomics shortcuts that only prepare editable drafts; “Workbench” remains the guided project surface, and the research-and-analysis panel appears only after a real task or project exists. Pasted screenshots and native file drops become project-local file references, clarification lists scroll safely, mathematical notation renders with KaTeX, and transparent wrapper runs remain correctly recognized. These bounded improvements were selectively adapted from Open Science 0.2.1 without restoring its generic research starters. The final 91,021,588-byte Intel DMG `AI4HEOR_0.1.56_x64.dmg` has SHA-256 `ed1107407ef6e3c8d46d0c674d368540c82811868a673a0e1c4d2dd3a2962358`; independent read-only verification confirmed x86-64, OpenCode 1.17.13, uv 0.11.26, all 359 controlled resources, all 177 packaged HEOR tests, isolated first launch, legacy-workspace migration and cleanup. The exact packaged OpenCode passed a credential-free streaming fixture, and a macOS-Keychain-supplied MiniMax-M3 turn passed with the isolated credential removed afterward and zero tracked-source hits. The rollback-capable installer replaced 0.1.55 in `/Applications`, preserved its backup, and confirmed the 0.1.56 main process plus bundled OpenCode were ready. The focused release workflow now builds Intel macOS plus Windows x64 EXE/MSI and requires the Windows verifier to inspect package identity/resources, install, first launch and cleanup before the Windows artifact may be handed off. Linux and Apple Silicon remain outside this test delivery; all packages remain unsigned and are for internal testing only.

2026-07-20 · feat(ai4heor): separated projects from conversations and delivered the Intel macOS `0.1.54` product-owner candidate. The product now follows the Codex distinction: a project is a named durable local workspace shared by multiple conversations, while a conversation is an independent task that may be standalone or project-bound. Global “New conversation” starts unrestricted standalone work; each project `+` starts a conversation inside that project; standalone conversations have a private app-issued local scope and retain the same assistant, Skills, HEOR methods, attachments, review, run, provenance, capability-growth, and preference-learning functions. Standalone scope metadata is excluded from the Projects list, rapid creation receives collision-free folders, and attachments or the local teaching example initialize the scope before their files are written. The arbitrary folder chip was removed from the composer so scope comes from the explicit product action. Sidebar and storage explanations were updated in all seven interface languages. A project-local Codex Run harness and a rollback-capable verified DMG installer were added. Full checks passed: 611 frontend tests, 332 Rust tests with one ignored, 177 deterministic HEOR tests, 336 developer contract tests, 38 release-harness tests, typecheck, lint, production frontend build, resource preflight for 39 sources/359 files, and diff/JSON checks. A slow packaged-runtime probe exposed that the release verifier retried connection failures but not response `TimeoutError`; the verifier and its regression test now cover both. The 90,080,697-byte `AI4HEOR_0.1.54_x64.dmg` has SHA-256 `2c8c70bd96fc9f78778f0c9653518b23de3d915c2198467c968345fd4b372aff`. Read-only package verification confirmed x86-64, OpenCode 1.17.13, uv 0.11.26, all controlled resources, all packaged HEOR tests, fresh first launch, legacy-workspace migration and cleanup. Packaged OpenCode streaming and a real Keychain-supplied MiniMax-M3 turn passed with no research data, no credential in the proof, zero tracked-source hits and cleanup of the isolated credential. The candidate replaced 0.1.53 in `/Applications`, preserved a rollback backup, reached main-process and bundled-OpenCode readiness, and was visually confirmed on the HEOR home surface. It remains unsigned and unnotarized for internal Intel Mac testing only.

2026-07-20 · feat(ai4heor): unified the product entry and released the Intel macOS `0.1.53` product-owner candidate. AI4HEOR is now the only user-facing research workbench: “开始新的研究项目” is an action, “研究工作台” is the destination, and legacy `/live` links resolve to the same AI4HEOR surface instead of exposing an inherited Open Science mode. The empty state now asks what HEOR work the researcher wants to do and offers six pharmacoeconomics/HEOR starting tasks; the generic climate/demo analysis starters and the duplicate starter component were removed. Command-palette examples now prepare a draft for Human review instead of sending work immediately. The same product hierarchy and sample notice were corrected in all seven interface languages. Visual review at 1280×720 confirmed the unified navigation and six HEOR cards; typecheck, lint, the 39-source/359-file resource preflight, and all 611 frontend tests passed. Separately, the packaged OpenCode failure was traced to the provider request prefix: `@ai-sdk/anthropic` appends `/messages` directly, so the MiniMax China Token Plan base URL must end in `/anthropic/v1`. The preset, verifier and regression tests now use that endpoint and the current `MiniMax-M3` model. A new credential-free packaged-runtime fixture proved streaming and assistant-marker handling through the exact bundled OpenCode, and a real Keychain-backed MiniMax turn passed without research data; the credential existed only in the isolated owner-only auth file, was removed afterward, never entered the proof record, and had zero tracked-source hits. The final `AI4HEOR_0.1.53_x64.dmg` has SHA-256 `77e59c78f9210e154272d50aec3e4f5e4ad3e66265e0da480f18752aef893c75`; read-only verification confirmed version 0.1.53, x86-64, OpenCode 1.17.13, uv 0.11.26, 359 byte-identical resources, all 177 packaged HEOR tests, fresh first launch, lossless legacy-workspace migration and cleanup. This candidate is ready for internal Intel Mac testing but remains unsigned and unnotarized; it has not replaced the application in `/Applications`, which was not running at final status inspection.

2026-07-20 10:42 · test(ai4heor): converted the remaining MiniMax acceptance item into a reusable, credential-safe Intel-macOS release gate without weakening the already shipped third-party inventory boundary. `scripts/release/verify_live_provider_boundary.py` mounts an exact DMG read-only, copies only its packaged OpenCode into an owner-only temporary profile, confirms the selected Keychain credential is absent from tracked source, checks the official provider model catalog, configures provider metadata through `/global/config` and the credential through `/auth/:provider`, proves the credential exists only in the isolated auth file, waits until the rebuilt workspace instance exposes the selected provider/model, opens the same directory-scoped event stream used by the desktop client, and removes auth plus the complete profile after the turn. The verifier writes only fixed booleans, versions, hashes and counts; raw logs, responses, paths and credentials are never emitted. Six focused unit tests cover assistant-only marker matching, credential-location and permission boundaries, safe failure classification, workspace readiness, and secret-free proof writing. The live gate was run repeatedly against the exact 90,047,182-byte 0.1.52 DMG using the existing product-owner-authorized macOS Keychain item: provider catalog authentication, `MiniMax-M2.7` listing, metadata/auth separation, owner-only persistence, workspace provider/model readiness and cleanup all passed, but `prompt_async` produced neither assistant completion nor a session/provider error before the bounded timeout. An explicit per-turn model diagnostic did not change the result and was removed rather than promoted without evidence. Therefore no live-turn proof JSON exists and 0.1.52 is not claimed to have passed a real model turn. Original installed-app PIDs and the single-instance socket remained unchanged. Separately, the release registry and seven-language product surface were reverified: the registry is empty, there is no external Skill directory, and no quarantined/rejected third-party product option or counter is retained; rewritten capabilities ship only as 52 first-party Skills. The current blocker is isolated to the packaged OpenCode 1.17.13 asynchronous session completion path; product-owner testing of deterministic/local capabilities remains valid, while live model acceptance stays open.

2026-07-20 10:05 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.52` internal product-owner candidate from clean tracked source commit `0b7750492651d47d9fa3a1763697ceb5eb0c6b82` without interrupting the installed application. Tauri's production build reran the fail-closed 39-source/359-file resource preflight. The 90,047,182-byte `AI4HEOR_0.1.52_x64.dmg` has SHA-256 `8f4e2feb2badc36db8372012169f68bbe867403386fc66cd5d0e89075a398138`. Independent read-only verification confirmed the 0.1.52 identity, thin x86-64 application, bundled OpenCode `1.17.13`, uv `0.11.26`, all 359 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged deterministic HEOR tests. An exact temporary app copy then launched beside the installed app, created a fresh isolated `Documents/AI4HEOR`, separately migrated a legacy `Documents/OpenScience` marker without data loss, and cleaned both candidate process trees and temporary homes. The verifier atomically restored the installed app's single-instance socket; original PIDs `4304`/`4337` and the socket's 01:41:48 timestamp remained unchanged, with no DMG mount or verification socket left behind. The external release registry remains empty: no quarantined or rejected third-party product option is retained. This candidate supersedes 0.1.51 and is ready for product-owner testing, but remains unsigned and unnotarized for internal Intel Mac use only. Product-owner use/acceptance, a real locally configured MiniMax turn, Developer ID signing and notarization remain open.

2026-07-20 09:59 · feat(ai4heor): versioned source as `0.1.52` and added a first-party privacy-preserving diagnostic-report export for product-owner acceptance testing. Settings now explains and exposes the report in all seven shipped interface languages. The native command exports only the product version, operating-system/architecture constants, one managed-runtime lifecycle bit, a generation timestamp, and aggregate event counts from at most the final 1 MiB of the app-private frontend diagnostic log. It never serializes raw log lines, credentials, authentication files, provider/model/endpoint details, workspace paths or files, conversations, command output, or research data; linked or non-regular diagnostic logs are refused, and Unix exports are forced to owner-only mode. A regression fixture deliberately places a fake credential, private study path, session identifier and local endpoint in the raw log and proves none enters the JSON report. Full source verification passed 615 frontend tests across 98 files, 177 deterministic HEOR tests, 336 development contracts, 331 Rust tests plus the established fixed-public-network test ignored, typecheck, lint, production frontend build, 25 release/resource/macOS distribution contracts, legal-inventory regeneration and validation, and the 39-source/359-file packaging preflight. The external release registry remains valid and empty, so no quarantined or rejected third-party option is retained. Tracked-content scanning found no provided credential prefix. This is a source milestone: a clean tracked commit, Intel macOS DMG build, independent package verification and exact isolated first launch are still required before 0.1.52 replaces the verified 0.1.51 product-owner candidate.

2026-07-20 09:38 · test(ai4heor): completed exact isolated first-launch and legacy-workspace-migration verification for the already built Intel macOS `0.1.51` candidate without interrupting the installed application. The first parallel attempt correctly exposed the macOS single-instance socket as the cause of the temporary process exiting before readiness; it was not reported as a product pass. The package verifier now temporarily renames and later restores the existing Unix-domain listener while keeping process proof and cleanup scoped to the temporary app copy's exact main/OpenCode executable paths. Twenty release regressions prove exact-path classification, exclusion of a coexisting installed app, candidate-only cleanup, and byte-preserving restoration of the original live socket. The second real DMG run passed all 177 packaged HEOR tests, launched an exact temporary 0.1.51 app plus its bundled OpenCode, created a fresh isolated `Documents/AI4HEOR`, then separately migrated `Documents/OpenScience` with an exact marker preserved and the legacy path removed. Both temporary process trees and homes were cleaned; the DMG was detached; the original `/Applications/AI4HEOR.app` PIDs `4304`/`4337` and `/tmp/com_ai4s_workbench_si.sock` remained live and unchanged. Package identity remains 90,040,670 bytes with SHA-256 `b06e4cc6f46d52599edbe0ee4e359268840927ddc999fb9021ce4b43abb2e70c`, x86-64, OpenCode `1.17.13`, uv `0.11.26`, 359 source-identical controlled resources, and `first_launch=True`. This makes 0.1.51 the current technically verified Intel-macOS product-owner candidate; actual product-owner use/acceptance, a real locally configured MiniMax turn, Developer ID signing and notarization remain open.

2026-07-20 09:27 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.51` internal product-owner candidate from tracked source commit `17989c73ac328e36166fb038c56dbde422b8906a` without interrupting the installed application. Tauri's production build reran the fail-closed preflight and admitted exactly 39 configured sources and 359 files. The 90,040,670-byte `AI4HEOR_0.1.51_x64.dmg` has SHA-256 `b06e4cc6f46d52599edbe0ee4e359268840927ddc999fb9021ce4b43abb2e70c`. Independent read-only DMG verification confirmed the 0.1.51 identity, thin x86-64 application, bundled OpenCode `1.17.13`, uv `0.11.26`, all 359 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged deterministic HEOR tests. The external release registry is still empty: no quarantined or rejected third-party product option is retained after replacement or exclusion. The running `/Applications/AI4HEOR.app` and bundled OpenCode child remained untouched, so `first_launch=False`; this candidate supersedes 0.1.50 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only; signing, notarization, Apple Silicon, Windows and Linux remain outside the paused cross-platform scope. A real MiniMax natural-language turn remains pending local credential entry by the product owner; no provided credential entered source, build logs, documentation or package configuration.

2026-07-20 09:20 · feat(ai4heor): versioned source as `0.1.51` and corrected the model-provider credential boundary before the next Intel macOS handoff. Custom endpoint configuration now contains only adapter, endpoint and model metadata; credentials use OpenCode's separate authenticated `/auth/:provider` path and owner-only app-private `auth.json`. The unused legacy Tauri/Rust command that wrote provider keys into `opencode.json` was removed, explicit OpenCode-login import now tightens the copied file, and deleting a custom provider removes its credential before its metadata. Settings adds an editable MiniMax China Token Plan preset for the official Anthropic-compatible `https://api.minimaxi.com/anthropic` endpoint and `MiniMax-M2.7`, with credential-boundary guidance in all seven interface languages; no credential is bundled or written into the preset. An isolated real bundled-OpenCode check proved absent-delete idempotence, fake credential creation at mode 600, exact removal and temporary-process/tree cleanup. This is routing and storage evidence, not a real MiniMax model turn; no live credential is currently configured in the app-private runtime. Full verification passed 614 frontend tests, 177 deterministic HEOR tests, 336 development contracts, 329 Rust tests plus the established fixed-public-network test ignored, typecheck, lint, production build, 23 resource/release/macOS distribution contracts and the 39-source/359-file release preflight. The external release registry remains empty, so no quarantined or rejected third-party option enters the runtime or user list. Tracked-content scanning found no provided credential prefix. The existing installed AI4HEOR/OpenCode processes remain untouched; a clean tracked-source commit, Intel DMG build and independent read-only package verification are still required before 0.1.51 becomes the current product-owner candidate.

2026-07-20 08:54 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.50` internal product-owner candidate from tracked source commit `19ac87b9eb024afe599aa7611a5fe32cd92f86be` without interrupting the installed application. Tauri's real production log showed the new fail-closed resource preflight running before Rust compilation and admitting exactly 39 configured sources and 359 files. The 90,082,675-byte `AI4HEOR_0.1.50_x64.dmg` has SHA-256 `327607afb9e1d7e10c90c7ccce4a7355244611b754e3d7a2e75cbf6c38988a86`. Independent read-only DMG verification confirmed the 0.1.50 identity, thin x86-64 application, bundled OpenCode `1.17.13`, uv `0.11.26`, all 359 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged deterministic HEOR tests. The running `/Applications/AI4HEOR.app` and bundled OpenCode child remained untouched, so `first_launch=False`; this candidate supersedes 0.1.49 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only; signing, notarization, Apple Silicon, Windows and Linux remain outside the paused cross-platform scope.

2026-07-20 08:49 · chore(ai4heor): versioned source as `0.1.50` and closed the release-resource pollution path at both causes. Every Python development and packaged-HEOR test entry point now disables bytecode writes, including child processes that previously recreated four `__pycache__` trees inside bundled first-party Skills. A dependency-free cross-platform Node preflight runs from Tauri's production `beforeBuildCommand` after the frontend build and fails before Rust compilation for missing resources, links, generated Python/tool/OS caches, non-regular entries or destination collisions; CI and five focused failure-path tests exercise the same gate, while independent post-build package verification remains unchanged. After a clean start, all 332 development contracts and all 177 deterministic HEOR tests completed without leaving any controlled-resource cache, and the preflight admitted exactly 39 configured sources and 359 files. Verification also passed 612 frontend tests, 332 Rust tests with the established fixed-public-network test ignored, typecheck, lint, production build, nine release-evidence tests and nine macOS distribution-contract tests. The existing installed AI4HEOR/OpenCode processes remain untouched; the Intel macOS 0.1.50 package still requires a clean tracked-source commit, build and read-only DMG verification before it becomes the current product-owner candidate.

2026-07-20 08:30 · feat(ai4heor): shipped source-bound target-journal submission checks in 0.1.49. The first-party `journal-submission-check` Skill and native Rust evaluator accept only a researcher-saved current official author-guide snapshot, URL/access/version metadata, exact SHA-256 bindings, and 1–64 rules with explicit `guide_locator` values. Supported mechanical checks cover required files, extensions, byte caps, Markdown title/document/section counts, headings, tables, and figures; unknown fields, unsupported rules, symlinks, stale sources, changed outputs, and Agent-claimed approval fail closed. Bilingual Markdown, structured JSON, and generation audit outputs always remain `awaiting_human_review`; no journal instructions, reporting checklist, CSL style, or submission template is bundled, and passing never establishes compliance or permission to submit. The Skill catalog now contains 52 first-party entries in seven interface languages and the external release registry remains empty. Verification: Skill Creator validation, 612 frontend tests, 177 HEOR tests, 330 development-contract tests, 332 Rust tests passed plus 1 ignored, TypeScript production build, ESLint, new-file rustfmt, no provided model credential in tracked content, and empty external-adapter registry. The first generated DMG was correctly rejected because prior tests had left Python `__pycache__` directories in packaged resources; after cleaning the generated caches and rebuilding from source commit `d0da1af`, read-only package verification passed all 177 mounted HEOR tests and confirmed x86_64, version 0.1.49, OpenCode 1.17.13, uv 0.11.26, 359 source-identical controlled resources, and no Python cache. Final candidate `AI4HEOR_0.1.49_x64.dmg` is 90,082,873 bytes with SHA-256 `691d4c3be8bccca5b133905927b2a0f3b52d3acec06da7b77c1fba7527d17b12`. First launch is still intentionally unverified because the installed AI4HEOR/OpenCode processes remained running and were not interrupted; signing, notarization, Apple Silicon, Windows, and Linux remain outside the current Intel-macOS acceptance scope.

2026-07-20 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.48` internal product-owner candidate from tracked source commit `a4f08f1db724649d882ae5195872dfbd70fcd9fd` without interrupting the installed application. The 90,027,878-byte `AI4HEOR_0.1.48_x64.dmg` has SHA-256 `876f1f875eebbd6c8dcc2167cc9ae6a4552ef63acfb0a91a261b97afabf3da5b`. Read-only DMG verification confirmed the 0.1.48 identity, thin x86-64 application, bundled OpenCode `1.17.13`, uv `0.11.26`, all 354 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged deterministic HEOR tests. The candidate contains the 51st first-party, seven-language `research-tables` Skill and its native source-bound XLSX/CSV renderer; the release external-adapter registry remains empty and no quarantined or rejected third-party choice enters the runtime or build. The running `/Applications/AI4HEOR.app` and bundled OpenCode child remained untouched, so `first_launch=False`; this candidate supersedes 0.1.47 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only, and generated tables require researcher review and do not establish source correctness, scientific validity, methodological fitness, cost-effectiveness, reimbursement, pricing, policy or release authority.

2026-07-20 · feat(ai4heor): versioned source as `0.1.48` and added the 51st first-party, seven-language `research-tables` Skill for configurable, source-bound research tables. A bounded manifest now defines 1–16 tables, typed and unit-labelled columns, explicit evidence/analysis-output/assumption basis, row-level source locators and exact SHA-256 bindings to 1–32 current local files. Native Rust independently validates the manifest and deterministically produces a macro-free, formula-free XLSX, one CSV per table and a generation audit; formula-like CSV text is neutralised, stale or symlinked inputs fail closed, changed or untracked outputs are never overwritten, and every result remains `awaiting_human_review`. A real generated workbook passed ZIP/XML/formula checks, artifact-engine import and visual inspection after improving the bilingual review and provenance layout. The default browser surface retained the AI4HEOR brand and natural Chinese HEOR entry points with no inherited Open Science showcase; the project-conditioned table card passed three focused component tests. Verification passed 609/609 frontend tests, 177/177 deterministic HEOR tests, 327/327 development contracts, and 327/328 Rust tests with only the fixed-public-network test ignored, plus typecheck, lint, production build, legal/resource/Skill/product-document gates, source-secret absence and targeted `rustfmt --check`. Clippy reports only 10 pre-existing warnings outside the new module. The release external-adapter registry remains empty and exposes no quarantined or rejected third-party choices. The Intel macOS package remains pending a clean tracked-source commit, build and read-only verification; the running installed application has not been interrupted.

2026-07-20 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.47` internal product-owner candidate from tracked source commit `263512e3bce0e0900ccf88484859183653c8771e` without interrupting the installed application. The 89,990,766-byte `AI4HEOR_0.1.47_x64.dmg` has SHA-256 `7f2353af4bb973a2afc84e4b1d26b1f1f071970b14d5385f325c78c35de74907`. Read-only DMG verification confirmed the 0.1.47 identity, thin x86-64 application, bundled OpenCode `1.17.13`, uv `0.11.26`, all 349 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged deterministic HEOR tests. The candidate contains the 50th first-party, seven-language `citation-formatting` Skill and its native source-bound formatter; no third-party CSL styles, quarantined choices or rejected choices enter the runtime or build. The running `/Applications/AI4HEOR.app` and bundled OpenCode child remained untouched, so `first_launch=False`; this candidate supersedes 0.1.46 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only, and generated citations require researcher review and do not establish bibliographic correctness, journal compliance, scientific validity, methodological fitness, reimbursement, pricing, policy or release authority.

2026-07-20 · feat(ai4heor): versioned source as `0.1.47` and added the 50th first-party, seven-language Skill for source-bound citation formatting. The native app now validates an exact local `references/library.json` SHA-256 and a bounded `references/citation-plan.json`, then deterministically creates named in-text citation clusters, a reference list, and an audit using three AI4HEOR-owned profiles. Unknown references, unsafe paths, symlinks, unsupported locators, stale libraries and invented metadata fail closed; missing author, year, container, pages or identifier stays visible for researcher review, existing Human-edited output is not overwritten, and every result remains `awaiting_human_review`. The product does not bundle third-party CSL styles or claim AMA, Vancouver, GB/T 7714 or target-journal compliance; the external-adapter registry remains empty and exposes no quarantined or rejected third-party options. The natural-language action was verified in the real HEOR analysis pane and prefills a constrained `$citation-formatting` task without sending it. Verification passed 606/606 frontend tests, 177/177 deterministic HEOR tests, 324/324 development contracts, 322/323 Rust tests with only the fixed-public-network test ignored, typecheck, lint, production build, legal/resource/Skill/product-document gates, source-secret absence and `rustfmt --check`; Clippy reports only the same 11 pre-existing warnings in untouched modules. The Intel macOS package remains pending a clean tracked-source commit, build and read-only verification, and the running installed application has not been interrupted.

2026-07-20 · feat(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.46` internal product-owner candidate from tracked source commit `e44339fe9d27dc617bbc7422a99c4eafe969ab8b` without interrupting the installed application. The 89,903,352-byte `AI4HEOR_0.1.46_x64.dmg` has SHA-256 `1e17461b0482004fc6f929f8a519e744cb34ac96d5e4e987e7006bb11577184a`. The first-party conceptual-model surface now lets the researcher arrange existing state nodes by pointer or keyboard and deterministically exports source-bound SVG, editable GraphML, layout and audit files; state and transition semantics remain authoritative only in `heor/conceptual-model.json`, all outputs remain `awaiting_human_review`, source drift makes them stale, and external edits are never overwritten. Real SVG/GraphML QA passed XML parsing, 3-node/6-edge preservation, no-script/no-external-link checks and Chrome rendering after correcting header overlap, hidden short-edge arrows and a long transition crossing an intermediate node. The external-adapter registry remains empty and the product presents no quarantined or rejected third-party options; the excluded AI4S Mindmap Renderer is replaced by this independent first-party path and does not enter the runtime or build. Verification passed 603/603 frontend tests, 177/177 deterministic HEOR tests, 324/324 development contracts, 317/318 Rust tests with only the fixed-public-network test ignored, typecheck, lint, production build, source-secret and controlled-cache checks. Strict Clippy remains blocked by 11 pre-existing warnings in untouched modules; the new Rust module passes `rustfmt --check`. Read-only DMG verification confirmed 0.1.46, thin x86-64, OpenCode `1.17.13`, uv `0.11.26`, all 347 controlled resources byte-identical to source, no packaged Python cache, and all 177 packaged HEOR tests. The running `/Applications/AI4HEOR.app` and bundled OpenCode child were left untouched, so `first_launch=False`; this candidate supersedes 0.1.45 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only, and diagram export does not establish scientific validity, method fitness, reimbursement, pricing, policy or release authority.

2026-07-20 · feat(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.45` internal product-owner candidate from clean tracked source commit `0fc405640059a2b487c7e908ac8004535878c3e3` without interrupting or replacing the installed application. The 89,755,004-byte `AI4HEOR_0.1.45_x64.dmg` has SHA-256 `876f37346edd9d99bc39428f23c0abe9be3d332a811a9a0123cc6b1251e733db`. The first-party native report renderer now produces a deterministic source-bound DOCX/PDF/XLSX set; the formula-free, macro-free workbook preserves typed report-package values and explicit nulls across five bilingual sheets for summary, results, report tables, reporting coverage, and sources/review, and it never recalculates the economic model. Output currentness binds all three hashes, and externally changed files are never silently overwritten. A real generated XLSX passed ZIP CRC, all-XML parsing, absence-of-formula/macro/external-link checks, artifact-engine import and visual rendering of every sheet. Full verification passed 600/600 frontend tests across 257 suites, 314/315 Rust tests with only the fixed-public-network test ignored, 177/177 deterministic HEOR tests, 322/322 development contracts, typecheck, lint, production build, legal inventory, tracked-secret-prefix and controlled-cache checks. Read-only DMG verification confirmed AI4HEOR 0.1.45 identity, a thin x86-64 main program, OpenCode `1.17.13`, uv `0.11.26`, all 346 controlled resources byte-identical to source, and all 177 packaged HEOR tests. The external-adapter registry remains empty and the product retains no quarantine or rejection choices: the incompatible Anthropic document sources remain permanently excluded while the needed XLSX capability is independently implemented first-party. The running `/Applications/AI4HEOR.app` and bundled OpenCode child were left untouched, so `first_launch=False`; this candidate supersedes 0.1.44 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. It remains unsigned and unnotarized for internal Intel Mac testing only, and report generation does not establish scientific validity, methodological fitness, cost-effectiveness, reimbursement, pricing, policy or release authority.

2026-07-20 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.44` internal product-owner candidate from clean tracked source commit `7f29e4a014603d99e957f255732847fd267da5e5` without interrupting or replacing the installed application. The 89,752,369-byte `AI4HEOR_0.1.44_x64.dmg` has SHA-256 `d55ba19e964330396970b39a2f10998b1e5da58a98fcfeb99bce24343028ab5a`. Read-only mounting confirmed AI4HEOR 0.1.44 identity, a thin x86-64 main program, bundled OpenCode `1.17.13`, uv `0.11.26`, and all 346 controlled resources byte-identical to source, including the seven-language first-party `heor-reporting` Skill, its report-export contract, and the admitted Source Han Sans CN license record; all 177 packaged deterministic HEOR tests passed. The native app now re-audits an exact source-bound report package and Markdown report before producing deterministic macro-free DOCX and PDF files, embeds the admitted Chinese font, records output hashes, and leaves both outputs awaiting researcher review. The release external-adapter registry remains empty: completed first-party replacements ship directly, while unfinished, isolated, rejected, and licence-incompatible third-party candidates are not retained as product options. The existing `/Applications/AI4HEOR.app` and bundled OpenCode child remained running, so the verification record intentionally has `first_launch=False`; this candidate supersedes 0.1.43 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. The DMG remains unsigned and unnotarized and is limited to internal Intel Mac testing; generated documents do not establish scientific validity, methodological fitness, cost-effectiveness, reimbursement, pricing, policy or release authority.

2026-07-20 03:16 · chore(ai4heor): built and independently verified the Intel macOS AI4HEOR `0.1.43` internal product-owner candidate from clean tracked source commit `9ff815a` without interrupting the installed application. The 81,654,020-byte `AI4HEOR_0.1.43_x64.dmg` has SHA-256 `6524e6bb1cdd4d1e04438e021307b0d48bafaba4c626979dc3d4d69762022bf9`. Read-only mounting confirmed AI4HEOR 0.1.43 identity, a thin x86-64 main program, bundled OpenCode `1.17.13`, uv `0.11.26`, and all 342 controlled resources byte-identical to source, including the complete first-party `literature-review` Skill and local reference-library tool; all 177 packaged deterministic HEOR tests passed. The existing `/Applications/AI4HEOR.app` process and its bundled OpenCode child remained running, so the verification record intentionally has `first_launch=False`; this candidate supersedes 0.1.42 but does not yet replace the fully first-launch-verified 0.1.41 handoff or establish product-owner acceptance. The DMG remains unsigned and unnotarized and is limited to internal Intel Mac testing; no bibliographic correctness, CSL style rendering, scientific validity, methodological fitness, reimbursement, pricing, policy or public-release authority is claimed.

2026-07-20 03:09 · feat(ai4heor): versioned source as `0.1.43` and replaced the placeholder `literature-review` directory with the 49th first-party, seven-language Skill so the previously documented AI4S Literature Survey replacement is now executable rather than aspirational. Natural-language work can invoke a dependency-free local tool to import, normalize, deduplicate, validate and deterministically export project-bound RIS, bounded BibTeX and CSL-JSON metadata; every record retains the import path, exact source SHA-256 and source record key, compatible duplicates merge by DOI then PMID then title/year, different identifiers remain separate, and non-identity metadata conflicts remain visible for researcher review. The tool rejects workspace escapes, symlinks, files above 10 MiB, more than 10,000 records, malformed RIS, unsupported CSL fields, BibTeX macros/string concatenation and different existing export bytes; atomic library writes and identical-import byte idempotence were verified. It makes no network call, does not fetch full text, screen evidence, render CSL styles or claim bibliographic/scientific correctness. The capability matrix now records this bounded delivery and moves DOCX/PDF, XLSX and editable concept-diagram generation to the remaining first-party backlog. Verification passed 11/11 focused reference-library contracts including corporate-author BibTeX round-trip; 321/321 development contracts; 597/597 frontend tests across 92 files; typecheck, lint and production build; 308/309 Rust tests with only the fixed-public-network test ignored; 177/177 deterministic HEOR tests; 49-Skill/seven-locale, resource, product-document, version, legal-inventory and diff checks. The `0.1.43` Intel macOS DMG remains pending a clean tracked-source commit, build and independent package verification; the running installed application was not interrupted.

2026-07-20 02:43 · chore(ai4heor): built the replacement Intel macOS AI4HEOR `0.1.42` candidate after converting the external-asset surface to the release-only final-state model. Clean tracked source commit `b9f5b61` produced the 81,638,295-byte `AI4HEOR_0.1.42_x64.dmg` with SHA-256 `0c7d25ecda62a903c78ac4e14d4a64e3c8347bb21112ab16aaa22910d4425db9`. Independent read-only verification confirmed the thin x86-64 application, AI4HEOR 0.1.42 identity, OpenCode `1.17.13`, uv `0.11.26`, all 340 controlled resources byte-identical to source, and all 177 packaged deterministic HEOR tests. The already installed AI4HEOR processes at PIDs 4304 and 4337 were not interrupted, so exact isolated first launch of this final candidate remains unverified and the verification JSON intentionally contains no first-launch claim. The unsigned and unnotarized DMG remains an internal product-owner test candidate; it does not yet replace the fully verified 0.1.41 acceptance handoff or establish scientific validity, methodological fitness, reimbursement, pricing, policy, or public-release authority.

2026-07-20 02:35 · fix(ai4heor): replaced the inherited long-lived third-party quarantine/rejection catalog with a release-only external-adapter inventory after the product owner required completed replacements rather than permanent non-admission options. Registry schema `1.1.0` now accepts only fully validated, hash-locked adapters that actually ship and validly contains zero rows; unfinished or excluded sources fail closed and cannot enter the release inventory. Methods & Tools no longer renders 0/10/4 counters, quarantine groups, rejected groups, or clean-room action choices in any of the seven interface languages; it states that no third-party tool currently ships and will list only completed adapters. The seven AI4S Skills, HEORAgent MCP, Paper Search MCP, and BioMCP were mapped to bounded first-party HEOR/harness/evidence/reporting/VOI capabilities or explicit first-party build work, while the four license-incompatible Anthropic document sources were removed from the registry, candidate UI, and the 4.3 MB ignored local review cache; their authorization and replacement rationale remains only as a read-only audit trail. First-party `research-presentation` already supplies PPTX, while reference-library RIS/BibTeX/CSL support, DOCX/PDF export, XLSX export, and editable conceptual-model diagrams remain explicit product work rather than hidden developer-Skill dependencies. Verification passed 597/597 frontend tests across 92 files, typecheck, lint and production build; 308/309 Rust tests with only the fixed-public-network test ignored; 310/310 development contract tests; 177/177 deterministic HEOR tests; release-registry, seven-locale JSON, product-document, diff, controlled-cache and secret-prefix-absence checks. No existing installed application process or DMG was changed, and this source milestone does not claim that the final `0.1.42` Intel macOS package has been rebuilt or completed isolated first-launch acceptance.

2026-07-20 00:44 · feat(ai4heor): released the Intel macOS AI4HEOR `0.1.41` internal acceptance build after auditing the platform's general scientific-research foundation as distinct from its HEOR-specific methods. The first-party inventory now contains 48 Skills with complete names and descriptions in all seven shipped interface languages. The new `research-presentation` Skill prepares a bounded, source-hash-bound manifest through the natural-language conversation; a project-authored native Rust renderer then creates a deterministic, macro-free 16:9 PPTX and a generation audit without another model call, scientific approval, release decision or hidden dependency on the developer's Codex installation. Portable Python and native Rust independently reject stale sources, unsafe paths, symlinks, malformed images, missing citations, omitted limitations and Agent-claimed Human approval. The review pane keeps the form auxiliary: it asks the Agent to prepare content, enables generation only after validation, opens the current PPTX, and marks it stale after any bound input drift. A real generated fixture passed byte-determinism, ZIP CRC, all-XML `xmllint`, and macOS Quick Look rendering. The capability-candidate dialog also now leads with human-readable purpose, licence, limits and checks while keeping provider, model, internal ID and exact SHA-256 available in a collapsed audit section; native exact-byte activation remains unchanged. Full verification passed 592/592 frontend tests across 91 files, typecheck, lint and production build; 304/305 Rust tests with only the fixed-public-network test ignored; 177/177 deterministic HEOR tests; 308/308 development contract tests; Skill validation, JSON, licence inventory, diff, cache and secret-absence checks. Tracked source commit `38e061708e56ee82d53ea3d68a79b95dd25ea5dc` produced the 81,600,567-byte `AI4HEOR_0.1.41_x64.dmg` with SHA-256 `9fc18e035748a2aa67e06443409dce9ffbd2aed83496ab9f1003138391c18ee6`. Independent read-only package verification confirmed the thin x86-64 app, OpenCode `1.17.13`, uv `0.11.26`, all 340 controlled resources byte-identical to source, all 177 packaged HEOR tests, isolated fresh launch, content-preserving legacy migration and process cleanup. The DMG is unsigned and unnotarized and remains limited to product-owner internal Intel Mac testing; the researcher must still check every slide and retains all scientific, methodological, external-use and release authority.

2026-07-19 23:50 · test(ai4heor): repeated the real-provider compatibility check against the exact OpenCode sidecar copied from the read-only-mounted AI4HEOR `0.1.40` Intel macOS DMG. In a fresh `/private/tmp` HOME/XDG workspace, bundled OpenCode `1.17.13` called `minimax-cn/MiniMax-M2.7` and returned the required `AI4HEOR_MINIMAX_OK` marker with exit code 0. The product-owner-authorized credential was read from macOS Keychain without being printed or passed on the command line; an exact-value scan found it only in the expected mode-600 temporary OpenCode config and in no other test file. The mounted image and complete temporary tree were removed, and a final repository scan found zero credential-prefix hits. This proves the packaged model-call path remains usable with the authorized MiniMax service; it does not test pharmacoeconomic methods, evidence quality, analysis validity, candidate-Skill activation, researcher judgment or final product acceptance.

2026-07-19 23:43 · fix(ai4heor): released the Intel macOS AI4HEOR `0.1.40` internal acceptance build after closing the capability-dialog comprehension gap raised during product-owner testing. The review dialog now states what the proposed project Skill does, shows the localized name and description plus the exact request that produced it, and says plainly that this is not a pharmacoeconomic analysis and cannot select methods, evidence, parameters or conclusions for the researcher. The same purpose and boundary text is present in all seven shipped interface languages; the original bilingual candidate content, exact hash, license note, limitations, acceptance checks and app-owned Human activation remain unchanged. Full verification passed 590/590 frontend tests across 90 files, typecheck, lint and production build; 299/300 Rust tests with only the fixed-public-network test ignored; JSON, product-document, license-inventory, diff and secret-absence checks. The first package audit correctly rejected Python caches generated by native calibration and RWE fixture tests; those test subprocesses now disable bytecode generation, targeted reruns leave the controlled Skill tree cache-free, and the package was rebuilt from tracked source commit `3afdc49b223d92c9b71e8c8e28c3ea8aa6844bf8`. The final 81,478,172-byte `AI4HEOR_0.1.40_x64.dmg` has SHA-256 `ddebf35ac7c18d6d2e357a4b61d419761f9ddefb1688eb74037a614fbb882f5f`. Independent read-only verification confirmed the x86-64 payload, OpenCode `1.17.13`, uv `0.11.26`, all 335 controlled resources byte-identical to source with no Python caches, all 177 packaged deterministic HEOR tests, isolated first launch, content-preserving legacy migration and process cleanup. The package is unsigned and unnotarized and remains limited to product-owner internal Intel Mac testing; visual comprehension, methodological fitness and final product acceptance remain Human gates.

2026-07-19 23:48 · docs(ai4heor): refreshed the compact local pharmacoeconomics knowledge base and replaced the Intel macOS acceptance package without broadening the release scope. The 25-file standalone Chinese library and the bundled application copy remain byte-identical; all internal wiki links resolve, every document has an H1, and local searches for opportunity cost, QALY, ICER, budget impact, PSA, real-world evidence, model validation and the China 2020 guideline return results. Current official pages from the China Pharmaceutical Association, National Health Commission, National Healthcare Security Administration, NICE and CDA-AMC were rechecked; NICE PMG48 now uses its canonical `/process/pmg48` path, and the 2026 Chinese real-world comprehensive-value series remains explicitly labelled a consultation draft rather than a current hard rule. Manifest hashes, 6/6 packaged-resource checks, native hash-bound library search and the secret-absence scan passed. Tracked source commit `92a3e19fc6b8ce65ecb9cdc9d7015c4a5655299a` produced the replacement 81,477,696-byte `AI4HEOR_0.1.39_x64.dmg` with SHA-256 `0fa3ca87c98dc810ef8617aa4416fae910a33142eeefe9719e6ba74046846d15`; independent read-only verification confirmed x86-64, OpenCode `1.17.13`, uv `0.11.26`, all 335 resources, all 177 packaged deterministic HEOR tests, isolated first launch, content-preserving legacy migration and process cleanup. The unsigned and unnotarized package remains limited to product-owner internal Intel Mac testing; product-owner acceptance and public distribution remain external gates.

2026-07-19 23:05 · fix(ai4heor): closed the mixed-language capability-review defect exposed by the packaged `0.1.39` native test and replaced the interim DMG. Candidate schema `ai4heor-skill-candidate/v2` now requires complete English and Simplified-Chinese names, descriptions, plain-language license notes, limitations, and Human acceptance checks; old or incomplete v1 candidates fail closed until regenerated. The native control revalidates and hashes all localized checks, while the desktop dialog selects one complete locale copy instead of combining Chinese controls with English review material. Full verification passed 590/590 frontend tests across 90 files, typecheck, lint and production build; 299/300 Rust tests with only the fixed-public-network test ignored; 177/177 deterministic HEOR tests; 113/113 portable HEOR contracts; 5/5 core-Skill, 10/10 product-documentation, 6/6 packaged-resource, 8/8 harness, 8/8 asset-admission, 3/3 candidate-validator, 2/2 preference-validator and 5/5 teaching-example tests; plus sidecar, Rust-security, local-evidence, run-guard, JSON, diff and secret-absence checks. Source commit `8f15a2019ec3247a5ce1e30542046f52cea8bd62` produced the 81,477,589-byte `AI4HEOR_0.1.39_x64.dmg` with SHA-256 `00382922bc37183c4509d48bc39d45d7e0b1e72810fab25032924cb22548bf96`. Independent read-only verification confirmed the thin x86-64 payload, OpenCode `1.17.13`, uv `0.11.26`, all 335 controlled resources byte-identical to source, 177 packaged HEOR tests, isolated first launch, content-preserving legacy migration, and process cleanup. A separate isolated packaged UI run visually confirmed that the Chinese candidate dialog contains Chinese checks, limitations, and license note; it did not record an activation. The package is unsigned and unnotarized and is authorized only for product-owner internal testing. This evidence does not establish product-owner acceptance, public distribution readiness, authenticated identity, automatic self-modification, scientific validity, methodological fitness, reimbursement, pricing, or policy authority.

2026-07-19 22:27 · feat(ai4heor): completed the AI4HEOR `0.1.39` source milestone as a bounded harness-engineering implementation of project capability growth and local preference learning. The app-owned capability control independently revalidates the exact candidate manifest, bilingual metadata, deny-by-default permissions, source/license note, file hashes, acceptance checks, validation report and instruction-only Skill tree before a Human can activate, reject or revoke it for the current project; same-name, changed, missing, symlinked or unmanaged content fails closed and is never overwritten or deleted. A separate native preference control accepts only language, presentation, workflow and audit proposals supported by at least two unique interactions, rejects sensitive data, secrets and scientific-authority changes, and lets the Human inspect evidence, edit wording, accept, pause, resume or delete the exact current preference. Both controls require a reviewer label, rationale and explicit confirmation, write immutable workspace snapshots, and extend owner-only app-private SHA-256 event chains; neither chain authenticates Human identity or transfers scientific authority. The natural-language Methods & Tools surface keeps forms auxiliary and exposes both review loops in all seven shipped interface languages. Full verification passed 589/589 frontend tests across 90 files, typecheck, lint and production build; 299/300 Rust tests with only the fixed-public-network test ignored; 177/177 deterministic HEOR tests; 113/113 portable HEOR contracts; 5/5 core-Skill, 10/10 product-documentation, 6/6 packaged-resource, 8/8 harness, 8/8 asset-admission, 2/2 candidate-validator, 2/2 preference-validator and 5/5 teaching-example tests; plus sidecar, Rust-security, local-evidence, run-guard, JSON, diff and secret-absence checks. This is clean source and local test evidence, not automatic self-modification, scientific validity, authenticated identity, product-owner acceptance or a verified `0.1.39` package; tracked source commit, Intel DMG rebuild, independent package verification and native packaged exercise remain pending.

2026-07-19 21:46 · chore(ai4heor): built, independently package-verified and visually exercised the Intel macOS AI4HEOR `0.1.38` internal acceptance package from tracked source commit `a6940a610e096325886f6a30434ac3d089fcc0c6`; cross-platform publication remains paused. The 81,288,293-byte `AI4HEOR_0.1.38_x64.dmg` has SHA-256 `04c4c8713a9e5e1ef1c480bca76709294fdee79db3c5f1c03044627421fc43c7`. Read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 payload, bundled OpenCode `1.17.13` and uv `0.11.26`, all 334 controlled resources byte-identical to source, all 177 packaged deterministic HEOR tests passing, and isolated fresh launch plus content-preserving legacy-workspace migration with verified process cleanup. A separate canonical `/private/tmp` native run visually confirmed the AI4HEOR mark, Simplified-Chinese `从研究问题开始` workspace, six HEOR-specific starters, and the explicit no-model/no-send boundary; the process tree and temporary test tree were then removed. The package contains no configured model credential. This package is unsigned and unnotarized and authorized only for product-owner internal testing; it does not establish product-owner acceptance, Skill-candidate activation, scientific or methodological validity, reimbursement, pricing, policy authority or public redistribution readiness.

2026-07-19 21:30 · feat(ai4heor): versioned the product-owner handoff source as AI4HEOR `0.1.38`, corrected the public status from the stale `0.1.35` package claim to the independently verified `0.1.37` Intel macOS artifact, and added an explicit English/Simplified-Chinese acceptance checklist covering brand and HEOR entry, new-project harness v2, bundled knowledge, deterministic teaching calculation, selected-provider behavior, inactive Skill candidates, Runs/Provenance, credential absence, unsigned-package handling, and known acceptance limits. The two capability-growth Skills now tell every model to invoke their validator from the exact `Base directory for this skill` reported by the runtime rather than assuming a source checkout. Real provider evidence used the product-owner-authorized MiniMax China Coding Plan API key directly from macOS Keychain without placing it in source, configuration, documentation or process output: the official models endpoint returned HTTP 200; bundled OpenCode `1.17.13` produced the exact compatibility marker through `minimax-cn/MiniMax-M2.7`; and a second isolated run created `chinese-table-note-style` from the natural-language request. Independent validation accepted the bilingual, instruction-only candidate with decision SHA-256 `2080280ab582ed1257e691a77d6a7e69db479be882a4dced2fd84e98050399e1`, `status=candidate`, all four permissions false and no activation record. Exact-key scanning found zero persistence hits across 10 compatibility-run files and 3,825 harness-run files before both isolated trees were deleted. Full verification passed 587/587 frontend tests across 88 files, typecheck, lint, 290/291 Rust tests with only the fixed-public-network test ignored, 177/177 deterministic HEOR tests, 5/5 core-Skill, 8/8 product-documentation, 6/6 packaged-resource, 8/8 harness, 8/8 asset-admission and 4/4 new validator tests, plus diff and secret-absence checks. This is clean source, real-provider and isolated harness evidence, not a bundled credential, automatic Skill activation, scientific-validity claim or final `0.1.38` package; tracked commit, Intel DMG rebuild, independent package verification and native acceptance remain pending.

2026-07-19 21:24 · chore(ai4heor): built, independently verified and natively exercised the Intel macOS AI4HEOR `0.1.37` internal test package from tracked source commit `f1858c912d5f6edbe9630274ee72e9d2e1e926a5`; Windows, Linux, Apple-Silicon and cross-platform release work remain paused. The 81,288,283-byte `AI4HEOR_0.1.37_x64.dmg` has SHA-256 `702c9ae95f7099c2af87d7433bbe3ada53b06ce0959d858502b9a911d8b9c826`. Read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 payload, bundled OpenCode `1.17.13` and uv `0.11.26`, all 334 controlled resources byte-identical to source, all 177 packaged deterministic HEOR tests passing, and isolated first launch plus legacy-workspace migration with verified process cleanup. A separate isolated native UI run visually confirmed the product-owner-provided AI4HEOR logo, the Chinese `从研究问题开始` HEOR workspace, and the natural-language `创建或评估方法工具` entry. The macOS accessibility tree confirmed the two new first-party Skills use Simplified-Chinese names and descriptions. Creating a project through the packaged UI materialized the complete harness v2 tree, `candidate_status=inactive`, the two-independent-observation preference threshold, and an empty local preference store. An initial manual launch from the symlinked `/tmp` alias correctly failed the sidecar path boundary; rerunning from canonical `/private/tmp` started the packaged app and local service normally, so no security check was relaxed. The package remains unsigned and unnotarized and is authorized only for internal testing. Public redistribution remains blocked by the unresolved `buffers@0.1.1` authoritative license, incomplete final third-party notice corpus, missing public logo/trademark clearance, and absence of a signed/notarized final-byte audit; no scientific-validity, methodological-fitness, reimbursement, pricing or policy-authority claim is made.

2026-07-19 21:12 · feat(ai4heor): implemented the AI4HEOR `0.1.37` capability-growth milestone as a bounded local harness rather than an autonomous scientific authority. New projects receive the exact `ai4heor-research-assistant-harness/v2` source tree: natural-language requests may create inactive Skill candidates under `capabilities/candidates`, while observed language, presentation, workflow or audit preferences remain inactive proposals until at least two independent interactions support them and a Human accepts them into the editable/deletable local preference store. The harness explicitly forbids self-activation, modification of core Skills, governance or deterministic calculation engines, and storage of secrets or sensitive content. Two first-party Skills add candidate authoring/validation and preference-proposal validation with fail-closed path, symlink, size, hash, metadata, authority and common-secret checks; candidate metadata and all 47 first-party Skill descriptions are localized across the seven shipped languages. The product-owner-provided AI4HEOR mark now replaces the inherited Open Science interface and application icons from a reproducible local normalized source. A repository and packaged-resource authorization audit confirms MIT core source, no distributed third-party MCP implementation, no admitted external Skill, and adds machine-readable inventories for 266 npm production packages and 565 Cargo-lock packages. Internal Intel macOS testing is permitted, but public redistribution remains blocked pending authoritative resolution or replacement of `buffers@0.1.1`, a complete final dependency/font/OpenCode/uv notice corpus, public logo and trademark clearance, and re-audit of the final signed/notarized bytes. Verification passed 587/587 frontend tests across 88 files, typecheck, lint, 290/291 Rust tests with only the fixed-public-network test ignored, 177/177 deterministic HEOR tests, 4/4 core-Skill, 7/7 product-documentation, 6/6 packaged-resource, 8/8 harness, 8/8 asset-admission, and 4/4 new candidate/preference validator tests, plus diff checks. This is clean source and governance-contract evidence; the Intel macOS `0.1.37` test package, package audit and native UI exercise remain pending.

2026-07-19 20:09 · fix(ai4heor): converted the bundled learning-library reinstall claim from a shallow directory check into byte-level native evidence after the rebuilt `0.1.36` DMG exposed a real idempotency defect. A fresh packaged UI run created an isolated HEOR project, installed all 26 controlled bundle files, produced a complete 26-document local index, and returned the expected page-1 SHA-256-bound `机会成本` hit with no model or network. Repeating the visible install action preserved every source byte but changed `heor/evidence-library.json` because installation unconditionally rebuilt the SQLite index and rebound its physical-file hash. The native installer now still verifies the complete installed bundle first, but reuses a complete, searchable, non-stale manifest/index pair byte-for-byte; it rebuilds only when the existing local evidence state is incomplete, unsearchable or stale. The strengthened Rust regression captures both manifest and index bytes before reinstall and requires both to remain exact afterwards. The full native suite passes 290/291 tests with only the fixed-public-network test ignored, and generated resource caches were removed. The DMG used to discover the defect is invalidated and must be rebuilt from the new tracked source commit before product-owner testing; this fix does not weaken edited-file rejection, stale-index recovery, Human review, or source-hash validation.

2026-07-19 19:57 · fix(ai4heor): corrected the `0.1.36` first-use wording after product-owner review so the visible boundary no longer reduces the in-product assistant to clerical file work or pushes the whole study back to the researcher. The Simplified-Chinese HEOR workspace now begins with `从研究问题开始`, asks for the target population, intervention, comparator and outcomes in customary pharmacoeconomic terms, and states the operating model directly: under the researcher's direction AI4HEOR can retrieve and organize evidence, carry out analyses and check results, while the researcher reviews the study plan, key assumptions, evidence choices and use of conclusions. The separate first-run guide uses `开始前，请先了解这四点` so it is not exposed as a duplicate accessible heading, and its Chinese and English scientific-authority copy now matches the same execute-under-direction/review-key-decisions boundary. A Chinese copy contract rejects the replaced `资料整理、分析记录和结果检查可以交给助手` sentence. Verification passed 12/12 targeted first-use, route and project tests, the complete 573/573 frontend suite across 88 files, typecheck, lint and diff checks. This is a product-copy and role-boundary correction, not a claim that model output has scientific validity or that Human review has occurred. The previously built `0.1.36` DMG predates this correction and is invalidated; the Intel macOS package must be rebuilt and reverified from the new tracked source commit before product-owner testing.

2026-07-19 19:42 · feat(ai4heor): versioned the Intel-macOS-first source milestone as AI4HEOR `0.1.36` and connected the existing deterministic local-evidence engine to a useful first-run pharmacoeconomics learning asset instead of requiring every researcher to assemble a folder before the `学习药物经济学基础` route can work. A new versioned `ai4heor-bundled-knowledge-base/v1` Simplified-Chinese bundle contains 25 Markdown sources covering navigation, basic theory, methods, working practice, dated recent progress, templates, and source/update rules; its exact sorted file inventory and SHA-256 values separate stable theory/method sections from dated material, retain Human scientific authority, and require current-source verification for policy or recent-progress use. The auxiliary `安装内置药物经济学学习库` action is available in the existing review pane without displacing the natural-language conversation. Native installation validates the packaged resource directory, manifest, safe relative paths, file caps, regular-file status and every SHA-256; copies into a versioned project library directory without a model or network call; retains the exact manifest; builds the existing project-bound SQLite index and `heor/evidence-library.json`; and records a best-effort Git snapshot. Identical reinstall is idempotent. Missing, extra, linked, oversized, conflicting or edited bytes fail closed, and an edited installed copy is preserved rather than overwritten. The deterministic Chinese query `机会成本` returns a hash-bound page-1 citation to the bundled foundational source. Chinese and English starter, install, success and known-failure copy explains the dated/current-source boundary without exposing raw engine text. Verification passed 573/573 frontend tests across 88 files, 177/177 deterministic HEOR tests, 290/291 Rust tests with only the fixed-public-network test ignored, 7/7 product-documentation contracts, 4/4 packaged-resource contracts including all internal knowledge-base links, typecheck, lint, production build, teaching-example and harness contracts, JSON and diff checks. This is source, packaging-contract and deterministic local-search evidence, not native packaged installation, completeness of the literature, current policy verification, methodological fitness, scientific validity, reimbursement or policy-authority evidence. The `0.1.36` Intel macOS package remains pending clean tracked source commit, build, independent verification and native install/search/edit-preservation exercise; Windows, Linux, Apple-Silicon and cross-platform release work remain paused.

2026-07-19 19:15 · chore(ai4heor): built, independently verified and natively exercised the final Intel macOS AI4HEOR `0.1.35` test package from clean tracked source commit `ad9f139975cef9be943cb23b702fd79bdcfe0592`; Windows, Linux, Apple-Silicon and cross-platform release work remain paused. The 80,139,562-byte `AI4HEOR_0.1.35_x64.dmg` has SHA-256 `11d1e4eb8924d1834003a78042b78e4befdce4b25ee2ecc1ca0253e449654003`. Read-only-mounted verification confirmed AI4HEOR identity/version, all 286 controlled resources byte-identical to source, all 177 packaged deterministic HEOR tests passing, and isolated first launch plus legacy-workspace migration with verified process cleanup. A separate isolated native UI run selected the Chinese `运行成本-效果教学示例`, installed the governed example, kept its natural-language request editable and unsent, required the explicit `运行固定计算` confirmation, and completed without a configured model. The result card reported incremental cost CNY 28,241.08 per person, incremental QALYs 0.403709, ICER CNY 69,954.05/QALY and the declared one-way cost sensitivity range CNY 43,962.59–95,945.51/QALY. The base, low and high JSON files had SHA-256 `8daa5b3936b905d269748ac144370decc87f9c7fc7941cf49769efdb04253604`, `67ffb9010a72a01e3bdc565c6e2fef877959a43fac2d55b3a40502f2081c32a1` and `acda25de08717569a0ea6c1fa4f140c55a0e8c11909d706232653bda9894d50d`; all retained a null cost-effectiveness claim. Run `run_8f3d073ddfbfce44` recorded local surface, no model field, three outputs and three linked Provenance entries. After the installed model-input CSV was deliberately changed, a second run failed with zero outputs, the three prior file hashes and Provenance count remained unchanged, and the visible message used direct Chinese wording explaining that AI4HEOR preserved the change and would not treat it as the built-in teaching case. Distribution trust remains deliberately unclaimed: strict `codesign` reports `code object is not signed at all`, and Gatekeeper rejects it with `source=no usable signature`. This is an internal Intel macOS test package and deterministic teaching calculation, not Developer-ID signing/notarization, third-party user acceptance, complete translation certification, methodological fitness, scientific validity, an official Chinese threshold, cost-effectiveness, reimbursement, pricing or policy-authority evidence.

2026-07-19 18:48 · feat(ai4heor): versioned the Intel-macOS-first source milestone as AI4HEOR `0.1.35` and closed the model-configuration gap in the synthetic cost-effectiveness starter without making forms or the Agent the scientific control surface. Selecting the existing starter still installs the governed case and leaves a natural-language request editable and unsent; a separate auxiliary `Run locally` action now requires explicit researcher confirmation, verifies the installed `run_analysis.py`, analysis specification, model-input CSV and expected-result bytes against the bundled resources, then executes the fixed base case plus the declared CNY 14,400 and CNY 21,600 stable-state-cost sensitivity values without a configured model or model-provider disclosure. The native boundary rejects changed or linked governed inputs, linked output directories, unsupported or claim-bearing results, oversized output/log data and conflicting existing result bytes; it never overwrites a differing file. A successful run writes three local JSON results, displays base-case incremental cost, incremental QALYs, ICER and the low-to-high ICER range, and reuses the existing local Runs, Provenance and best-effort Git snapshot records. Chinese and English confirmation, progress, result and failure framing explicitly identify synthetic teaching assumptions, local-only execution, numerical reproducibility and the absence of cost-effectiveness or policy conclusions; known input, output and Python failures use localized researcher-facing explanations instead of exposing raw engine text. Verification passed 572/572 frontend tests across 87 files, 177/177 deterministic HEOR tests, 288/289 Rust tests with only the fixed-public-network test ignored, 6/6 product-documentation contracts, typecheck, lint, production build, JSON and diff checks. This is source and deterministic-reproduction evidence, not native packaged execution, scientific or model validity, an official Chinese threshold, cost-effectiveness, reimbursement, pricing or policy-authority evidence. The `0.1.35` Intel macOS package remains pending clean-source commit, build, independent verification and native confirmation-flow exercise; Windows, Linux, Apple-Silicon and cross-platform release work remain paused.

2026-07-19 18:14 · chore(ai4heor): built, independently verified and natively exercised the Intel macOS AI4HEOR `0.1.34` test package from clean tracked source commit `37cb7d4c502357d49e19a8532a39eb244d8c1d3f`; Windows, Linux, Apple-Silicon and cross-platform release work remain paused. The 80,116,453-byte `AI4HEOR_0.1.34_x64.dmg` has SHA-256 `4596f631cd3fafbd5c959cb95a58cc3c14eddeb54f85a7f0272f679f7fcd7476`. Read-only-mounted verification confirmed AI4HEOR identity/version; thin x86-64 main, bundled OpenCode `1.17.13` and uv `0.11.26`; all 286 controlled resources byte-identical to source; all 177 packaged HEOR-core tests passing; and isolated fresh plus legacy-migration LaunchServices starts with one app process, one bundled OpenCode child and verified cleanup. A separate isolated native UI run visually confirmed the Chinese `先说清楚研究问题` surface with six HEOR-only starters. Selecting `运行成本-效果教学示例` installed all six governed example files into the local workspace, left the complete Chinese request editable and unsent with `暂无对话` still visible, and the installed runner independently matched expected result SHA-256 `8daa5b3936b905d269748ac144370decc87f9c7fc7941cf49769efdb04253604`. Distribution trust remains deliberately unclaimed: strict `codesign` reports `code object is not signed at all`, and Gatekeeper rejects it with `source=no usable signature`. This is an internal Intel macOS test package, not Developer-ID signing/notarization, third-party user acceptance, complete translation certification, methodological fitness, scientific validity, an official China threshold, cost-effectiveness, reimbursement, pricing or policy-authority evidence.

2026-07-19 18:06 · feat(ai4heor): versioned the Intel-macOS-first source milestone as AI4HEOR `0.1.34` and made the bundled cost-effectiveness teaching case a model-provider-independent, deterministic calculation rather than an Agent-improvised demonstration. The default Chinese and English HEOR workspace now exposes a sixth research starter that installs `heor-cost-effectiveness` locally without overwriting existing files, prepares an editable unsent request, explains that every value is a teaching assumption, and requires explicit Human confirmation before execution. The dependency-free standard-library runner validates the exact specification and CSV hashes, preserves cohort mass, applies declared trapezoidal state occupancy plus end-cycle discounting, emits costs, QALYs, life-years, ICER, illustrative net monetary benefit and bounded one-way intervention-cost sensitivity, and binds the runner/spec/input hashes into a byte-stable expected result; `expected/base-case-result.json` has SHA-256 `8daa5b3936b905d269748ac144370decc87f9c7fc7941cf49769efdb04253604` and deliberately leaves `cost_effectiveness_claim` null. Browser-level Chinese interaction confirmed all six HEOR-only starters and that selecting the example fills but does not send the request. Verification passed 569/569 frontend tests across 87 files, 177/177 deterministic HEOR tests, 15/15 teaching-example/product-documentation/core-Skill contracts, typecheck, lint, production build, and 286/287 Rust tests with only the fixed-public-network test ignored. A parallel Rust test failure was traced to same-nanosecond temporary-workspace collisions, fixed with process/time/atomic uniqueness, and then passed five consecutive 16-thread module runs plus the complete suite. This is deterministic-reproduction and interface evidence, not scientific validity, a China official threshold, cost-effectiveness, reimbursement, pricing or policy-authority evidence. The `0.1.34` macOS package remains pending clean-source commit, build, independent package verification and native visual exercise; Windows, Linux, Apple-Silicon and cross-platform release work remain paused.

2026-07-19 17:39 · chore(ai4heor): built, natively exercised and visually checked the Intel macOS AI4HEOR `0.1.33` test package from clean tracked source commit `a43650b40ae7d2ba2d4bc6e2847933032f7d1902`; Windows, Linux, Apple-Silicon and cross-platform release work remain paused. The final 80,110,025-byte `AI4HEOR_0.1.33_x64.dmg` has SHA-256 `38985b9f541a4067c5dab3754201730fc7c804a6ffa805b5649b15806f4d0de5`. The first package attempt correctly failed independent verification because test-created Python caches had entered four bundled Skill directories; the caches were removed, the package was rebuilt without relaxing the verifier, and the rejected intermediate DMG was overwritten. Read-only-mounted verification then confirmed AI4HEOR identity/version; thin x86-64 main, bundled OpenCode `1.17.13` and uv `0.11.26`; all 283 controlled resources byte-identical to clean source with no Python caches; all 177 packaged HEOR-core tests passing; and isolated fresh plus legacy-migration launches with one app process, one bundled OpenCode child and verified cleanup. A separate isolated native UI run showed the Chinese HEOR starter surface with `先说清楚研究问题`, five pharmacoeconomics tasks, the local-model boundary and natural researcher-facing copy, with no inherited generic Open Science demonstration. Distribution trust remains deliberately unclaimed: strict `codesign` reports `code object is not signed at all`, and Gatekeeper rejects it with `source=no usable signature`. This is an internal Intel macOS test package, not Developer-ID signing/notarization, third-party user acceptance, complete translation certification, methodological fitness, scientific validity, reimbursement or policy-authority evidence.

2026-07-19 17:11 · feat(ai4heor): versioned AI4HEOR `0.1.33` and rewrote the Simplified-Chinese desktop surface as direct researcher-facing language grounded in common Chinese pharmacoeconomics usage, without changing scientific authority or turning the workbench into a form-led product. First use now starts from the research question and states in plain Chinese that AI4HEOR can organize evidence, run analyses and check results while the researcher decides methods, evidence and interpretation; the five starters now cover pharmacoeconomics learning, cost-effectiveness analysis design, literature search, model-parameter evidence and analysis-plan review. Navigation, settings, review panes, provenance, runs, notebooks, tools and all 45 first-party Skill descriptions replace inherited Agent/Human, runtime, audit, artifact and promotional phrasing with concrete task language and established terms including 评价视角, 时间范围, 健康产出, 成本-效果分析, 成本-效用分析 and 预算影响分析. Previously hard-coded English download, file-manager, Jupyter, notebook and generated-assistant prompts now use locale resources across all seven shipped languages; a new recursive Chinese-copy contract rejects exposed English role labels, `代理` and broken spacing, while the Skill contract proves every bundled Skill has non-fallback metadata in every shipped locale. Verification passed 565/565 frontend tests across 86 files, 177/177 deterministic HEOR tests, 9/9 core-Skill/product-documentation contracts, typecheck, lint, production build, JSON parity and diff checks. The five non-Chinese advanced HEOR panel namespaces still use their established English fallback and are not claimed as fully translated; this milestone is Chinese product-copy and shared-prompt localization evidence, not translation certification, methodological fitness, scientific validity, reimbursement or policy-authority evidence. Windows, Linux, Apple-Silicon and cross-platform release work remain paused during Intel macOS product acceptance.

2026-07-19 16:34 · chore(ai4heor): built and natively verified the Intel macOS AI4HEOR `0.1.32` test package from clean tracked source commit `2bd1bea0de2fb151c8f11a57b28600223eee34ce`, while Windows, Linux, Apple-Silicon and cross-platform release work remain paused. The 80,106,479-byte `AI4HEOR_0.1.32_x64.dmg` has SHA-256 `2bbac3379a826be022a0467255707187ac1182dbc0e0bfed177e70b2203d83c2`. Two sandboxed DMG attempts failed at Tauri's Finder-layout step; removing three stale read-only mounts did not change the failure, while the explicitly authorized desktop-automation build succeeded without changing bundle rules, establishing missing Finder automation permission as the packaging blocker. Independent read-only-mounted verification confirmed AI4HEOR identity/version; thin x86-64 main, OpenCode `1.17.13` and uv `0.11.26`; all 283 controlled resources byte-identical to source with aggregate SHA-256 `f80558ad35ada992d4695ae84b9719e84b47ee2baf5a28d675dbf607aff2fd23`; no resource links or generated Python caches; and all 177 packaged HEOR-core tests passing. Two isolated LaunchServices runs proved fresh `Documents/AI4HEOR` creation and content-preserving `Documents/OpenScience` migration, each with exactly one copied app process, one child bundled OpenCode process and verified cleanup. The generated `ai4heor-release-evidence/v1` record independently binds the exact clean commit, DMG, sidecars, resource inventory and nine checks. Distribution trust remains deliberately unclaimed: `codesign` reports `code object is not signed at all`, strict signature verification fails, and Gatekeeper rejects the app with `source=no usable signature`. This is internal macOS test readiness, not Developer-ID signing/notarization, third-party user acceptance, accessibility acceptance, authenticated-Human identity, methodological fitness, scientific validity, reimbursement, or policy-authority evidence.

2026-07-19 12:03 · feat(ai4heor): versioned AI4HEOR `0.1.32` and replaced soft/best-effort new-project harness installation with a fail-closed, model-provider-neutral research-assistant contract while deliberately pausing Windows, Linux, Apple-Silicon and cross-platform release work until the Intel macOS path is accepted. Every new HEOR project now requires the exact eight-file harness tree, including versioned `ai4heor-research-assistant-harness/v1` `policy.json`; the machine contract fixes natural-language-primary interaction, exclusive Human scientific leadership and provider selection, no silent provider fallback, model output as draft pending Human review, deterministic versioned code as calculation authority, app-owned approvals, unknown default data classification, and external/model/tool/MCP content as untrusted data rather than instructions. Native validation rejects missing, extra, empty, altered-policy or linked source resources; target files, root and parent-directory symlinks; and missing required Human-authority text. It copies only missing files, verifies every newly seeded byte, and preserves existing Human-authored files. Both typed-project and dated-workspace creation now initialize and verify the contract before Git snapshot or active-workspace persistence; failure is visible and removes only the newly created incomplete directory. Verification passed 7/7 Python harness contracts, 5/5 Rust positive/adversarial harness tests within 286/287 passing Rust tests (the fixed-public-network test remains explicitly ignored), 4/4 core-Skill and 5/5 product-documentation contracts, 177/177 deterministic HEOR tests, 555/555 frontend tests across 85 files, typecheck, lint, production build, and strict Clippy with only three established unrelated lint classes explicitly allowed. This is a verified source milestone; the previously verified `0.1.31` x64 DMG remains the current macOS test package until a clean-commit `0.1.32` package is built and natively exercised. No Developer-ID signing, notarization, Gatekeeper acceptance, scientific validity, reimbursement, or policy-authority claim is made.

2026-07-18 · docs(ai4heor): replaced the remaining public Open Science capability drift across all seven localized READMEs with the shipped AI4HEOR surface. English, Simplified Chinese, Japanese, Spanish, German, French and Korean now lead with Human-directed, natural-language-first HEOR work; show the current `0.1.31` first-use, HEOR starter and editable cost-effectiveness-request screenshots; describe representative local-evidence, Human-authorized search, model-design, deterministic-model, uncertainty/VOI, budget-impact, validation, reporting and reproducibility Skills in each language; and state that all 45 exact first-party `$skill-id` entries have localized names and descriptions. The inherited autonomous `ai4s-agent` pipeline, generic scientific screenshot set, Materials/FRED/weather/water one-click connector claims, `Open Science.app` recovery path, and default build-time external-Skill fetch were removed from these product pages. The documented connector boundary now matches the app: fixed-endpoint PubMed/ClinicalTrials.gov metadata access only after Human authorization, Jupyter as the sole managed one-click local tool, researcher-added MCPs explicitly unmanaged, and external Skill downloads only an optional non-bundled review cache. A new five-check cross-platform documentation contract binds all seven READMEs to the 45 bundled Skills, seven locale catalogs, three current HEOR screenshots, governed connector terms, retired-default denylist and upstream-benchmark authority disclaimer; both build workflows run it and HEOR-core CI now triggers on localized README changes. The new 5/5 documentation tests and existing 4/4 core-Skill localization/leadership tests pass. This is product-surface accuracy evidence, not translation certification, scientific validity, platform signing or native cross-platform acceptance.

2026-07-18 · chore(ai4heor): cross-built and bounded the current AI4HEOR `0.1.31` Apple Silicon package to the same clean tracked source commit `2834785e057ac54477a9633f07390bc173251644` as the x64 macOS package, without weakening the native release gate. The 76,095,510-byte `AI4HEOR_0.1.31_aarch64.dmg` has SHA-256 `86b0583e36480affb90ec08b84d8c4276ec702b92e69ef90894f58c2888da42e`. Independent read-only-mounted inspection confirmed the AI4HEOR identity/version; thin arm64 main, OpenCode and uv executables; packaged OpenCode/uv SHA-256 values exactly equal to the pinned arm64 source sidecars; all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`; no linked or generated-cache resource; and all 177 packaged HEOR-core tests passing against the mounted core. The strict native verifier correctly stopped at the packaged OpenCode version command with `Errno 86: Bad CPU type in executable` on this Intel host, detached the DMG, and emitted no formal `ai4heor-release-evidence/v1` JSON. Signing inspection reports only an ad-hoc linker signature, no Team ID, no sealed resources and no stapled ticket; strict `codesign` and Gatekeeper assessment fail. This is bounded cross-host package-content evidence, not native Apple-Silicon execution or first start, Developer-ID signing, notarization, Gatekeeper acceptance, third-party-machine/accessibility acceptance, authenticated-Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy-authority evidence.

2026-07-17 18:18 · chore(ai4heor): bound the keyboard/contrast source milestone to a locally built and strictly verified AI4HEOR `0.1.31` x64 macOS DMG from clean tracked commit `2834785e057ac54477a9633f07390bc173251644`. The 80,111,236-byte `AI4HEOR_0.1.31_x64.dmg` has SHA-256 `eac573e5c36073c3150ad4a99d20396c9ee7a4c647a259a66311507dba47c580`. Independent read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`, no linked or generated-cache resource, and all 177 packaged HEOR-core tests passing. Two isolated LaunchServices runs proved fresh `Documents/AI4HEOR` creation and content-preserving `Documents/OpenScience` marker migration; each observed exactly one copied app process and one child bundled OpenCode process and verified cleanup. Schema `ai4heor-release-evidence/v1` independently re-verifies the exact DMG, source commit, nine named checks, resource inventory, sidecars, process proof and both workspace outcomes. The strict distribution verifier separately reran all 177 packaged tests and then failed closed at `codesign --verify --deep --strict` because the app is not signed. No Developer-ID, hardened-runtime, notarization, stapled-ticket, Gatekeeper-acceptance, native Apple-Silicon/Windows, current-version Linux, four-target manifest, third-party-machine, assistive-technology, authenticated-Human, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement or policy-authority claim is made.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.31` and closed the first product-wide keyboard/contrast boundary without changing the Human-led, natural-language-first HEOR flow. A fresh seven-language shell now exposes a focus-only “skip to HEOR workspace” link before the sidebar and makes the main region programmatically focusable; every interactive element receives a visible two-pixel product-level focus indicator even when a component otherwise uses only a subtle border change. The light-theme supporting-text token moved from `#8c877d` to `#746f67` and the accent from `#c15f3c` to `#ad5335`: live computed-style checks at the desktop viewport measured 4.99:1 for guide text on cards, 4.57:1 for HEOR supporting text on the paper background, and 5.17:1 for white small text on the primary button. Current-run screenshots and DOM inspection confirmed that the guide still states local storage, model choice, reviewable actions and exclusive Human scientific authority; continuing reveals the five HEOR natural-language starters; selecting cost-effectiveness only focuses an editable draft and does not send it. The skip link visibly enters at keyboard focus and activation moves focus to `#ai4heor-main`. Full verification passed 555/555 frontend tests across 85 files, including an automated contrast-token contract; 177/177 deterministic HEOR tests; 281/282 Rust tests with the fixed-public-network test ignored; 146/146 selected release/resource/Skill/harness/artifact contracts; typecheck; lint; production build; strict Clippy with only the four established unrelated lint classes allowed; and JSON/diff/cache checks. Rust 1.97 also surfaced one over-precise microsimulation test literal, which was normalized to its exact representable `f64` spelling without changing the value. No full WCAG, screen-reader, switch-control, zoom/reflow, physical-user, native-package, scientific-validity, reimbursement or policy-authority claim is made.

2026-07-17 · chore(ai4heor): closed the current-version x86_64 Linux package and legacy-workspace migration gate for AI4HEOR `0.1.30` from the same clean tracked source commit `0beb7b2bcb04a796f256bd8f8528bb787aa77319` used by the macOS packages. An isolated Ubuntu 22.04 builder used pinned OpenCode `1.17.13` and uv `0.11.26`, passed 177/177 deterministic HEOR tests, 281/282 Rust tests with the sole fixed-public-network test ignored, the bounded NMA/MAIC/RWE/semi-Markov and release-contract suites, strict Clippy with only four confirmed pre-existing lint classes allowed, typecheck, lint, production build, and 546/546 frontend tests across 83 files before Tauri packaging. The 96,467,274-byte `AI4HEOR_0.1.30_amd64.deb` has SHA-256 `56540571d7b3339b051041c731c06322357352af89798515469665d66502fb5e`; the 89,279,509-byte `AI4HEOR-0.1.30-1.x86_64.rpm` has SHA-256 `ae61c0644430281471f45ce3d980e2784c7eeffb98f033d6d9b182b4643ca51a`. Independent extraction verified exact identity/version/architecture, x86-64 ELF main/OpenCode/uv binaries, DEB/RPM payload parity apart from the expected Tauri bundle marker, no links or generated caches, all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`, and all 177 packaged HEOR tests independently against each format. Schema `ai4heor-release-evidence/v1` binds both exact packages, source commit, resource inventory and five named checks and independently re-verifies. A brand-new Ubuntu 22.04 non-root headless start proved version `0.1.30`, exactly one desktop process, one bundled OpenCode process and fresh `~/Documents/AI4HEOR`; a separate Fedora 42 RPM run preserved an exact seeded marker while migrating `~/Documents/OpenScience` to `~/Documents/AI4HEOR`, removed the legacy root, and observed the same single-process boundary. Expected container portal/FUSE/PipeWire warnings are environmental and do not establish physical Linux visual or accessibility acceptance. No Windows or native Apple-Silicon first start, four-target manifest, signing/notarization, third-party-machine acceptance, authenticated-Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy-authority claim is made.

2026-07-17 · chore(ai4heor): cross-built the current AI4HEOR `0.1.30` Apple Silicon DMG from the same clean tracked source commit `0beb7b2bcb04a796f256bd8f8528bb787aa77319` on an Intel Mac without weakening the native release gate. The 76,097,859-byte `AI4HEOR_0.1.30_aarch64.dmg` has SHA-256 `593a0037d5fb125aedb8138f08ce7767197c3c8bad9f0872ce7119417b2ac469`. Read-only-mounted inspection confirmed the AI4HEOR identity/version; thin arm64 main, OpenCode and uv executables; packaged OpenCode/uv bytes identical to their repo-pinned arm64 sidecars; all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`; and all 177 HEOR-core tests passing against the mounted core. The strict verifier correctly stopped with `Errno 86: Bad CPU type in executable` when this x86-64 host attempted to execute the arm64 OpenCode sidecar, so it emitted no formal arm64 release-evidence JSON and no native first-start claim is made. `codesign` reports only an ad-hoc linker signature with no Team ID or sealed resources; strict verification and Gatekeeper assessment fail. This is bounded cross-host package-content evidence, not Apple-Silicon execution, Developer-ID signing, notarization, Gatekeeper acceptance, third-party-machine acceptance, accessibility acceptance, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy-authority evidence.

2026-07-17 · chore(ai4heor): bound the Human-led first-use milestone to a locally built and strictly verified AI4HEOR `0.1.30` x64 macOS DMG from clean tracked source commit `0beb7b2bcb04a796f256bd8f8528bb787aa77319`. The 80,113,561-byte `AI4HEOR_0.1.30_x64.dmg` has SHA-256 `22d8f055ab4df914df83a076e0c0f63ae2df13ebe9e169ea92967ca09bf2d69c`. Independent read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`, no linked or generated-cache resource, and all 177 packaged HEOR-core tests passing. Two isolated LaunchServices runs proved fresh `Documents/AI4HEOR` creation and content-preserving `Documents/OpenScience` marker migration; each observed exactly one copied app process and one child bundled OpenCode process and verified cleanup. Schema `ai4heor-release-evidence/v1` independently re-verifies the exact DMG, source commit, nine named checks, resource inventory, process proof and both workspace outcomes. The package preserves the non-form first-use boundary and remains local/ad hoc: `codesign` reports `code object is not signed at all` for x86-64 and Gatekeeper rejects it with `source=no usable signature`. No native Apple-Silicon, Windows, current Linux, third-party-machine, accessibility, authenticated-Human, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement or policy-authority claim is made.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.30` and closed the missing first-use control boundary without turning onboarding or research into a form-led workflow. A fresh local profile now sees one compact guide above the still-visible HEOR natural-language entry; it explains local-by-default project storage, researcher-selected model providers/local endpoints, send-time content transfer, the default approval boundary for destructive/install/network/remote/privileged actions, and the Human researcher's exclusive authority over methods, evidence, assumptions, interpretation, validation and release. The guide contains no fields, selects no model or scientific choice, starts no Agent turn, and keeps all HEOR starters and the composer available; its `ai4heor.onboarding.v1` completion marker is only a local UI preference, not consent, identity, approval or release evidence, and opening model/privacy settings does not complete it. English, Simplified Chinese, Japanese, Spanish, German, French and Korean ship native copy. Real in-app-browser inspection at the desktop viewport confirmed the guide and natural-language entry coexist without clipping, confirmation reveals the full HEOR starter surface, no form exists in either state, and the console has zero errors. Verification: 546/546 frontend tests across 83 files, 177/177 deterministic HEOR tests, 281/282 Rust tests with the fixed-public-network test ignored, 200/200 combined portable/local-evidence/method-watchlist/NMA/MAIC/RWE/microsimulation/admission/Skill/harness/resource/sidecar/release/distribution contracts, typecheck, lint, production build, strict Clippy with only four confirmed pre-existing unrelated lint classes allowed, JSON/diff/cache checks. The version-bump audit caught and reversed an invalid mechanical change to the unrelated locked `schannel 0.1.29` dependency before Rust validation. This is a verified source milestone; no `0.1.30` installer, native Apple-Silicon/Windows/current-Linux evidence, Developer-ID signing, notarization, Gatekeeper acceptance, third-party-machine/accessibility acceptance, authenticated Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy-authority claim is made yet.

2026-07-17 · chore(ai4heor): bound the HEOR-first entry and runtime-branding boundary to a locally built and strictly verified AI4HEOR `0.1.29` x64 macOS DMG from clean source commit `b1a177a26d0d1c2f0d51a637e2a9c511faa9784e`. The 80,151,210-byte `AI4HEOR_0.1.29_x64.dmg` has SHA-256 `66fcafc57ed29cbe0eb6a29be550d410010b37aa4e88d59ac413741e16b934b3`. Independent read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 282 controlled resources byte-identical to source with aggregate SHA-256 `94c80aa04d6f18661b1444c87396a9619b98a02952dde17929654f600ce13bad`, no linked or generated-cache resource, and all 177 packaged HEOR-core tests passing. Two isolated LaunchServices runs proved fresh `Documents/AI4HEOR` creation and content-preserving `Documents/OpenScience` marker migration; each observed exactly one copied app process and one child bundled OpenCode process and verified cleanup. Schema `ai4heor-release-evidence/v1` binds the exact DMG, clean source commit, nine named checks, resource inventory, process proof and both workspace outcomes. The engine remains an internal implementation boundary: ordinary research and closed-Settings surfaces expose only the localized AI-assistant state, while OpenCode identity and endpoint require an explicit Advanced diagnostics action. The app is not signed or notarized: `codesign` reports `code object is not signed at all` for x86-64 and Gatekeeper rejects it with `source=no usable signature`. No native Apple-Silicon, Windows, current Linux, third-party-machine, accessibility, authenticated-Human, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement or policy-authority claim is made.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.29` and made the HEOR project—not the inherited generic Open Science session—the true first product action. `/` now enters `/heor`; “New HEOR project” creates version-2 metadata with `kind: "heor"`, seeds the researcher-led harness, switches to the project workspace, keeps project-owned sessions under `/heor/:sessionId`, and places a localized unsent natural-language intake draft in the composer. The draft asks the Human researcher to define the decision/research/learning question and intended use; creation neither sends a model turn nor chooses scientific scope, method, evidence, assumptions, threshold, interpretation or approval. Legacy version-1 project markers remain readable as HEOR projects, while explicit non-HEOR markers fail closed for app-owned HEOR commands and legacy loose sessions remain readable under `/live`. The bundled OpenCode engine is now treated as implementation rather than product identity: the HEOR conversation and disconnected state expose only localized AI-assistant readiness and recovery, while engine name and local endpoint are available solely after opening Settings → Advanced diagnostics; model-provider names remain truthful. English, Simplified Chinese, Japanese, Korean, German, Spanish and French navigation, session, Settings and runtime-capability copy follow the same boundary. Real headed browser inspection confirmed English and Chinese HEOR-first screens, zero OpenCode/command/port leakage on the default research and closed-Settings surfaces, and exact engine/endpoint visibility after diagnostics expansion. Verification: 545/545 frontend tests across 83 files, 177/177 deterministic HEOR tests, 281/282 Rust tests with the fixed-public-network test ignored, 113/113 portable artifact contracts, all local-evidence/method-watchlist/NMA/MAIC/RWE/microsimulation/admission/Skill/harness/resource/sidecar/release/distribution contracts, typecheck, lint, production build, strict Clippy with only four confirmed pre-existing unrelated lint classes allowed, JSON/diff/cache checks. This is a verified source milestone; no `0.1.29` installer, native Apple-Silicon/Windows/current-Linux evidence, Developer-ID signing, notarization, Gatekeeper acceptance, third-party-machine/accessibility acceptance, authenticated Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy-authority claim is made yet.

2026-07-17 · chore(ai4heor): bound the governed-connector milestone to a locally built and strictly verified AI4HEOR `0.1.28` x64 macOS DMG from clean source commit `5b4afb3077c8754ecba40c1ca9820ea41c7139f8`. The first package was correctly rejected because full Python regression had generated `__pycache__` files inside controlled HEOR Skill resources; those generated caches and the failed bundle were removed from the build path, the app was rebuilt with bytecode generation disabled, and no verifier rule was weakened. The final 80,106,940-byte `AI4HEOR_0.1.28_x64.dmg` has SHA-256 `5eaef04224e3a71d6ccae57f5791cdd547edba5f743a9b912a51d317268f3962`. Independent read-only-mounted verification confirmed AI4HEOR identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 282 controlled resources byte-identical to source, no linked or generated-cache resource, and all 177 packaged HEOR-core tests passing. Two isolated LaunchServices runs proved fresh `Documents/AI4HEOR` creation and content-preserving `Documents/OpenScience` marker migration; each observed exactly one copied app and one child bundled OpenCode process and verified cleanup. Schema `ai4heor-release-evidence/v1` binds the exact DMG, clean source commit, nine named checks, resource inventory, process proof and both workspace outcomes. The app is not signed or notarized: `codesign` reports no signature and Gatekeeper rejects it with `source=no usable signature`. No native Apple-Silicon, Windows, current Linux, third-party-machine, accessibility, authenticated-Human, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement or policy-authority claim is made.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.28` and replaced the inherited generic Open Science one-click MCP catalog with a governed HEOR capability boundary. Settings now presents the first-party `$heor-evidence-search` route as fixed-endpoint PubMed/ClinicalTrials.gov metadata retrieval with an exact request hash and Human-only network authorization, keeps Jupyter as the only one-click managed local computation server, and labels researcher-added local/remote MCP servers as unmanaged external capabilities without scientific or approval authority. Existing configured legacy MCP entries remain visible, runnable and removable on upgrade; new installations no longer offer Paper Search, BioMCP, Materials Project, FRED, space-weather, Open-Meteo or USGS as product defaults. The arbitrary Python MCP provisioner, its Rust command surface and the stale curated catalog were removed. English, Simplified Chinese, Japanese, Korean, German, Spanish and French settings copy now describes the HEOR evidence source and external boundary; the previously shipped seven-language first-party Skill name/description contract remains intact. Paper Search MCP revision `c8b642183bb725f0a7faec89e58b558df09079d1` and BioMCP revision `b586085ac032f079671ede0c08cbf17816183bd3` are now individually registered as non-distributed quarantined candidates: the former has broad multi-host search/full-text/download and optional Sci-Hub routing, while the latter's inherited package/launch definition was stale and its current broad biomedical source/credential surface still lacks per-tool rights, egress, approval, adversarial, cross-platform and kill-switch evidence. The registry therefore contains 14 reviewed external assets, 0 admitted, 10 quarantined and 4 rejected. Verification: 543/543 frontend tests across 83 files, 177/177 deterministic HEOR tests, 280/281 Rust tests with the fixed-public-network test ignored, 8/8 asset-admission tests, 3/3 packaged-resource tests, typecheck, lint, production build, JSON/diff checks, and headed English/Chinese Settings inspection with 0 console errors. Codex built and verified the software; inside AI4HEOR the Human researcher still leads the study and owns methods, evidence, assumptions, interpretation and approval.

2026-07-17 · chore(ai4heor): bound the public workspace-identity milestone to a locally built and strictly verified AI4HEOR `0.1.27` x64 macOS DMG from clean source commit `fc7657d109728bff89bd647387e5381e26d77fc7`. The 80,131,277-byte `AI4HEOR_0.1.27_x64.dmg` has SHA-256 `c35572e6490683b70eb825535dcd916b95b299c8708eb7711ff29064e7331196`. Independent read-only mounted verification confirmed AI4HEOR identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 282 controlled resources byte-identical to source with aggregate SHA-256 `a343d205009a5aaed6e2e75bbb2ed5016de836ef63372ff6ce66382398db3f89`, no admitted payload drift, and all 177 packaged HEOR-core tests passing. The verifier copied the exact mounted app into an isolated temporary Applications directory and executed two native LaunchServices gates without touching the researcher's real HOME: a fresh HOME created `Documents/AI4HEOR`; a second HOME seeded with `Documents/OpenScience/2026-07-17-legacy/marker.txt` preserved the exact marker under `Documents/AI4HEOR`, removed the legacy root, and avoided a parallel tree. Each gate observed exactly one copied app process and one child bundled OpenCode process and verified cleanup. Schema `ai4heor-release-evidence/v1` binds the exact DMG, clean source commit, nine named checks including `workspace-migrated`, resource inventory, content proof, process proof, and both workspace outcomes, and independently re-verifies against the artifact. Packaging/notarization inspection correctly found `code object is not signed at all` and Gatekeeper rejection `source=no usable signature`; this is an ad hoc local artifact, not a distribution-ready release. No native Apple-Silicon, Windows, current Linux, Developer-ID, hardened-runtime, notarization, stapled-ticket, Gatekeeper-acceptance, third-party-machine, visual/accessibility, authenticated-Human, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement, or policy-authority claim is made.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.27` and removed the remaining public default-workspace dependency on the Open Science platform name. Fresh installs now create `~/Documents/AI4HEOR`; when that target is absent, an existing default `OpenScience`, older `Open Science`, or app-private legacy workspace is atomically renamed, an active dated-workspace pointer below it is rewritten to the same relative child, and a failed rename reuses the legacy root instead of silently creating a parallel empty tree. An explicit Human-selected base folder still wins, and when both AI4HEOR and a legacy root already exist the runtime neither merges nor deletes either. Hidden `.openscience` metadata, the `com.ai4s.workbench` application identifier, and existing local settings keys remain deliberate storage-protocol compatibility boundaries. Current and legacy workspace paths are both de-noised in conversation logs; visible local Git authorship, update-check user agent, runtime diagnostics, install commands, citation metadata, product/technical documentation, and macOS/Windows first-launch expectations now name AI4HEOR. The macOS package verifier was strengthened from one fresh launch to two isolated LaunchServices scenarios: a fresh HOME must create `Documents/AI4HEOR`, then a separate HOME seeded with `Documents/OpenScience/.../marker.txt` must migrate the exact marker, remove the legacy root, run exactly one copied app and one child bundled OpenCode process, and clean both processes. Release evidence now binds this as an explicit macOS `workspace-migrated` check. Verification: 4 new native migration/failure/precedence tests within 282/283 passing Rust tests (one fixed-public-network test ignored), 553/553 frontend tests across 83 files, typecheck, lint, production build, 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, local-evidence/method-watchlist/NMA/MAIC/RWE/microsimulation/admission/Skill/harness/resource/sidecar/release/distribution contracts, JSON/YAML/Python/rustfmt/diff/cache checks. This source milestone does not yet claim a `0.1.27` installer, current Windows/Linux/Apple-Silicon package behavior, Developer-ID signing, notarization, Gatekeeper acceptance, third-party-machine or accessibility acceptance, authenticated Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement, or policy authority.

2026-07-17 · chore(ai4heor): bound the HEOR-focused delivery-surface milestone to a locally built and strictly verified AI4HEOR `0.1.26` x64 macOS DMG from clean source commit `d864d47cf19eec51d18b07d5ccb9742162c1bafa`. The 80,145,287-byte `AI4HEOR_0.1.26_x64.dmg` has SHA-256 `e745fa13fa659f27fb8f7af8bde4ae745bdc993e12d64ddb597bca8176944e3e`. Independent read-only mounted verification confirmed the `AI4HEOR` identity and `0.1.26` version, thin x86-64 main/OpenCode/uv executables, all 282 controlled resources byte-identical to the clean source with aggregate SHA-256 `a343d205009a5aaed6e2e75bbb2ed5016de836ef63372ff6ce66382398db3f89`, the sole bundled example rooted at `examples/heor-cost-effectiveness/`, no generated caches or payload links, and all 177 packaged HEOR-core tests passing. The new first-start gate copied the mounted app into an isolated temporary Applications directory, launched that exact copy through LaunchServices, proved exactly one installed app process and one child bundled OpenCode process, observed creation of the isolated `Documents/OpenScience` workspace, and verified cleanup; it did not touch the researcher's real workspace. Schema `ai4heor-release-evidence/v1` binds the exact DMG, clean source commit, resource inventory, content checks, DMG-copy proof, process proof, and workspace-creation proof and re-verifies successfully. This package remains ad hoc/local: it is not Developer-ID signed or notarized, has no Gatekeeper acceptance, and provides no native Apple-Silicon, Windows, current Linux, clean third-party-machine, authenticated Human identity, accessibility, methodological-fitness, scientific-validity, cost-effectiveness, reimbursement, or policy-authority evidence.

2026-07-17 · feat(ai4heor): versioned AI4HEOR `0.1.26` as the first delivery surface that no longer exposes the generic Open Science examples by default. The empty natural-language session now offers only researcher-led pharmacoeconomic study design, HEOR evidence/data analysis, HEOR model/report audit, and a bounded synthetic two-strategy cost-effectiveness example; the command palette sends the same locale-specific HEOR prompts, the left sidebar no longer injects inherited cross-species/SCVI demo sessions, and the packaged example allowlist/resource map contains only `examples/heor-cost-effectiveness/`. Every prompt preserves the corrected authority split: Codex Agent builds and verifies AI4HEOR, while the Human researcher leads each study and owns question framing, methods, evidence, assumptions, interpretation, and approval. All 45 first-party Skills now have shipped names and descriptions in English, Simplified Chinese, Japanese, Korean, German, Spanish, and French; the UI keeps the exact `$skill-id` visible and falls back to unmodified runtime metadata for unknown project or third-party Skills. A release contract requires exact Skill-key parity and complete localized metadata across all seven languages. Native macOS package verification also gained an optional isolated LaunchServices first-start gate that copies the app from the DMG, launches the copied bundle with isolated HOME/XDG/TMP roots, proves exactly one app and one bundled OpenCode process plus first workspace creation, then cleans up; release evidence and native CI now bind these checks separately from Developer-ID signing/notarization. Real headed Playwright inspection of the Chinese UI confirmed the four HEOR starters, empty inherited-example sidebar, localized first-party Skill copy, visible `$heor-workbench`, and unchanged external Skill fallback with zero console errors. Verification: 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 4/4 core-Skill tests, 3/3 Tauri-resource tests, 9/9 macOS-distribution tests, 9/9 release-evidence tests, 552/552 frontend tests across 83 files, typecheck, lint, production build, targeted rustfmt/diff/cache checks, and 279 Rust tests with 278 passed and the fixed-public-network test ignored. This source milestone does not yet claim a `0.1.26` installer, clean-machine visual/accessibility acceptance, Developer-ID signing, notarization, Gatekeeper acceptance, Apple-Silicon/Windows/current Linux package evidence, scientific validity, cost-effectiveness, reimbursement, or policy authority.

2026-07-17 · chore(ai4heor): bound the bounded semi-Markov microsimulation capability to a locally built and strictly verified AI4HEOR `0.1.25` x64 macOS package from clean source commit `bffa6288b7796ae39bccafeea77143108aedaacb`. The first DMG was correctly rejected because earlier Python execution had left generated `__pycache__` files under the controlled RWE and model-calibration Skills; the caches were removed and the package rebuilt instead of weakening resource admission or reusing the polluted artifact. The final 80,130,948-byte `AI4HEOR_0.1.25_x64.dmg` has SHA-256 `0172aa50e8af7faf710849a14188d60aff325868905378c122a7109b31f90aa3`. Independent read-only mounted verification confirmed exact bundle identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 281 controlled scientific resources byte-identical to source with aggregate SHA-256 `959a212e7fe180183d18ac532c3d677889a9018e61db43319bebdcb7b65da3e6`, no payload links or generated caches, and all 177 packaged HEOR-core tests passing. Schema `ai4heor-release-evidence/v1` binds that exact DMG, source commit, resource inventory and five content checks and re-verifies cleanly. This proves the bounded microsimulation Skill, deterministic evaluator, independent native replay and app-owned eight-check Human review chain are present in the current x64 package; it does not prove clean-machine first start, visual/accessibility acceptance, Developer-ID signing, notarization, Gatekeeper acceptance, native Apple-Silicon, Windows or current-version Linux behavior, a four-target manifest, authenticated Human identity, methodological fitness, scientific validity, cost-effectiveness, reimbursement or policy authority.

2026-07-17 · feat(ai4heor): shipped the bounded `$heor-semi-markov-microsimulation` alpha and versioned the source milestone as AI4HEOR `0.1.25`, while preserving the corrected authority split: Codex develops and verifies the software, the Human researcher leads each study, and the in-product model/runtime only assists under explicit scientific choices. Request/result schema `0.1.0` admits one closed non-interacting individual state-transition model with 2–8 states, exactly one absorbing death state, 2–4 researcher-defined strategies, 1–3 capped event-entry trackers, non-overlapping time-in-state and tracker-count conditions, complete probability rows, annual state costs/utilities, transition-event costs, separate discounting, 3–20 independent replicates, 100–50,000 patients per replicate, prespecified sampled traces, and a hard five-million patient-strategy-cycle cap. A dependency-free Python evaluator uses a versioned counter-based SplitMix64 top-53-bit generator and synchronized common random numbers across strategies, reports occupancy, tracker summaries, costs, QALYs, life-years, paired increments, patient-level standard errors and between-replicate variation, and never applies an automatic stability or strategy-selection threshold. Portable replay regenerates every patient cycle and trace row; native Rust independently revalidates the request/evidence/evaluator graph and recomputes every random draw, transition, reward, aggregate, paired comparison and sampled trace. The exact five-artifact graph stops at an app-owned eight-check Human method review with immutable workspace snapshot plus app-private unanchored SHA-256 event chain. Natural-language preparation and repair remain primary; the form is auxiliary and neither the Agent nor the Skill can create acceptance, select a strategy, replace a downstream input, or make cost-effectiveness, reimbursement or policy claims. Interactions, transmission, shared resources, queues, open populations, continuous-time discrete-event simulation, continuous/changing covariates, dynamic treatment policies, parameter uncertainty, calibration and structural averaging remain excluded. Verification: official Skill Creator validation; 8 portable/adversarial microsimulation tests; a cross-language 4,800-step/160-trace-row fixture plus PRNG golden vectors; all 177 deterministic HEOR tests; all 281 developer, portable, security, Skill, harness and packaged-resource contracts; 277/278 Rust tests with one fixed-public-network test ignored; 553 frontend tests across 82 files; typecheck, lint, production build, target rustfmt, strict Clippy with only four confirmed pre-existing unrelated lint classes allowed, 16 release/distribution contracts, Python/JSON/YAML/diff checks, and CI matrix wiring. This is a verified source capability milestone; no `0.1.25` installer, clean-machine first start, current-version Windows/Linux/Apple-Silicon evidence, four-target release manifest, signing/notarization, authenticated reviewer identity, methodological fitness, scientific validity or policy authority is claimed yet.

2026-07-17 · chore(ai4heor): bound the new calibration capability to a locally built and strictly verified AI4HEOR `0.1.24` x64 macOS package from clean source commit `2036174b92c7091650b802cea5123455453891f9`. The first DMG was correctly rejected because full-suite execution had left one generated `__pycache__` file under the packaged RWE Skill; the cache was removed and the package rebuilt instead of weakening resource admission. The final 80,021,138-byte `AI4HEOR_0.1.24_x64.dmg` has SHA-256 `36d0cbc21e66eedc76b99cfa0ae75726c2a215d2f9c79c54b6f2a78262d80aa4`. Independent read-only mounted verification confirmed exact bundle identity/version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 273 controlled scientific resources byte-identical to source with aggregate SHA-256 `a1aad1dfe1f735ad49638515ea4b70a14e7a7921bb68564733658bfc807648fe`, no payload links or generated caches, and all 177 packaged HEOR-core tests passing. Schema `ai4heor-release-evidence/v1` now binds that exact DMG, source commit, resource inventory and five content checks and re-verifies cleanly. A separate distribution-trust run repeated all 177 tests and then correctly failed closed at strict `codesign` verification with `code object is not signed at all`; it emitted no trust JSON and detached the volume. This proves the bounded calibration Skill and native review engine are present in the current x64 package; it does not prove first start, visual/accessibility acceptance, Developer-ID signing, notarization, Gatekeeper acceptance, native Apple-Silicon or Windows `0.1.24` behavior, current Linux `0.1.24` packages, the four-target manifest, authenticated Human identity, methodological fitness, scientific validity, or policy/reimbursement authority.

2026-07-17 · feat(ai4heor): shipped the bounded `$heor-model-calibration` alpha while preserving the project/research authority split: Codex builds and verifies AI4HEOR, but the Human researcher defines and leads every study inside it. The new first-party natural-language Skill accepts only a researcher-prespecified 2–6-state homogeneous continuous-time natural-history model, one to four bounded rate parameters, aggregate state-occupancy calibration targets, exact evidence bindings, more fitting targets than parameters, and at least one held-out validation target. A deterministic standard-library Python engine uses exact uniformization, a fixed tensor grid and bounded coordinate refinement, retains every evaluated point in immutable `search.csv`, and reports point fit, held-out residuals and local Jacobian diagnostics without presenting target standard errors as a likelihood or local rank as global identifiability. Portable replay rebinds and re-executes the complete request/evidence/evaluator/runtime/trace graph; native Rust independently challenges the selected point, transition probabilities, predictions, loss, validation and local-identifiability calculations, with cross-language fixture agreement below `1e-12`. The natural-language review entry remains primary and the eight-check form auxiliary; only the Human can accept a hash-bound candidate, and acceptance cannot automatically overwrite an economic-model input. Bayesian/probabilistic calibration, covariance-aware targets, parameter-uncertainty propagation, microsimulation, time-varying rates, structural calibration, automatic target/bound/model/fit selection and scientific-validity claims fail closed or remain out of scope. Verification passed the official Skill validator; 8 calibration portable/adversarial tests; all 136 selected Python contract tests; 274/275 Rust tests with one fixed-public-network test ignored; 552 frontend tests across 82 files; typecheck, lint, production build, Python/JSON/YAML/diff checks and target rustfmt. Version `0.1.24` is a source capability milestone at this point; its native package payload and first-start behavior are not yet evidenced.

2026-07-17 11:48 · chore(ai4heor): closed the current-version x86_64 Linux package and native headless-first-start gap for AI4HEOR `0.1.23` from clean source commit `98daee8c0279c00f4c41791e52489915191e9b4f`. An isolated amd64 Ubuntu 22.04 builder used Node 20.19.4, Rust 1.97.0, Python 3.10.12, frozen pnpm dependencies, and SHA-pinned Linux OpenCode `1.17.13` and uv `0.11.26`; 13 sidecar/resource/core-Skill/researcher-led-harness prechecks passed before native Tauri release compilation. The resulting 96,188,580-byte `AI4HEOR_0.1.23_amd64.deb` has SHA-256 `6075cc81d068398ff70e6cdd2493b5e38c263e687a244291d58e7ffebba1d59b`; the 89,053,633-byte `AI4HEOR-0.1.23-1.x86_64.rpm` has SHA-256 `1d08acf125cbb06e72240b40b908f3d10fe9c7a855f8eab46c4215840ccb122a`. Independent extraction verified exact package identity/version/architecture, x86-64 ELF main/OpenCode/uv binaries, sidecar versions and DEB/RPM byte parity, main-binary parity apart from the expected Tauri bundle marker, no payload links or generated Python caches, and all 265 configured scientific resources byte-identical to source with aggregate SHA-256 `1dfc9dc71d710000a99dabafdb3dfc10833ab1eb261d0a99eff999b32bed6b64`; each package independently passed all 177 packaged HEOR tests. A schema `ai4heor-release-evidence/v1` Linux record binds both exact packages, the source commit, resource inventory and five verified checks, and independently re-verifies against the copied artifacts. Brand-new Ubuntu 22.04 and Fedora 42 containers then installed the DEB and RPM respectively; each native headless first start returned `READY=1 MAIN=1 OPENCODE=1 WORKSPACE=1`, proving version `0.1.23`, exactly one desktop process, exactly one bundled OpenCode service and a newly created `~/Documents/OpenScience`. Expected container portal/FUSE warnings do not constitute a visual failure, but neither do these checks establish physical Linux desktop visual/accessibility acceptance. This milestone does not prove current-version Windows or native Apple-Silicon packages/first starts, the four-target manifest, signing/notarization, reproducible-build equivalence, authenticated Human identity, methodological fitness, scientific validity, or policy/reimbursement authority.

2026-07-17 11:25 · chore(ai4heor): closed the current-source x64 macOS package-integration gap for AI4HEOR `0.1.23` at clean source commit `892de95e1f5c1b270febd91a2c05bad1a1aa294e`. The first DMG build completed, but the strict read-only verifier rejected the source/package comparison because prior full-suite execution had left four generated `__pycache__` files under controlled Skill resources; those caches were removed and the package was rebuilt rather than weakening the gate or accepting ignored files. The final 79,935,963-byte `AI4HEOR_0.1.23_x64.dmg` has SHA-256 `f2819941f0e7d175f13d4a43a27eca13b343c2f1d70919dff353278a1eb18f63`. Independent read-only mounted verification confirmed bundle identifier and version, thin x86-64 main/OpenCode/uv executables, pinned OpenCode `1.17.13` and uv `0.11.26`, all 265 configured scientific resources byte-identical to source with no links or generated caches, and all 177 deterministic HEOR tests passing against the packaged core. A second distribution-trust run repeated the content checks and then correctly failed closed at strict `codesign` verification with `code object is not signed at all`; no distribution-evidence JSON was emitted and the mount was cleaned up. This proves the new fixed-horizon RWE IPTW×IPOW implementation and its natural-language Skill are actually present in a current native x64 package; it does not prove first-start behavior, visual/accessibility acceptance, Developer-ID signing, notarization, Gatekeeper acceptance, native Apple-Silicon or Windows `0.1.23` behavior, Linux `0.1.23` packages, the four-target manifest, authenticated Human identity, methodological fitness, scientific validity, or policy/reimbursement authority.

2026-07-17 11:12 · feat(ai4heor): upgraded the first-party `$heor-rwe-causal-analysis` alpha from complete fixed follow-up to explicit fixed-horizon outcome loss without changing the accountability boundary: Codex develops and verifies AI4HEOR, while the Human researcher defines and leads the study inside it. Request/result/evaluator schema `0.2.0` now requires one local pseudonymous row per person, an exact `outcome_observed` indicator with blank outcomes only when unobserved, Human-prespecified baseline treatment–outcome common causes and an observation-model subset with observation–outcome roles, separate unpenalized main-effects Logistic treatment and observation models, and untrimmed/uncapped stabilized ATE-IPTW×IPOW for the source-cohort risk difference if nobody's fixed-horizon outcome were lost. The standard-library Python evaluator reports follow-up, propensity overlap, treatment/observation/combined weights, observed-row ESS, pre/treatment/combined SMD, complete-case and weighted contrasts, and deterministic arm-stratified bootstrap refits of both models; portable replay checks every draw and exact hash binding, while native Rust independently reparses the cohort and recomputes both point models, coefficients, standardization, weights, balance, ESS, overlap and effects. The natural-language Skill, generated prompt and bilingual review pane expose this bounded method and retain the existing app-owned eight-check Human method gate as an auxiliary decision surface; no Agent method authority or automatic economic-model use was added. The contract follows current NICE RWE guidance plus Hernán/Robins, Cole/Hernán and Jackson on target-trial definition, informative outcome loss, multiplied treatment/censoring weights, assumptions and diagnostics. Verification: 11/11 RWE portable/adversarial contracts, 270/271 Rust tests with one fixed-public-network test ignored, 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, 26/26 admission/core-Skill/researcher-led-harness/local-evidence/methods-watchlist/resource contracts, 551/551 frontend tests across 82 files, typecheck, lint, production build, target rustfmt, Python/JSON/YAML/diff checks, and Rust 1.97 strict Clippy with only the two documented unrelated lint classes allowed. Version `0.1.23` is a verified source milestone, not a packaged release: time-varying censoring, intermittent observation, survival, competing risks, missing baseline covariates, imputation or MNAR identification, treatment switching, time-varying confounding, matching/trimming, doubly robust estimation, causal validity, authenticated Human identity, external validation, cross-platform packages and signing/notarization remain unproved or out of scope.

2026-07-17 02:20 · feat(ai4heor): converted macOS signing/notarization from a release-note aspiration into a tag-only fail-closed distribution contract without misrepresenting the current unsigned `0.1.22` artifacts. Manual workflow dispatch still creates explicitly unsigned previews and receives no Apple secrets; a `v*` tag now stops before build unless six non-empty secrets identify a Developer ID Application certificate plus Apple ID app-password notarization account and valid Team ID, while logging only presence/type summaries. Tauri receives those secrets only on the two tagged macOS runners and has explicit hardened runtime enabled. After the native content verifier checks bundle metadata, target architecture, pinned sidecar versions, all controlled resources and 177 packaged HEOR tests, distribution mode strictly verifies the complete app and every nested Mach-O, a common Developer ID chain whose Team ID exactly matches the credentialed notarization account, secure timestamps, hardened runtime on executables, sealed resources, absence of true `get-task-allow`, a valid stapled ticket, and Gatekeeper `Notarized Developer ID`; tagged macOS release evidence now requires the same four trust checks and eight proof fields, so a missing flag or incomplete proof cannot enter the four-target manifest. Matrix builds no longer create or populate releases: only the final manifest job may create a new draft after finding exactly six verified installers and four target evidence files, and an existing same-tag release fails rather than absorbing stale assets. Eight macOS distribution and eight release-evidence contract tests pass and are wired across macOS, Windows and Linux contract CI; the current Tauri CLI also accepted the explicit hardened-runtime config and built an x64 debug app. YAML parsing and diff checks pass. Real failure-path validation ran the current x64 DMG through all 177 mounted-core HEOR tests, then correctly rejected it as `code object is not signed at all` and emitted no distribution JSON; an empty-secret preflight likewise rejected all six missing names without revealing values. Normal unsigned package verification still passes and emits no trust claim. The success path remains unproven until valid Apple credentials execute both native macOS tag jobs; this milestone is not a signed/notarized artifact, Apple notarization result, Windows Authenticode result, native Apple-Silicon first start, four-target manifest, visual acceptance, scientific-validity judgment, or policy/reimbursement authority.

2026-07-17 01:57 · chore(ai4heor): closed the locally executable Apple Silicon build and package-inspection gap for AI4HEOR `0.1.22` without weakening the native release gate. Clean source commit `d2363138449566137c8374386acdf1f8774faad3` passed the arm64 Rust target build with Rust 1.97 strict Clippy, 551 frontend tests across 82 files, typecheck, lint and production build, 177 deterministic HEOR tests, 113 portable artifact contracts, release-evidence/admission/core-Skill/researcher-led-harness/methods-watchlist/resource checks, and 9 each of NMA, anchored-MAIC and RWE causal tests. SHA-pinned Apple Silicon OpenCode `1.17.13` and uv `0.11.26` sidecars were fetched and independently checked as arm64 before the release build. The resulting 75,863,598-byte `AI4HEOR_0.1.22_aarch64.dmg` has SHA-256 `56fb9db248f3284e6d8326ac95c94f5d0fa03b915d0986f8bd03fb0594538329`. Read-only mounted inspection confirmed bundle identity/version, exactly three Mach-O executables and all three as thin arm64, packaged sidecar bytes equal to their pinned source files, all 265 configured scientific resources byte-identical to source with no links/caches, and all 177 packaged HEOR tests passing. Because the build host is Intel, the strict native verifier correctly stopped with `Errno 86: Bad CPU type in executable` when it attempted the packaged arm64 OpenCode version command and emitted no formal arm64 release-evidence JSON; native Apple Silicon execution and first start therefore remain open rather than inferred from structure. Independent signing inspection also found only an ad-hoc linker signature (`TeamIdentifier` absent, `Info.plist` unbound, sealed resources absent); strict `codesign` verification and Gatekeeper assessment both failed, so this is not a signed/notarized distribution artifact. This milestone proves a current-source arm64 package and its scientific payload, not native Apple Silicon runtime behavior, Gatekeeper acceptance, notarization, Windows evidence, a four-target CI manifest, physical Linux visual acceptance, authenticated Human identity, methodological fitness, scientific validity, or policy/reimbursement authority.

2026-07-17 01:43 · chore(ai4heor): closed the current-version x86_64 Linux package gap for AI4HEOR `0.1.22` while preserving the product boundary that Codex builds and verifies the software but the Human researcher leads scientific work inside AI4HEOR. Rust 1.97 strict Clippy first exposed eight existing release warnings; the behavior-preserving fix replaced four literal-boolean assertions, used direct map-value iteration, documented one intentionally wide test fixture helper, and now waits for two killed test bridge processes so they cannot become zombies. The exact clean source commit `6c5257eb5f20858e911c457049233bdfe11d85e2` then passed 177 deterministic HEOR tests, 113 portable artifact contracts, release-evidence/admission/core-Skill/researcher-led-harness/methods-watchlist/resource checks, 9 NMA, 9 anchored-MAIC and 9 RWE causal tests, 270 Rust tests with one fixed-public-network test ignored, Rust 1.97 strict Clippy with only the two documented unrelated lint classes allowed, 551 frontend tests across 82 files, typecheck, lint and production build. Pinned Linux/x64 OpenCode `1.17.13` and uv `0.11.26` archives were SHA-256 verified before bundling. The resulting 96,185,464-byte `AI4HEOR_0.1.22_amd64.deb` has SHA-256 `44b562c595e45083d8a0016fff9f210e1100fc72746cf6a09f315b6caadc4d06`; the 89,042,167-byte `AI4HEOR-0.1.22-1.x86_64.rpm` has SHA-256 `c40de1fd09487641e36fcc2742acbe30df999207be4dfed0d536dc7ce2782417`. Independent extraction verified package identity/version/architecture, x86-64 ELF binaries, exact sidecar versions, no payload links or generated Python caches, all 265 configured resources byte-identical to source with aggregate SHA-256 `7f04518f18b155ef2589ad64ae1a7dcc965260bdef77135c565e745997071467`, and DEB/RPM payload parity apart from the expected Tauri bundle marker; each package independently passed all 177 packaged HEOR tests. Linux-native release evidence was generated and re-verified inside a Linux container so sidecar version output and clean Git identity were both target-valid. Brand-new Ubuntu 22.04 and Fedora 42 environments natively installed the DEB and RPM respectively; each isolated headless first start reported `READY=1 MAIN=1 OPENCODE=1 WORKSPACE=1`, proving version `0.1.22`, exactly one desktop process, exactly one bundled `opencode serve`, and a newly created `~/Documents/OpenScience`. Expected container portal/FUSE warnings are not visual acceptance. This evidence proves current x86_64 Linux package integrity, native installation and headless startup; it does not prove a physical Linux desktop session, Windows or Apple Silicon current-commit packages/first runs, signing/notarization, reproducible-build equivalence, authenticated Human identity, methodological fitness, scientific validity, or policy/reimbursement authority.

2026-07-17 01:00 · fix(ai4heor): released the local AI4HEOR `0.1.22` alpha after removing a false Human-authority path from the methods-currency watchlist. The `0.1.21` workspace schema allowed an Agent-writable `human_disposition` field to make a change appear resolved; that contradicted the product contract even though the review card itself was read-only. Schema `0.2.0` now rejects `human_disposition` and every equivalent unknown field, limits `heor/methods-watchlist.json` to source/change evidence plus Agent preparation states, and keeps every change unresolved to the portable validator. Only the native desktop can record `accept_revalidation` or `dismiss_change`: the auxiliary form requires an explicit local actor label, rationale and exact-hash confirmation, while the app writes an immutable review snapshot plus a private bounded SHA-256 event chain binding project, watchlist, change, action, sequence and prior event. Any watchlist-byte change invalidates the effective disposition; malformed chains, unsafe files, forged workspace authority and exported-record drift fail closed. Natural-language conversation remains the preparation and repair surface, and the form records only the Human researcher's decision. Verification: 5/5 watchlist contract tests, official Skill Creator validation, 3/3 core-Skill, 5/5 researcher-led harness, 2/2 packaged-resource and 7/7 asset-admission contracts, 113/113 portable artifact contracts, 177/177 deterministic HEOR tests, 270/271 Rust tests passed with one fixed-public-network test ignored, 551/551 frontend tests across 82 files, typecheck, lint, production build, Clippy with only two pre-existing unrelated lint classes, JSON/rustfmt/diff checks, and a rebuilt x64 macOS debug package. The final 88,989,911-byte `AI4HEOR_0.1.22_x64.dmg` has SHA-256 `59094d3c4b42cb186a03f3ad8b9ec2f64c8675c1b1ef22fc5e71624da1602e0a`; read-only mounted verification confirmed version `0.1.22`, x86-64 executable, pinned OpenCode `1.17.13` and uv `0.11.26`, all 265 controlled resources byte-identical to source, zero packaged links/caches, and all 177 packaged HEOR tests. This remains an unanchored local Human assertion, not authenticated identity, external notarization, methodological approval, independent validation, scientific validity, or reimbursement authority; current-commit Windows/Linux and Apple Silicon packages, signing/notarization, and physical-device visual/accessibility acceptance remain unverified.

2026-07-17 00:41 · feat(ai4heor): released the local AI4HEOR `0.1.21` alpha with the first-party `$heor-methods-watchlist` platform asset while preserving the corrected accountability boundary: Codex leads software construction and verification, the Human researcher leads scientific work inside AI4HEOR, and the configured model/runtime only assists. A 2026-07-17 primary-source licensing review found that NICE syndication requires an approved licence and API key with AI use declared, while CDA-AMC and Australian Department content reuse remains constrained by source-specific terms; AI4HEOR therefore did not disguise an unlicensed HTA scraper as a ready connector. The natural-language-first Skill instead creates strict schema `0.1.0` `heor/methods-watchlist.json` records for official HTTPS links, observed revisions, dated Human checks, next-check dates, access/rights status, affected platform contracts, detected changes, required actions, and Human-owned disposition/revalidation. Optional lawful local snapshots must stay under `heor/method-sources/`, be regular non-symlink files, and match an exact SHA-256; the portable validator and independent native Rust audit reject unknown fields, order/date/path/hash/rights drift, expose overdue checks and unresolved changes, and calculate completion relative to the reproducible `as_of_date` without claiming that the file remains current today. The bilingual auxiliary review card reports the snapshot and routes preparation back to the conversation; it adds no form, network call, downstream rewrite, or approval gate. CI now tests the contract on macOS, Windows, and Linux, and product, technical, ecosystem, integration, and README documents record the licensed-connector boundary. Verification: 5/5 watchlist contracts, official Skill Creator validation, 3/3 core-Skill contracts, 5/5 researcher-led harness contracts, 2/2 packaged-resource contracts, 7/7 asset-admission contracts, 113/113 portable artifact contracts, 177/177 deterministic HEOR tests, 268/269 Rust tests passed with one fixed-public-network test ignored, 550/550 frontend tests across 82 files, typecheck, lint, production build, JSON/diff checks, and a rebuilt x64 macOS debug package. The first DMG was correctly rejected because test-generated Python caches had entered the resource tree; those generated files were removed and the package rebuilt rather than weakening the verifier. The final 88,928,971-byte `AI4HEOR_0.1.21_x64.dmg` has SHA-256 `8506a9807864c90c6c89adddefeeaf2afb38975bc5cac1ba55d858597cd0f178`; read-only mounted verification confirmed version `0.1.21`, x86-64 executable, pinned OpenCode `1.17.13` and uv `0.11.26`, all 265 resources byte-identical to source, zero packaged links/caches, and all 177 packaged HEOR tests. This milestone does not establish live monitoring, source truth, reuse permission beyond the Human-recorded basis, methodological fitness, independent validation, authenticated Human identity, scientific validity, current-commit Windows/Linux or Apple Silicon packages, signing/notarization, or policy/reimbursement authority.

2026-07-17 00:12 · fix(ai4heor): corrected the runtime-level role boundary that remained behind the already-correct public copy: Codex Agent leads construction and verification of the AI4HEOR software, while every newly created AI4HEOR project now seeds a bounded research assistant whose Human researcher owns the research question, methods, evidence choices, model structure, assumptions, interpretation, gates, and permitted use. The inherited single-agent/self-evolution contract was removed from `runtime/harness/`: the assistant may inspect state, run deterministic checks, draft labelled alternatives, and complete a researcher-selected reversible plan, but it must stop for unresolved scientific choices, cannot invent a research programme, cannot rewrite `AGENTS.md` or promote task-local lessons into policy, cannot manufacture approval evidence, and cannot treat self-checking as independent validation. Natural-language conversation remains the primary interface and forms remain auxiliary app-owned review/decision records. The `$heor-workbench` Skill and generated interface metadata now carry the same researcher-led boundary; product, technical, and integration documentation distinguish Codex's development role from the in-product assistant role and state the deliberate non-clobber rule for existing user-authored projects. A new semantic harness contract is wired into the macOS/Windows/Linux HEOR matrix and build workflow, and the prior deterministic-core harness test was updated to enforce the stronger contract. Verification: 5/5 harness contracts, 3/3 core-Skill contracts, official Skill Creator validation, 2/2 packaged-resource contracts, workflow YAML, JSON, and diff checks, all 177 deterministic HEOR tests, all 113 portable artifact contracts, 265 Rust tests with one fixed-public-network test ignored, 549 frontend tests across 82 files, typecheck, lint, and production build. The 88,850,542-byte `AI4HEOR_0.1.20_x64.dmg` has SHA-256 `0424f5c00cdd8f486dec621efa01d6424f7adba1f2f3bddd39be1cb4c44328d8`; read-only mounted verification confirmed identity/version, x86-64 executable, pinned sidecars, all 260 resources byte-identical to source, zero packaged links/caches, and all 177 packaged HEOR tests. Existing projects are intentionally not silently rewritten, and this milestone does not establish current-commit Windows/Linux or Apple Silicon packages, signed/notarized distribution, authenticated Human identity, methodological fitness, independent validation, or scientific validity.

2026-07-16 23:53 · feat(ai4heor): released the local AI4HEOR `0.1.19` alpha connection between a researcher-selected HEOR knowledge base and the Human-led natural-language workspace, while correcting the remaining public product copy that still implied autonomous or Agent-led science. The researcher can now explicitly add a knowledge-base folder from the auxiliary review pane; native import deterministically preserves its relative hierarchy under a unique `heor/library/<selected-folder>/` root, copies only PDF/TXT/Markdown/CSV/JSON, reports hidden and unsupported entries, enforces the existing 500-file/25-MiB-per-file/500-MiB-total caps, rejects source/workspace overlap and every symbolic link, rolls back an incomplete imported root, then locally extracts, hashes and indexes the exact bytes without a model or network call. A new bilingual `Learn from my local HEOR library` starter only drafts a researcher-reviewable natural-language request, first asks the researcher's learning goal and level, reuses the existing deterministic `$heor-local-evidence` Skill with path/page/SHA-256 citations, and neither auto-sends nor selects a model or contacts the network; no duplicate learning Skill was created. The welcome text and English/Chinese README now state the actual boundary: Codex leads construction and verification of AI4HEOR, while the Human researcher frames questions and makes scientific decisions inside it and the configured model/runtime assists. The existing local pharmacoeconomics KB was audited as seven selectable content directories containing 24 supported Markdown files (104 KB), no symlinks and no over-limit files; its enclosing project root intentionally fails the overlap rule because it contains the workbench, so content directories must be selected individually. Verification: 9/9 targeted native library tests, all 266 Rust library tests with one fixed-public-network test ignored, 549/549 frontend tests across 82 files, typecheck, lint, production build, JSON/rustfmt/diff checks, and a clean x64 macOS debug package. The first package was rejected by the verifier because an RWE Skill test had left a source `__pycache__`; the generated cache was removed and the package rebuilt rather than weakening the gate. The final 88,849,809-byte `AI4HEOR_0.1.19_x64.dmg` has SHA-256 `6e4e9c49d4a001ef8364b90d6fa29a77beebd95a86649b01d8317234c7ae4e32`; read-only mounted verification confirmed bundle identity/version, x86-64 executable and pinned OpenCode `1.17.13`/uv `0.11.26`, all 260 configured resources, zero packaged links/caches, and all 177 packaged HEOR tests. The current local KB shape was audited but not imported through an automated native dialog, and this milestone does not establish visual/accessibility acceptance of the folder picker, Windows/Linux `0.1.19` packages, Apple Silicon, signing/notarization, authenticated Human identity, source/appraisal truth, methodological fitness, or scientific validity.

2026-07-16 23:31 · chore(ai4heor): extended the release harness from a Windows-only evidence design to one fail-closed four-target manifest while preserving the product boundary that Codex builds and verifies AI4HEOR but the Human researcher leads scientific work inside it. Native build jobs are now pinned to macOS 15 arm64, macOS 15 Intel x64, Windows Server 2022 x64, and Ubuntu 22.04 x64; each emits `ai4heor-release-evidence/v1` with nonempty platform verification details, while the final job downloads and re-hashes every DMG/MSI/NSIS/deb/rpm and accepts only unique supported targets with one source commit, app version, 260-file resource inventory, GitHub workflow run/attempt, exact installer bytes, sidecar identities, and runner images before writing `ai4heor-release-manifest/v1`. The new read-only DMG verifier checks bundle identity/version, exact Mach-O architecture, sidecars, resource bytes, links/caches, and packaged HEOR execution; the Linux verifier now records package metadata, deb/rpm parity, sidecars and resource counts. Verification: 7/7 release-evidence adversarial contracts, 7/7 asset-admission, 3/3 sidecar-integrity, 2/2 Tauri-resource and 2/2 core-Skill contracts, Python compilation, YAML parsing, actionlint and diff checks passed. The existing `0.1.18` x64 DMG (`04f3563028154d2ec0dda53a9cd05146a43bad314478362a3d6d1065813e0700`) passed the new mounted verifier and all 177 packaged-core tests with 260 matching resources; the existing deb/rpm (`6c418b91438e9af32fd105de412dfd0047eaa7e215f48fffb2b11a814c28bde7` / `e69d467b3ec107d526435f71b682888c1175ebe2d1191ba55db08c8baebbfa05`) each passed all 177 packaged-core tests in the prior x86_64 Ubuntu builder and emitted the expected verification JSON. Those packages predate this harness commit and therefore calibrate the verifiers only; they are not a current-commit four-target manifest. Windows execution/visual acceptance, Apple Silicon package/visual acceptance, physical Linux desktop behavior, signing/notarization, reproducible-build proof, signed provenance, authenticated Human identity, and scientific validity remain unverified.

2026-07-16 23:16 · chore(ai4heor): added a fail-closed Windows release-evidence path without changing scientific ownership: the explicit Windows 2022 build now requires one MSI and one NSIS installer, verifies MSI identity and x86-64 payloads, exact configured scientific-resource bytes, pinned OpenCode/uv versions, absence of links and Python caches, and all packaged HEOR tests, then silently installs NSIS and requires exactly one installed desktop process, one bundled OpenCode process, and a newly created local workspace before emitting `ai4heor-release-evidence/v1`; the portable evidence tool binds the clean source commit, app version, target, installer hashes, all 260 source-resource records, sidecar hashes/version outputs, runner identity, named checks and first-start details, rejects tampering or mixed-source manifest assembly, and uploads the JSON beside Windows workflow artifacts. Verification: 6/6 new evidence contracts, 7/7 asset-admission, 3/3 sidecar-integrity, 2/2 Tauri HEOR-resource and 2/2 core-Skill contracts passed; both Actions files parsed as YAML, the 245-line PowerShell verifier parsed without errors in PowerShell 7.4, Python compilation and diff checks passed. This macOS run cannot execute WindowsInstaller COM, MSI administrative extraction, NSIS registry installation or Windows first launch, so Windows current-commit build/host evidence, visual acceptance and signing remain explicitly unverified until the configured Windows job produces and the Human inspects the bound evidence artifact.

2026-07-16 22:59 · fix(ai4heor): closed a Human-control defect discovered during the first actual Linux packaged-window visual audit. The verified `0.1.18` `.deb` correctly rendered the natural-language-first AI4HEOR entry and stated that the Human reviews assumptions and approves gates while forms remain secondary, but a starter card still immediately created a session and submitted a constrained Agent request even though the sidebar truthfully said `Model Not Set`; the turn then entered `Working…`, ran local steps and exposed a failure without an immediate recovery path. AI4HEOR starters now only place their complete suggestion into the natural-language composer for Human review and editing, never auto-send; an explicit no-model gate disables sending, explains that no Agent turn starts until the Human chooses a model, and links to the existing visible model settings without selecting a provider automatically. Two regression tests bind both the reviewable-draft behavior and the fail-closed no-model boundary. Verification: 3/3 targeted route tests, 548/548 frontend tests across 82 files, typecheck and lint passed; an isolated Ubuntu 22.04 build produced a temporary visual-QA-only `.deb` with SHA-256 `684be4e38d42933a41794ab6a72ab6c4d92bf18c6ac31da9a557e0205e61e39d`, which was installed over the clean test environment and re-rendered in WebKitGTK 2.50.4 at a 1440×900 app window. Seven ordered screenshots were inspected at original resolution and preserve the before/after evidence: first launch, AI4HEOR entry, the defective auto-submit and no-model working state, the fixed explicit-model gate, the Human-reviewed draft, and the settings handoff. This temporary package does not replace the previously hash-bound `0.1.18` release artifact. Container-rendered WebKit evidence does not establish physical Linux desktop behavior, full accessibility, configured-provider response behavior, Windows/macOS acceptance, signing, authenticated Human identity, or scientific validity.

2026-07-16 22:40 · chore(ai4heor): closed the current-version Linux release-evidence gap for AI4HEOR `0.1.18` without changing scientific ownership: Codex performed software construction, test orchestration, packaging and documentation, while the human researcher remains the scientific lead inside the workbench and the configured model/runtime remains an assistant. A clean archive of commit `79920aaa8084baedb2670a2b8cd2742e313764a1` was built in an isolated x86_64 Ubuntu 22.04 environment with Node 20.19.4, Python 3.10.12 and Rust/Clippy 1.97.0. Before packaging it passed 177 deterministic HEOR tests, 113 portable artifact contracts, 4 local-evidence, 7 asset-admission, both core-Skill and both Tauri-resource checks, 9 NMA, 9 anchored-MAIC and 9 RWE causal-analysis tests, 262 Rust tests with one fixed-public-network test ignored, strict Linux Clippy with only the two documented unrelated lint classes allowed, 546 frontend tests across 82 files, typecheck, lint and production build. Pinned Linux/x64 OpenCode `1.17.13` and uv `0.11.26` archives passed SHA-256 verification before bundling. The resulting 96,052,116-byte `AI4HEOR_0.1.18_amd64.deb` has SHA-256 `6c418b91438e9af32fd105de412dfd0047eaa7e215f48fffb2b11a814c28bde7`; the 88,895,969-byte `AI4HEOR-0.1.18-1.x86_64.rpm` has SHA-256 `e69d467b3ec107d526435f71b682888c1175ebe2d1191ba55db08c8baebbfa05`. The independent verifier confirmed package name/version/architecture, x86-64 ELF binaries, exact sidecar versions, absence of payload links and generated Python caches, all 260 configured resources byte-identical to source, identical sidecars, and main-program parity apart from the expected Tauri bundle marker; each extracted package independently passed all 177 packaged-core tests. A brand-new Ubuntu 22.04 environment installed the `.deb`, and a brand-new Fedora 42 environment natively installed the `.rpm`; each headless X first start reached exactly one `ai4s-workbench` process, exactly one bundled `opencode serve` process, a newly created `~/Documents/OpenScience` workspace, and `READY=1`. One initial Ubuntu probe was discarded and rerun because its `dpkg-query` format string was incorrectly expanded under `set -u`; the package installation had succeeded, and the corrected probe passed from a fresh container. Container portal/FUSE warnings remain expected in a headless session and are not treated as visual acceptance. This evidence proves current x86_64 Linux package integrity, native installation and headless startup; it does not prove a real Linux desktop visual session, Windows `0.1.18` build or first run, signing/notarization, clean non-container hardware behavior, authenticated Human identity, or scientific validity.

2026-07-16 22:30 · feat(ai4heor): released the local AI4HEOR `0.1.18` alpha with a bounded first-party `$heor-rwe-causal-analysis` capability while preserving the corrected accountability boundary: Codex leads construction, testing and packaging of the software; the human researcher leads each scientific study inside AI4HEOR; and the configured model/runtime only assists through natural language, with the form limited to recording Human method judgment. The admitted design is exactly one Human-defined active-comparator new-user target trial over a pre-shaped local pseudonymous cohort with two strategies, shared time zero, fixed complete follow-up, one binary outcome, and 1–12 Human-prespecified baseline confounders. A fixed unpenalized main-effects logistic propensity model standardizes continuous covariates, leaves binary covariates unchanged, and estimates the source-cohort ATE risk difference using untrimmed and uncapped stabilized IPTW; it also reports unadjusted and weighted risks, descriptive risk/odds ratios, overlap, weight, ESS, and pre/post SMD diagnostics without inventing automatic scientific thresholds. A deterministic PCG32 arm-stratified bootstrap refits 1,000–5,000 replicates, retains every failure, and blocks review if any replicate fails. Complete portable replay binds and regenerates the exact request, cohort, evidence, evaluator, result and draw bytes; native Rust independently re-reads the current graph and recomputes point estimates and diagnostics, while truthfully leaving bootstrap refitting to the portable challenger. Only an app-owned immutable snapshot and private eight-check Human event chain can accept the exact current result for later consideration; acceptance does not establish exchangeability, positivity, consistency, transportability, causal truth, economic-model suitability, reimbursement value, or policy authority. Missing data, censoring, variable follow-up, time-varying treatment/confounding, survival, matching, trimming, overlap weighting, ATT/ATC, outcome regression, doubly robust/TMLE, g-methods, IV, DiD, negative controls, QBA, automatic design choices, causal claims, and automatic downstream use fail closed. Verification: 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, 9/9 RWE adversarial/replay tests, adjacent NMA/MAIC/local-evidence/admission/sidecar/resource suites, 262 Rust tests passed with one fixed-public-network test ignored, 546/546 frontend tests across 82 files, typecheck, lint, production build, strict library Clippy with only two pre-existing unrelated lint classes explicitly allowed, both changed Skills through the official Skill Creator validator, JSON/diff checks, and real portable-to-native fixture challenge. The final read-only-mounted 88,812,285-byte x64 macOS debug DMG has SHA-256 `04f3563028154d2ec0dda53a9cd05146a43bad314478362a3d6d1065813e0700`, contains 48 core Skill directories, all 260 configured resources byte-identical to source, and zero generated Python caches; it preserves the new eight-file RWE Skill byte for byte and passes all 177 tests against its packaged HEOR core. This milestone does not authenticate researcher identity, prove that the Human-specified target trial or covariates identify a causal effect, validate source truth or data rights, support the excluded designs, establish external scientific validity, or complete Windows/Linux `0.1.18`, signing/notarization, remote macOS, or clean-machine visual first-launch validation; the DMG is unsigned.

2026-07-16 21:10 · feat(ai4heor): integrated the existing app-owned Human method reviews into one current-result queue without making the Agent the scientific lead or creating a new approval authority. The analysis-review pane now includes only paired-survival-bootstrap, NMA, anchored-MAIC, and advanced-VOI results that already have a native audit and result digest; it orders rejected, awaiting, blocked, and accepted work, shows the exact bound path, opens the existing Human checklist only for a reviewable result, and routes rejected or blocked work back to the natural-language conversation. Acceptance remains tied to each method's separate private event chain and exact current result/replay bytes. The integration also corrected a presentation bug in which an accepted paired-bootstrap record was shown as rejected whenever that bootstrap was not required by the current uncertainty plan: display status now reflects the exact current review, while analysis-plan gating still requires and binds it only when the uncertainty manifest declares it required. English and Simplified Chinese copy state that forms record Human judgment and conversation remains the preparation/repair surface. Verification: 15/15 targeted review-pane tests, 545/545 frontend tests across 82 files, typecheck, lint, JSON parsing, production build, and a real Chromium render at 900px and 390px. The 390px viewport had no horizontal overflow (`scrollWidth=390`), all three actionable controls had explicit accessible names, and direct image inspection found the status order, semantic colors, path truncation, density, and typography consistent with the existing panel. The temporary visual-QA page, screenshots, browser session, and dev server were removed. This milestone consolidates locally asserted review state; it does not authenticate reviewer identity, prove scientific or external validity, merge method event chains, permit Agent approval, complete Windows or visual Linux release validation, or alter reimbursement and policy authority.

2026-07-16 20:57 · chore(ai4heor): completed current-version native Linux package validation for AI4HEOR `0.1.17` without changing the accountability boundary: Codex leads software construction and verification, the human researcher leads scientific work inside AI4HEOR, and the configured model/runtime only assists. A clean source snapshot in an isolated x86_64 Ubuntu 22.04 builder passed 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, 258 Rust tests with one fixed-public-network test ignored, strict Linux Clippy, 543/543 frontend tests across 82 files, typecheck, lint, production build, sidecar/resource/admission/local-evidence checks, and the targeted NMA and anchored-MAIC suites. Strict Linux Clippy exposed a real cross-platform boundary error: the macOS-only `scutil` proxy parser was compiled but unused in non-test Linux production builds; the minimal source fix now compiles it only on macOS or in tests, and the same Rust test/Clippy gates pass in the main macOS worktree. The clean snapshot produced a 95,948,728-byte `AI4HEOR_0.1.17_amd64.deb` (SHA-256 `6d0cff68585b6d7e662e12f76f3cd4ba9aa760bd51b31117c90f23ba2676a251`) and an 88,826,233-byte `AI4HEOR-0.1.17-1.x86_64.rpm` (SHA-256 `5986aa0619d35340b4a7ae73f49bb493c5f019f096e8e5b689911f7ca947a949`). A separate read-only verifier checked exact name/version/architecture, x86-64 ELF binaries, bundled OpenCode `1.17.13` and uv `0.11.26`, whole-payload link and generated-cache absence, all 252 configured resources byte for byte, sidecar identity, and main-program parity apart from the expected Tauri bundle marker; each extracted package independently passed all 177 HEOR tests. A brand-new Ubuntu 22.04 environment installed the `.deb`, and a brand-new Fedora 42 environment natively installed the `.rpm`; each headless first start reported `READY=1`, one desktop process, one bundled `opencode serve` process, and a newly created `~/Documents/OpenScience` workspace. This establishes current x86_64 Linux package integrity and native headless startup, not a real visual Linux desktop acceptance, Windows `0.1.17` build/first run, signing/notarization, scientific or external validity, authenticated Human identity, or reimbursement and policy authority.

2026-07-16 20:15 · feat(ai4heor): released the local AI4HEOR `0.1.17` alpha with the bounded first-party `$heor-advanced-value-of-information` capability while preserving the corrected accountability boundary: Codex leads software construction and verification, the human researcher leads each study inside AI4HEOR, and the configured model/runtime only assists. The natural-language-first route starts only from a converged uncertainty result and requires Human-owned affected population, lifetime, discounting, correlation-closed EVPPI groups, a supported study likelihood, candidate sample sizes, delay, and research costs. The dependency-free engine computes population EVPI, nested-Monte-Carlo EVPPI, one-parameter EVSI, study cost, ENBS and MCSE, with an exact compact replay; native Rust re-reads current model inputs and independently recomputes every reported summary before an app-owned eight-check Human method review can accept the exact plan/result/replay graph. Implementation exposed and corrected a contract mismatch rather than hiding it: standard uncertainty `0.10.0` supplies HR/Uniform uncertainty, which cannot satisfy the declared independent Lognormal, log-scale Normal-Normal EVSI model, so the standard route is limited to reachable OR/Lognormal schema `0.9.0`; current fixed-survival component uncertainty `0.13.0` is also admitted, while joint survival, split correlation groups, correlated or multi-parameter learning, unconverged PSA, silent distribution approximations, automatic study design, funding, reimbursement, and policy authority fail closed. The auxiliary UI reports conditional population EVPI, EVPPI, EVSI and ENBS without absorbing them into the release report graph or replacing researcher judgment. Verification: 177/177 deterministic HEOR tests, 113/113 portable artifact contracts, 258 Rust tests passed with one fixed-public-network test ignored, 543/543 frontend tests across 82 files, typecheck, lint, production build, strict library Clippy with only two pre-existing unrelated lint classes explicitly allowed, core-Skill and packaged-resource tests, official Skill Creator validation, JSON/diff checks, and packaged-core replay. The final read-only-mounted 88,621,241-byte x64 macOS debug DMG has SHA-256 `a572a1311aa80369b38723ee1471b24e4befe2f7bb0465fc97a8215e68d5fd9a`, reports `0.1.17`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 47 Skill directories and 250 byte-identical controlled resources, and contains zero application symlinks or generated Python caches. This milestone does not establish source truth, scientific or external validity, unbiased nested estimates at arbitrary settings, the validity of the Human-specified data model, authenticated reviewer identity, optimal research design, funding value, reimbursement, or policy suitability; the DMG is unsigned, Linux remains verified at `0.1.16`, and Windows `0.1.17`, remote macOS, signing/notarization and clean-machine visual first launch were not rebuilt or inferred from older evidence.

2026-07-16 19:12 · chore(ai4heor): rebuilt and admitted the current AI4HEOR `0.1.16` native x86_64 Linux packages without changing the product-accountability boundary: Codex leads software construction, testing, documentation and packaging, while the human researcher leads scientific work inside AI4HEOR and the configured model/runtime only assists. A clean source snapshot in an isolated Ubuntu 22.04 builder passed 170/170 deterministic HEOR tests, 111/111 portable contracts, 254 Rust tests with one fixed-public-network test ignored, typecheck, 541/541 frontend tests across 82 files, lint and production build before producing the 95,837,098-byte `AI4HEOR_0.1.16_amd64.deb` (SHA-256 `b6ea75027362ef4ae98ce585101c8180aee69ca190ec1ac90bc163a3377bd85e`) and 88,743,666-byte `AI4HEOR-0.1.16-1.x86_64.rpm` (SHA-256 `580c9dd870f3498da25caa93aae257af3aef6e9421927e17c99271febf89b271`). A separate read-only-mounted verification container checked exact package identity/version/architecture, x86-64 ELF binaries, OpenCode `1.17.13`, uv `0.11.26`, whole-payload link absence, generated-cache absence, 246 configured scientific resources byte for byte, identical sidecars, and identical main-program bytes apart from the expected Tauri bundle marker; it then passed all 170 HEOR tests independently from each extracted payload. Two brand-new Ubuntu 22.04 containers installed the `.deb`; the concise proof run reported `ai4heor 0.1.16 amd64`, one desktop process, one bundled OpenCode process, the newly created `/tmp/ai4heor-clean-home/Documents/OpenScience` workspace and `READY=1`. The package verifier now rejects links across the complete Debian payload as already done for RPM and disables Python bytecode generation during package tests. This evidence establishes current-version package integrity and headless startup, not a real Linux desktop visual session, native RPM installation, signing, notarization, Windows `0.1.16` build/first-run, or scientific validity.

2026-07-16 18:36 · feat(ai4heor): released the local AI4HEOR `0.1.16` alpha with a bounded first-party `$heor-population-adjusted-comparison` capability while preserving the corrected accountability boundary: Codex leads construction and verification of the software, the human researcher leads each study inside it, and the model/runtime only assists. The natural-language-first route admits exactly one anchored MAIC question over two independent randomized parallel two-arm trials sharing a common comparator, with local pseudonymous IPD for B versus A, aggregate C versus A evidence and target means, one explicit target population, 1–8 Human-selected scale-specific effect modifiers, and log OR or mean-difference effects; it rejects unanchored MAIC, STC, ML-NMR, larger or disconnected networks, survival/RR/HR/SMD effects, missing data, categorical encodings, higher moments, interactions, automatic modifier selection, trimming/capping, observational or single-arm evidence, and automatic economic-model use. The dependency-free standard-library engine solves positive mean-one exponential-tilting weights, exposes balance, weight and overall/arm ESS diagnostics, reports unadjusted and adjusted B:A plus anchored B:C effects, and refits every deterministic PCG32 stratified bootstrap replicate; any failure is retained and blocks review. Complete portable replay verifies the exact request/IPD/aggregate/evidence/evaluator/draw graph and every bootstrap draw. Native Rust independently re-reads current bytes and recomputes calibration coefficients, weights, balance, ESS, point effects and indirect arithmetic while truthfully excluding bootstrap uncertainty from native replay; a real generated portable fixture passed this native audit. The English/Chinese review pane keeps conversation primary and its eight-check form auxiliary; only an app-owned immutable snapshot plus private unanchored event chain can accept the exact seven-artifact graph as eligible for later evidence selection, without approving an analysis plan or selecting/populating an economic model. Product, technical, ecosystem, CI and Skill-routing documents mark only this anchored alpha shipped and keep unanchored MAIC, STC and ML-NMR in backlog. Verification: 170/170 deterministic HEOR tests, 111/111 portable artifact contracts, 9/9 MAIC and 9/9 NMA adversarial tests, 254 Rust tests passed with one fixed-public-network test ignored, 541/541 frontend tests across 82 files, typecheck, lint, production build, strict library Clippy with only the three pre-existing unrelated lint classes explicitly allowed, core-Skill/resource/admission/sidecar tests, both changed Skills through the official Skill Creator validator, JSON/diff checks, and a real portable-to-native fixture audit. The final read-only-mounted 88,479,823-byte x64 macOS debug DMG has SHA-256 `a289994ae39e80e778950937a58d807d9bc232a935961867695ef0f0afe43bde`, reports `0.1.16`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 46 Skill directories and 247 byte-identical packaged resources, passes all 170 tests against its packaged HEOR core, and contains zero application symlinks or generated Python caches. This milestone does not establish complete effect-modifier capture, overlap, exchangeability, transportability, source truth, data rights, clinical or substantive model validity, treatment value, economic-model suitability, reimbursement authority, authenticated reviewer identity or external replication; the DMG is unsigned, and Windows/Linux `0.1.16`, remote macOS, signing/notarization and clean-machine visual first launch were not rebuilt or inferred from older evidence.

2026-07-16 17:45 · feat(ai4heor): released the local AI4HEOR `0.1.15` alpha with a bounded first-party `$heor-network-meta-analysis` capability while preserving the corrected accountability boundary: Codex leads construction of the software, the human researcher leads each study inside it, and the model/runtime only assists. The natural-language-routed workflow accepts one Human-specified outcome/timepoint, 3–32 treatments, one independent randomized two-arm contrast per study, log OR/RR/HR, MD or SMD, and common-effect or common-heterogeneity REML random-effects analysis; it rejects disconnected networks, duplicate/multi-arm studies, arm-level likelihoods, Bayesian models, network meta-regression, population adjustment and automatic treatment/model/economic-input selection. A fixed local-only `Rscript --vanilla` adapter uses an already installed `netmeta` library without installation, binds the exact request, source, evidence, adapter, runtime and five backend outputs, and produces normalized league, heterogeneity, inconsistency and P-score artifacts. Dependency-free Python auditing independently reconstructs network geometry and weighted least-squares estimates, compares every normalized backend artifact, and truthfully limits the random-effects cross-check to estimates conditional on the backend tau rather than claiming independent REML heterogeneity estimation. Native Rust independently rechecks safe paths, hashes, contracts, geometry, core diagnostics and estimates; an app-owned immutable review snapshot plus private unanchored event chain makes later evidence selection eligible only after all eight Human checks for eligibility, transitivity, geometry, effect measure, heterogeneity, global/local incoherence, ranking direction and limitations. The English/Chinese review pane keeps conversation primary and the eight-checkbox form auxiliary; acceptance does not approve an analysis plan, select a treatment, or automatically feed an economic model. Product, technical, ecosystem, CI and Skill strategy documents now mark this alpha shipped. A disposable Linux/x86_64 real-backend smoke passed common and heterogeneous random runs with R 4.6.1, netmeta 3.6.1, meta 8.5.0 and metafor 5.0.1. Verification: 170/170 deterministic HEOR tests, 111/111 portable artifact contracts, 9/9 NMA adversarial tests, 250 Rust tests passed with one fixed-public-network test ignored, 540/540 frontend tests across 82 files, typecheck, lint, production build, strict library Clippy with only the three pre-existing unrelated lint classes explicitly allowed, core-Skill/resource/admission/sidecar tests, both changed Skills through the official Skill Creator validator, R/Python parsing and diff checks. The final read-only-mounted 88,238,435-byte x64 macOS debug DMG has SHA-256 `508e5d1b2ad87b869bdcba889d4af0c11eb47535fbc524cdf4279054cf0f1124`, reports `0.1.15`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 45 Skill directories and 236 byte-identical bounded scientific resources, passes all 170 tests against its packaged HEOR core, and contains zero application symlinks or generated Python caches. This milestone does not establish transitivity, source truth, data rights, clinical or substantive model validity, treatment ranking authority, economic-model suitability, authenticated reviewer identity or external replication; GPL R packages remain user-installed and unbundled, the DMG is unsigned, and Windows/Linux `0.1.15`, remote macOS, signing/notarization and clean-machine visual first launch were not rebuilt or inferred from older evidence.

2026-07-16 16:26 · feat(ai4heor): released the local AI4HEOR `0.1.14` alpha with the first-party `$heor-dynamic-budget-impact` capability while preserving the corrected accountability boundary: Codex leads construction of the software, the human researcher leads each study inside it, and the model/runtime only assists. Static BIA schema `0.1.0` remains compatible; dynamic schema `0.2.0` adds a deterministic three-year annual-boundary cohort ledger with explicit prevalent and incident populations, common mortality, scenario-specific initial and incident uptake, comparator displacement, intervention-start capacity, treatment persistence, itemized costs, one-way sensitivity and alternative scenarios. Its fixed event order opens stock, adds incident cohorts, prioritizes incident intervention starts within capacity, displaces comparator with remaining capacity, charges full-year costs, applies mortality, then applies persistence; intervention discontinuers move to comparator at the next boundary and comparator discontinuers exit the treated market. Results expose requested, delivered and unmet starts, opening/closing treatment stocks, treated counts, deaths, discontinuations, shares, category costs and annual/cumulative budget impact as fractional expected values. Without-access intervention flows and sensitivity/scenario mutations fail closed, Python and native Rust independently validate the contract, and dynamic BIA conditionally adds a nineteenth independent-validation requirement for external event/state and mass-balance replication before Human approval. The English/Chinese review pane selects the static or dynamic natural-language Skill and exposes the bounded flow ledger without describing expected cohorts as observed patients. Product, technical, ecosystem, reporting, workbench and Skill strategy documents now mark dynamic BIA shipped alpha. Verification: 170/170 deterministic HEOR tests, 111/111 portable artifact contracts, 247 Rust tests with 246 passed and one fixed-public-network test ignored, 539/539 frontend tests across 82 files, 7/7 asset-admission tests, both core-Skill and both packaged-resource tests, official Skill Creator validation, typecheck, lint, production build, strict Clippy with only the three pre-existing unrelated lint classes explicitly allowed, targeted rustfmt, AST/JSON and diff checks. The final read-only-mounted 88,056,093-byte x64 macOS debug DMG has SHA-256 `297fbbb4ff6a7c84aa3aeba5b0246c26d254262b87b286ddd347593b6e9e72be`, reports `0.1.14`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 44 Skill directories and 229 byte-identical scientific resources including the admission registry, passes all 170 tests against its packaged HEOR core, and contains zero application symlinks or generated Python caches. This alpha deliberately excludes partial-cycle entry/costing, re-initiation, treatment-specific mortality, capacity queues, induced diagnosis, combination therapy, severity or disease-state migration, more than two active treatments, and patient-level history; it does not establish source truth, data rights, epidemiological, clinical or substantive model validity, affordability, reimbursement acceptance, or authenticated reviewer identity. The DMG is unsigned, and Windows/Linux `0.1.14`, remote macOS, signing/notarization and clean-machine visual first launch were not rebuilt or inferred from older evidence.

2026-07-16 15:28 · feat(ai4heor): released the local AI4HEOR `0.1.13` alpha with the first-party `$heor-reproducibility-package` and corrected the product-accountability boundary: Codex leads construction, testing, documentation and packaging of AI4HEOR, while the human researcher leads every study inside the workbench; the model/runtime only assists under the researcher's question, method choices, evidence judgments, assumptions, interpretation and release authority. The new natural-language-routed Skill, template and dependency-free portable validator derive schema `0.1.0` only from a complete current report, inventory exactly the report graph plus declared synthesis, record exactly three deterministic replay recipes, bind the current AI4HEOR/platform/Python/result-engine identity and standard-library-only core boundary, reproduce the exact analysis/BIA source union with explicit availability/licence states, register exactly three result exhibits, and require exactly seven CHEERS/BIA claim links. Restricted source content is never copied, extra files and undeclared sources fail closed, and structural traceability is never described as scientific validity or successful external replication. Native Rust independently repeats the contract, treats a missing replay runtime as an incomplete release companion rather than blocking researcher-led exploratory calculation, exposes the audit in the English/Chinese review pane, and makes the existing Human release event bind the exact companion in addition to the report graph after deterministic replay; no second approval gate or Agent authority was created. PSM replay binds the complete current method/input argument graph and joint-survival files where required; artifact, report, runtime, recipe, source, availability, claim and release-binding drift are adversarially tested. Product, PRD, technical, reporting, workbench and Skill/plugin strategy documents now encode this role and release boundary, and the previously planned P1 asset is marked shipped alpha. Verification: 165/165 deterministic HEOR tests, 108/108 portable artifact/core-Skill contracts, 243/243 Rust tests with one fixed-public-network test ignored, 538/538 frontend tests across 82 files, 7/7 asset-admission and 2/2 packaged-resource tests, official Skill Creator validation, typecheck, lint, production build, strict Clippy with only the three pre-existing unrelated lint classes explicitly allowed, AST/JSON/rustfmt/diff checks, and a two-pass package audit. The first DMG audit correctly rejected four generated Python-cache directories; after source-cache cleanup and rebuild, the final read-only-mounted 88,063,464-byte x64 macOS debug DMG has SHA-256 `9eb63eee048dc99ca4f05f755b7d3f5b72074b08eba2f2a86477d410fc1e6fd7`, reports `0.1.13`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 43 Skill directories and 224 byte-identical scientific resources, passes all 165 tests against its packaged HEOR core, and contains zero symlinks or generated Python caches. This milestone does not establish source truth, data rights, substantive model or clinical validity, authenticated reviewer identity, journal/regulatory/reimbursement acceptance, or external-machine reproduction; the DMG is unsigned, and Windows/Linux `0.1.13`, remote macOS, signing/notarization and clean-machine first launch were not rebuilt or inferred from prior-version evidence.

2026-07-16 13:51 · feat(ai4heor): released AI4HEOR `0.1.12` and closed the current partitioned-survival-model release-integration gap without weakening the natural-language-first, model-neutral, local-first or Human-in-the-loop boundary. Independent-validation schema `0.2.0` now binds the exact 13-artifact PSM graph, while report schema `0.2.0` binds the exact 15-artifact release graph; both portable Python validators and native Rust audits select the schema from the current analysis plan, require the exact key/path/hash set, verify PSM and uncertainty source hashes, and retain schema `0.1.0` for non-PSM work. The app now accepts only the bounded first-party PSM result path, exposes dynamic bindings in the review pane, requires the current analysis approval to bind all six PSM method/input artifacts, requires independent validation to bind every current method/input/result artifact, and deterministically replays PSM `0.7.0`, uncertainty `0.13.0` or joint `0.14.0`, and BIA with their complete exact argument graph before release approval; Agents still cannot create analysis, validation, bootstrap-method or release authority. The live external capability refresh found no Skill or plugin suitable for wholesale method-authority integration: HEORAgent remains a quarantined optional discovery connector; survHE remains the highest-priority isolated R adapter; hesim, BCEA and heemod remain optional cross-check candidates; AER-Skills contributes reproducibility and claim-evidence patterns only; K-Dense and broader economics/scientific catalogs remain per-asset discovery sources; and mcp-stata remains a user-installed P2 bridge. A new first-party P1 `heor-reproducibility-package` is now specified alongside dynamic BIA, NMA, population-adjusted comparisons, RWE causal analysis, advanced VOI and source connectors, with per-Skill license, egress, provenance, domain-risk and adversarial-test admission retained. Verification: 165/165 deterministic HEOR tests, 100/100 portable artifact contracts, 240 Rust tests passed with one fixed-public-network test ignored, 538/538 frontend tests across 82 files, typecheck, lint, production build, 7/7 asset-admission tests, both core-Skill inventory tests, both packaged-resource tests, and both changed Skills through the official Skill Creator validator; AST/JSON parsing, touched-file rustfmt and diff checks also pass. The final read-only-mounted 87,977,776-byte x64 macOS debug DMG has SHA-256 `6ba066ec7ae6b31aa99169ebdc79ecce3bb4b530b14246c2ae2c4551460d9101`; it reports `0.1.12`, contains x86_64 OpenCode `1.17.13` and uv `0.11.26`, 42 Skill directories and 219 byte-identical scientific resources, passes all 165 tests against its packaged HEOR core, and contains zero symlinks or generated Python caches. This milestone does not establish substantive model, extrapolation, evidence or clinical validity; reviewer identity remains locally asserted; the DMG is unsigned; and Windows/Linux `0.1.12`, remote macOS, signing/notarization and clean-machine visual first launch were not rebuilt or rerun, so the prior `0.1.11` cross-platform evidence is not promoted to this version.

2026-07-15 21:02 · feat(ai4heor): converted AI4HEOR `0.1.11` from a macOS-local alpha with aspirational Linux CI into a reproducibly built and audited native x86_64 Linux delivery, while preserving the local-first, model-neutral, natural-language-first and Human-in-the-loop HEOR boundary. OpenCode `1.17.13` and uv `0.11.26` now have 12 exact SHA-256 pins covering every supported macOS/Windows/Linux x64/arm64 release archive; both fetchers verify bytes before extraction, fail closed on missing/duplicate/drifted pins, and clean temporary downloads. Linux CI truthfully builds only `.deb` and `.rpm`, uses Tauri-supported zstd level-3 RPM compression, and runs a new package admission verifier that checks package identity/version/architecture, x86-64 ELF binaries, exact sidecar versions, all 217 byte-identical scientific resources, absence of links/generated Python caches, and all 165 HEOR tests independently from each extracted payload; it also proves both formats contain identical sidecars and main-program bytes except for the one expected Tauri bundle marker. A clean Ubuntu 22.04 linux/amd64 build passed 165 Python HEOR tests, 96 artifact contracts, 237 Rust tests with one fixed-public-network test ignored, 538 frontend tests across 82 files, typecheck, lint and production build. Its 95,534,930-byte `AI4HEOR_0.1.11_amd64.deb` has SHA-256 `a6e8d76fb4ea598d2b0c3eeddf001fe6440d3d604a1e17f04bfdd467e31ec491`; its 88,439,118-byte `AI4HEOR-0.1.11-1.x86_64.rpm` has SHA-256 `64e66365473c2da21a072e81c8c75101bafac7f0c979c576b953d18e94243470`, valid RPM header/payload SHA-256, and a cleanly extractable zstd payload. A brand-new Ubuntu 22.04 container then installed the `.deb` as `ai4heor 0.1.11 amd64`, launched it under an isolated HOME/X session, reached simultaneous `ai4s-workbench` and bundled `opencode serve` readiness, and created `~/Documents/OpenScience`; this verifies installation/runtime startup, not a visual desktop acceptance test. The refreshed 87,939,538-byte x64 macOS debug DMG has SHA-256 `ae400c2673389537867b4b6479d7818bb858809431ad73bc4603973809f86547`; its read-only mount reports `0.1.11`, contains x86_64 OpenCode/uv at the pinned versions, 42 Skill directories and 217 byte-identical resources, passes all 165 packaged-core tests, and contains zero generated Python caches. README, requirements, design and CI now state the real package names and evidence without claiming AppImage, signing, notarization, a non-headless Linux visual session, native RPM installation, Windows first-run, or signed installers; those remain host/Human release gates.

2026-07-15 19:51 · feat(ai4heor): closed the first-party paired PFS/OS bootstrap P0 into an app-owned Human-in-the-loop analysis gate and released the local x64 macOS alpha as AI4HEOR `0.1.10`. The fixed natural-language workflow still prepares and executes schema `0.1.0` through `$heor-paired-survival-bootstrap`, preserves every fit/failure, and leaves the complete result `awaiting_bootstrap_method_review`; forms remain secondary and only the native review pane can accept or reject the exact result. A new dependency-free Rust boundary re-reads the fixed request and full nine-artifact execution graph, rejects unsafe/symlinked paths and stale bytes, regenerates every PCG32 whole-subject within-arm frequency, recalculates every reported natural-parameter curve, verifies all replicate/candidate rows and PFS≤OS coherence, and truthfully stops short of claiming an independent statistical refit. Human acceptance requires all seven explicit checks for resampling design, paired endpoints/censoring, selected families, failures/convergence, follow-up/extrapolation, parallel-arm conditional independence, and clinical plausibility. The app writes an immutable workspace review record plus a separate app-data unanchored SHA-256 event chain; Agents and Skills may reference but cannot create or edit that authority, and a later rejection for the same execution invalidates its earlier acceptance without invalidating unrelated executions. Current joint-survival manifest `0.5.0` binds the exact accepted record and result for `paired_patient_bootstrap`; prior-current `0.4.0` and `0.3.0` remain readable, while new paired analysis approval requires `0.5.0`. Python, the standalone validator, and native Rust enforce the same seven-check/source/result contract. Analysis-plan approval binds the current review record, uncertainty audit exposes whether that gate is actually required so an unrelated paired result cannot block a joint-posterior analysis, and every native uncertainty run rechecks the current app review chain before calculation. English/Chinese natural-language actions, review status, exact hashes, replicate/failure/curve counts, and accept/reject form are integrated without granting the Agent review authority. Verification: 165/165 deterministic HEOR tests, 96/96 portable artifact contracts, 237 Rust tests passed with one fixed-public-network test ignored, 538/538 frontend tests across 82 files including 8 review-pane cases, typecheck, lint, production build, strict Clippy with five pre-existing unrelated lint classes explicitly allowed, 2/2 core-Skill tests, 2/2 packaged-resource tests, 7/7 asset-admission tests, and all 36 discoverable core Skills through the official Skill Creator validator. The final mounted 87,944,534-byte x64 macOS debug DMG has SHA-256 `52960e47f92117749347847665e2710548c5ad07b52dded26140936aea212828`; it reports version `0.1.10`, contains byte-identical paired-bootstrap and joint-survival Skills plus the current Python engine, exposes 42 bundled Skill directories, passes all 165 core tests against mounted resources, and contains zero generated Python caches. The debug app remains unsigned; native statistical refitting parity, substantive censoring/endpoint/model/extrapolation/clinical validity, anchored or cryptographically verified Human identity, Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain unrun, unsupported, or Human-owned.

2026-07-15 18:55 · feat(ai4heor): shipped the first-party `$heor-paired-survival-bootstrap` P0 as a natural-language-routed, local-only execution capability without granting an Agent model-selection or approval authority. Request/result schema `0.1.0` accepts one strict pseudonymous CSV row per subject containing both PFS and OS observations, exact current analysis `0.15.0` / PSM `0.7.0` / materialization `0.2.0` bindings, Human-selected curve families, 1,000–10,000 iterations, and a Human command-authorization gate. A fixed versioned PCG32 stream writes a byte-reproducible frequency plan, resamples complete subject rows within independent parallel arms, preserves arm sizes and the same sampled indices for both endpoints, and runs a fixed `Rscript --vanilla` survHE/flexsurv adapter against an explicitly supplied existing isolated library without installation. Every replicate and failure remains visible; a dependency-free portable auditor regenerates the whole resampling plan, independently evaluates every natural parameter on the analysis grid with the shared all-family evaluator, rejects stale adapter/evaluator/source/result bytes, curve drift, invalid survival, PFS>OS, filtered/reordered rows, and symlinked output parents, and emits noncanonical joint-draw candidates only when the entire batch is complete. The method contract truthfully distinguishes preserved within-subject/within-strategy PFS/OS dependence from independently resampled parallel strategies: the latter declare `conditional_independence_given_parallel_arm_design` and never claim observed between-strategy correlation. Canonical joint-survival manifest `0.4.0` now records strategy design and between-strategy assumption; joint posterior rows may declare both within- and between-strategy dependence, paired parallel-arm bootstrap rows may declare only within-strategy dependence, and prior current schema `0.3.0` remains readable. Python, portable validation, and native Rust independently audit canonical rows before PSA consumption; detailed bootstrap refitting replay remains portable rather than being mislabelled as native re-fitting. The external Skill/plugin refresh still finds no candidate safe to bundle wholesale as HEOR method authority, so HEORAgent and broader catalogs remain quarantined/reference candidates while this narrow capability is first-party. A disposable Linux/x86_64 real-backend run with R 4.6.1, survHE 2.0.51, flexsurv 2.3.2, survival 3.8.6 and a non-scientific synthetic paired fixture completed 1,000/1,000 replicates, 4,000 fits, zero failures, full numerical/coherence audit, and eligible candidate generation; this proves the interface, not censoring, clinical, extrapolation, or statistical validity. Verification: 164/164 deterministic HEOR tests, 96/96 portable artifact contracts, 8/8 paired-bootstrap adversarial tests, 7/7 asset-admission, both core-Skill and both packaged-resource tests, all 36 core Skills through the official Skill Creator validator, 234 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, strict clippy with five pre-existing unrelated lint classes explicitly allowed, R/Python parse and diff checks, real-backend smoke, and mounted-package verification. The final 87,674,374-byte x64 macOS debug DMG has SHA-256 `e3ea0dcdd4282aec19258a638021f90ba6d69d30317ec947e4026c17ef7ef5fb`; its read-only mount contains the byte-identical 9-file paired-bootstrap Skill, valid 36-Skill tree, all 164 passing packaged-core tests, and zero generated Python caches. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, native refitting reimplementation, matched/clustered/crossover designs, informative-censoring methods, reconstructed IPD, treatment switching, cure/mixture/spline/Bayesian models, substantive evidence/model validity, and Human method/approval decisions remain unrun, unsupported, or Human-owned.

2026-07-15 18:15 · feat(ai4heor): completed the source-model covariance half of the current survival-uncertainty gap and refreshed the external HEOR Skill/plugin landscape without granting external code methodological or decision authority. `$heor-survival-fit-execution` keeps request/model schema `0.1.0`, adds current result schema `0.2.0`, and preserves legacy result readability; each converged survHE/flexsurv family now emits a hash-bound parameter-uncertainty artifact containing estimation-scale coefficients, exact family-specific order, full covariance, explicit inverse transforms, inverse-observed-Hessian method, and an asymptotic multivariate-normal basis. The fixed R adapter reads `flexsurvreg$res.t` and `flexsurvreg$cov`; portable Python and native Rust independently reject stale model bytes, order/transform drift, non-finite or wrong dimensions, asymmetry, non-positive-definiteness, and failure to reproduce bound natural parameters. Missing covariance remains explicitly unavailable and is never repaired, jittered, projected, or reconstructed; point-fit review eligibility stays separate. Every artifact declares `within_model_curve_only` and `joint_curve_draw_authority=false`, so independent PFS, OS, or strategy draws cannot satisfy the existing joint-survival contract. A disposable Linux/x86_64 run with R 4.6.1, survHE 2.0.51, flexsurv 2.3.2, survival 3.8.6, and the public 228-row `survival::lung` data converged all eight families, passed all eight independent curve challenges with maximum survival/hazard errors `1.665e-15`/`6.072e-17`, and produced eight audited positive-definite covariance artifacts; this is interface evidence, not clinical or extrapolation validity. The live external review identifies HEORAgent MCP as the strongest directly relevant optional connector candidate, but its pinned Git package/version mismatch, 12 audited dependency vulnerabilities including 4 high, broad combined network/write/method/calculation authority, and repeated test-configuration warnings keep it quarantined; 1,521 tests passed and 6 skipped. Awesome Econ AI Stuff, OpenBioMed, SciAgent-Skills, Open Science, and existing candidates remain idea or individually admitted connector sources rather than bulk runtime imports. The roadmap now makes first-party `heor-paired-survival-bootstrap` the next P0 and `heor-hta-source-connectors` a P1 asset; no redundant per-curve sampling Skill will be created. Verification: 162/162 deterministic HEOR tests, 96/96 portable artifact contracts, 6/6 survival-execution, 7/7 asset-admission, 4/4 local-evidence, record-run, core-Skill and packaged-resource tests, all 35 core Skills through the official Skill Creator validator, 234 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, clippy with two pre-existing unrelated lint classes allowed, Python/JSON/diff checks, and real-backend smoke validation. The final 87,640,097-byte x64 macOS debug DMG has SHA-256 `a371aba2ed2c64eea46225aca3561a475b1a088377cc0b634921f9e87421b089`; its read-only mount contains byte-identical core Skills, a valid packaged execution Skill, all 162 passing HEOR core tests, and zero generated Python caches. Joint endpoint/strategy dependence, paired bootstrap design, substantive validity, covariates, multiple arms per fit, cure/mixture/spline/Bayesian models, model averaging, automatic selection, Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, and Human evidence/method/approval decisions remain unsupported, unverified, or Human-owned.

2026-07-15 17:34 · feat(ai4heor): completed the prior P0 by extending first-party standard-parametric survival execution, independent numerical challenge, and Human-selected curve materialization from exponential/Weibull to all eight admitted survHE/flexsurv families without changing the natural-language-first or Human-selection boundary. Execution evaluator `ai4heor-survival-crosscheck@0.2.0` now independently reproduces survival and positive-time hazard for exponential, Weibull AFT, Gompertz, gamma, generalized gamma, generalized F, lognormal, and loglogistic natural parameters; every converged candidate must pass, failed fits remain visible, exponential and Weibull remain mandatory baseline successes, and a numerical cross-check failure is a first-class ineligible result rather than an exception or `not_applicable`. Materialization schema/evaluator `0.2.0` binds the exact first-party review `0.3.0` and normalized selected-model bytes, maps only explicit natural names to year-based typed names, recomputes every cycle boundary, and preserves legacy schema `0.1.0` only for external exponential/Weibull imports. Python, the portable Skill validator, and native Rust independently enforce family, parameterization, sign/domain, hash, review, fit-output, basis, grid, monotonicity, and value contracts; negative Gompertz shape is reproduced but explicitly retained as a non-zero-tail clinical-plausibility risk for Human review. The affected Skills and discovery metadata were regenerated and validated under the official Skill Creator instructions. Fixed-parameter golden values for all eight families match `flexsurv` 2.3.2, including generalized-gamma/F limiting cases. A real isolated Linux/x86_64 run with R 4.6.1, `survHE` 2.0.51, `flexsurv` 2.3.2, and `survival` 3.8.6 converged 8/8 candidates and passed all 8 cross-checks; maximum absolute errors ranged from zero to `3.265e-15` for survival and `1.474e-11` for hazard. That real run exposed and fixed an extreme generalized-F Beta-tail cancellation bug by switching complementary regularized-Beta branches and stabilizing `s1`/`s2`, rather than weakening the `1e-8` tolerance. Verification: 162/162 deterministic HEOR tests, 96/96 portable artifact contracts, both core-Skill and both packaged-resource tests, all 35 core Skills through the official validator, 233 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, clippy with two pre-existing unrelated lint classes allowed, targeted Rust formatting, Python compilation, JSON/diff checks, and repeated real-backend smoke validation. The final 87,621,299-byte x64 macOS debug DMG has SHA-256 `6a2c91c6ab2eb1eb1c075ad2a33648edbc3f88741ac823269dc45d5e80c04337`; its read-only mount contains the new Python core evaluator, portable all-family evaluator, updated materialization contract, working generalized-F golden evaluation, and zero generated Python caches. The next P0 is source-model covariance and joint draw export for the newly materializable families; covariates, multiple arms per run, left/interval censoring, competing/recurrent events, cure/mixture/spline/Bayesian models, probabilistic family/model averaging, automatic selection, substantive evidence/clinical/external validity, Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, and Human evidence/method/approval decisions remain unsupported, unverified, or Human-owned.

2026-07-15 16:53 · feat(ai4heor): shipped the bounded optional `survHE` execution path that the prior milestone left as P0, while preserving AI4HEOR's natural-language-first, local-first, model-independent and Human-in-the-loop boundaries. New first-party `$heor-survival-fit-execution` schema `0.1.0` accepts one Human-authorized strict UTF-8 two-column local time/event CSV with no direct identifiers; preflights current hashes, row/event/censor counts, observed/horizon grid, 2–8 pre-specified standard candidates including exponential and Weibull, exact installed package versions and a new fail-if-present output; invokes a fixed `Rscript --vanilla` adapter against an explicitly supplied existing library without package installation; fits every family separately; preserves failures, AIC/BIC/log likelihood, natural-scale parameters, warnings, session, log, plots and all file hashes; and never serializes a fitted object or copies source data into the result bundle. The dependency-free Python validator and new native Rust auditor re-read the request and current CSV, verify the complete runtime/result graph, and independently recalculate exponential and Weibull survival/hazard landmarks; numerical agreement is explicitly interface validation rather than statistical, clinical or external validity. `$heor-survival-extrapolation-review` now retains schema `0.2.0` for external local-fit imports and adds schema `0.3.0` for first-party bundles, requiring exact target, candidate order, runtime, normalized model outputs, diagnostics and transitive execution-audit agreement before the existing awaiting-Human curve-selection gate. The natural-language English/Chinese action routes local fitting through the new Skill first and keeps forms secondary; the review pane reports execution environment and cross-check state without gaining selection authority. The backend remains user-installed GPL software outside the MIT core and package. Local macOS R 4.1.1 correctly failed the probe because the packages are absent; a disposable Linux/x86_64 environment with networking disabled then ran R 4.6.1, `survHE` 2.0.51, `flexsurv` 2.3.2 and `survival` 3.8.6: 2/2 candidates converged, and independent maximum absolute survival/hazard errors were `3.608e-16`/`0` for exponential and `2.290e-16`/`1.388e-15` for Weibull. The first real run exposed and fixed a natural-versus-transformed parameter export error instead of relaxing tolerance. Verification: 158/158 deterministic HEOR tests, 116/116 portable artifact/core-Skill/resource/local-evidence/admission/execution tests, all 41 discoverable core Skills through the official Skill Creator validator, 232 Rust tests passed with one explicit public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, strict clippy with the three pre-existing unrelated lint classes allowed, targeted Rust formatting, JSON/diff and cache checks, and repeat real-backend smoke validation. The final 87,623,104-byte x64 macOS debug DMG has SHA-256 `0a1968c928ee81b5ebc2662313419fd523c81e93fa40f0a164fbd48132013eb8`; its read-only mount contains byte-identical complete HEOR core and Skill trees, zero Python caches, valid packaged execution/review Skills, a working packaged request-validator entrypoint, and all 158 core tests pass against the mounted engine. The next P0 is independently materializing and golden-validating additional admitted fitted families before economic-model use; covariates, multiple arms per run, left/interval censoring, competing/recurrent events, cure/mixture/spline/Bayesian models, covariance/model averaging, automatic selection, joint PFS/OS validity, substantive evidence validity, Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch and Human evidence/method/approval decisions remain unsupported, unverified or Human-owned.

2026-07-15 16:41 · feat(ai4heor): shipped a bounded composition of reviewed joint PFS/OS survival draws with raw cost, utility, and event-component uncertainty in the same partitioned-survival PSA, without claiming complete structural uncertainty. Uncertainty schema `0.14.0` / engine `0.15.0` now hash-binds the exact analysis, PSM `0.7.0`, treatment-duration, cost-normalization, utility, event-disutility, joint-survival manifest `0.3.0`, and joint-draw bytes; each PSA iteration consumes exactly one complete joint curve row and one component draw, rebuilds the economic schedules and aggregates, and evaluates them together. Base case, DSA, discount and half-cycle scenarios, and alternative treatment-effect-duration scenarios retain reviewed deterministic curves. Outputs are explicitly `joint_curve_and_component_parameter_uncertainty` / `joint_survival_curves_and_cost_utility_event_components`, require visible omissions for curve-family selection, extrapolation assumptions, and source-model validity, and state that survival-to-economic cross-domain dependence is not represented; a Human must justify independence or retain the dependence as a blocker. Python, the dependency-free portable validators, CLI, native Rust audit/execution, exact schema/hash pairing, TypeScript contract, English/Chinese natural-language repair route, and secondary review UI enforce the same fail-closed boundary. The official Skill Creator regenerated and validated the affected first-party assets; `$heor-uncertainty-analysis`, `$heor-joint-survival-uncertainty`, workbench, validation, and reporting Skills were extended, and a dedicated joint-component template was added rather than creating a duplicative Skill. Method boundaries were refreshed against current NICE PMG36 joint synthesis, extrapolation, structural/parameter uncertainty, correlation, Monte Carlo, and disaggregated-output guidance plus the ISPOR-SMDM PSA task-force report and published correlated-input and joint multistate methods. Verification: 158/158 deterministic HEOR tests, 111/111 portable artifact/core-Skill/resource/local-evidence/admission tests, all 34 discoverable core Skills through the official validator, 229 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, strict clippy with three pre-existing unrelated lint classes explicitly allowed, targeted Rust formatting, JSON and diff checks, and packaged-resource verification. The final 87,527,970-byte x64 macOS debug DMG has SHA-256 `9c87b7478aca4b805f52c18c87797250f0360242d0b4a0f50f92538f111508c3`; its verified read-only mount contains byte-identical complete HEOR core and Skill trees, zero Python caches, the valid new uncertainty template, and all 158 core tests pass while importing the mounted engine. The next P0 is an isolated optional survHE-compatible backend with cross-implementation validation; cross-domain dependence, probabilistic structural model averaging, curve-family and extrapolation uncertainty, substantive evidence validity, independent release validation, Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, and Human evidence/method/approval decisions remain unrun, unshipped, blocked, or Human-owned.

2026-07-15 15:32 · feat(ai4heor): shipped a bounded, auditable correlated cost–utility–event component-uncertainty path while preserving natural language as the primary interaction and Human ownership of distributions, dependence, evidence fitness, and model structure. Current analysis `0.15.0` / PSM `0.7.0` now pairs with uncertainty schema `0.13.0` / engine `0.14.0`, hash-binds the exact PSM plan, survival materializations, treatment-duration, cost-normalization, utility, and event artifacts, and admits only allowlisted raw annual quantity, unit price, cost adjustment, non-dead source utility, utility adjustment, event schedule, decrement, or acute-duration targets. Every DSA endpoint and PSA iteration rebuilds normalized prices, annual state costs, full cycle utility schedules, per-event QALY loss, and strategy/cycle/state aggregates before evaluating fixed survival curves; aggregate and derived targets, stale bytes, domain drift, non-finite draws, unsupported event modes, and implied utility below -1 fail closed. Independent Uniform, Gamma, and Lognormal marginals are supported; mixed Uniform/Lognormal correlation requires a Human-supplied, evidence-linked latent-standard-normal Gaussian-copula matrix, and shared provenance never creates implicit dependence. Output is explicitly `component_parameter_uncertainty` / `cost_utility_event_components_only`, with every PFS/OS curve omitted and conditional CEAC/CEAF/per-person EVPI never presented as complete PSM or structural uncertainty. Python, the standalone portable validator, native Rust audit/execution/hash checks, packaged CLI, TypeScript contract, English/Chinese natural-language repair route, and secondary result warning enforce the same boundary. The official Skill Creator updated and validated `$heor-uncertainty-analysis`; ecosystem review found no external Skill/plugin with this fail-closed contract, so the first-party asset was extended rather than importing methodological authority. The next P0 is composition with reviewed joint-survival rows, while source-model validity, structural uncertainty, non-annual cost structures, model-validation/report integration, and Human evidence/method/approval decisions remain blocked or Human-owned. Methods were refreshed against current NICE PMG36 plus the ISPOR-SMDM PSA task-force report and published correlation methods. Verification: 155/155 deterministic HEOR tests, 96/96 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill and 2/2 Tauri-resource tests, all 34 discoverable core Skills through the official validator, 229 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, strict clippy with six pre-existing unrelated lint classes explicitly allowed, targeted Rust formatting, JSON/diff checks, and packaged-resource verification. A packaging contract caught and corrected the initially missing new Python resource before release. The final 87,519,477-byte x64 macOS debug DMG has SHA-256 `22619c27b95ec3e01dafdfec82f61a409d31de3dfb0e77d933ee3b10fef6d8d1`; its verified read-only mount contains byte-identical complete HEOR core and Skill trees, zero Python caches, a valid packaged uncertainty Skill, and all 155 core tests pass while importing the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, composed joint survival plus component uncertainty, substantive evidence validity, and release-ready independent validation remain unrun or unshipped.

2026-07-15 14:51 · feat(ai4heor): shipped a bounded, auditable event-disutility capability without letting an Agent infer event selection, severity, incidence, decrement, duration, additivity, or overlap policy. New first-party `$heor-event-disutilities` schema `0.1.0`, structure-neutral analysis schema `0.15.0`, and PSM schema/engine `0.7.0` bind the exact analysis, health-state-utility, and event-artifact bytes; support one-time probability, recurrent expected-event, and continuous-exposure schedules; reproduce per-occurrence, per-cycle, and strategy/cycle/state QALY losses; require every affected utility item to name the exact event in `excluded_effects` and never in `captured_effects`; reject dead-state losses, multi-cycle one-time probabilities, acute durations longer than one model cycle, unknown authority fields, arithmetic/hash drift, and combined implied utility below -1. The PSM engine weights event losses using the same state occupancy, half-cycle correction, and outcome discounting as state utilities, and reports pre-event QALY, event QALY loss, and final QALY separately. Python, a dependency-free portable validator, native Rust, packaged CLI, approval hashing, the natural-language-first English/Chinese repair route, and secondary audit/result fields enforce the same boundary; legacy analysis/PSM versions remain readable. Long sequelae require explicit health or tunnel states; event costs, interactions, caregiver spillovers, and correlated cost/utility/event component uncertainty remain blocked. The method boundary follows current NICE PMG36, Canada's Drug Agency guidance, NACI guidance, a 2024 Value in Health methods paper, and the published systematic review while leaving evidence fitness and method decisions Human-owned. The official Skill Creator instructions governed the concise Skill entrypoint, separate method reference, strict template, UI metadata, and executable validator. The roadmap now advances to correlated cost/utility/event component uncertainty. Verification: 149/149 deterministic HEOR tests, 96/96 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill contract tests, 2/2 Tauri-resource tests, all 34 discoverable core Skills through the official Skill Creator validator, 228 Rust tests passed with one fixed-public-network test ignored, 537/537 frontend tests across 82 files, typecheck, lint, production build, targeted Rust formatting, JSON/diff and source-cache checks. A final scope audit caught and reversed 19 unrelated recursive formatting changes before a clean rebuild. The final 87,484,435-byte x64 macOS debug DMG has SHA-256 `f6fe418881e363d4f96225ace7e3fe5a0147173ddd3a058905de653f53c42637`; its verified read-only mount contains byte-identical complete HEOR core and Skill trees, zero Python caches, and all 149 core tests pass while importing the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, component uncertainty, substantive evidence validity, model-validation/report-package release integration, and Human evidence/method/approval decisions remain unrun, unshipped, or Human responsibilities.

2026-07-15 14:10 · feat(ai4heor): shipped a bounded, auditable health-state-utility input capability and completed the current external HEOR Skill/plugin admission review without granting an external tool methodological or decision authority. New first-party `$heor-utility-inputs` schema `0.1.0`, structure-neutral analysis schema `0.14.0`, and PSM schema/engine `0.6.0` bind exact analysis and utility-artifact bytes; require one record for every ordered strategy/state with instrument, version, respondent, source population/sample, elicitation/value-set/license status, mapping provenance where applicable, captured and excluded effects, overlap assessment, and explicit Human-reviewable bases; independently reproduce the complete cycle-specific utility schedule from the base value and multiplicative age, comorbidity, and population-alignment adjustments; require first-cycle agreement with aggregate `state_utilities`; and reserve the dead state as an unadjusted zero-QALY anchor. Python, the dependency-free portable validator, native Rust, packaged CLI, approval hashing, natural-language English/Chinese repair route, and auxiliary review surface enforce the same exact-field, arithmetic, coverage, schema-pairing, and no-authority-field boundary. Event disutilities remain explicitly separate and blocked, while component-level utility uncertainty metadata is visible but execution fails closed until a correlated component contract exists. The generic engine does not hardcode EQ-5D, a country value set, mapping algorithm, or draft guidance. The design follows current NICE PMG36, ISPOR health-state-utility and mapping good practices, EuroQol value-set/licensing controls, and Canadian economic-evaluation guidance; the 2026 NICE UK EQ-5D-5L consultation is recorded as proposed rather than silently treated as current policy. The official Skill Creator instructions governed the Skill entrypoint, model-neutral natural-language workflow, strict template, method boundary, UI metadata, and portable validator. The ecosystem audit reconfirmed that HEORAgent's useful bounded multi-source-search pattern should be independently implemented first-party while the current broad MCP remains quarantined for mixed-authority/network/privacy/dependency/version-identity risks; general research MCPs and domain libraries remain references or isolated optional adapters, not bundled HEOR methods. The executable asset roadmap now prioritizes `$heor-event-disutilities`, then correlated utility-component uncertainty; a separate `$heor-utility-evidence-review` is deferred until the present workflow actually outgrows one Skill. Legacy analysis `0.13.0` / PSM `0.5.0` remains readable. Verification: 137/137 deterministic HEOR tests, 96/96 portable artifact contracts, 2/2 core-Skill contract tests, 2/2 Tauri-resource tests, all 33 discoverable core Skills through the official Skill Creator validator, 225 Rust tests passed with one fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, asset admission (12 reviewed, 0 admitted, 8 quarantined, 4 rejected), JSON/diff and source-cache checks. A package audit caught five generated Skill bytecode directories and triggered a clean rebuild. The final 87,480,790-byte x64 macOS debug DMG has SHA-256 `2bd57775c997121355b79b1b91446b19772d5c2591a0f59be1367cdd7cf5f440`; its verified read-only mount contains byte-identical complete HEOR core and Skill trees, zero Python caches, and all 137 core tests pass while importing the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, event-disutility calculation, correlated utility-component uncertainty, substantive evidence validity, model-validation/report-package release integration, and Human evidence/method/approval decisions remain unrun, unshipped, or Human responsibilities.

2026-07-15 13:20 · feat(ai4heor): shipped a bounded, ingredient-level annual state-cost normalization alpha instead of letting aggregate `state_costs` hide resource and price arithmetic. New first-party `$heor-cost-input-normalization` schema `0.1.0`, structure-neutral analysis schema `0.13.0`, and PSM schema/engine `0.5.0` bind the exact analysis and normalization bytes; require 1–1,000 ordered strategy/state resource items with positive annual quantity, compatible unit, non-negative source unit price, currency, price year, jurisdiction, distinct list/net/tariff/paid/negotiated/microcost/opportunity-cost basis, explicit tax status, and evidence or proposed-assumption IDs; apply inflation exactly when price years differ, currency conversion exactly when currencies differ, and at most one separately evidenced price adjustment; independently reproduce `normalized_unit_price = source_price × product(factors)`, `annual_item_cost = quantity × normalized_unit_price`, and every strategy/state aggregate against the analysis plan. Python, the dependency-free portable Skill validator, and native Rust enforce the same exact-field, hash, unit, basis, arithmetic, aggregate, and no-authority-field boundary; the packaged CLI and native PSM runner require the artifact, approval bindings include its hash, and the conversation-first English/Chinese repair route uses the Skill while the review pane exposes item count as a secondary surface. The official Skill Creator scaffold and validator governed the Skill entrypoint, UI metadata, strict template, method contract, and portable validator. The bounded design follows current NICE PMG36 resource/cost, paid-price, uncertainty, inflation, exchange-rate, and tax guidance; ISPOR drug-cost unit/local-currency/price-year guidance; Canadian jurisdiction/opportunity-cost guidance; and WHO ingredients-based quantity-times-price costing, without treating any source as an automatic jurisdictional default. Inflation-index, exchange-rate, tax, price-concept, scope, opportunity-cost, confidentiality, and transferability choices remain Human method decisions. Legacy analysis `0.12.0` / PSM `0.4.0` remains readable; cost-component uncertainty deliberately rejects `0.13.0` rather than reusing aggregate distributions. Event, one-time, time-varying, capital, dynamic-BIA, and societal-cost structures, utility-input industrialization, model-validation/report-package release integration, and substantive evidence validity remain blocked or future assets. Verification: 128/128 deterministic HEOR tests, 96/96 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 32 discoverable core Skills through the official Skill Creator validator, 223 Rust tests passed with one fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, cargo tests, targeted rustfmt, diff and source-cache checks. A first package audit caught generated Skill bytecode and triggered a clean rebuild. The final 87,414,021-byte x64 macOS debug DMG has SHA-256 `ea6f50f76efe1629f10763c982d778e5b8030ee105a320b9c4f748342af562ea`; its verified read-only mount contains byte-identical complete HEOR core and affected Skill trees, zero Python caches, and all 128 core tests pass while importing the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain unrun and are not inferred from local success.

2026-07-15 12:36 · feat(ai4heor): replaced the partitioned-survival alpha's implicit full-duration assumption with an explicit, hash-bound treatment-effect-duration contract without inferring durability from an HR point estimate. New first-party `$heor-treatment-effect-duration` schema `0.1.0`, PSM schema/engine `0.4.0`, and native Rust audit require exactly two ordered strategies; retain immutable reviewed PFS/OS source curves through a declared evidence horizon; then rebuild the intervention tail from comparator cumulative-hazard increments and an endpoint-specific evidence-bound HR. Every analysis must provide complete sustained, immediate-stop, and log-linear-waning scenarios with shared endpoint horizon/HR bases, a Human-reviewable base-case ID, exact duration/materialization/analysis hashes, complete grid, strictly positive source survival, and PFS ≤ OS; unsupported treatment counts, non-cycle boundaries, stale bytes, silent repair, switching, non-HR effects, and authority fields fail closed. The deterministic PSM result and uncertainty result now return bounded cost/QALY summaries for all duration scenarios. Economic-only uncertainty binds the duration artifact while retaining fixed PFS/OS omissions; joint-survival manifest schema `0.2.0` binds the selected duration structure and no longer mislabels it as omitted, while alternative durations remain separate deterministic structural results rather than claimed probabilistic integration. Legacy PSM `0.3.0` and joint manifest `0.1.0` remain readable. The official Skill Creator scaffold and validator governed the new Skill package, natural-language workflow, strict template, method reference, portable recalculation script, and UI metadata; `$heor-workbench`, partitioned-survival, uncertainty, joint-survival, validation, reporting, and English/Chinese repair routes now use that asset with forms secondary. The desktop review card exposes duration-scenario count and base ID, and the result card shows scenario-level incremental cost and QALY. Method boundaries follow current NICE PMG36 duration/scenario and proportional-hazards guidance, CADTH guidance to test no waning/no post-trial effect/decline-to-cessation and justify PSM causal assumptions, and recent NICE examples, while leaving clinical plausibility and Human method choice outside code authority. Verification: 123/123 deterministic HEOR tests, 94/94 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 31 discoverable core Skills through the official Skill Creator validator, 220 Rust tests passed with one fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, cargo check, targeted Rust formatting, JSON/diff and source-cache checks. The final 87,370,312-byte x64 macOS debug DMG has SHA-256 `9c5afd0eac5d8d127385e802c9576e31834914b7b069ce31818c317ae7403000`; its verified read-only mount contains byte-identical duration/PSM/uncertainty engine modules and Skill trees, zero Python caches, and all 123 core tests pass against the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, joint survival fitting, probabilistic integration across duration alternatives, broader treatment-effect structures, model-validation/report-package release integration, clinical/external validity, and Human evidence/method/approval decisions remain unrun, unshipped, or Human responsibilities.

2026-07-15 12:30 · feat(ai4heor): replaced the partitioned-survival alpha's fixed-curve PSA boundary with an explicitly joint, source-bound survival-draw contract without pretending that artifact integrity proves statistical or structural validity. Uncertainty schema `0.12.0` / engine `0.13.0` now require one backend-neutral row per PSA iteration containing every ordered strategy's complete PFS and OS curves, generated only as `joint_posterior` or `paired_patient_bootstrap`; independent endpoint sampling is forbidden, and every row must preserve the exact time grid, start at one, be non-increasing, remain in `[0,1]`, and satisfy PFS ≤ OS. The dependency-free Python engine consumes the joint row together with separately sampled economic inputs, while base case, DSA, and structural scenarios retain deterministic reviewed curves. A Skill Creator-governed first-party `$heor-joint-survival-uncertainty` package now provides the natural-language-first workflow, method contract, strict template, example, UI metadata, and standalone source-hash/JSONL validator; `$heor-uncertainty-analysis` and `$heor-workbench` route to it, and the English/Chinese repair actions tell the Agent to create or repair these artifacts rather than making the user fill a primary form. Native Rust independently streams and audits the manifest, draw rows, and exact local source bytes before execution, passes both files to the packaged CLI, and rejects engine/native hash drift; the review card distinguishes joint curve-plus-economic parameter uncertainty from the retained economic-only compatibility path. The design follows current NICE uncertainty guidance and DSU partitioned-survival warnings that independently modeled PFS/OS are a structural assumption, while accepting joint posterior or paired patient-bootstrap rows as a model-backend-neutral interchange rather than bundling a licensed/statistical fitting plugin. Curve-family selection, extrapolation assumptions, treatment-effect duration, source-model correctness, clinical/external validity, and Human method/evidence/approval decisions remain explicit omissions or release blockers. Verification: 114/114 deterministic HEOR tests, 94/94 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 30 discoverable core Skills through the official Skill Creator validator, 217 Rust tests passed with one fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, cargo check, targeted rustfmt, JSON/diff and source-cache checks. The final 87,269,912-byte x64 macOS debug DMG has SHA-256 `285afd5f468b107562100cdd26cee8018313bd1e28fa31a49d91190076cb9a72`; its verified read-only mount contains byte-identical complete HEOR engine, new joint-survival Skill, and updated uncertainty Skill trees, zero Python caches, and all 114 core tests pass against the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, joint survival fitting itself, full structural uncertainty, model-validation/report-package release integration, and Human decisions remain unrun, unshipped, or Human responsibilities.

2026-07-15 11:10 · feat(ai4heor): added deliberately partial, economic-input-only uncertainty for the partitioned-survival alpha without presenting fixed survival curves as a complete PSA. Analysis `0.12.0` now pairs only with uncertainty schema `0.11.0` / engine `0.12.0`; the dependency-free Python engine, portable validator, and native Rust audit admit exact scalar state-cost/state-utility DSA and PSA targets plus bounded economic structural scenarios, bind the exact current PSM plan and curve-materialization bytes, and require every ordered strategy's PFS and OS curve to be named in `omitted_parameters`. Results are classified `partial_parameter_uncertainty` with scope `economic_inputs_only`; CEAC, CEAF, convergence and per-person EVPI are explicitly conditional on fixed survival curves, while survival-parameter covariance, curve selection, extrapolation and joint PFS/OS uncertainty remain release blockers. The natural-language-first English/Chinese repair route, review card warning, conditional EVPI label, Skill Creator-governed `$heor-uncertainty-analysis` template/reference/validator, and product/technical/ecosystem/Skill roadmap expose the same boundary and route PSM work through `$heor-partitioned-survival` and `$heor-economic-inputs`. Final QA also corrected the economic provenance contract from an unreviewable scalar dot path to the existing evidence-auditable reward-vector path and removed irrelevant Markov-transition limitations from PSM output. Verification: 108/108 deterministic HEOR tests, 93/93 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 29 core Skills through the official validator, 214 Rust tests passed with one fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, targeted rustfmt, JSON/diff and source-cache checks. The final 87,265,591-byte x64 macOS debug DMG has SHA-256 `b9e20f8558f971b948626ff5b737a5afab23ab018e06f5696a5136ede0bcb6db`; its verified read-only mount contains byte-identical complete HEOR engine and Skill trees, the new schema `0.11.0` template, zero Python caches, and all 108 core tests pass against the mounted engine. The debug app is intentionally unsigned; Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, full PSM survival/joint uncertainty, model-validation/report-package release integration, and Human evidence/method/approval decisions remain unrun, unshipped, or Human responsibilities.

2026-07-15 05:37 · feat(ai4heor): removed the partitioned-survival alpha's fake Markov dependency with a model-structure-neutral economic-input contract. New analysis schema `0.12.0` links PSM explicitly and gives every ordered strategy exactly `name`, `state_costs`, and `state_utilities`; initial distributions, transition matrices, and transition schedules are forbidden. New PSM schema/engine `0.3.0` consumes the reusable economic specification while legacy PSM `0.2.0` remains calculation-compatible with existing Markov-shaped plans. Python, the dependency-free portable validators, native Rust approval audit, and browser preview independently enforce the same schema pairing, common-input provenance, value bounds, exact strategy keys, and no-transition boundary. The official Skill Creator-generated `$heor-economic-inputs` asset adds a natural-language-first workflow, strict template, standalone validator, and method contract grounded in current NICE PMG36, NICE DSU partitioned-survival guidance, and ISPOR modeling/utility guidance; `$heor-partitioned-survival`, `$heor-input-provenance`, `$heor-workbench`, English/Chinese repair actions, product documentation, and the executable Skill/plugin roadmap now route through it. Release remains blocked until PSM uncertainty, model-validation, and report-package contracts bind the PSM inputs/results; treatment-effect composition, unsupported survival families, and substantive clinical/external validity also remain outside this milestone. Verification: 103/103 deterministic HEOR tests, 92/92 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 29 core Skills through the official validator, 214 Rust tests with 213 passed and the fixed-public-network test ignored, 536/536 frontend tests across 82 files, typecheck, lint, production build, targeted rustfmt, JSON/diff and source-cache checks. The final 87,216,799-byte x64 macOS debug DMG has SHA-256 `2c2c2b5a492d53f75f1da3cbd240e05cd421a08e6f1ebade86cd00ce9433072c`; its verified read-only mount contains byte-identical economic-input and PSM engine modules, the complete officially valid new Skill tree, zero Python caches, and all 103 core tests pass against the mounted engine. Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, and Human evidence/method/approval decisions remain unrun or Human responsibilities.

2026-07-15 05:10 · feat(ai4heor): closed the partitioned-survival alpha's numerical-derivation gap with the first-party `$heor-survival-curve-materialization` asset instead of treating review hashes or copied rows as proof of reproducibility. PSM schema/engine `0.2.0` now requires exact `heor/survival-curve-materializations.json` bytes in declared strategy order, PFS then OS. Each curve binds the current analysis, exact schema `0.2.0` review, Human-selected converged family, one strict typed fit-output file, explicit exponential-rate or Weibull AFT shape/scale parameters, evaluator `ai4heor-parametric-survival@0.1.0`, full cycle grid, reproduced values, and the exact ordered review/fit/evaluator basis IDs copied into the PSM rows. The dependency-free Python engine and standalone standard-library validator independently apply `exp(-rate*t)` or `exp(-(t/scale)^shape)` and fail closed on path escape, stale bytes, review/fit semantic drift, unsupported parameterization, wrong order, parameter/value/grid/basis mismatch, or hidden authority fields. Native Rust separately re-reads and fully audits the review, verifies the selected model's exact typed fit binding, recalculates every admitted value, binds the manifest/reviews/fits into analysis-plan approval, passes the manifest to the packaged engine, and rejects an engine hash mismatch. The official Skill Creator-generated Skill, strict templates, method contract, paired PSM Skill, natural-language-first English/Chinese repair route, product/technical/ecosystem/asset strategy, auxiliary audit UI types, and packaged resources now expose the same boundary. The capability deliberately does not fit data, transform backend coefficients, auto-select a distribution, infer covariance, apply treatment effects, admit other survival families, or claim clinical/external validity. Verification: 97/97 deterministic HEOR tests including exact-source-byte portable failures, 90/90 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 28 core Skills through the official validator, 210 Rust tests with 209 passed and the fixed-public-network test ignored, 535/535 frontend tests across 82 files, typecheck, lint, production build, targeted rustfmt, JSON/diff and source-cache checks. The final 87,242,589-byte x64 macOS debug DMG has SHA-256 `1991839d0f3dff5bc00001c1457529c2b772f99363f4881aada1f5f00fca4d4c`; its read-only mount contains byte-identical materialization/PSM engine modules and both Skill trees, an officially valid packaged Skill, 97/97 tests passing against the mounted engine, and zero Python caches. PSM DSA/PSA and structural uncertainty, treatment-effect duration/composition, competing estimands, non-exponential/Weibull-AFT evaluators, the shared economic-input contract, model-validation/report integration, substantive clinical/external validity, Human curve selection and approval, Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain explicitly unshipped, unrun, or Human responsibilities.

2026-07-15 04:26 · feat(ai4heor): shipped the bounded `$heor-partitioned-survival` alpha as a structurally separate three-state PFS/OS calculation instead of disguising state occupancy as a Markov transition matrix. The dependency-free engine derives progression-free, progressed, and dead occupancy as `PFS`, `OS-PFS`, and `1-OS` on the exact analysis cycle grid, applies the approved analysis plan's state rewards, half-cycle rule, discounting, strategy order, and threshold, and returns cost, QALY, baseline-pairwise, full incremental-frontier, and primary-threshold results. Python, the portable Skill validator, and native Rust fail closed on stale analysis or review bytes, wrong states, missing rationales/bases, time drift, increasing curves, PFS above OS, incoherent occupancy, unsafe paths, hidden authority fields, or incomplete validation plans. Each curve binding now names the exact `partitioned_survival.strategies.<strategy>.<endpoint>` target and Human-selected converged family; the desktop reuses the complete schema `0.2.0` survival-review audit and checks the PFS/OS context, time origin, and year unit before binding the plan and every review into analysis-plan approval. The natural-language-first English/Chinese workflow asks the Agent to create or repair the artifact, while the auxiliary review pane exposes strategy/curve/time-point counts, blockers, execution, and exploratory results. Release remains intentionally blocked whenever PSM is linked because model-validation, uncertainty, and report-package contracts do not yet bind its plan/result. The live external registry refresh retained HEORAgent, AIPOCH, awesome-rosetta, and ai4s-skills as quarantined idea/test sources and mcp-stata as a P2 user-installed AGPL/licensed-Stata connector candidate; no external asset displaced the narrower first-party Skill/engine/app-gate design. Verification: 92/92 deterministic HEOR tests, 90/90 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 27 discoverable core Skills through the official Skill Creator validator, 208/209 Rust tests with the fixed-public-network test ignored, 535/535 frontend tests across 82 files, typecheck, lint, production build, target rustfmt, JSON/diff and source-cache checks. The final 87,145,569-byte x64 macOS debug DMG has SHA-256 `a40036724e8810da7cedcec7f54229199a5e4f0e24fb41c9478322ef2fc467f5`; its read-only mount contains byte-identical engine and Skill entrypoint files, an officially valid complete Skill tree, an importable packaged PSM function, and zero Python caches. PSM DSA/PSA and structural uncertainty, treatment-effect duration/composition, competing estimands, a typed curve-materialization artifact that reproduces values from selected fit parameters, a dedicated shared economic-input contract that removes the alpha's unused transition-input requirement, model-validation/report integration, substantive clinical/external validity, Human curve selection and approval, Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain explicitly unshipped, unrun, or Human responsibilities.

2026-07-15 03:43 · feat(ai4heor): admitted the indexed multi-curve survival-extrapolation review contract without pretending that an integrity envelope is a partitioned-survival method. A single parametric-survival target remains backward-compatible at `heor/survival-extrapolation-review.json`; plans with 2–32 targets now require schema `0.1.0` `heor/survival-extrapolation-reviews.json` in exact plan-target order and one safely named schema `0.2.0` review per target. The standalone collection validator and native Rust boundary independently reject missing, extra, duplicated, reordered, malformed, stale-hash, oversized, or escaping reviews, re-audit each selected distribution and its fit evidence, and bind the manifest plus every review file into analysis-plan approval. Base-case, uncertainty, and budget-impact execution now require every bound hash to remain current. The natural-language-first English/Chinese workflow routes the Agent to single or collection preparation, while the auxiliary review pane reports collection coverage and per-curve selection/convergence/recommendation without turning recommendations into authority. The Skill Creator-governed Skill gained the ordered template, portable collection validator, explicit multi-target workflow, and PFS/OS stop rules; product, technical, ecosystem, and platform-asset strategy now distinguish independently auditable control/intervention or PFS/OS curves from still-unshipped cross-curve coherence. Verification: 86/86 deterministic HEOR tests, 90/90 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all 26 non-placeholder bundled core Skills through the official validator, 206/207 Rust tests with the fixed-public-network test ignored, 535/535 frontend tests across 82 files, typecheck, lint, production build, target rustfmt, JSON/diff and source-cache checks. The final 87,098,327-byte x64 macOS debug DMG has SHA-256 `3dca0a22a761cefdaf89ef8f9278d7f0c02e4b6bb29a711f5894d8ae7e884e44`; its read-only mount contains a byte-identical officially valid Skill tree with both collection assets, the native collection path symbol, zero Python caches, and a packaged draft collection that fails closed. PFS ≤ OS, arm/time-origin alignment, curve crossings, joint covariance and structural uncertainty, partitioned-survival occupancy, optional isolated `survHE` execution, patient-level fitting, substantive external/clinical validity, Human curve selection, Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain explicitly unshipped, unrun, or Human responsibilities.

2026-07-15 03:12 · feat(ai4heor): closed the survival-extrapolation review's app-native trust-chain gap instead of adding another disconnected Skill. Schema `0.2.0` now names the exact current `analysis_id` and sole `parametric_survival_to_transition_schedule` provenance path; the standalone validator and a new Rust-native audit independently require that target match, require the Human-selected analysis-plan distribution to be a converged pre-specified candidate, validate the bounded survHE import contract, stream-verify every local bundle/command/session/model/diagnostic hash without following workspace escapes, preserve failed fits and Human-only authority, and fail closed for malformed or multi-curve plans until an indexed collection contract exists. The natural-language-first English/Chinese review pane now shows not-required, incomplete, or bound status plus target, selected family, convergence, scenarios, analyst recommendation, and blocking gaps; forms remain auxiliary and recommendation is never approval. Analysis-plan approval now appends the exact review SHA-256, and base-case, uncertainty, and budget-impact authorization all invalidate stale or missing survival-review bindings. The Skill Creator-governed Skill, template, contract, validator, product/technical/ecosystem/asset strategy, and adversarial tests were upgraded together. Verification: 86/86 deterministic HEOR tests, 88/88 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, every bundled core Skill through the official Skill Creator validator, 205/206 Rust tests with the one fixed-public-network test ignored, 534/534 frontend tests across 82 files, targeted UI/i18n tests, typecheck, lint, production build, target rustfmt, JSON/diff checks, and source/package cache checks. The final 87,062,684-byte x64 macOS debug DMG has SHA-256 `6d2ff7cc3d3816b881cf2086a2b8f0cfbf4e919c001f995bc034ad02e609ce60`; its read-only mount contains a byte-identical and officially valid schema `0.2.0` Skill, the native command symbol, zero bundled Python caches, and a fail-closed packaged draft-template audit. The optional isolated `survHE` fitting backend, patient-level fitting, indexed multi-curve/PFS-OS review, substantive external/clinical validity, Human curve selection, Windows/Linux and remote macOS builds, signing/notarization, and clean-machine first launch remain explicitly unshipped, unrun, or Human responsibilities.

2026-07-15 02:42 · feat(ai4heor): bundled the first-party `$heor-survival-extrapolation-review` alpha as the first dedicated asset from the HEOR Skill/plugin roadmap. The Skill Creator-generated package adds schema `0.1.0`, UI metadata, a draft template, a method contract grounded in NICE PMG36/DSU TSD14 and official `survHE` documentation, and a standalone validator for one absolute time-to-first-event curve. It requires an imported local fit bundle with 2–8 pre-specified standard parametric candidates, exact fit-bundle/command/session/model/diagnostic hashes, visible failures and deviations, common observed and extrapolated survival/hazard landmarks, AIC/BIC/log likelihood, KM/log-cumulative-hazard/hazard views, clinical and external plausibility, limitations, at least two structural scenarios, and an `awaiting_human_selection` gate; it rejects post-hoc candidate-order drift, stale or escaping files, invalid/non-comparable landmarks, fewer than two converged candidates, and embedded execution or scientific approval fields. Natural-language Chinese/English interaction remains primary and forms auxiliary. Two design-audit findings were closed before packaging: execution approval was removed from the agent-authored artifact because only the app-owned chain may authorize commands, and direct patient-level fitting was removed because IPD remains outside the current MVP boundary; the alpha imports already-generated local `survHE` evidence only. Verification: 86/86 deterministic HEOR tests, 87/87 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, all bundled core Skills through the official Skill Creator validator, 199/200 Rust tests with one fixed-public-network test ignored, 534/534 frontend tests across 82 files, targeted post-hardening contract/UI/type checks, lint, production build, diff checks, and source/package cache checks. The final 87,054,756-byte x64 macOS debug DMG has SHA-256 `d9038fd41c6eb043708cfa642f2fb232ef4b54d4699c7d378303cd1361e68db2`; its read-only mount contains a byte-identical, officially valid Skill tree with zero Python caches and a fail-closed draft-template audit. The optional isolated R/IPD backend, app-native artifact viewer/auditor, exact review-hash binding into analysis-plan approval, Windows/Linux and remote macOS builds, signing/notarization, clean-machine first launch, substantive external/clinical validity, and Human curve selection remain explicitly unshipped or Human responsibilities.

2026-07-15 02:19 · feat(ai4heor): shipped the first-party `$heor-hazard-ratio-adapter` and converted the requested Skill/plugin search into an executable platform asset strategy. Analysis schema/engine `0.11.0` admits exactly one positive constant HR applied to cycle-aligned increments of one selected non-negative, non-decreasing baseline cumulative-hazard curve for a single absorbing time-to-first event in exactly two states: `p=-expm1(-HR*(H0(i)-H0(i-1)))`, with `H0(0)=0`. Every hazard, the HR, and the exact `endpoint_alignment`, `population_transportability`, `proportional_hazards_assumption`, `effect_constancy_over_horizon`, and `treatment_switching_assessment` review bases bind evidence or proposed assumptions without creating approval. Python, portable provenance, native Rust, browser TypeScript, and the standalone Skill independently recompute every complete matrix and fail closed on stale output, non-monotone/all-zero hazards, probability saturation, invalid bases, unsupported fields, non-proportional/time-varying HRs, waning/stopping, unresolved switching, competing/recurrent events, fitting/selection, or partitioned survival. Paired uncertainty schema `0.10.0` varies only `hazard_ratio.value` with strictly positive bounded Uniform support whose DSA/PSA high reproduces a finite full schedule; unbounded HR support is deferred. Natural-language Chinese/English actions remain primary and forms auxiliary. The 2026-07-15 ecosystem refresh found no external HEOR Skill or MCP asset suitable for direct bundling: HEORAgent Git remained at the quarantined revision while its repository and npm release channels reported divergent versions; AIPOCH and awesome-rosetta offered useful task/test ideas but retained generic defaults, heuristic units/confidence, or unpinned method dependencies; mcp-stata is a valuable AGPL, licensed-Stata, broad-code-execution P2 connector candidate; `survHE`, `BCEA`, `hesim`, and `heemod` remain higher-value isolated R adapter/cross-check candidates. `docs/SKILL_AND_PLUGIN_STRATEGY.md` now separates Skills, deterministic engines, connectors, knowledge packs, and test assets and prioritizes first-party survival extrapolation review, partitioned survival, treatment-effect duration, cost normalization, and utility inputs before dynamic BIA, NMA/MAIC, RWE, and advanced VOI. Verification: 86/86 deterministic HEOR tests, 84/84 portable artifact contracts including HR provenance/uncertainty, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, 199/200 Rust tests with the one fixed-public-network test ignored, 534/534 frontend tests across 82 files, typecheck, lint, production build, target rustfmt, strict Clippy with only the three documented unrelated legacy lint classes, every bundled core Skill through the official Skill Creator validator, packaged standalone execution, JSON/Python/diff checks, and source-tree cache cleanup. Package QA caught generated Skill `__pycache__` in the first build; the final 87,070,232-byte x64 macOS debug DMG has SHA-256 `b08b65fb1b44c496518b8020af250ee60fed1dbd6386334d3fb8a746bfd6985f`, contains zero Python caches, has byte-identical packaged engine and all 11 changed/new Skill trees, passes the packaged Skill/adapter checks, and runs all 86 core tests against the read-only mounted engine. Windows/Linux and remote macOS CI, signing/notarization, clean-machine first launch, substantive PH/endpoint/transportability/effect-duration/switching assessment, clinical/model validity, upstream latest-package re-audits, and Human approval remain unrun or Human-in-the-loop responsibilities and are not inferred from local deterministic or packaging evidence.

2026-07-15 01:20 · feat(ai4heor): shipped the first-party `$heor-relative-effect-adapter` across the deterministic Python engine, portable provenance and uncertainty validators, native Rust review, browser TypeScript review, natural-language UI, Skills, documentation, and packaged desktop. Analysis schema/engine `0.10.0` admits one exact `relative_effect_to_transition_schedule` contract: one absorbing event in exactly two states, equal model/effect intervals, one value-plus-basis baseline risk per cycle with at least one positive value, one evidence- or assumption-bound RR or OR, and the exact Human-in-the-loop review bases `endpoint_alignment`, `population_transportability`, and `effect_constancy_over_cycles`. All four calculation/audit layers and the standalone adapter independently recompute every complete matrix with `q*RR` or `q*OR/(1-q+q*OR)` and fail closed on stale output, all-zero baselines, probability saturation, unsupported measures, competing/recurrent events, or invalid interval, basis, state, and extraction shapes. Paired uncertainty schema `0.9.0` targets only `relative_effect.value`: RR DSA and bounded Uniform PSA highs stay strictly below `1/max(q>0)` and unbounded RR distributions are rejected before execution, while OR permits Lognormal or strictly positive bounded Uniform PSA; every draw recomputes the whole schedule and baseline risks, measure, intervals, operation, and review bases remain fixed. Natural-language actions remain primary and forms auxiliary; HR routes to the future `$heor-hazard-ratio-adapter`, followed by separately bounded partitioned-survival and survival-fitting/extrapolation-review assets, while cost normalization, utility mapping, dynamic-cohort BIA, and NMA/MAIC remain later unshipped candidates. A 2026-07-15 external refresh found HEORAgent unchanged at its quarantined audited revision and no new HEOR-specific capability in the 27-file `ai4s-skills` upstream delta, so no external executable Skill or plugin was admitted. Verification: 80/80 deterministic HEOR tests, 80/80 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, 197/198 Rust tests with the one explicit public-network test ignored, 533/533 frontend tests across 82 files, typecheck, lint, production build, touched-file rustfmt, strict Clippy with only three documented unrelated legacy lint classes allowed, all 10 changed/new Skills through the official Skill Creator validator, standalone template execution, JSON/diff checks, and source-tree cache cleanup. The 87,003,077-byte x64 macOS debug DMG has SHA-256 `8b1dfc2dbc9782f09165f46f89647d84f20ea65aa8c036f8ad17cf2eb10708e9`; its read-only mount contains the new engine module and complete Skill, all changed packaged engine and Skill trees match source bytes, the packaged standalone adapter and official Skill validator pass, and all 80 core tests pass against the mounted engine without writing Python cache. Windows/Linux and remote macOS CI, signing/notarization, clean-machine first launch, substantive endpoint/transportability/effect-constancy assessment, clinical/model validity, and Human approval remain unrun or Human-in-the-loop responsibilities and are not inferred from local deterministic or packaging evidence.

2026-07-15 00:25 · feat(ai4heor): shipped a bounded, auditable background-plus-excess mortality method across the AI4HEOR source and packaged desktop layers. Analysis schema/engine `0.9.0` admits exactly two states with absorbing death, any finite positive cycle length, an age-aligned general-population life table, and one constant additive non-negative excess hazard; each annual probability, excess value, population-exchangeability basis, and no-double-counting basis is explicitly bound, while the two review bases remain evidence/assumptions rather than approval. Python, portable provenance, standalone Skill, native Rust, and browser TypeScript independently apply `h_bg=-log1p(-q)` and `p=-expm1(-(h_bg+h_excess)*cycle_length)`, use `floor(start_age + elapsed model time)`, allow the legitimate zero-mortality base case, and reject non-finite or probability-1 floating-point saturation. Paired uncertainty schema `0.8.0` admits only strictly positive DSA/PSA variation of the exact `excess_mortality_rate_per_year.value`, fixes every life-table and transformation field, and restricts required external structural scenarios to state cost/utility scalars, discount rates, or half-cycle correction so an approvable scenario cannot fail later by invalidating the mortality schedule. The new packaged `$heor-background-mortality` Skill, template, validator, natural-language-first Chinese/English actions, related Skill routing, reporting, and product/technical/ecosystem contracts retain Human-in-the-loop stopping rules for already all-cause inputs, cause-specific/subdistribution confusion, calendar improvement, mixtures/heterogeneity, time-varying excess, competing non-death events, multiplicative/SMR alternatives, and partitioned survival. Independent review found and closed cross-layer zero-bound, subannual-age, probability-saturation, target-allowlist, and audit-pass/runtime-fail gaps. Verification: 74/74 deterministic HEOR tests, 75/75 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, 2/2 core-Skill tests, 2/2 Tauri-resource tests, 194/194 Rust tests with one explicit public-network test ignored, 532/532 frontend tests across 82 files, typecheck, lint, production build, target rustfmt, strict Clippy with only the documented unrelated `project.rs` sorting lint allowed, all eight changed/new Skills through the official Skill Creator validator, JSON/diff checks, and no packaged Python cache. The 86,999,648-byte x64 macOS debug DMG has SHA-256 `6129bff29c86e2ff0ab5654deb8339fc1bd20d6351537dd23032b40592c2096e`; its read-only mount contains the new engine module and complete Skill, all changed packaged engine/Skill trees match source bytes, the standalone adapter validates the half-year template, and all 74 core tests pass against the mounted engine. Windows/Linux and remote macOS CI, signing/notarization, clean-machine first launch, and substantive clinical/model validity remain unrun or Human-in-the-loop responsibilities and are not inferred from local packaging evidence.

2026-07-14 22:10 · chore(ai4heor): closed the local-green/remote-unknown harness gap before adding another HEOR method. The PR workflow now triggers on deterministic HEOR, every bundled core Skill, Rust/native review code, frontend review code, dependency locks, and HEOR design documents; it runs Python core, portable artifact, local-evidence, asset-admission, Skill, and Tauri-resource contracts on macOS/Windows/Linux, runs the full Rust native suite on all three systems, and separately requires frontend typecheck, 531-test suite, lint, and production build. A new dependency-free core-Skill gate rejects malformed frontmatter, folder/name drift, placeholder text, invalid descriptions, and missing/escaping relative resources, while a second gate requires the Tauri resource manifest to contain every deterministic `heor_core` Python module at its exact packaged path. The release matrix now pins Python 3.12, runs both gates before bundling, uses AI4HEOR in the draft release and macOS instructions, and fails when no installer is produced. Product and technical contracts now reflect analysis `0.8.0`, uncertainty `0.7.0`, 2–16 cost-effectiveness strategies with explicitly pairwise BIA, engine `0.8.0`, AI4HEOR naming, and Linux `.deb`/`.rpm` delivery constraints instead of stale two-strategy/v0.1 language. Method review ranked background mortality/competing-risk semantics ahead of relative-effect application and PFS/OS partitioned survival; development intentionally paused at this harness gate so those methods cannot enter an unguarded package. Local verification passes: 64/64 deterministic HEOR tests, 70/70 portable artifact contracts, 4/4 local-evidence tests, 7/7 asset-admission tests, both new Skill checks, both new resource checks, 191/191 Rust tests with one explicit public-network test ignored, 531/531 frontend tests, typecheck, lint, production build, YAML parse, and diff checks. Current-commit Windows/Linux runner, installer, first-launch, sidecar, and packaged-engine evidence remains unavailable until this branch is pushed and the remote workflows are run; no remote success is inferred from local tests.

2026-07-14 21:47 · feat(ai4heor): removed the deterministic cost-effectiveness core's fixed comparator/intervention ceiling with approvable analysis schema `0.8.0` and uncertainty schema `0.7.0`. A new analysis declares 2–16 safe strategy IDs in an exact baseline-first order; the engine reports each strategy versus the declared baseline and separately performs fully incremental analysis, grouping equivalent cost/QALY points before strict and extended dominance, constructing the efficiency frontier, and calculating sequential ICERs only between adjacent frontier strategies. Seeded DSA/PSA now lets every declared strategy compete on net monetary benefit, preserves aligned per-strategy costs and QALYs, reports each strategy's unique-optimal CEAC probability plus ties, keeps CEAF null when expected NMB is tied, and calculates per-person EVPI against the exact maximum expected NMB with stable summation. The bounded BIA remains explicitly pairwise and must select two exact declared IDs; it does not claim a multi-treatment market model. Cross-layer adversarial repair closed ignored legacy strategies, malformed/non-string/dynamic strategy IDs, JSON Pointer-like BIA identifiers, non-finite derived arithmetic, dominated duplicate outcomes, tolerance-tie EVPI drift, malformed report summaries, and portable/native/UI version drift. Natural-language Chinese/English repair actions, Skills, templates, contracts, report bindings, and the Human-in-the-loop review surface now expose the same schema pairing while forms remain secondary. Verification: 531/531 frontend tests across 82 files, 191/191 Rust tests (1 explicit live-network test ignored), 64/64 HEOR core tests, 70/70 artifact contracts, 7/7 asset-admission tests, 4/4 local-evidence tests, typecheck, lint, production build, target rustfmt, strict clippy with the single observed unrelated `project.rs` `unnecessary_sort_by` allowance, six official Skill Creator validations, JSON/diff/cache checks, and real-browser QA with zero console errors and one existing React Router development warning pass. The final 86,965,016-byte x64 macOS debug DMG has SHA-256 `bd1da1504c5bb7f7ba63e7bfd0f8b5b59b266c15bc9d2d950154cf43f5930e48`; its read-only mount contains both new templates and no Python cache, all six changed packaged Skill trees and six changed core modules match source bytes, all Skills validate, and all 64 core tests pass against the mounted engine. Windows/Linux and remote macOS CI, signing/notarization, clean-machine first launch, and substantive clinical/model validity remain unrun or Human-in-the-loop responsibilities and are not inferred from this deterministic delivery evidence.

2026-07-14 16:50 · feat(ai4heor): closed the bounded survival-parameter uncertainty gap and made the user's “valuable but not directly reusable assets must be improved to industrial delivery standard before admission” rule explicit and testable in the platform strategy. Uncertainty schema `0.5.0` and engine `0.6.0` can now target only the exact positive exponential `rate_per_year` or Weibull `shape`/`scale_years` value inside an admitted analysis-schema-`0.6.0` survival transformation; each parameter uses its sole extraction or proposed-assumption basis, positive DSA bounds, and gamma, lognormal, or strictly positive uniform PSA. Every DSA run and seeded PSA draw applies all replacements to an ephemeral plan, recomputes each affected complete per-cycle schedule exactly once, updates the derivation snapshot, and runs ordinary model validation. Direct derived-row mutation, old schemas, mismatched curve families, unlinked bases, nonpositive values, beta/Dirichlet, and unsupported transforms fail closed across Python calculation, the portable Skill validator, and native Rust audit. Current lognormal-Cholesky groups remain available in schemas `0.4.0` and `0.5.0`; known but unsupported dependence remains a review blocker. Natural-language Chinese/English actions, the workbench, cohort, rate, survival, uncertainty, and validation Skills, contracts, template, product/technical/ecosystem decisions, and Skill metadata now expose the same boundary. NICE PMG36 and DSU TSD 14/21 support explicit alternatives, validity review, and uncertainty, but this slice deliberately does not fit/select curves, reconstruct covariance from incomplete output, combine PFS/OS, apply treatment effects, model background mortality/competing risks, or claim extrapolation validity. The asset-admission record now defines industrially deliverable evidence explicitly: revision/license, retained and removed authority, adaptation delta, positive/adversarial tests, cross-platform evidence, methods/security review, rollback/kill switch, notices/dependencies, and zero blockers; candidates otherwise remain quarantined. Verification: 526/526 frontend tests across 82 files, 181/181 Rust tests (1 explicit live-network test ignored), 48/48 HEOR core tests, 58/58 artifact contracts, 7/7 asset-admission tests, 4/4 local-evidence tests, typecheck, lint, production build, targeted rustfmt, strict clippy with exactly the three documented unrelated allowances, official Skill Creator generation and validation for both materially changed Skills, JSON/Python/diff checks, and mounted-package execution pass. Packaging QA caught and removed a generated `__pycache__` from the first build. The final 86,904,724-byte x64 macOS debug DMG has SHA-256 `9889c1deda3417060cdfca67964fe8fabe8891fd1307b673c91bbfd1881058c4`; its read-only mount contains no Python caches, both packaged Skills pass the official validator, and all 48 core tests pass against the packaged source. Windows/Linux and remote macOS CI, signing/notarization, clean-machine first launch, and substantive survival-model validity remain unrun or outside this deterministic boundary and are not inferred from local success.

2026-07-14 16:19 · feat(ai4heor): applied the platform asset-industrialization rule to an initial survival-analysis fragment and admitted first-party analysis schema/engine `0.6.0` plus `$heor-survival-curve-adapter`. An already-selected exponential or Weibull scale/shape curve can now generate a complete per-cycle transition schedule only for exactly two states and one absorbing all-cause event endpoint, using stable cumulative-hazard increments; every positive parameter binds exactly one strict-JSON extraction or `proposed` assumption, and schedule, cycle length, state indices, parameterization, sources, and derivation snapshot fail closed on drift. The Python engine, standalone Skill CLI, portable provenance validator, native Rust approval audit, and browser preview independently recompute the output. The Skill Creator workflow turned the method into a packaged Skill with metadata, contract, template, validator, explicit NICE PMG36/DSU method basis, and stopping rules; the Chinese/English review panel exposes a natural-language “derive from survival curve” action while forms remain secondary. This is deterministic evaluation, not curve fitting/selection, KM/IPD reconstruction, PFS/OS partitioned survival, flexible/cure/mixture modeling, relative-effect application, background mortality, competing risks, clinical extrapolation validation, or survival-parameter DSA/PSA; those remain explicit Human-in-the-loop gaps rather than inferred capability. Verification: 526/526 frontend tests across 82 files, 180/180 local Rust tests (1 explicit live-network test ignored), 57/57 HEOR artifact contracts, 46/46 HEOR core tests, four changed/new Skills passed the official Skill Creator validator, typecheck, lint, production build, targeted rustfmt, strict clippy with exactly the three documented unrelated lint allowances, JSON/Python checks, diff checks, and real browser QA with 0 console errors and 1 existing React Router development warning pass. The final 86,897,236-byte x64 macOS debug DMG (`0efef74c4d894186b8f10bc4eccd811ae3e4e629b88495e84ca6e8bf76652bd6`) passed a read-only-mount audit: the packaged engine and five changed/new Skill trees match source bytes, contain zero Python caches, the mounted engine passes all 46 tests, and the packaged standalone adapter reproduces the three-cycle hand-checkable schedule. Windows/Linux, remote macOS, signing/notarization, clean-machine first launch, and substantive survival-model validity remain unrun or outside the deterministic boundary and are not inferred from local success.

2026-07-14 15:41 · feat(ai4heor): industrialized evidence-bound cross-parameter dependence as first-party uncertainty schema `0.4.0` and engine `0.5.0`: PSA can now sample 2–32 scalar lognormal parameters per group from a declared latent standard-normal correlation matrix using dependency-free Cholesky decomposition, including jointly estimated competing event rates whose complete transition input is recomputed for every draw; every group requires finite symmetric unit-diagonal strictly positive-definite coefficients, a rationale, common basis IDs linked by every member distribution, unique membership, and deterministic ordering, while perfect/singular/non-positive-definite matrices, unsupported marginals/scales/methods, arbitrary copulas or rank correlations, correlation repair/inference, and any known omitted dependence fail closed rather than being guessed. The bounded method was adapted from current NICE/ISPOR/DSU guidance into calculation code, portable/native validators, natural-language repair actions, five hardened Skills/references/templates, UI audit counts, product/technical documentation, exact hand calculations, and adversarial tests instead of copying an external package or treating a prompt as a releasable asset; substantive evidence interpretation, dependence selection, scientific validity, and approvals remain Human-in-the-loop. Verification: 525/525 frontend tests across 82 files, 179/179 local Rust tests (1 explicit live-public-source test ignored), 54/54 HEOR artifact contracts, 43/43 HEOR core tests, 4/4 local-evidence tests, 7/7 asset-admission tests, all five changed Skill validations, typecheck, lint, production build, strict clippy with only the three documented pre-existing unrelated lint allowances, targeted formatting, JSON/Python checks, diff checks, and real browser QA with zero console errors pass. The final 86,825,866-byte x64 macOS debug DMG (`8a4a7edb99a855bfafbd224aa2e7cb4c15d1401e857957234b119a1ce1526c27`) passed direct-app and read-only-mount audits: the packaged Python source and five changed Skill trees match source bytes, contain no Python caches, and the mounted engine passes all 43 tests; Windows/Linux, remote macOS, signing/notarization, and a clean-machine first launch remain unrun and are not inferred from local success.

2026-07-14 14:41 · feat(ai4heor): industrialized a common but previously blocked evidence transformation as first-party schema `0.5.0` and `$heor-transition-rate-adapter`: constant cause-specific competing event rates can now deterministically generate a complete static transition matrix or model-cycle schedule using total event mass `1 - exp(-sum(rate) × cycle_length)` and proportional cause allocation, with structural zeros omitted and absorbing rows explicit. Every event binds exactly one selected extraction (including strict-JSON pointer support) or `proposed` assumption; Python calculation, the portable provenance validator, native Rust approval audit, and browser preview independently recompute the full output, require exact cycle/phase/row/basis-set contracts, and fail closed on altered rates, stale matrices, invalid pointers, unused bases, or schema misuse. The method guidance from ISPOR-SMDM, PHARMAC, and the multi-state conversion literature was adapted into bounded code, golden calculations, adversarial tests, natural-language actions, six hardened Skills/references, and product/technical documentation rather than copying an external tool or treating a prompt as an industrial asset. The explicit boundary remains: this is a constant competing-first-event calculation with at most one state change per cycle, not probability time conversion, HR/RR/OR application, pooling, calibration, survival extrapolation, general CTMC matrix exponentiation, within-cycle multi-step paths, or rate-space DSA/PSA; rate-derived transition outputs remain fixed in the current uncertainty engine, and substantive appropriateness plus approval remain Human-in-the-loop. Verification: 525/525 frontend tests across 82 files, 177/177 local Rust tests (1 explicit live-public-source test ignored), 52/52 HEOR artifact contracts, 37/37 deterministic HEOR core tests, 7/7 asset-admission tests, all six changed/new Skill validations, typecheck, lint, production build, targeted Rust formatting, strict clippy with only the three documented pre-existing unrelated lint allowances, diff checks, and real browser QA with zero console errors pass. Packaging audit caught and fixed an initially missing `transition_rates.py` resource declaration. The final 86,839,082-byte x64 macOS debug DMG (`df3442d6682321365e490d9824b1b54cf77823b4bffc90469eb4212ab2248b36`) passed a read-only-mount audit: all 15 changed packaged engine/Skill/reference/script assets match source bytes, no Python caches are present, and the packaged schema/engine `0.5.0` core reproduces comparator QALY 2.44, intervention QALY 2.71, incremental QALY 0.27, and ICER 1,003.703704. Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 14:03 · feat(ai4heor): removed the deterministic core's remaining time-homogeneous cohort limitation with a bounded, evidence-reviewable schema `0.4.0` `transition_schedule`: each strategy now declares exactly one static matrix or a sequence of complete matrices at unique one-based model-cycle change points, the last phase persists through the horizon, every phase is independently validated for probability, dimension, order, horizon, and mass conservation, and prior static schemas remain calculable and approvable where already supported. The result records static versus piecewise model-cycle behavior and effective start cycles; DSA/PSA can target complete scheduled rows and structural scenarios can vary declared change points, while time in state, tunnel/semi-Markov memory, patient history, rate/hazard conversion, pooling, calibration, heterogeneity, interactions, individual simulation, and time-varying rewards remain explicitly unsupported rather than approximated invisibly. NICE PMG36 and ISPOR-SMDM state-transition good-practice questions were adapted into a new first-party `$heor-cohort-state-transition` Skill, executable contract, natural-language audit action, provenance/uncertainty/validation gates, golden case, and cross-layer tests instead of directly importing an external asset; this follows the platform rule that useful outside material must be bounded, hardened, tested, licensed where relevant, and independently admitted before distribution. Human approval and semantic model adequacy remain outside the Agent and engine. Verification: 524/524 frontend tests across 82 files, 174/174 local Rust tests (1 explicit live-public-source test ignored), 51/51 HEOR artifact contracts, 33/33 deterministic HEOR core tests, 4/4 local-evidence tests, 7/7 asset-admission tests, all four changed/new Skill validations, typecheck, lint, production build, strict clippy with only the three documented pre-existing unrelated lint allowances, targeted formatting, Python compilation, diff checks, and real browser QA with zero console errors pass. The 86,769,518-byte x64 macOS debug DMG (`56539f378499b2a5b69b89eee3617c7cd6fd61eb9473770c5b6bebe96c5a9ba5`) passed direct-app and read-only-mount audits: all 15 changed packaged engine/Skill/reference/template/script assets match source bytes, the packaged validator rejects missing input with usage exit 2, and the packaged engine reproduces the schema/engine `0.4.0` time-varying golden case with intervention cost 348.9, QALY 3.489, incremental QALY 0.05, and ICER 6,978; Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 13:31 · feat(ai4heor): closed the remaining evidence-value-to-model-value integrity gap with executable derivation contracts instead of trusting path/source metadata alone — approvable analysis plans now require schema `0.3.0`, and every required input must declare `direct_evidence`, `explicit_assumption`, or `monetary_adjustment` plus an exact model-value snapshot; direct evidence must parse the selected extraction as strict JSON and equal the complete model value recursively, explicit assumptions must have no fabricated source/extraction binding and link at least one proposed assumption, and monetary adjustment must bind every source value to an exact extraction scalar or array index before independently reproducing `source_value × factor`. The portable validator, native Rust approval/runtime audit, browser preview, deterministic engine, natural-language prompts, three hardened first-party Skills, references, template, tests, and product/technical documentation now enforce this boundary; schemas `0.1.0` and `0.2.0` remain calculable for reproducibility but cannot pass approval. Free-form formulas, pooling, probability/rate conversion, matrix assembly, calibration, interpolation, or other unsupported transformations cannot be hidden in rationale and remain explicitly incomplete until bounded deterministic adapters exist; semantic appropriateness and reviewer judgment remain Human-in-the-loop, browser preview cannot replace the native audit against current synthesis bytes, and local reviewer labels are still unauthenticated. Verification: 523/523 frontend tests across 82 files, 172/172 local Rust tests (1 explicit live-public-source test ignored), 48/48 HEOR artifact contracts, 28/28 deterministic HEOR core tests, 4/4 local-evidence tests, 7/7 asset-admission tests, all three changed Skill validations, typecheck, lint, production build, strict clippy with only the three observed pre-existing unrelated lint allowances, targeted Rust formatting, Python compilation, diff checks, and real browser QA with zero console errors pass. The 86,745,389-byte x64 macOS debug DMG (`fcef01d2b4ae950eefb815b6107542fd13e99d9c004bcfb0dcd80ad3f020ea5d`) passed both direct-app and read-only-mount audits: all nine changed packaged engine/Skill/reference/template/script assets match source bytes, the packaged validator returns its expected usage exit, the packaged engine returns schema/engine `0.3.0`, basis CNY/2026, delta cost 6,174.670240, delta QALY 0.194334, and golden ICER 31,773.564495, and no stale mount remains; Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 13:04 · feat(ai4heor): established an executable monetary normalization boundary for deterministic HEOR analysis: schema `0.2.0` declares one model-wide ISO 4217-format currency and price year, every monetary model input records its source value/currency/year, multiplicative adjustment factor, method, and linked evidence or proposed-assumption IDs, and approval fails closed unless `source_value × factor` reproduces the model value and all target bases match; direct same-basis inputs forbid invented adjustment metadata, while adjusted inputs require an explicit method and traceable basis IDs, so AI4HEOR never silently selects inflation indices, exchange rates, or VAT treatment. Legacy schema `0.1.0` remains calculable for exploratory compatibility but carries a null economic basis and cannot pass the approval gate. The dependency-free Python engine, portable validator, native Rust boundary, report package, Skills/references/templates, and conversation-first review UI now preserve and display the exact economic basis without hard-coded CNY; the Human-in-the-loop reviewer still judges whether the chosen index, exchange-rate source, perspective, tax treatment, and composite factor are substantively appropriate. Verification: 522/522 frontend tests across 82 files, 169/169 local Rust tests (1 explicit live-public-source test ignored), 43/43 HEOR artifact contracts, 27/27 deterministic HEOR core tests, 4/4 local-evidence tests, 7/7 asset-admission tests, all three changed Skill validations, typecheck, lint, production build, strict clippy with only the three observed pre-existing unrelated lint allowances, Python compilation, diff checks, and real browser QA with zero console errors pass. The 86,748,407-byte x64 macOS debug DMG (`87d8f3a3c08b0ec9072c71bedf81f1b404905c942dec2d7f05060725831f1457`) passed a read-only mount audit: 12 changed packaged assets match source bytes, no Python cache files are bundled, both packaged validators execute their expected entrypoints, and the packaged engine returns schema `0.2.0`, basis CNY/2026, and the golden ICER 31,773.564495; Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 12:31 · feat(ai4heor): upgraded the first-party uncertainty capability from a single willingness-to-pay result to a backward-compatible, declared-threshold decision-uncertainty contract. Schema `0.2.0` requires 2–101 unique, increasing, non-negative thresholds containing the analysis plan's primary value and a rationale sourced from the natural-language decision context or explicit human instruction; legacy `0.1.0` remains readable at the primary threshold but rejects a silently ignored grid. The dependency-free two-strategy engine reuses one seeded PSA draw set to calculate expected incremental NMB, intervention/comparator/tie optimal probabilities, CEAC, CEAF, per-person EVPI, and Monte Carlo error at each threshold. It explicitly leaves population EVPI and EVPPI null and cannot infer research funding, study design, reimbursement, or policy advice. The review pane adds an accessible 0–100% CEAC/CEAF line chart as a read-only secondary evidence surface, while conversation remains the primary interaction. The exact decision-uncertainty object now flows through portable and native report-package audits without manufacturing values for legacy results. This capability industrializes useful BCEA/methods concepts as first-party schemas, calculation code, validators, tests, documentation, and packaged assets rather than embedding BCEA, R, or an upstream prompt; the concise Skill delegates formulas and contract detail to its reference and script. Verification: 521/521 frontend tests across 82 files, 167/167 local Rust tests (1 explicit live-public-source test ignored), 41/41 HEOR artifact contracts, 25/25 deterministic HEOR core, 4/4 local-evidence tests, 7/7 asset-admission tests, all three changed Skill validations, typecheck, lint, production build, strict clippy with three observed pre-existing unrelated lint allowances, Python compilation, browser QA with zero console errors and an accessible chart snapshot, and diff checks pass. The 86,728,973-byte x64 macOS debug DMG (`4fb21d72cfabb079074b05e2f70d92ecbddb155809e1f58bcabccded4c28226c`) passed a read-only mount audit after a first audit caught and removed generated Python cache files: no `.pyc` or `__pycache__` remains, 12 changed packaged assets match source bytes, both packaged validators execute their expected entrypoints, and the packaged engine reproduces the five-threshold golden result (primary CEAC/CEAF 0.974, per-person EVPI 54.251888, MCSE 13.974120, population EVPI/EVPPI null). Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 11:59 · feat(ai4heor): industrialized the current NICE PMG36 technology-appraisal reference case as AI4HEOR's first non-China jurisdiction profile, while preserving conversation-first operation and strict jurisdiction isolation. The packaged `NICE-PMG36-2026-current` asset pins the 31 March 2026 manual revision to the independently re-fetched 1,032,391-byte official PDF SHA-256, exposes 15 locator-backed requirements, and adds fail-closed native checks for complete decision scope, England jurisdiction, NHS plus personal social services perspective, 3.5% cost/outcome discounting, QALYs, and the structured EQ-5D/UK-general-population/3L-or-DSU-mapped-5L reference case. Profile metadata now rejects non-HTTPS sources, malformed or non-calendar dates, invalid digests, unknown checks/applicability, duplicate requirements, and unsupported levels before assessment; the portable validator mirrors the structural contract, and a Rust test reads the actual packaged profile to prevent native/portable rule drift. This remains an executable subset rather than a claim of full PMG36 or NICE acceptance; cost comparison, pediatric HRQoL, diagnostics, equality/severity, managed access, and topic-scope judgments remain outside the profile. CDA-AMC's current fourth-edition guideline stays a researched candidate because this environment could not independently retrieve and hash the official source bytes through its Cloudflare boundary; no guessed or proxy digest was admitted. Verification: the official NICE bytes and digest were re-fetched exactly; 520/520 frontend tests across 82 files, 163/163 local Rust tests (1 explicit live-public-source test ignored), 39/39 HEOR artifact contracts, 22/22 deterministic HEOR core, 4/4 local-evidence tests, 7/7 asset-admission tests, both changed Skill validations, typecheck, lint, production build, strict clippy with the three observed pre-existing unrelated lint allowances, Python compilation, and diff checks pass. The 86,719,854-byte x64 macOS debug DMG (`154b2906f5cb9f5af567d96a9ac6059681f555e5234e7c0eccb25ab9e02d8003`) passed a read-only mount audit: eight changed packaged assets match source bytes, the packaged validator returns its expected usage exit, both profile copies contain all 15 requirements, and the copyrighted NICE PDF is not bundled. Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 11:25 · feat(ai4heor): upgraded app-owned evidence verification from one local assertion to a fail-closed dual-confirmation/rejection boundary — every eligible extraction is now shown with its exact value, target, record, source location, and applicability, and a selected model input is verified only when two distinct case-insensitive local reviewer labels confirm the exact current synthesis SHA-256 with no rejection. A rejection blocks that extraction until the synthesis bytes change; duplicate decisions by the same label are rejected, legacy schema-v1 confirmations remain hash-compatible but count only once, and the schema-v2 hash chain binds each confirmed/rejected decision. This is deliberately narrower than Cochrane's independent duplicate extraction requirement: local labels are not authenticated identities, and the app does not claim blinded independence, consensus, or arbitration. The natural-language research workflow remains primary; the bounded confirmation/rejection dialog is an auxiliary Human-in-the-loop gate. The evidence-synthesis and input-provenance Skills, method references, portable validators, and adversarial contracts were hardened before packaged admission and explicitly leave app review unchecked. Verification: 520/520 frontend tests across 82 files, 160/160 local Rust tests (1 explicit live-public-source test ignored), 35/35 HEOR artifact contracts, 22/22 deterministic HEOR core, 4/4 local-evidence tests, 7/7 asset-admission tests, both changed Skill validations, typecheck, lint, production build, strict clippy with the three observed pre-existing lint allowances, browser QA with 0 console errors and verified natural-language-first plus 0/2 and 0/4 review states, and an 86,713,395-byte x64 macOS debug DMG whose six changed packaged Skill/reference/script assets match source bytes and whose two packaged validators execute their expected entrypoints. Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 10:57 · feat(ai4heor): closed the evidence-to-model-input Human-in-the-loop boundary with an app-owned, project-scoped SHA-256 event chain — workspace-authored `human_checked`/`verified_by` fields no longer grant authority; a human must explicitly verify the exact current `heor/evidence-synthesis.json` bytes and selected extraction IDs in the desktop, and the analysis plan must bind that exact digest plus per-input extraction IDs whose targets and record links match the mapped model path and evidence source. Analysis-plan approval records the synthesis as a related artifact, while authorization, deterministic execution, uncertainty, and budget-impact paths all independently re-audit the current synthesis, selection links, verification chain, and approval binding; changed bytes, tampered logs, wrong targets, unlinked records, conflicts, missing selections, or unverified extractions fail closed. Assumption-only inputs remain explicit and supported without fabricated evidence. The natural-language research workflow remains primary; the only new form is the auxiliary human verification dialog with actor, rationale, and confirmation. Existing high-value validators were industrialized before admission: the synthesis validator's canonical-JSON digest defect was replaced with exact raw-byte hashing, and the input-provenance Skill gained an executable model-target/record-link validator, adversarial contract tests, bounded app authority, updated method documentation, and packaged-resource verification. Verification: 519/519 frontend tests across 82 files, 157/157 local Rust tests (1 explicit live-public-source test ignored), 35/35 HEOR artifact contracts, 22/22 deterministic HEOR core, 4/4 local-evidence tests, 7/7 asset-admission tests, both changed Skill validations, typecheck, lint, production build, strict clippy with the three observed pre-existing lint allowances, browser QA with 0 console errors and verified natural-language-first evidence states, and an 86,640,989-byte x64 macOS debug DMG whose mounted Skill scripts and template match source bytes and execute their packaged entrypoints. Windows/Linux and remote macOS packaging remain unrun and are not inferred from local success.

2026-07-14 09:50 · feat(ai4heor): shipped the first-party local evidence-library alpha as a natural-language-first, fail-closed knowledge-base path — the desktop now imports supported PDF/TXT/Markdown/CSV/JSON sources into `heor/library`, rejects selected and in-library symlinks plus unsafe/oversized corpora, preflights whole-library file/byte caps before non-overwriting import, binds exact source and derived-index bytes in `heor/evidence-library.json`, extracts PDF text page by page locally through pinned MIT `pdf-extract` 0.12.0, and stores a rebuildable, concurrency-serialized SQLite index under `.openscience`. Source changes, manifest/index drift, page-text hash failures, malformed/encrypted/unsupported inputs, empty text, and image-only PDFs remain explicit; OCR-required files are never presented as reviewed. The new `$heor-local-evidence` Skill independently re-verifies current-project identity, link-free paths, manifest fields, whole-index, full document bindings, source bytes, page sequence, and document/page hashes before deterministic Latin/CJK lexical retrieval, then emits compact path/page/SHA-256 citations without network access; extracted text remains distinct from interpretation, appraisal, and model-input selection. The HEOR review pane makes this natural-language action primary while retaining local file selection/rescan as auxiliary controls. `pdf-extract` was adapted behind panic containment, source/file/count/extracted-text caps, and provenance boundaries rather than reused as research authority, with its MIT notice packaged. RustSec review confirmed its `lopdf` 0.42.0 lock is the fixed baseline for RUSTSEC-2026-0187; the transitive `plist` chain was also upgraded to `quick-xml` 0.41.0. A pinned weekly and dependency-change `cargo-audit` gate now blocks new known vulnerabilities; its two temporary Linux build-only exceptions are narrowly documented because `wayland-scanner` parses only immutable bundled protocol XML and has not yet released its upstream 0.41 fix. Verification: 154/154 local Rust tests (1 explicit live-public-source test ignored), 518/518 frontend, 32/32 HEOR artifact contracts, 22/22 deterministic HEOR core, 7/7 asset-admission tests, 4/4 portable local-search and tamper tests, 4/4 Rust security-policy tests, a passing RustSec scan with no unreviewed vulnerability, 11/11 HEOR Skill validations, typecheck, lint, production build, strict clippy with three pre-existing unrelated lint allowances, browser QA with 0 console errors and verified natural-language handoff, and an 86,560,480-byte x64 macOS debug DMG whose mounted resources execute the packaged Skill and include the parser notice. macOS/Windows/Linux contract CI now includes the portable local-search test, but remote cross-platform jobs remain unrun and are not inferred from the local macOS package.

2026-07-14 09:15 · feat(ai4heor): closed the human-authorized search-to-synthesis gap with a native, fail-closed candidate-import boundary — the desktop now audits `heor/evidence-synthesis.json`, verifies the app-owned authorization chain, active project, safe immutable run path, exact request/run hashes, fixed PubMed/ClinicalTrials.gov endpoints, response hashes, caps, and normalized record set, then compare-and-swap writes only `not_assessed` candidates. Import is idempotent, reconciles source links, rejects symlinks/unknown fields/stale synthesis bytes, and never overwrites screening, appraisal, extraction, or conflict judgments. “Safely importable” is distinct from “research complete”: pending/unclear screening and unresolved conflicts remain visibly incomplete without blocking lossless import. The natural-language-first review pane shows bound searches and research-work status and sends `$heor-evidence-synthesis` continuation work back to the Agent; forms remain limited to human authorization and deterministic import. The search and synthesis Skills, portable validator, template, and industrialization documentation now preserve indivisible app bindings and forbid fabricated provenance. 149/149 Rust tests (148 default plus the separately executed live public-source test), 32/32 HEOR artifact-contract tests, 518/518 frontend tests, 10/10 HEOR Skill validations, typecheck, lint, production build, strict clippy with three pre-existing unrelated lint allowances, browser workflow QA with 0 console errors, and an 84,995,493-byte AI4HEOR x64 debug DMG/resource audit pass. macOS/Windows/Linux CI packaging remains pending remote execution and is not inferred from this local macOS build.

2026-07-14 08:14 · feat(ai4heor): replaced implicit third-party Skill copying with a fail-closed industrial asset-admission system — a versioned machine-readable registry now requires per-asset pinned revisions, license/distribution evidence, workspace/network/execution/authority boundaries, adaptation deltas, contract and adversarial tests, macOS/Windows/Linux evidence, security and HEOR methods review, kill switches, zero blockers, and an exact symlink-rejecting SHA-256 tree before any `validated-adapter` can be packaged or deployed. All 12 researched candidates are inactive by construction: seven MIT ai4s-skills and HEORAgent remain quarantined for adaptation or isolated-pilot work, while four Anthropic document skills are rejected because their per-skill source-available terms prohibit retaining copies, derivatives, and redistribution; the previous fetch/package/deploy claims were corrected. The Skills surface is now natural-language review first and deterministic registry/form evidence second, while runtime startup prunes stale external deployments and verifies packaged bytes rather than trusting labels. 7/7 asset-admission adversarial tests, 22/22 HEOR core, 25/25 artifact contracts, 136/136 Rust, and 518/518 frontend tests, typecheck, lint, production build, clippy with one pre-existing unrelated warning, browser visual QA with 0 console errors, and a mounted 80 MB AI4HEOR x64 DMG resource audit pass (12 reviewed, 0 admitted, 8 quarantined, 4 rejected; no external candidate code present). Cross-platform gates are configured in CI but macOS/Windows/Linux packaging evidence remains pending until those remote jobs run.

2026-07-14 06:38 · feat(ai4heor): shipped the first-party `heor-budget-impact` alpha capability as the MVP's second complete calculation path — a hash-bound `heor/budget-impact-plan.json` now drives a transparent three-year, undiscounted, two-strategy payer budget-impact calculator with annual eligible population, before/after access shares, intervention and condition-related costs, implementation costs, full input provenance, one-way sensitivity, alternative scenarios, and explicit exclusions/limitations. The app independently audits the artifact, binds its exact hash alongside the uncertainty plan at analysis-plan approval, invalidates stale approvals, and keeps complex dynamic cohorts, induced demand, severity/treatment mix, affordability, cost-effectiveness, and reimbursement conclusions outside this calculator's authority. The natural-language-first review pane exposes the BIA audit and `$heor-budget-impact` repair task while retaining buttons as secondary controls; the asset passed the same adapt→harden→verify→pilot→release admission policy as other HEOR capabilities. 516/516 frontend, 122/122 Rust, 22/22 HEOR engine, and 16/16 artifact-contract tests, seven HEOR skill validations, typecheck, lint, production build, strict clippy with the three pre-existing unrelated lint allowances, browser workflow QA (0 console errors), packaged-resource execution matching hand calculations (annual 550,000 / 1,120,000 / 1,810,000; cumulative 3,480,000), and an 81 MB AI4HEOR x86_64 macOS debug DMG pass. The configured macOS/Windows/Linux HEOR CI matrix has not yet run remotely, so cross-platform packaging remains unverified rather than inferred from local success.

2026-07-14 05:54 · feat(ai4heor): shipped the first-party `heor-uncertainty-analysis` alpha capability — a hash-bound `heor/uncertainty-plan.json` now drives evidence-linked one-way DSA, seeded PSA with versioned PCG32/beta/gamma/lognormal/uniform/Dirichlet transforms, convergence checkpoints, explicit omissions/dependence assumptions, and bounded structural scenarios in the dependency-free deterministic core. Analysis-plan approval schema v2 binds the exact uncertainty artifact hash while preserving schema-v1 log readability; changed artifacts, unsafe targets/symlink escapes, invalid distributions, unlinked bases, omitted known correlations, incomplete reference/evidence audits, or stale approval bindings fail closed. The natural-language-first review pane exposes an uncertainty audit and `$heor-uncertainty-analysis` repair action, with execution controls secondary. External HEOR assets remain governed by adapt→harden→verify→pilot→release rather than direct reuse. 515/515 frontend, 118/118 Rust, 16/16 HEOR engine, and 13/13 artifact-contract tests, six HEOR skill validations, typecheck, lint, production build, clippy with the three pre-existing unrelated lint allowances, browser workflow QA (0 console errors), package-resource execution, and an 81 MB AI4HEOR x86_64 macOS debug DMG pass. A new macOS/Windows/Linux HEOR contract workflow is configured but remains unverified until GitHub CI runs; cross-platform floating-point output is tolerance-based rather than claimed byte-identical.

2026-07-14 05:09 · feat(ai4heor): shipped the first-party `heor-reference-case` capability and replaced metadata-only guideline selection with a version-bound, fail-closed compliance matrix — bundled China 2020 current and China 2026 consultation-draft profiles now carry official source snapshots, locators, required/recommended levels, applicability, and deterministic checks; `heor/reference-case-assessment.json` is bound to the exact profile and analysis-plan bytes, required gaps/draft guidance/stale hashes/unsafe evidence paths block both plan approval and runtime authorization, and the review panel exposes gaps through a natural-language repair action. Industrial hardening makes cost scope and deterministic/probabilistic/structural uncertainty plans machine-checkable, requires separate face/internal/external validation plans, and accepts `not_applicable` only when the current horizon or analysis type deterministically supports it. 514/514 frontend, 111/111 Rust, 10/10 HEOR engine, and 10/10 artifact-contract tests, five HEOR skill validations, typecheck, lint, production build, clippy excluding three pre-existing unrelated lint classes, browser workflow smoke QA, packaged-resource inspection, and the AI4HEOR macOS debug app/DMG pass.

2026-07-14 04:18 · feat(ai4heor): shipped the first-party `heor-evidence-synthesis` and `heor-model-design` capabilities as versioned local artifacts with templates, deterministic validators, explicit search/screen/extract/appraisal/conflict and conceptual-structure/alternative contracts, then replaced the conceptual gate’s shared analysis-plan hash with an independently audited `heor/conceptual-model.json` hash and made runtime authorization fail closed on missing, changed, incomplete, or cross-analysis artifacts; 513/513 frontend, 105/105 Rust, 10/10 HEOR engine, and 8/8 artifact-contract tests, four HEOR skill validations, typecheck, lint, production build, clippy excluding one pre-existing lint class, browser QA with distinct plan/model hashes and evidence-lock confirmation, clean resource inventory, and the AI4HEOR macOS debug app/DMG pass.

2026-07-14 03:48 · feat(ai4heor): added a fail-closed HEOR input-provenance gate and an industrialization policy for reusable external assets — every required model input must now map to a valid source or a complete proposed assumption before the analysis plan can be approved, while unsupported calculations remain visibly exploratory; shipped the first-party `heor-input-provenance` skill, deterministic Rust/TypeScript audit parity, natural-language repair action, ecosystem admission criteria, candidate asset decisions, and an adapt→harden→verify→pilot→release pipeline. 10/10 HEOR Python, 100/100 Rust, 512/512 frontend tests, both HEOR skills validation, typecheck, lint, build, clippy, browser QA, and the AI4HEOR macOS debug app/DMG pass; HEORAgent local audit recorded 1,521 passed/6 skipped tests but 12 dependency vulnerabilities, so it remains a research-only hardening candidate rather than a bundled asset.

2026-07-14 03:22 · feat(ai4heor): finalized the platform name as AI4HEOR and shipped its first natural-language-first pharmacoeconomics workflow — the existing conversation runtime now owns HEOR research, evidence work, and an agent-maintained analysis plan, while a secondary app-owned review pane provides ordered human approvals, deterministic Markov execution, audit hashes, and a permanent not-decision-ready boundary; the reusable HEOR workbench skill, product contract, route, localization, tests, and desktop branding are included. 10/10 HEOR Python, 96/96 Rust, and 510/510 frontend tests, skill validation, lint, typecheck/build, clippy, browser QA with no console errors, and the AI4HEOR macOS debug app/DMG pass.

2026-07-14 02:43 · feat(heor-governance): added a project-bound app-owned HEOR approval service with fail-closed validation and an explicitly unanchored SHA-256 chain, removed analysis-JSON self-approval, and bundled a desktop bridge that matches the current input hash to the effective approved plan and verifies current/draft status against the packaged China reference-case registry while keeping validation/release separate; 10/10 HEOR, 96/96 Rust, and 504/504 frontend tests, typecheck, lint, clippy (excluding three pre-existing lint classes), and a debug Tauri build pass.

2026-07-14 02:34 · feat(harness): replaced the bundled autonomous self-goal template with a human-governed HEOR research harness — five explicit approval gates, restricted/unknown-data egress stop, deterministic-calculation boundary, no self-approval, and no self-claimed independent validation are now seeded and locked by contract tests; HEOR suite passes 11/11.

2026-07-14 02:20 · feat(heor): started the pharmacoeconomics specialization on upstream 42c8101 with a product contract, versioned China current/draft reference-case metadata, and a dependency-free deterministic two-strategy cohort Markov core; its golden case, probability/mass validation, dominance handling, draft-guidance guard, and structural Human-in-the-loop approval gate pass 9/9 Python tests, while the unchanged desktop baseline passes 504/504 tests, typecheck, and lint (Rust unverified locally because cargo is not installed).

2026-07-13 05:23 · fix(mcp): harden first-run Python provisioning against the Windows "uv venv failed: Using CPython …" report — `uv venv` reused a broken/locked system Python; it now tries reuse first and falls back to a downloaded managed Python (new `uv::create_venv`, science-MCP + jupyter), and the bundled uv finally inherits the configured proxy plus new optional PyPI / Python-download mirror settings (`uv_network_env`; Settings › Agent runtime) since a GUI-launched uv otherwise ignored the proxy and hung on restricted networks; 87/87 Rust + SettingsPage FE tests, typecheck, lint pass (Windows-only repro — DMG verify-only on macOS).

2026-07-12 23:45 · fix(runtime): model switches no longer flash the page — OpenCode's instance rebuild closes /event ~1s after the PATCH (outside the `switching` mask) and the SDK self-recovers in ~250ms, but the store mirrored the transient ready→connecting flip straight into the UI; a ready→connecting flip is now held for a 2s grace window and surfaced only if the stream stays down (errors still show immediately); 504/504 tests, typecheck pass, DMG rebuilt.

2026-07-12 23:30 · fix(settings): the model picker no longer bounces back to the previous model after a switch — `getDefaultModel` read the instance-scoped `/config`, which lags the `/global/config` PATCH by ~1s while OpenCode rebuilds its instance, so the reconnect's `loadCatalog` could clobber the new selection with a stale read; now the read targets `/global/config` and `loadCatalog` leaves `defaultModel` alone mid-switch; 501/501 tests, typecheck pass, DMG rebuilt.

2026-07-12 08:01 · fix(settings): PR #13 review fixes on `feat/model-browser-fixes` — the store now owns model-switch failure state (`modelSwitchError`; fixes the null===null first-boot misrender, the retry that collapsed the browser, and the stale-catalog-after-URL-change write hazard), catalog gained a loading state scoped to `listProviders`, the providers card stays collapsible when disconnected, model rows keep focus via aria-disabled, the dead `model.notSet` key is live again, and the two force-added `docs/superpowers/` files are removed; 500/500 tests, typecheck, lint, build pass.

2026-07-12 15:10 · fix(desktop): #12 — the app had no zoom mechanism at all, so WSLg users hit by its HiDPI scale-factor bug (fonts shrink on maximize; upstream wslg#23/#388/#1335) had no way to recover; enabled `zoomHotkeysEnabled` + the `core:webview:allow-set-webview-zoom` capability so Ctrl/Cmd +/- zoom now works on all three platforms; DMG rebuilt.

2026-07-12 11:25 · feat(settings): replaced the large native model dropdown with a searchable two-column browser (all/favorites/recent/provider filters), local favorite/recent persistence, immediate masked switching, and a separate collapsed provider-management card; full frontend tests, typecheck, lint, Windows Tauri build, and visual QA pass.

2026-07-12 01:32 · fix(runtime): serialized the complete OpenCode sidecar lifecycle behind one lock, cleared stale URLs on failed restarts, and deduplicated React StrictMode bootstrap calls so concurrent starts cannot double-spawn, overwrite the owned child, or launch dueling reconnect loops; Rust 87/87 + frontend 448/448 tests and the production build pass.

2026-07-10 21:55 · fix(mcp): #10 root cause — enabling a second Python MCP re-ran `uv venv` on the shared env, and uv deletes + rewrites the interpreter even with `--allow-existing` (verified: inode changes per run); Windows cannot replace the python.exe the first connector's running MCP server holds, so the second enable always died with "uv venv failed". Venv is now created only when the interpreter is missing (science-MCP + jupyter setup); `uv pip install` into the existing env verified end-to-end with the bundled uv.

2026-07-10 10:45 · feat(projects): project concept shipped — a project is a named shared-workspace folder under the base dir, marked only by `.openscience/project.json` (no registry/DB); sessions group under it by their `directory`. Sidebar gains a Projects section (collapsible groups, inline create + double-click rename, per-project new session); loose dated-folder sessions unchanged; same-project concurrency allowed and git snapshots now name the session that made them. All tests pass (447 FE + 87 Rust); DMG rebuilt.

2026-07-10 07:35 · docs(citation): repo is now academically citable — CITATION.cff + a citation section in all 7 READMEs (team authorship "The Open Science Desktop Contributors"); Zenodo↔GitHub archiving enabled, first DOI mints on the next release (then: add the badge + doi field, and sync the cff version each release). Sibling repo ai4s-skills shipped v0.1.0 the same way and already has DOI 10.5281/zenodo.21297455.

2026-07-10 04:04 · release: v0.1.9 published (network-proxy setting, connection self-heal, #6 #7 #8 #9 fixes) — all 8 platform installers built by CI.

2026-07-10 03:35 · fix(runtime): #9 root cause found by live probe — the browser delivers the xAI OAuth code fine; the sidecar's token exchange to auth.x.ai hangs because GUI-launched children inherit no proxy config. Added a network-proxy setting (follow system via scutil / custom URL / direct) in Settings → Agent runtime, injected as HTTP(S)_PROXY env at sidecar spawn with NO_PROXY for loopback; system+none apply on select via the masked-restart flow.

2026-07-10 01:40 · fix(settings): OAuth browser-login hardening (#9) — retry the callback wait on webview network drops (WKWebView kills idle fetches ~60s, far short of xai's 5-min window; opencode's pending closure is re-invocable, verified in v1.17.13 source), never re-authorize while the same login is waiting (a second authorize makes the xai plugin close the loopback server the new attempt needs), and fix a latent oauth method-index mismatch after filtering. Provider-side token-exchange rejections (no SuperGrok, blocked auth.x.ai) remain possible and are swallowed upstream — awaiting reporter feedback.

2026-07-10 00:55 · fix(sdk): self-heal the event stream (#8) — the global-config PATCH kills /event ~2s AFTER acknowledging, past af58765's masked reconnect, and EventSource never retries a terminally closed stream; the client now reopens it itself with backoff (verified in debug.log: drop → ready in ~300ms, no manual Connect). "Old model" self-reports are a prompt-level illusion — the app never pins a model per session.

2026-07-09 23:06 · fix: force UTF-8 on the Python kernel stdio (#6, garbled Chinese output on Windows); surface the active session folder new notebooks are created in on the Notebooks page (#7).

2026-07-09 13:05 · fix(models): switching the default model now reconnects transparently (masked by `switching`, like setApprovalMode) instead of closing the event stream and stranding the app disconnected until a manual Connect.

2026-07-09 11:52 · fix(sessions): stopped the open-session effect firing twice (currentId dep) and made openSession bail before a duplicate reconnect, fixing connection-pool exhaustion that left later sessions stuck on the loading spinner; also unstuck the /new·/clear guard when clearing from a draft.

2026-07-09 11:50 · fix(git-snapshot): only auto-commit into app-created workspace repos (marker-gated) so a user's own repo is never touched, and serialized snapshot commits behind a process lock to avoid index.lock races.

2026-07-09 11:25 · fix(sessions): added timeouts for session list/history/recovery requests and made failed history loads render an error row instead of an endless skeleton.

2026-07-09 11:18 · fix(sessions): made session idle folding idempotent so duplicate OpenCode idle events render only one done line.

2026-07-09 11:15 · fix(sessions): reconnected the current-folder runtime before the first post-clear turn and added timeouts for hanging session creation.

2026-07-09 11:11 · fix(sessions): preserved the local /new and /clear divider state when clearing from an existing session route.

2026-07-09 11:03 · fix(runtime): added an OpenCode SSE handshake timeout so first-launch auto-connect can keep retrying instead of hanging forever.

2026-07-09 10:02 · docs(readme): added the ResearchClawBench #1 recognition line across all README languages.

2026-07-09 09:39 · feat(sessions): added local /new and /clear commands that clear chat context while keeping the same workspace folder, and added best-effort local git snapshots for new workspaces and workspace file changes.

2026-07-09 08:53 · fix(updates): moved the update card to the bottom of Settings, made failed checks display as failed instead of "up to date", and switched desktop checks to the GitHub Releases Atom feed to avoid anonymous API rate limits.

2026-07-09 08:14 · feat(updates): added 24-hour-throttled GitHub Release update checks with manual checking, dismissible Settings badge, improved language switching UI, tests, and verified web/Tauri builds.

2026-07-09 06:00 · release(v0.1.8): bumped version to 0.1.8 and tagged; CI builds macOS/Windows/Linux installers into a draft GitHub Release. Covers the full i18n rollout (7 UI languages) and Open Science Desktop rebrand since v0.1.7.

2026-07-09 05:40 · docs(brand): updated the Open Science Desktop tagline and GitHub description/topics to include Linux alongside macOS and Windows.

2026-07-09 04:02 · docs(brand): repositioned the project as Open Science Desktop in README/agent metadata, added a neutral OpenScience comparison page, and updated the GitHub repo description/topics.

2026-07-09 03:25 · chore(ignore): removed tracked docs/superpowers planning artifacts and ignored docs/superpowers/ to keep local superpowers files out of git.

2026-07-09 02:36 · docs(readme): folded language support into the capability tables, restored emoji in README tables of contents, and added the Open Science Discord invite.

2026-07-09 01:14 · docs(readme): refreshed README for v0.1.7 current capabilities and added README translations for all seven shipped UI languages.

2026-07-08 20:47 · fix(remote-compute): remote/modal run outputs are now immutable per run. Root cause: skills required recording but still fetched reruns into stable paths like `results/humanoid-walk/humanoid_walk.mp4`, so `.openscience/remote-runs.jsonl` kept distinct hashes while old bytes were overwritten. Updated remote-compute/modal-run skills to use fresh `RESULT` directories, and both `record_run.py` helpers now reject any output path already recorded by an earlier remote run. Added a dev regression test covering both helpers; red → green verified.

2026-07-08 20:28 · fix(preview): right-pane GIF/image previews no longer generate dead URLs for bare artifact names. Root cause: `preview_url` returned a URL without validating relative paths, so `humanoid_walk.gif` could become a 404 `<img>` URL (question mark) while the Files browser worked via an already-resolved path; `relativize` now validates literal paths and falls back to the existing bounded basename resolver before building the URL. Verified with a failing-then-passing regression test and full Rust lib tests (78 passed).

2026-07-08 09:45 · fix(clipboard): copy failed in the desktop app ("could not copy to the clipboard"). WKWebView's navigator.clipboard.writeText rejects even inside a user gesture. Added tauri-plugin-clipboard-manager (+ capability clipboard-manager:allow-write-text) and a shared `copyText` helper that uses the native clipboard in-app, navigator as browser fallback; routed the file context menu's Copy path/relative-path and Runs' Copy command through it. tsc + clippy + eslint clean.

2026-07-08 09:30 · feat(files): right-click context menu in the file explorer — Reveal in Finder/Explorer/File-manager (label adapts per OS; via opener's cross-platform `reveal`, macOS `open -R` / Windows `explorer /select` / Linux portal+DBus with folder fallback), Copy path (absolute), Copy relative path, Open in default app. New Rust `reveal_path` + `absolute_path` commands (sandboxed under the chosen root); shared `FileContextMenu` (Radix context-menu) wraps rows in both the global Files page and the session files pane; clipboard via navigator + toast feedback. tsc + clippy + eslint clean; 372 frontend tests green.

2026-07-08 08:55 · fix(remote-compute): make run recording mandatory in the skill. A casual "re-run run.sh" showed the run in NEITHER Runs view — because the agent (Kimi) did the mechanical relaunch+scp but skipped record_run.py entirely, and passive capture deliberately ignores remote (ssh) commands. Hardened both SKILLs: §4 is now "REQUIRED, every time" and the tracking step spells out that a finished run MUST be fetched AND recorded — including quick re-runs — or it's invisible. (Prompt-level; the structural fix — folding fetch+record so recording can't be skipped — is proposed pending user confirmation.) Scratchpad script back-records the orphaned run into its session.

2026-07-08 08:20 · fix(remote-compute): attach remote runs to their session. Symptom: the humanoid-walk SSH run showed in the GLOBAL Runs view (with mp4/env/code — provenance intact) but NOT in the session's Runs pane, which filters by `sessionId` — and the skill's record_run.py call carried none (OpenCode exposes no session-id env var to bash; the id lives only frontend-side). Fix: new `mark_session` command writes the active session's id to `<workspace>/.openscience/session.txt`; `openSession` calls it (it already has id+directory); the remote-compute/modal-run SKILLs now pass `--session-id "$(cat .openscience/session.txt)"` (empty-safe — record_run.py drops a blank id). Future remote runs attach; a scratchpad script back-attaches the existing run (stamp JSONL + drop the runs.db cache to force re-ingest). tsc + clippy + eslint clean; 372 frontend + rust lib green.

2026-07-08 07:45 · feat(preview): inline video (mp4/webm/mov/m4v) preview. A robotics-sim session (ses_0be546ef) produced humanoid_walk.mp4 — recorded correctly in the run's outputs (the SSH-provenance fix worked) but the app treated it as an opaque binary ("no preview"), so it felt lost. Added a `video` PreviewKind → native <video> player in FilePreviewInspector; the local preview server now answers HTTP Range (206 Partial Content + Accept-Ranges) so WKWebView streams/seeks and large videos never load whole into memory; mime_for gains video types; mp4/webm/mov/m4v join REF_EXTS (clickable in prose) + EXT_KIND (figure icon) + Files-page Film icon. 5 rust preview_server tests (added Range/suffix/416) + 372 frontend tests green; tsc + clippy + eslint clean.

2026-07-08 05:15 · fix(remote-compute): complete provenance for SSH runs. Root cause found by auditing a real session (ses_0bec2fed0ffe): record_run.py was called with only the wrapper (--code run.sh, not the real humanoid_sim.py) and one output (result.json, not trajectory.npz), no environment, and a GPU hardware string for a CPU-only job — because the SKILL template modelled exactly that. Fixed: record_run.py gains --env-file (parses a run-time manifest → structured `env` with a content-addressed pip-freeze lockfile under .openscience/env/, matching local runs) and warns on missing/empty code/outputs; remote-compute SKILL now has run.sh emit env.txt at run time (ambient SSH env is otherwise lost), fetch every output, and record every script + output + env + honest hardware; modal-run SKILL records every output (env stays anchored by the versioned modal.Image, no --env-file). runtime.rs injects OPENSCIENCE_APP_VERSION so the off-app helper can stamp env.app. 9 rust runs-tests green (added env round-trip); clippy clean on changed files.

2026-07-08 03:34 · Release v0.1.7 — remote compute over SSH shipped, plus cross-platform hardening (single-line probe, atomic persist), base-workspace global machine config auto-materialized into each session, ssh run-surface in Runs/Reproduce, and stale-skill prune (hpc-slurm → remote-compute). Tagged v0.1.7 → CI builds macOS/Windows/Linux installers + draft GitHub Release.

2026-07-08 01:35 · feat(remote-compute): generalized Cluster (HPC) → Remote compute — connect any SSH machine (CPU or GPU, Slurm optional), one-round-trip capability+usage probe, multi-machine compute.json (migrates legacy hpc.json), RemoteComputeCard with capability chips + expandable snapshot/queue, and a unified remote-compute agent skill that runs jobs directly (setsid+PID+exit_code) or via Slurm. Built via subagent-driven TDD (9 tasks); 72 rust + 372 frontend green.

2026-07-08 01:27 · Design: documented the App Operator protocol as a session-scoped control lease with a visible controller session, generic action registry, return-to-owner flow, busy/interruption rules, and safety boundaries in docs/APP_OPERATOR.md.

2026-07-07 23:15 · Fix (Cluster): connecting a reachable host with no Slurm ("home-3090", a plain workstation — verified over SSH it has no sbatch/squeue/scancel anywhere, no slurm pkg/service) saved the host then unconditionally read the queue → `squeue` not found → "Could not read the queue" error. ClusterCard now reads the queue only when Slurm is detected; a reachable-but-no-Slurm host shows an explanatory note instead of a failing queue table (doesn't lock out module-based clusters or the agent's submit-time `module load`). Also fixed ClusterCard's own `btnAccent` opacity flicker (same WKWebView compositing bug as SettingsPage). Updated the empty-queue test to assert the queue read is skipped. 373 frontend tests green.

2026-07-07 23:00 · Fix (Bug 2 root cause): Settings controls flickering in packaged WKWebView was NOT the store subscription (store is quiet at rest) — it was `opacity`. `btnAccent`/`btnGhost` used `transition-opacity hover:opacity-90 disabled:opacity-50`; CSS `opacity` promotes a GPU compositing layer, so hovering one button mis-repainted neighbouring disabled (opacity-50) buttons. Confirmed by user repro: hovering one Enable flickered another. Replaced opacity states with alpha bg/text COLOR (plain paint, no layer), and swapped the two `-translate-y-1/2` overlay-icon centerings (transform → layer) for margin centering. Kept the per-field selector change from before. DMG rebuild for verification.

2026-07-07 21:05 · Fix: Jupyter/connector provisioning no longer breaks when you leave Settings — the setup state + progress listener lived on the SettingsPage component, so navigating to a chat unmounted them (download looked frozen; a re-click collided on the same env dir). Moved the whole flow into a new app-lifetime `useSetupStore` with a single app-level progress listener and a double-start guard (the uv download was never actually killed — only the UI lost it). Also fixed Settings controls flickering in the packaged WKWebView: SettingsPage subscribed to the WHOLE runtime store (`useRuntimeStore()` with no selector) and re-rendered on every unrelated mutation — switched to per-field selectors. 373 frontend tests green (+3 setup-store). DMG rebuild pending user verification of the WKWebView flicker.

2026-07-07 20:10 · Follow-up (v0.1.7): the "Open JupyterLab" button now also lives in the notebook editor header (conversation inspector + full page), not just the Notebooks list, and deep-links to the open file (`/lab/tree/<workspace-relative path>?token=…`) instead of the lab home — verified the URL returns 200 against a live lab rooted where the notebook lives. Base/cross-session notebooks (outside the lab root) still open the lab home. 370 frontend + 66 rust tests green.

2026-07-07 19:20 · Follow-up (v0.1.7): the app-managed Jupyter env is now the DEFAULT local kernel when present (order: manual override → jupyter-env → system auto-detect), so the Run button and the agent's MCP share one Python; added numpy/pandas/matplotlib to that env (~500 MB) so the unified kernel is usable out of the box; split python discovery + enriched PATH per-OS (macOS Homebrew/framework vs Linux /opt/conda·Linuxbrew vs Windows); added a one-click "Open JupyterLab" button (Notebooks page) that starts the headless server and opens it in the browser with token. 370 frontend + 66 rust tests green.

2026-07-07 18:55 · Fix (v0.1.7): notebook "Run" said "no Python found" even after the ~300 MB Jupyter setup, because the local kernel and the Jupyter MCP env were two disconnected Python worlds and Windows discovery was weak. `kernel::python_bin` now resolves in order (manual override in `runtime/python-path.txt` → system discovery → the uv `jupyter-env` fallback), broadened Windows candidates (ProgramData/C:\ Anaconda + versioned python.org dirs) with a Windows `enriched_path` (conda `Library\bin` on PATH so numpy loads); new `python_interpreter`/`set_python_path` commands drive a Settings "Local Python kernel" card + a notebook-header indicator. `setup_jupyter`/`setup_science_mcp` now stream live `setup-progress` events via `uv::run_uv` and abort with a readable error after 10 min of silence instead of hanging forever. 370 frontend + 66 rust tests green; DMG rebuilt.

2026-07-07 18:40 · Fix: clicking a file chip in the chat showed a broken image in Artifacts while the same file opened fine from the Files tree — `locate_under` returned an absolute agent-prose path (e.g. `/Users/…/2026-07-07-0902/test_image.png`) with only the leading slash stripped instead of root-relative, so the preview server resolved `<workspace>/Users/…` and 404'd; it now strip_prefixes against the canonicalized root. 370 frontend + 63 rust tests green.

2026-07-07 09:25 · v0.1.6 — Runs redesign + SQLite global index + global/session dual-view. Reworked the Runs experience on aesthetics, usability, AND scale. AESTHETICS: replaced the boxed/striped cards + green left-bar with a calm borderless "log ledger" — status is a small dot, sticky day-group labels (Today/Yesterday/…), aligned tabular meta, relative timestamps. USABILITY: search (command + output path), clickable facet chips (OK/Failed + remote surfaces, with counts) that combine (ANDed in SQL), a time filter (Anytime/24h/7d/30d), clickable outputs (open in OS), copy-command; removed the duplicate count display. SCALE (best practice): the read path is now an event-log + materialized-read-model — runs.jsonl stays the durable append log, and a GLOBAL SQLite index (`<base>/.openscience/runs.db`, new rusqlite bundled dep) is derived lazily by byte-watermark across ALL session folders; queries are indexed, keyset-paginated (infinite scroll, 50/page), faceted, and searched server-side — fast + low-memory at 100k+. DUAL-VIEW (matches Files/Notebooks): global Runs (sidebar, all sessions) + per-session Runs (session-header toggle, shown only when the session has runs, filtered by session_id) share one `RunsView`; the pane is mutually exclusive with the artifact/Files panes (new PaneState.showRuns). 370 frontend + 63 rust tests green, tsc + clippy + eslint clean.

2026-07-07 06:10 · v0.1.5 — crash fix + HPC/Modal remote back-fill. CRASH (shipped in v0.1.4, hit on first real test): the Runs view threw "undefined is not an object (d.code.length)" for any run with no captured code or no outputs — the Rust RunRecord serialized code/outputs with `skip_serializing_if = Vec::is_empty`, omitting empty arrays, so the frontend's `r.code.length`/`r.outputs.length` hit undefined (most runs produce no files). Fixed: code/outputs now always serialized (even empty); TS type made optional and every access guarded (tsc-enforced); survives the empty-array records v0.1.4 already wrote. REMOTE BACK-FILL: remote runs (HPC/Modal) now recorded by the driving skills, not passive capture (which no longer stamps the laptop's env/hardware on off-box runs) — a bundled `record_run.py` helper appends accurate records (host, jobId, remoteHardware, status, wall, fetched outputs) to `.openscience/remote-runs.jsonl`, which `read_runs` merges; both SKILL.md files gained a "Record the run" step (gather `sacct` NodeList/state for HPC, `gpu=` for Modal). Reproducibility anchor stays the spec (sbatch modules / Modal Image pins, already provenance-versioned), not enumeration. NOTE: remote path verified at unit+contract level only (no cluster/Modal to E2E test). 367 frontend + 58 rust tests green, tsc + clippy + eslint clean.

2026-07-07 05:30 · Reproducibility — high-effort review pass + hardening (v0.1.4). A workflow-backed adversarial review (16 agents) of the runs/provenance feature surfaced 8 real defects, 6 fixed: (1) parse_code_files misclassified a data file named as an argument (`--out results.csv`) as "code" and dropped it from outputs — now only the first source-extension file is the entry script; (2) env-var-prefixed commands (`CUDA_VISIBLE_DEVICES=0 python train.py`) were never recorded — command classification now strips leading `VAR=val`/`cd` prefixes and matches per `&&`/`;`/`|` segment; (3) HPC markers matched anywhere, so `git commit -m "…sbatch…"` was a false run — markers now anchor at a segment head (ssh-unwrapped), never inside quoted args; (4) failed runs stamped their partial outputs as "produced by run" — provenance linking now happens only for ok runs; (5) the mtime attribution window truncated the start to whole seconds + 2s grace — now millisecond precision; (6) record_run appended to provenance.jsonl under the wrong mutex (race w/ record_provenance) and re-read the whole store per output — replaced with a single batched link (link_run_outputs) under ProvenanceState. Deliberately NOT changed: the runtime.ts interrupt-guard `.delete`→`.has` (intentional/documented, pre-existing). A second manual pass over the fix code itself found 2 more latent bugs: (7) `./run.sh` / `python ./train.py` captured no code entry because `root.join("./x")` leaves a CurDir component that rel_key rejects — now strips the leading `./`; (8) record_run held ProvenanceState across capture_env's one-time pip-freeze/nvidia-smi shell-out (blocking concurrent writes) — env is now captured before the locks. 365 frontend + 57 rust tests green, tsc + clippy + eslint clean.

2026-07-07 04:05 · Reproducibility P2 — the three deferred pieces. ① edit diff lineage: provenance now keeps an edit's unified `diff` (from the event) when the full content wasn't captured, so the History shows what changed instead of "content not captured"; extracted a shared `DiffView` (ProvenancePanel + ToolGroup both use it). ② standalone Runs view: new `/runs` route + sidebar entry (RunsPage) listing every recorded run as an expandable recipe card — command, status, duration, env + hardware, code/outputs, Reproduce + Open conversation. ③ surface capture: `surfaceForCommand` tags each run local/hpc/modal/jupyter; HPC (`sbatch`/`srun`, even over `ssh`) / Modal (`modal run`) / notebook-batch (`papermill`, `nbconvert --execute`) submissions are now recognized as runs even when the marker isn't the command head, recorded honestly with a surface badge and a "outputs live off-box" note (remote outputs not captured); interactive local-kernel and single Jupyter MCP cells are deliberately NOT recorded (exploration, not reproducible batch runs). 361 frontend + 55 rust tests green, tsc + eslint clean.

2026-07-07 03:45 · Reproducibility, reframed from files to runs (P1). Root-caused the user's confusion: provenance only fired on write tools, so authored source files (train.py/model.py) got versions + a meaningless "Reproduce" (re-author the code?) while the results that actually need reproducing — produced by running code — got no record at all; the "content not captured (produced by running code)" message was doubly misleading (a content-less version was an `edit`, whose text lives in `newString` ∉ CONTENT_KEYS). Reframed: a *run* (an execution) is the reproducibility unit. New `.openscience/runs.jsonl` (Rust `runs.rs`) captures every experiment execution passively from the bash `tool.updated` events the app already receives — command, code version (entry scripts hashed), env + NEW hardware capture (nvidia-smi/sysctl/proc, cached per app-run), captured log, and outputs (files whose mtime falls in the run window, bounded scan). No new agent tool, no behavior change — rejected the initial "run experiments via a special tool" design as fragile. Produced files link back via a new `runId` on their provenance version; Reproduce on a run drafts a real recipe (re-run the command in the recorded env, compare outputs — not re-author source). ProvenancePanel now shows "Produced by run …" + recipe Reproduce, and an honest fallback message. 353 frontend + 55 rust tests green, tsc + eslint clean. DMG rebuild pending.

2026-07-07 02:27 · Fixed blank PPTX previews. A generated deck (valid single-master, opens fine in WPS/PowerPoint) declared phantom `[Content_Types].xml` Override entries for slideMaster2..10 — parts that were never written. pptx-preview tried to load the missing parts and silently rendered ZERO slides (no exception, so OfficePreview's try/catch never fired — the user saw a blank pane). Root-caused by reproducing the real file through the preview path (a blank vs full render hinged entirely on those overrides; also learned jsdom's cross-realm `instanceof ArrayBuffer` fails, so JSZip/pptx-preview need a Uint8Array in tests). Fix: `normalizePptxForPreview` now also strips Content_Types overrides whose part is absent (new pure `dropMissingContentTypeOverrides`), in-memory only, before feeding pptx-preview — same "never break the preview" philosophy as the existing defRPr merge. 344 tests green (+3), tsc clean; DMG rebuilt.

2026-07-07 02:07 · Fixed the macOS fullscreen titlebar gap. In native fullscreen the traffic lights slide away, but `overlayTitlebar = isTauri && isMac` (computed independently in AppShell, LiveSessionPage, Sidebar, RightPane) stayed true, so the ~78px traffic-light inset became an empty gap — the collapse/expand buttons floated oddly indented. Added `isFullscreen` to the UI store, synced by one `watchFullscreen` listener in AppShell (Tauri `getCurrentWindow().isFullscreen()` + `onResized`), a pure `trafficLightsPresent(tauri, mac, fullscreen)` helper, and a shared `useOverlayTitlebar()` hook now used by all four components. Verified `core:window:default`/`core:event:default` already grant `allow-is-fullscreen` + `allow-listen`, so no ACL denial (unlike the earlier drag-region miss). 341 tests green (+3), tsc clean; DMG rebuilt.

2026-07-07 01:39 · Session header cleaned up + stop-button noise fixed. Header: title moved to the left as the identity anchor; the Files toggle moved to the right and restyled as a quiet ghost control (no border/fill until hover/active), and notebook chips unified to the same ghost style so the right cluster reads as one language. Bug: pressing stop mid-turn printed "Aborted / done / done / Interrupted" — two root causes: the `interruptedSessions` guard was armed AFTER the abort POST's await (so the server's trailing SSE burst raced in first), and it was consumed by the FIRST session.idle (an abort emits two). Fixed by arming the guard before the await and holding it across every trailing idle (`.has`, cleared on the next turn); added a regression test that fires the burst during the await. 338 tests green, tsc clean; DMG not yet rebuilt.

2026-07-07 00:23 · New sessions now seed an agent harness instead of an empty folder: the evolve-agent scaffold (AGENTS.md rules, KNOWLEDGE.md, knowledge/, notes/) is maintained in-repo at `runtime/harness/`, bundled as a Tauri resource, and copied into each new dated folder by `harness::seed_harness` (non-clobbering, only on `new_dated_workspace` — never on switching to an existing session); per-session state files reset to blank templates, CLAUDE.md symlink dropped for cross-platform safety; DMG rebuilt and verified to contain Contents/Resources/harness.

2026-07-07 00:10 · Failed tool steps no longer break out as prominent cards (they read like generated artifacts and drown the thread): failures/warnings stay quiet rows inside the group — red ✗, error output expandable — with a red "· N failed" count on the collapsed summary; only waiting-approval still renders standalone; 337 tests green, DMG rebuilt.

2026-07-06 23:55 · Session-open jank fixed: collapsed tool details now lazy-mount (closed content stays out of the DOM; opening mounts collapsed and expands next frame, closing unmounts after the 300ms transition) — a history like the training session (16×50KB webfetch outputs, 20 file contents, 12 line-per-div diffs) no longer mounts ~1MB of hidden text on click; 335 tests green, DMG rebuilt.

2026-07-06 23:32 · Codex-style tool-call UI: consecutive quiet steps fold into a one-line summary group with per-step expandable detail (shell panel, colored diff, inline file content); bash titles are de-noised commands (cd-prefix stripped, verb + subject, full command in the detail); running commands show a live stdout tail + elapsed timer — the server already streamed `state.metadata.output`, the SDK just never read it; folds coalesce at 250ms and `\r` progress bars collapse in place — 335 tests green, DMG rebuilt.

2026-07-07 05:35 · v0.1.2 published — collapsible sidebar + single-row titlebar + resizable/maximizable right pane + 48px headers, plus the provider-credential and macOS-13 fixes; CI matrix green, 6 installers attached (https://github.com/ai4s-research/open-science/releases/tag/v0.1.2).

2026-07-06 21:45 · Header polish: all header rows (session titlebar, sidebar strip, every pane header) unified at 48px (1.5× the old 32px); macOS traffic lights re-centered for the taller row via trafficLightPosition {x:13, y:22} (measured on screen: lights and icons both center at 23.75px); header icons normalized to 14px / 1.5 stroke / solid text color so they match the 12px lights' height and no longer look washed out — 319 tests green, DMG rebuilt.

2026-07-06 21:20 · Maximized pane header is now a single row with the traffic lights: PaneTitlebarInset (a 62px drag spacer rendered at the start of every pane header, macOS+maximized only) replaced the extra titlebar strip; cross-platform audit of the recent titlebar/pane work found no Windows/Linux issues — every drag region and pl-[78px] inset is gated on isTauri+Mac UA, titleBarStyle/hiddenTitle are macOS-only config keys, and allow-start-dragging is valid on all desktop platforms — 319 tests green, DMG rebuilt.

2026-07-06 21:05 · Right pane (artifacts/Files) is now resizable like the sidebar: left-edge divider drags within 360–960px (persisted, capped at 70% of the window), dragging far right snaps it closed; new maximize/restore button in every pane header covers the whole window with just the artifact; session header separators softened — draft pages have none, open sessions (and examples) use a new faint border token — 319 tests green, DMG rebuilt.

2026-07-06 20:37 · Window-drag root-caused: no capabilities file existed, and core:window:default does not include start-dragging, so every data-tauri-drag-region invoke was silently ACL-denied since day one — fixed with capabilities/default.json (core:default + allow-start-dragging, double-click-zoom now works too); drafts also lost the header title/folder chip (workspace picker now sits in the composer row right of attach) — 319 tests green, DMG rebuilt.

2026-07-06 20:20 · Session header is now a Codex-style single-row titlebar: collapsed = traffic lights → expand button → folder toggle → title in one 32px draggable row (drafts hide the Files toggle until the session folder exists), expanded = collapse button lives in the sidebar's top strip right of the lights so the toggle never moves; non-live routes keep a fallback strip — 319 tests green, DMG rebuilt.

2026-07-06 20:12 · Collapsed-sidebar titlebar overlap fixed: with the sidebar closed the page header slid under the macOS traffic lights (and the floating expand button sat on top of it) — the main pane now reserves the same h-8 drag strip the sidebar uses, with the expand button in-flow right of the lights, restoring window dragging when collapsed — 319 tests green, DMG rebuilt.

2026-07-06 20:05 · First-boot page flicker root-caused and fixed: every reconnect attempt tore down the old client, which emitted "offline" into the store, so while macOS TCC held the sidecar the page flipped between the offline card and the connecting screen ~1×/s — reconnects now unhook the status listener before close (explicit Disconnect unchanged), regression test added, 319 tests green, DMG rebuilt.

2026-07-06 19:55 · Sidebar is now collapsible (Codex-style): toggle button in the header + ⌘/Ctrl+B, floating expand button next to the traffic lights when closed, divider drags between 184–340px and snaps shut below 140px, width/state persisted — 318 tests green, DMG rebuilt.

2026-07-06 18:30 · Code review of the connector fixes surfaced 4 confirmed bugs, all fixed: instance dispose now also hits the ?directory= workspace instance (chats see new credentials), the OAuth wait captures its generation at flow start and is abortable (cancel/save/retry no longer leak or resurrect waits), a post-cancel completed login still refreshes the provider list, and every SDK error now carries the server diagnostic — 318 tests green, DMG rebuilt.

2026-07-06 17:45 · Model-connector bugs fixed against a live opencode 1.17.13 sidecar (verified E2E): the SDK disposes the server's cached instance after every credential change so new providers appear immediately, "auto" OAuth logins wait for the browser in the background (cancellable, no page-wide busy lock), and SDK errors now carry the server's diagnostic message.

2026-07-06 16:40 · macOS 12 incompatibility root-caused: the Bun-built opencode sidecar declares LC_BUILD_VERSION minos 13.0 (verified via otool on the v0.1.1 dmg), so on Monterey the app shell (minos 11.0) opens but the sidecar is killed on exec → endless "connecting" → "could not open OpenCode event stream". Fix: bundle.macOS.minimumSystemVersion=13.0 (next release refuses cleanly on old macOS) + macOS 13+ requirement documented in both READMEs and the v0.1.0/v0.1.1 release notes.

2026-07-06 16:05 · v0.1.1 PUBLISHED — Windows performance release live at https://github.com/ai4s-research/open-science/releases/tag/v0.1.1 (6 installers, all 4 CI targets green, ~13 min tag-to-release); updater tar.gz assets removed as in v0.1.0. Ships the UI-freeze / console-window / fake-python fixes below. Field verification on a real Windows machine still pending — the fix addressed root causes confirmed in code, not yet re-tested on the reporting machine.

2026-07-06 14:58 · Windows perf/UX bugs root-caused and fixed: (1) heavy Tauri commands were synchronous and ran on the WebView2 UI thread — `kernel_execute` blocked the whole app for the duration of every notebook cell (now `async` + `spawn_blocking`), and `detect_tools`/`record_provenance`/`start_runtime`/`start_jupyter`/file-preview commands froze startup and browsing (now `#[tauri::command(async)]`, 17 commands total); (2) every direct `std::process::Command` spawn popped a black console window on Windows — new `quiet_command` helper sets CREATE_NO_WINDOW at all 8 spawn sites; (3) `interpreter_ok`/tool probe accepted the Windows Store fake `python.exe` alias (runs but exits non-zero), causing endless kernel respawn + window spam — both now require `--version` to succeed, with regression tests. 51 Rust + 314 frontend tests green, zero warnings; Windows-specific code cross-checked against x86_64-pc-windows-msvc. Needs a CI Windows build to confirm on a real Windows machine.

2026-07-06 14:57 · Product promo video shipped: `videos/open-science-claude-science-alt/` contains a 28s portrait HyperFrames promo positioning Open Science as "Claude Science, but open", reusing real project showcase assets; `npm run check` is green (lint 0/0, validate no console errors and WCAG text pass, inspect 0 layout issues), and the rendered MP4 is `renders/open-science-claude-science-alt.mp4` (28.000s, 18 MB).

2026-07-06 14:20 · v0.1.0 PUBLISHED — first public release live at https://github.com/ai4s-research/open-science/releases/tag/v0.1.0 with 6 installers (mac arm64/x64 dmg, Windows exe/msi, Linux deb/rpm), release notes incl. unsigned-build bypass instructions; updater tar.gz assets removed (no updater configured).

2026-07-06 13:45 · Release flow VERIFIED end-to-end: tag `v0.1.0` → CI builds all 4 targets green (mac arm64 4m, mac x64 3m, Windows 5m, Linux 13m) → draft GitHub Release auto-created with 8 assets (dmg×2, exe, msi, deb, rpm, app.tar.gz×2); publishing = press Publish on the draft. Five CI iterations fixed real bugs: pnpm version pin conflict with packageManager; GITHUB_TOKEN needed `permissions: contents.write` for release creation; redundant .app upload doubled mac artifacts; Linux AppImage permanently dropped (linuxdeploy runs ldd on the Bun-built opencode sidecar, ldd exits 1, hard abort — tauri#8929) so Linux ships deb+rpm only. Remaining known gaps: no code signing (Gatekeeper/SmartScreen warnings on browser downloads) and Windows/Linux UI never eyeballed (titleBarStyle Overlay is mac-only). CI-built arm64 dmg handed over for install testing.

2026-07-06 10:30 · Release pipeline now covers Linux: fetch-opencode.sh/fetch-uv.sh accept `*-unknown-linux-gnu` triples (opencode ships linux tar.gz, uv ships gnu tarballs — both asset names verified against the pinned releases), CI matrix gains ubuntu-22.04 (webkit2gtk-4.1 deps) producing .deb/.rpm/.AppImage, and a `v*` tag push now auto-creates a draft GitHub Release with all installers attached. Blocker: zero CI runs so far — macOS arm64 is the only locally-verified platform; Windows/Linux/x64-mac remain unverified until the first tag push.

2026-07-06 03:45 · P0-5 chemistry gate upgraded to a real RDKit round-trip (the named "library round-trip vs static patterns" gap). New `_rdkit_verdict(smi)` → "valid"/"invalid"/None: when RDKit is installed, `Chem.MolFromSmiles` (which sanitizes) is the AUTHORITATIVE judge — it catches far more than the static five-bond carbon (bad ring closures, impossible aromaticity, over-valent N/O/S) AND clears molecules the heuristic would wrongly flag; when RDKit is absent (verdict None) it degrades to the existing stdlib bond-counter, so the gate still works offline with zero deps. `_molecule_finding` routes every SMILES literal through this precedence, replacing the direct static calls in check_chemistry (python + R paths). TDD: the RDKit integration logic is driven deterministically by patching `_rdkit_verdict` (invalid→flag / valid→suppress-static-FP / None→static-decides), so the unit suite needs no RDKit — 37 domain-check tests green. VERIFIED end-to-end with REAL RDKit (2026.3.3 in a throwaway uv env): it flags 5-bond nitrogen and an unclosed ring `c1ccc` the static path misses, and clears caffeine + aspirin. DMG rebuilding to bundle the updated skill. Gap left in P0-5: POSCAR→pymatgen validity round-trip.

2026-07-06 03:20 · P0-6 CLOSED (✅ Done) — the large-file probe is now a one-click UI action, not just an agent skill. The "too large to preview" card gained **Inspect without loading**: a new Rust `probe_large_file` command (sandbox-resolves the path via scope_root/resolve_under, finds Python via kernel::python_bin, locates the bundled `skills-core/large-file/large_file_probe.py` resource with a dev fallback, runs it under the enriched PATH, returns the compact JSON pointer) → the pane renders a readable fact sheet (`LargeFilePointerPanel`: format, size+gzipped, rows/reads/sequences/variants, read-length min/max/mean, schema chips, HDF5 datasets, sample ids, "not loaded" note) or the probe's error. So a USER — not only the agent — can introspect a file bigger than the context window in one click. TDD: Rust pure `first_existing` helper (+1 test, 49 total); frontend probeLargeFile bridge + PreviewError inspect flow (+2 component tests: renders the pointer, surfaces a probe error) + updated the existing too-large test — 314 JS tests, tsc, eslint green. VERIFIED end-to-end: the probe bundled in the fresh DMG, run against a real 25 MB CSV (over the preview cap), returns schema + 900k row count + head/tail sample — the exact chain the command executes. This meets P0-6's acceptance (analysis over an over-window file completes by introspection/sampling, no whole-file load); status → ✅ Done in the summary table. DMG rebuilt (02:48).

2026-07-06 02:55 · P0-5 social-science domain gate shipped (5th discipline; the last unbuilt domain-check field). Two deterministic rules in the bundled `domain-check` skill, distinct from the P1-6 `stats-integrity` skill (which checks reporting/reproducibility) — this catches CODE that computes the wrong statistic: **social · multiple-comparisons** (a significance test — ttest/pearsonr/f_oneway/chi2/… — run inside a loop or ≥3 times with NO multipletests/FDR/Bonferroni correction anywhere; the named silent-p-hacking failure that inflates the family-wise false-positive rate) and **social · categorical** (a numeric reduction `.mean()`/`.median()`/`.std()` taken directly on a nominal code — gender/race/region/condition/… — treating an unordered label as interval). Precision-first, matching the other four gates: a SINGLE test isn't flagged (needs no correction), a correction anywhere silences it, and a `groupby('gender')` KEY is correct usage and not flagged (verified adversarially — groupby+col.mean, bare groupby.mean, single corr, 2-tests-no-loop all stay silent). Reuses the same `review` contract + per-discipline tag → zero UI change. TDD: +7 validator tests (loop/≥3/correction-ok/single-ok; categorical mean, groupby-ok, continuous-ok); 34 domain-check tests total, all green; end-to-end run() emits both social tags on a realistic file. DMG rebuilding. P0-5 now covers all five targeted disciplines; only library round-tripping (SMILES→RDKit, POSCAR→pymatgen) remains.

2026-07-06 02:30 · P0-6 genomics + remaining physical-science large-file formats shipped (the named gap; a 90 GB FASTQ is exactly the requirement's motivating case). Extended the bundled `large-file` probe: **FASTQ/FASTA/VCF are STDLIB and gzip-aware** (a new `is_gzip`/`stream_lines`/`count_lines_any` layer + double-extension detection that sees through `.gz`, so `reads.fastq.gz` → FASTQ not a gzip blob) — FASTQ returns read count + read-length min/max/mean over a bounded 100k-read scan + sample ids (never full sequences), VCF returns variant count + sample names from `#CHROM` + contigs, FASTA returns sequence count + residues. Binary **BAM/CRAM** (pysam header only), **GRIB** (cfgrib/pygrib), **ROOT** (uproot, metadata) introspect via their library or degrade to an install-hint pointer, never a raw dump. VERIFIED on this host: a 500k-read `.fastq.gz` → 462 B pointer streamed in CONSTANT memory (whole file never loaded), a 3,000-variant VCF → samples + contigs parsed. TDD: +7 probe tests (FASTQ reads/lengths, FASTQ gzip, FASTA, VCF samples, BAM/GRIB/ROOT install-hints); 18 probe tests total, all green. Only P0-6 remainder: wiring the probe as an automatic UI pre-read step. DMG rebuilding.

2026-07-06 02:05 · P1-2 physics + earth/climate connectors shipped — the two previously-EMPTY disciplines now have a domain database, so all five targeted disciplines meet the acceptance ("≥1 non-bio DB per discipline"). Added three vetted open-source MCP servers to the curated catalog (same one-click-into-isolated-uv mechanism): **spaceweather-mcp** (physics — NOAA SWPC/NASA DONKI/USGS: solar wind, flares, Kp/Dst, radiation storms, aurora; 15 tools; no key), **mcp-weather-server** (earth — Open-Meteo weather/climate/air quality; 8 tools; no key), **usgs-mcp** (earth — USGS streamflow/flood/sites; 10 tools; no key). METHOD (the lesson from the earlier false-friend episode): each candidate was proven with a REAL MCP `initialize` + `tools/list` stdio handshake inside the bundled-uv env BEFORE adding — wrote a `mcp_handshake.py` verifier and confirmed all three init and enumerate their tools via the EXACT launch shapes `connectorConfig` emits (console-script and `-m module`), in a fresh shared env. This also DISPROVED the earlier "usgs/open-meteo don't start" doubt (that was an inadequate check — a SOCKS-proxy env var on the test box, not the servers) and re-confirmed `astro-mcp` is Airflow, not astronomy. TDD: +4 connector tests (physics + earth disciplines present, three launch-command shapes); 312 JS tests, tsc, eslint green. Docs: CONNECT_YOUR_TOOLS.md lists all seven connectors with disciplines. DMG rebuilding. Gap: astronomy catalogs (ADS/SIMBAD/Gaia) have no pip-installable stdio MCP — GitHub-only, would need vendoring.

2026-07-06 01:40 · P0-5 chemistry domain gate shipped (4th discipline; the requirement's named next frontier — the C&EN caffeine "five-bond carbon" class of "runs but wrong"). New `chem · valence` rule in the bundled `domain-check` skill: a stdlib SMILES bond-counter walks a string literal (assigned to a `smiles`/`smi`/`mol` variable, or passed to `MolFromSmiles`/`MolFromSmarts`) — tracking chain/branch/ring bonds and bond orders (=/#/aromatic) — and flags any organic-subset atom whose EXPLICIT bonds exceed its valence: carbon >4 (the five-bond carbon) or a halogen >1. Precision-first: it BAILS (no finding) on bracket atoms `[..]` (which carry their own valence/charge/H), unbalanced branches, open rings, or any unmodeled token, so it never guesses — and only flags elements whose valence is unambiguous. Reuses the same `review` contract + per-discipline tag, so zero UI change. TDD: +8 tests (5-bond carbon via literal and via MolFromSmiles arg, over-bonded F, valid caffeine + acetic acid stay silent, non-chem string ignored, bracket-atom bail, R regex fallback); 27 domain-check tests total, all green; verified end-to-end that run() emits the review block naming "C has 5 bonds (max 4)". DMG rebuilt to bundle the updated skill. Gaps: full RDKit round-trip sanitization, a social-science correctness gate.

2026-07-06 01:25 · P0-7 moderate: tool detection now searches the enriched PATH. Settings' tool list probed with the app's own environment — a Finder/Dock-launched app gets a minimal PATH, so the user's anaconda/homebrew Python, R, uv, and Jupyter showed as "missing" while the agent and kernel (which already use `enriched_path()`) could run them fine. `probe` now searches the SAME enriched PATH (pure `probe_with_path` + fake-tool-on-temp-PATH test). 48 Rust (+1) + 308 JS tests, tsc, eslint green. DMG rebuilt.

2026-07-06 01:15 · P0-7 moderate: sidecar restart consolidated under a lifecycle lock. The kill→spawn sequence was copy-pasted in four config-changing commands (import login / remove config entry / approval mode / configure provider) with no serialization — two concurrent restarts could double-spawn and orphan a child. Now one `restart_sidecar()` holds the child mutex across the whole kill→spawn (the mutex IS the lifecycle lock), and all four commands route through it (−30 lines). 47 Rust + 308 JS tests, tsc, eslint green. DMG rebuilt.

2026-07-06 01:05 · P0-7 plaintext-keys interim minimums met — the last critical safety item is now addressed (keychain-at-rest stays deferred to signed releases per P2-3's deliberate trade-off). The gap: provider/connector keys + the Jupyter token live in `opencode.jsonc` at mode 644 (world-readable), and the sidecar rewrites that file at runtime with its own umask, so write-site chmod alone wouldn't hold. FIX: `tighten_private` (unix: dirs 700, files 600; Windows %APPDATA% is per-user ACL'd) applied to the whole app-private runtime root + config file on EVERY sidecar start (repairs existing installs) and after each Rust-side config write — the 700 directory is the load-bearing guarantee (unreachable to other users whatever the sidecar sets on files inside). VERIFIED empirically: an authenticated PATCH /global/config against the real bundled binary preserves a 600 config (in-place rewrite), so both layers hold; +1 Rust test (47). With this, every P0-7 CRITICAL checkbox is done or at its documented interim bar: approval modes ✓, sidecar auth + CORS ✓, workspace-bash binding ✓, keys ✓(interim), Windows injection ✓, kernel deadlock ✓. Remaining: moderate robustness (sidecar restart dedup/liveness, async-worker blocking, detect_tools PATH) + structure cleanup. DMG rebuilt.

2026-07-06 00:55 · P0-7 Windows command injection fixed (the last critical Rust defect — both are now closed). ROOT CAUSE: `open_url`/`os_open` used `cmd /C start "" <arg>` on Windows; `cmd` re-parses `&`/`^`/`|` after Rust's quoting, so an agent-emitted link like `https://x.com/?a=1&calc` would EXECUTE `calc` — and every legit URL containing `&` broke. FIX: both functions now call the `opener` crate — verified in its vendored source to use `ShellExecuteW` on Windows (no shell re-parsing), `open` on macOS, `xdg-open` on Linux; it also `wait()`s the helper (the old spawn-and-forget leaked one zombie per open, even on macOS). The http(s)-only scheme gate stays and gained a regression test (javascript:/file:/bare-command rejected). Verified: `opener` cross-compiles clean for `x86_64-pc-windows-msvc` (full Windows app build stays host-bound in CI — llvm-rc + real sidecar binaries, P1-4). 46 Rust tests (+1), 308 JS, tsc, eslint green. DMG rebuilt. P0-7 critical remainder: plaintext keys at rest (deliberate P2-3 trade-off, revisit for signed releases).

2026-07-06 00:45 · P0-7 kernel deadlock fixed + notebook Stop button (the 2nd critical Rust defect; acceptance: a `while True: pass` cell can be reset without restarting the app). ROOT CAUSE: `kernel_execute` held the GLOBAL kernel-map mutex across an unbounded blocking `read_line`, so a hung cell wedged every kernel command including `kernel_reset` — only an app restart recovered. FIX (per-kernel locks): the map holds `Arc<Kernel>` and is locked only for lookup/insert/remove; each kernel has an `io` lock (one cell at a time) and an INDEPENDENT `child` lock (kill/reap) — reset kills without waiting on io, the blocked read sees EOF and errors out, and the error path removes only its own Arc (ptr_eq) so a respawned kernel is never clobbered. Killed kernels are now `wait()`ed (zombie fix, same class). `kernel_reset` gained notebook-precise targeting (kills exactly that notebook's kernel). UI: a running cell now shows an ALWAYS-VISIBLE Stop (was: no way to interrupt at all + Run row hidden unless hovered); Stop resets this notebook's kernel and the cell reports "Interrupted — the kernel was restarted; variables were reset" instead of a raw error. VERIFIED with a REAL hung Python kernel in a Rust test: reset returns promptly (<5s guard, actual ms), the hung exec errors, the map empties for a clean respawn. 308 JS tests (+2: Stop resets + labels interruption, crash still reports error), 45 Rust tests (+1), tsc, eslint green. DMG rebuilt. Still open in P0-7: plaintext keys at rest, Windows cmd injection.

2026-07-06 00:31 · P0-7 sidecar auth + preview-server token shipped (the audit's remaining CRITICAL network exposure; REQUIREMENTS said "fix permission block + CORS before next release" — both now closed). MECHANISM (verified in the OpenCode 1.17.13 source): `--cors "*"` was an exact-match literal (never a wildcard) — the real hole was the built-in CORS allowlist trusting ALL `http://localhost:*`/`127.0.0.1:*` origins, so any local webpage could drive agent turns and read API keys via the unauthenticated `/global/config`. FIX: (1) sidecar — OpenCode's built-in Basic auth enabled via a per-run CSPRNG password (`OPENCODE_SERVER_PASSWORD`, in-memory `OnceLock`, never on disk; new `runtime_password` command hands it to the webview over app-only IPC); the SDK sends `Authorization` on every fetch and KEEPS the reliable-in-WKWebView EventSource SSE path via the server's `?auth_token=` query (same Basic payload); `--cors "*"` removed. (2) preview server — a per-run CSPRNG token as the URL's FIRST PATH SEGMENT (`/<token>/w/…`), so relative subresources in previewed HTML inherit it and the sandboxed iframe (opaque origin, no cookies) still works; tokenless/wrong-token → 403 before any filesystem work; dropped `Access-Control-Allow-Origin: *` (previews are iframe/img, never cross-origin fetch). (3) same-class fix: Windows Jupyter token was guessable pid+nanos — now the shared `random_hex` CSPRNG (getrandom). VERIFIED against the real bundled binary: no/wrong password → 401 on `/global/config`, `/session`, `/event`; correct → 200; `?auth_token=` streams SSE. TDD throughout: 306 JS tests (+3: Basic header on API calls, auth_token on the ES URL, store passes the password), 44 Rust tests (+2: token gating + no-CORS-header, random_hex shape), tsc, eslint green. DMG rebuilt. Still open in P0-7: plaintext keys at rest, Windows cmd injection, kernel deadlock.

2026-07-06 00:20 · Approval switch polished after user testing: (1) UI restyled to the Codex layout — the composer gains a bottom action row with a "✋ Approve for me ⌄" pill bottom-left and a titled upward menu (visually verified via browser screenshots); (2) FIX: menu didn't dismiss on outside click — ROOT CAUSE: WKWebView never focuses a clicked button, so the onBlur close never fired; replaced with a document-level mousedown listener (+1 test); (3) FIX: switching modes flashed the whole page — the sidecar restart flipped status to "connecting" and the page rendered as disconnected; reused the `switching` flag (same mechanism as workspace moves) so a deliberate restart renders as connected (+1 test). 303 JS tests, tsc, eslint green. DMG rebuilt (20:03).

2026-07-05 23:59 · P0-7 approval modes shipped (the audit's #1 safety-defaults gap). Codex-style two-mode switch in the composer: **Approve for me** (DEFAULT — deletion/install/remote/privilege commands + webfetch prompt first) and **Full access** (OpenCode builtin defaults, explicit opt-out that survives restarts). MECHANISM (verified from OpenCode 1.17.13 source + bundled binary): permission rules evaluate last-match-wins with user config appended after the builtin `"*":"allow"`, so the approve preset is pure `ask` rules — per dangerous token both `"T *"` (prefix; also matches bare `T`) and `"* T *"` (catches compound commands like `cd x && rm -rf y` without false-positives on words merely containing the token). Full access = `"permission": {}` (zero rules; the key's presence means "user chose", so startup seeding never overrides it). Rust: pure `set_permission_mode`/`permission_mode_of`/`seed_default_permission` (+6 tests) + `get_approval_mode`/`set_approval_mode` commands + first-run seeding in `spawn_sidecar` (compliance floor even if the UI never runs); switching restarts the sidecar via the configure_opencode flow. Frontend: store loads the mode on connect, `setApprovalMode` persists + reconnects (+2 store tests); composer dropdown with active-mode check (+3 component tests). VERIFIED on the real bundled binary: approve config accepted — all 86 bash ask rules land in the build agent's resolved ruleset AFTER the builtin allow; `{}` accepted (24 builtin rules, zero asks); a 22-case evaluation with OpenCode's verbatim wildcard/findLast algorithm classified every case as designed (rm / pip install / ssh / curl / git push / cd-&&-rm → ask; ls / python / git status / "echo confirm" → allow). 292 JS tests, 42 Rust tests, tsc, eslint green. DMG rebuilt. Still open in P0-7: `--cors *` unauthenticated sidecar, plaintext keys, Windows cmd injection, kernel deadlock.

2026-07-05 19:30 · fix(pptx-preview) + feat(office skills). (1) Decks styled via paragraph-level `<a:pPr><a:defRPr>` (valid OOXML; WPS/PowerPoint resolve it) rendered as 18px black text in pptx-preview (run-level rPr only) — invisible on dark slides. New pure `lib/pptx.ts`: `applyParagraphDefaults` merges each paragraph's defRPr into runs lacking rPr (run's own values win; covers `<a:r>`+`<a:fld>`), `normalizePptxForPreview` rewrites the slide XMLs in-memory before rendering. VERIFIED end-to-end on the failing deck (title: 18px black → >30px white); 4 regression tests. (2) Anthropic document skills (docx/pdf/pptx/xlsx, Apache-2.0) now bundled: `fetch-skills.sh` pins anthropics/skills@9d2f1ae into `external/anthropic-skills/`, shipped as `skills-office/` resource, deployed by `deploy_bundled_skills` on every sidecar start. VERIFIED inside the built DMG. 287 JS (+5) / 36 Rust tests, tsc, eslint green. DMG rebuilt.

2026-07-05 19:00 · fix(session): eternal "Working…" spinner + no interrupt. ROOT CAUSE (live-diagnosed on ses_0cb07e522f…): the turn had finished server-side (last assistant message time.completed set, no pending asks) but `session.idle` was lost in a deliberate SSE reconnect window 1.4 s earlier — `runningSessions` is event-driven with no recovery, and the `/event` stream is directory-scoped, so every cross-folder session open/workspace switch is a loss window. FIX: (1) `reconcileRunning()` checks each locked session against server truth (new `turnIsOver`: last message is a completed assistant message) on every reconnect + openSession + a 15 s poll while working, reloading the missed history tail and unlocking; (2) interrupt — SDK `abortSession` (POST /session/:id/abort), Stop button next to Working… + Esc (modals/palette keep priority), "Interrupted" line in the thread, abort's own error/idle events suppressed once; (3) visibility — the Working… row now names the currently running tool call. VERIFIED: turnIsOver validated against the real stuck session's live payload (→ unlocks); 282 JS tests (+8: reconcile stale/genuine/on-connect, interrupt lifecycle ×4, SDK completed-mapping+abort), tsc green. DMG rebuilt.

2026-07-05 23:30 · Architecture audit (4 parallel tracks: layering, Rust backend, frontend, safety defaults). Verdict: skeleton is high quality — SDK boundary real (zero raw HTTP in app), clean package deps, zero `any`/`ts-ignore` strict TS, 274 behavioral tests, uniform Rust path sandboxing. BUT the AGENTS.md non-negotiable safety defaults are NOT met: no `permission` config is ever written and the bundled OpenCode 1.17.13 defaults to `{"*":"allow"}` (bash/edit/installs run unprompted — contradicts P2-3's "approval mode ✅"; only the permission UI ships), sidecar runs `--cors *` unauthenticated (any local webpage can drive turns + read keys via `/global/config`), API keys plaintext in `opencode.json`. Two critical Rust defects: Windows `cmd /C start` command injection in open_url/os_open (agent link with `&` executes commands; also breaks legit URLs), and kernel_execute holding the global kernel mutex across an unbounded blocking read (a `while True: pass` cell wedges even kernel_reset). Full severity-ranked backlog recorded as REQUIREMENTS.md P0-7. Blocker: fix permission block + CORS before next release.

2026-07-05 22:40 · fix(preview): text files with unlisted extensions (.bib, .rst, .c, …) showed "Preview is available in the desktop app." INSIDE the desktop app. ROOT CAUSE: Rust `mime_for` defaulted unknown extensions to binary→base64, and the frontend text path only accepts utf8 — text stayed null and fell through to the misleading note. FIX: `read_artifact` now sniffs unknown extensions (valid UTF-8 without NUL bytes → utf8 text/plain, else base64); frontend reports genuinely binary files explicitly ("binary and has no preview — open it externally") instead of the desktop-app note. VERIFIED: Rust sniff test (bib text / NUL / invalid UTF-8 / known-binary never sniffed) + component test (base64 behind a text preview shows the binary message). 274 JS tests (+1), 10 artifact Rust tests (+1) green. DMG rebuilt.

2026-07-05 22:15 · P1-5 native table→chart surface shipped (closes P1-5's "native categorical chart surface when a real dataset needs one" gap). Any CSV/TSV (or xlsx-parsed) table preview now has a Table ↔ Chart toggle. New pure `lib/tableChart.ts`: `analyzeColumns` detects numeric columns (parses `%`, thousands separators, tolerates a minority of NA/labels via a 60% finite threshold), `defaultChartSpec` picks a sensible default (first categorical column as X → grouped bar; numeric X → line; numeric columns as Y series, capped at 8), `canChart` gates the toggle. New `TableChart.tsx`: renders line/bar/scatter with the app's shared `--series-1..8` palette (the SAME hues the agent's matplotlib figures use, theme-aware for light+dark), with a chart-type segmented control, an X-column select (incl. "row #"), per-series show/hide chips, y-gridlines, thinned x-labels, and a legend — so a generated figure and this native chart tile read as one design system (P1-5 acceptance a). Wired via a `TableView` wrapper in FilePreviewInspector (Chart tab appears only when the data has a numeric column). VERIFIED: numeric detection + default-spec (categorical→bar, numeric→line, 8-series cap, null when nothing numeric) — 6 logic tests; component renders type controls + series toggles + bar rects, switches to line polylines, and shows the no-numeric message — 3 component tests. 270 JS tests (+15 across tableChart+modal), tsc, eslint green. DMG rebuilt.

2026-07-05 21:45 · P2-2 Modal runner shipped (the named P2-2 gap; SSH+Slurm was already done). Same shape as the HPC integration — the app drives the user's OWN credentials, never handles tokens. New Rust `modal.rs`: `modal_status` runs `modal --version` under the enriched PATH (so a conda/user-installed modal is found) and reports {installed, version, authenticated, hint}; authentication is a pure, unit-tested `is_authenticated(home, token_env)` (a non-empty `MODAL_TOKEN_ID` or a `~/.modal.toml` file). New Settings "Cloud compute (Modal)" card (`ModalCard.tsx`): a status dot (ready/installed-not-authed/not-installed) + the fix hint + a Re-check button. New bundled `modal-run` skill: verify readiness, write a Modal function INTO the workspace (pinned image packages + fixed seed for reproducibility), `modal run` it with the user's token, capture results as artifacts; explicit rules — user's account only, never store tokens, cost-aware (bounded timeout, no GPU unless needed). VERIFIED: `is_authenticated` via token-env and via a real `~/.modal.toml` fixture (empty home never authed) — 2 Rust tests; detection correctly reports "not installed" on this host (no modal); ModalCard mounts — 1 component test. 264 JS tests (+1), 35 Rust tests (+2), tsc, eslint green. DMG rebuilt. Gap: multi-environment management; a live Modal run needs the user's own account.

2026-07-05 21:10 · P1-3 materials binary phase-diagram viewer shipped — completes the materials electronic/thermodynamic trio (DOS + band structure + phase diagram). New `lib/phase.ts` parses a `.phase` JSON (2 elements + entries with atomic composition + formation energy/atom), computes each phase's composition x = n_B/(n_A+n_B), builds the LOWER CONVEX HULL (Andrew's monotone chain) and marks each phase stable/metastable by its energy-above-hull (defined via eAboveHull ≤ tol, NOT hull-vertex membership — a phase exactly on a tie-line is collinear and dropped as a redundant vertex, but is still marginally stable; caught this in testing). `PhaseView.tsx`: SVG scatter of formation energy vs composition with the convex-hull tie-lines, stable phases filled + labeled, metastable phases open (series-6 ring) with their eAboveHull on hover, endpoints labeled with the element symbols, app palette. Registry: previewKind "phase" (`.phase`), needsText, Rust mime_for serves `.phase` as JSON text, FilesPage image icon. VERIFIED against a Li–O fixture: composition fractions, stable (Li2O/LiO2/endpoints) vs metastable (LiO at +1.0 eV above the Li2O–O2 tie-line), hull sorted by x — 5 parser tests; component renders system/counts/hull path + 4 circles + stable labels + error — 2 component tests. 263 JS tests (+7), 33 Rust tests, tsc, eslint green. DMG rebuilt. The materials row of P1-3 (band/DOS + phase diagrams) is now ✅. Gap: ternary phase diagrams; FITS corner plots; anomaly-map coastlines.

2026-07-05 20:40 · P2-4 file-open reliability: fixed an ineffective large-file preview guard (real OOM/freeze risk). ROOT CAUSE: `read_artifact` (the choke point for every preview — Files explorer + artifact clicks) did `std::fs::read(full)` — loading the ENTIRE file into memory — and only THEN checked the 25 MB cap, so opening a multi-GB file (exactly what P0-6 is about) could exhaust memory and freeze the app before the cap ever fired. FIX: stat first via `metadata().len()`, reject oversized files BEFORE reading a single byte, then read only within the cap; extracted `PREVIEW_CAP_BYTES` + a pure `exceeds_preview_cap()` helper. Frontend: a new `PreviewError` component special-cases the "too large" error into a helpful card — explains the cap, offers "Open externally", and points to the large-file probe (schema/sample instead of loading the whole file) — other errors still render as a plain line. Considered P1-2 physics/earth connectors this iteration but REJECTED them on the "no bugs / vetted" bar: `astro-mcp` is actually Astronomer/Airflow (not astronomy — a name mismatch I caught before adding), and `usgs-mcp`/`open-meteo-mcp` couldn't be confidently verified to start as stdio servers (both exited with no error even with stdin held open) — I won't ship a connector I can't verify launches. VERIFIED: `exceeds_preview_cap` boundary (0/at-cap allowed, +1/2 GB rejected) — Rust test; the too-large card renders + Open-externally fires, plain errors don't — 2 component tests. 256 JS tests (+2), 33 Rust tests (+1), tsc, eslint green. DMG rebuilt.

2026-07-05 20:05 · P1-3 materials band-structure viewer shipped — completes the standard electronic-structure pair (band structure + DOS) for materials. New `lib/bands.ts` parses VASP EIGENVAL (5 header lines + `NELECT NKPTS NBANDS` + per-k-point blocks of `bandIndex energy [energyDown] [occ…]`), structurally reading NKPTS blocks × NBANDS bands, detecting spin from the two-energy-column form; `BandView.tsx` plots every band's energy across the k-point index as thin polylines (spin-up series-1, spin-down series-6 with a legend), energy gridlines, and a k/energy hover readout, in the app palette. Registry: previewKind "bands" (extensionless EIGENVAL via `previewKindForName`, or `.eigenval`), needsBytes (TextDecoder — no Rust change), FilesPage flask icon. VERIFIED: parser reads non-spin (3 k-points × 3 bands, correct per-band energy series + eMin/eMax) and spin-polarized (two energy columns → up/down) EIGENVAL — 3 parser tests; component renders one polyline per band + axis labels + error — 2 component tests; `previewKindForName` EIGENVAL — registry test. 254 JS tests (+5), 32 Rust tests, tsc, eslint green. DMG rebuilt. Gap: phase diagrams (materials); FITS corner plots; anomaly-map coastlines.

2026-07-05 19:35 · P0-4 reviewer PDF-manuscript hardening shipped (the P0-4 gap was "hardening across document formats (PDF)"). New bundled `runtime/skills/core/traceability-review/pdf_extract.py`: a deterministic, multi-backend PDF extractor (tries PyMuPDF `fitz` → `pypdf` → `PyPDF2` → `pdfminer.six`, degrades to a clear "no backend" message — never a traceback) that returns `{backend, pages, chars, citations:{dois,arxiv,pmids}, claims:[{kind,text,context}], text}`. Citation identifiers (DOI `10.x/…`, arXiv new+old, PMID) and quantitative claims (p-values, percentages, N=, CIs) are pulled by regex with surrounding context, so the reviewer audits REAL identifiers from the manuscript rather than ones recalled from memory (the same tool-over-recall principle as P0-5). Updated the traceability-review SKILL.md: for a PDF, run pdf_extract.py FIRST and feed citations→Check 1, claims→Check 2, text→Check 3; on `{error}` say so and never fabricate. VERIFIED end to end: generated a real PDF (PyMuPDF) and the extractor pulled the text, all three identifier types (10.1234/example.2026, arXiv 2401.00001, PMID 31234567), and p-value/percent/sample-size claims — 7 Python tests (incl. backend-independent regex tests + clean error on a missing file). Found + fixed a test-fixture bug (PyMuPDF insert_text clips long lines at the page edge → identifiers past the margin were lost; fixture now uses short lines — the extractor itself was correct). DMG rebuilt to bundle the extractor. Gap: robustness for models weaker at tool use.

2026-07-05 19:05 · P1-3 native climate-anomaly map shipped — the LAST P1-3 discipline viewer, so ALL FOUR targeted disciplines (materials, physics/astro, earth, social) now have a native renderer, meeting P1-3's "≥1 domain renderer per discipline" acceptance. New `lib/anomaly.ts` parses a gridded `.anom` field in two portable text shapes (long CSV `lat,lon,value` with header sniffing, or a labeled grid with lons in the header row + lats in the first column) into an ascending lat/lon grid + a symmetric zero-centered range; `divergingColor` gives a blue↔white↔red scale. `AnomalyMapView.tsx`: a canvas renders the field on an equirectangular (plate carrée) projection (north up, missing cells gray), with an SVG overlay drawing a graticule + °N/°S/°E/°W labels + hover crosshair, a diverging colorbar (±absMax), and a lat/lon/value readout — the correct transform + diverging colormap the discipline expects. Registry: previewKind "anomaly" (ext `.anom`), needsText, Rust mime_for serves `.anom` as text/csv, FilesPage image icon. VERIFIED: parser reads long-CSV + labeled-grid + diverging color endpoints — 5 tests; component renders grid dims + canvas + graticule labels + error — 2 tests. 249 JS tests (+7), 32 Rust tests, tsc, eslint green. DMG rebuilt. Gap: materials band structure + phase diagrams; coastline basemap for the anomaly map; FITS corner plots.

2026-07-05 18:35 · P1-3 native qualitative-coding traceback viewer shipped (social-science renderer row ⬜→🟡; pairs with P1-6). New `lib/qcode.ts` parses a portable `.qcode` JSON (sources + codebook + span annotations), validates each annotation (flags unknown source/code and out-of-range spans instead of throwing), and — crucially — derives every quote as `source.slice(start,end)`, plus a boundary-segmentation pass that renders overlapping codes without loss. `QCodeView.tsx`: a codebook sidebar (per-code color from the app palette + counts), the source text with highlighted coded spans, two-way linking (click a code to isolate its spans; hover a span to see its codes), a skipped-annotation warning banner, and a "quotes are exact source spans" assurance — the decisive social-science integrity property is guaranteed by construction (a highlight is literally a slice of the source, so a code can never point at an invented quote). Registry: previewKind "qcode" (ext `.qcode`), needsText, Rust mime_for serves `.qcode` as JSON text, FilesPage highlighter icon. VERIFIED: parser reads sources/codes/annotations + exact quotes + overlap segmentation + warnings — 6 tests; component renders codebook + <mark> spans sliced from source + error — 2 tests. 242 JS tests (+16 across dos+qcode), 32 Rust tests, tsc, eslint green. DMG rebuilt. Gap: materials band structure + phase diagrams, earth climate anomaly maps.

2026-07-05 18:05 · P1-3 native materials DOS viewer shipped (second P1-3 domain viewer this session; materials band/DOS row ⬜→🟡). New `lib/dos.ts` parses VASP DOSCAR total density-of-states (5 header lines + `Emax Emin NEDOS Efermi` control line + NEDOS rows; 3 cols = non-spin, 5 cols = spin-polarized) + `DosView.tsx`: an SVG chart in the app palette — non-spin as one filled curve, spin-polarized with spin-up above the baseline and spin-down mirrored below (the canonical DOS presentation), the Fermi level as a dashed vertical marker, an "E − E_F ↔ absolute E" alignment toggle, and a hover readout. Made the previewer registry filename-aware: new `previewKindForName(filename)` recognizes extensionless scientific files (VASP DOSCAR) and the `.dos` extension, delegating to the ext registry otherwise; routed FilePreviewInspector + FilesPage (icon + inspector) through it. DOSCAR loads via needsBytes (TextDecoder-decoded), so no Rust text/binary change was needed. VERIFIED: parser reads non-spin + spin DOSCAR (energies, up/down channels, Efermi) — 3 parser tests; component renders two filled spin areas + Fermi marker + toggle, and a friendly error — 2 component tests; `previewKindForName` DOSCAR/`.dos`/fallback — 2 registry tests. 234 JS tests (+8), tsc, eslint green. DMG rebuilt. Gap: band structure + phase diagrams (materials), earth anomaly maps, social qualitative-coding traceback.

2026-07-05 17:35 · P1-3 native FITS astronomy viewer shipped (physics/astro was the lowest-completion discipline — no connector AND no viewer; now it has a renderer). New dependency-free FITS reader `lib/fits.ts` (parses 80-char header cards in 2880-byte blocks, big-endian data per BITPIX 8/16/32/64/-32/-64 with BZERO/BSCALE + BLANK→NaN, primary HDU; WCS keywords; 1-D spectrum vs 2-D image by NAXIS) + `FitsView.tsx`: a 2-D image HDU renders to a canvas with a scientific colormap (magma/viridis/gray), linear/log/asinh stretch, a colorbar, vertical flip for FITS bottom-left origin, and a hover readout of pixel value + approximate WCS sky coordinate (linear CDELT, cos-dec corrected); a 1-D HDU renders as a palette-matched SVG line chart with world-axis labels from CTYPE/CRVAL/CDELT. Dark astro backdrop, theme-independent like the molecule/mesh viewers. Wired the previewer registry: previewKind "fits" (+FITS_EXTS fits/fit/fts), FilePreviewInspector needsBytes→FitsView, Rust mime_for serves them as binary (base64→ArrayBuffer), extToKind "figure". VERIFIED against REAL astropy files (8×8 float32 image with a TAN WCS; 16-pt int16 spectrum): parser reads dims, decodes big-endian values (ramp 0..63; int16 0,3,6…), extracts WCS, maps the reference pixel to exactly CRVAL, and the 1D linear axis — 7 parser tests; component mounts with the right controls/DOM + friendly error — 3 component tests. 223 JS tests (+10), 32 Rust tests, tsc, eslint green. DMG rebuilt. Gap: materials band/DOS + phase diagrams, earth anomaly maps, social qualitative-coding traceback; FITS corner plots + full (non-linear) WCS.

2026-07-05 16:55 · P1-2 first non-bio domain connectors shipped (was 🟡 with ZERO non-bio DBs). Added two vetted, real open-source MCP servers to the curated catalog (`lib/scienceConnectors.ts`), same one-click-provision-into-isolated-uv-env mechanism as the literature/bio ones — we integrate, don't reimplement: **Materials Project** (`mcp-materials-project`, the requirement's prioritized materials DB; MP_API_KEY) and **FRED** (`fred-mcp`, Federal-Reserve economic time series for social science; FRED_API_KEY). Package names + entry points were verified against PyPI + each wheel's `entry_points.txt` BEFORE adding them (no invented names — the exact risk P0-5 guards against). Two mechanism upgrades: (1) `connectorConfig` now launches console-script servers (the common MCP shape) by resolving the script next to the managed interpreter — cross-platform (`<env>/bin/<s>` unix, `<env>/Scripts/<s>.exe` Windows) — in addition to `-m module`; (2) a per-connector free-API-key field passes the key via the MCP `environment` map (never into provenance/logs/exports — safety default) and Enable is gated until a required key is entered. Settings card gained a discipline chip + "Get a free key ↗" link + large-install note. VERIFIED end to end on this host: `fred-mcp` pip-installs, its console script lands EXACTLY at the path `connectorConfig` computes (`<venv>/bin/fred-mcp`), it rejects startup with no key, and with a key supplied via the environment it passes key validation and proceeds to run its transport — proving the launch + env-key chain (a live query needs the user's real key + OpenCode as the stdio client, same bar as the existing connectors). 216 JS tests (+8 connectorConfig: module/console-script/Windows-.exe/env-key/blank-key/breadth), tsc, eslint green. DMG rebuilt. Gap: physics/astro + earth/climate connectors (no sufficiently mature open MCP found yet), more chem/social DBs.

2026-07-05 16:20 · P0-6 large-file "reference, don't load" memory-pointer contract shipped (was 🟡 with the core contract unbuilt). New bundled skill `large-file` (`runtime/skills/core/large-file/large_file_probe.py`, stdlib-first, agent-facing tool — emits a compact JSON POINTER, not the review contract, so zero frontend change): given any data file it returns schema / shape / approx row count / head+tail sample / extracted log numbers in BOUNDED memory (streamed line count, tail via a 64 KB seek, per-cell truncation), so the agent references data instead of loading a file bigger than the context window. Coverage: tables (CSV/TSV, delimiter sniff + dtype inference), NDJSON (key union + count), Parquet (pyarrow metadata only), HDF5 (h5py dataset tree, no array read), FITS (astropy memmapped headers), NetCDF, and text/logs with DETERMINISTIC numeric extraction for VASP OUTCAR/OSZICAR (last `free energy TOTEN`, `energy(sigma->0)`, convergence flag — the numbers, not prose). Binary formats without their lib degrade to a clear install-hint pointer, never a raw dump. SKILL.md tells the agent to probe BEFORE reading and then read only bounded slices (nrows/usecols/sub-array). VERIFIED end to end on real files on this host: 13.5 MB CSV → 788 B pointer (700k rows, head+tail sample), real 1.6 MB Parquet → 447 B (100k rows, schema from metadata), 16 MB HDF5 → 456 B (dataset tree with shapes) — each ~18,000–37,000× smaller than the file, no whole-file load (matches the requirement's >16,000× memory-pointer finding). 11 Python probe tests (incl. an explicit "pointer stays <4 KB and <file/100" assertion). DMG rebuilt. Gaps (🟡): genomics BAM/CRAM/FASTQ (pysam), GRIB, ROOT; auto pre-read wiring in the UI.

2026-07-05 15:45 · P1-6 social-science analysis integrity shipped (the other ⬜, pairs with P0-5). New bundled skill `stats-integrity` (`runtime/skills/core/stats-integrity/stats_integrity_check.py`), same deterministic + pluggable shape and same `review` contract — added `integrity` to shared `ReviewCheck` (findings carry a `stats · …` tag, so no per-check UI change). Three checks map to the three acceptance points: (a) **execute-don't-interpret** — a `stats · interpretation` check flags causal/"provocative" language ("causes/leads to/drives/the effect of…") in a report that is reporting an association, and the SKILL.md instructs surface-estimates-don't-volunteer-causation (report coef+SE, name the design before any causal claim); (b) **prereg-aware** — a `stats · prereg` check parses regression formulas (`y ~ x1 + x2*x3`) in the code and flags any predictor/interaction absent from a `preregistration.*`/`analysis_plan.*`/`prereg.*` plan (HARKing guard); (c) **reproducibility** — a `stats · seed` check flags randomised analysis (np.random/train_test_split/bootstrap/R sample) with no fixed seed. VERIFIED the .dta→R round-trip acceptance ON THIS HOST: pandas `to_stata` → R `foreign::read.dta` → `lm` reproduces Python OLS to the printed precision (β=0.600870, SE=0.075000 both sides; the SKILL.md documents this recipe with fixed seeds — no new engine, reuses the shipped R kernel). Rules favour precision (association wording and non-stats docs stay silent; no plan → no prereg findings). 13 Python validator tests; CLI catches all three risks on a realistic workspace; 208 JS tests (+1 integrity-tag parse), tsc, eslint green. DMG rebuilt. Gaps (🟡): a first-class in-app preregistration artifact + auto pipeline↔plan diff every run; packaged Stata/SPSS reader UI; wrong-test-selection detection.

2026-07-05 15:10 · P0-5 domain-correctness gates shipped (was the only ⬜ P0, "runs" ≠ "right" — the highest-value new build). New bundled skill `domain-check` (`runtime/skills/core/domain-check/domain_check.py`): a DETERMINISTIC, pluggable validator layer — one `check_<field>` rule set per discipline, stdlib-only, analysing the code the agent actually wrote (AST for Python/notebooks, regex for R) rather than model recall. Three gates ship, each catching its acceptance case: **physics · units** (dimensional mismatch `t_seconds + d_meters`; trig on a degree-valued angle), **earth · crs** (Euclidean distance on lat/lon incl. the classic `(lat1-lat2)**2+(lon1-lon2)**2`; geopandas geometric op with no CRS set), **biology · coords/strand** (BED off-by-one — 0-based half-open so length is `end-start`, no `+1`, caught via AST so `int(end)-int(start)+1` is seen through the cast; strand-unaware sequence extraction). Rules favour precision (unknown units / no discipline signal stay silent; findings dedupe when rules overlap). Reuses P0-4's `review` contract end to end: extended shared `ReviewCheck` with `domain` + optional per-discipline `tag` (so new fields need NO UI change), parser + ReviewerCard render the tag; the skill tells the agent to run the gate before/after execution and relay the emitted ` ```review ` block. VERIFIED: 19 Python validator tests (3 acceptance catches + true-negative precision guards + syntax-error-no-crash + R fallback); CLI emits clean deduped findings on a multi-discipline sample; 207 JS tests (+2: domain-tag parse), tsc, eslint green; skill bundles into `Open Science.app/Contents/Resources/skills-core/domain-check` with NO test/pycache leakage; runs portably from the deployed `$XDG_CONFIG_HOME` path; and the bundled OpenCode sidecar DISCOVERS `domain-check` in its directory-scoped skill list alongside traceability-review. Gaps (kept 🟡): full library round-tripping (SMILES→RDKit, POSCAR→pymatgen), chemistry/materials + social-science gates, deeper per-field rules. DMG rebuilt.

2026-07-05 07:30 · Rewrote `docs/REQUIREMENTS.md` on a multi-discipline evidence base (v2). Five parallel community-research passes (physics/astro, chemistry/materials, earth/climate, social science, bio+cross-cutting) saved to `docs/research/2026-07-05-multidiscipline-needs.md`. Key finding: needs are ~80% shared / ~20% discipline-specific; the shared 80% (the moat) is largely shipped. Every item now carries a ✅/🟡/⬜ status marker. Added two NEW requirements from the evidence: **P0-5 domain-correctness gates** ("runs" ≠ "right" — units/CRS/off-by-one/stoichiometry/stat-selection; the largest gap, highest-value build) and **P1-6 social-science analysis integrity** (execute-don't-interpret + prereg + Stata/SPSS); split out **P0-6 large-file reference-don't-load**; expanded P1-2 connectors and P1-3 renderers into per-discipline tables (physics/chem/earth/social all ⬜ planned). Docs only; no code change.

2026-07-05 06:20 · Session header slimmed from 5 elements to 3 so it stays one line (user: slim the header content down or it wraps to two lines). The Files toggle now names the session folder itself (e.g. `2026-07-05-0319`, full path on hover), absorbing the redundant read-only folder chip — WorkspaceChip no longer renders for open sessions (draft picker unchanged); ConnBadge shows only the green dot when ready (full text on hover), text only for connecting/error. 206 JS tests (WorkspaceChip open-session test now expects empty render), tsc, eslint green; DMG rebuilt.

2026-07-05 05:10 · Right pane is now per-session with scroll memory (user: switching sessions leaked another session's open PDF into a workspace where the path no longer resolved; wanted each session to restore its own pane and positions). Store: global activeArtifact → `panes` map keyed by session id (DRAFT_KEY for drafts) holding {artifact, showFiles} — mutually exclusive by construction; draft pane grafts onto the created session id, startDraft/switchWorkspace reset the draft pane, deleteSession forgets its pane. New useScrollMemory hook (module-level Map, zero re-renders): remembers scrollTop per key, restores once content is ready, ignores clamp events while loading; wired into chat (`chat:<sid>`), NotebookEditor + FilePreviewInspector (`file:<path>`, history separate), Docx/Xlsx/Pptx inner scrollers (`office:<path>`), and the demo inspectors. Limitation: PDF/HTML render in native iframes — the file reopens but the viewer's internal position can't be captured. 206 JS tests (+13), tsc, eslint green; DMG rebuilt (user re-tested BEFORE the rebuild and saw the old global-pane behavior — the fix wasn't in the installed app yet).

2026-07-05 04:10 · Files/Notebooks are now GLOBAL pages; the session gets its own quick entry (user: pages looked global but silently showed per-session content; demanded the kernel-cwd gap be solved properly, "需要1个最佳实践"). Design per user: Files browses from the Settings base folder (root crumb names it), Notebooks lists every .ipynb across all session folders newest-first with a folder chip, and the chat header gains a Files button that opens the CURRENT session's folder in the right inspector (SessionFilesPane, like Artifacts). Plumbing: file commands (list_dir/read_artifact/preview_url/write_workspace_file/open_path/list_notebooks) take an explicit root scope "workspace"|"base" (no fallback guessing); preview URLs are scope-prefixed /w/… vs /b/…. Kernel best practice = Jupyter semantics: one kernel PER NOTEBOOK, cwd = the notebook's own folder (kernel map keyed by lang+absolute path, per-kernel R code file) — relative paths in any notebook now resolve correctly from any page, and state no longer bleeds between notebooks. 32 Rust + 194 JS tests, tsc, eslint green; DMG rebuilt.

2026-07-05 03:40 · Files page now names the folder it is browsing (user: "Workspace" root showed 3 files but no way to tell WHICH folder — expected the OpenScience base, was actually the session's dated folder). The breadcrumb root now shows the active workspace folder's basename (e.g. `2026-07-05-0319`) with the full path on hover, reusing WorkspaceChip's baseName (now exported). Browser dev keeps the "Workspace" fallback. 192 JS tests, tsc, eslint green; DMG rebuilt.

2026-07-05 03:45 · Follow-up on "still broken" report: the re-root fix WAS working (new jupyter correctly rooted + owns the port; e2e-verified by creating a notebook through the jupyter API — it landed in the active workspace). The remaining symptoms were pre-fix residue: nature_figure.ipynb had been misplaced into 2026-07-04-2204 by the old build (moved back to its session folder 2026-07-05-0319), and a Friday orphan jupyter-lab had survived TWO SIGTERM pkills (wedged in graceful shutdown) — force-killed it and hardened kill_orphan_jupyter to SIGKILL (pkill -9; Windows already used /F). 31 Rust tests green; DMG rebuilt.

2026-07-05 03:25 · Agent-created notebooks now land in the ACTIVE workspace (user: agent made a notebook but the preview said "file not found" and the Notebooks sidebar showed "No notebooks yet" despite many existing). ROOT CAUSE, same class as the 07-04 preview-server bug: jupyter-lab pins `--ServerApp.root_dir` at spawn time and `start_jupyter` is idempotent, so after workspace-per-session switches the agent's jupyter MCP kept writing notebooks into the OLD folder (proven live: running lab rooted at 2026-07-04-2204 + a Friday orphan rooted at the base folder, both on port 50868, while the UI scanned 2026-07-05-0319). FIX: `set_workspace` (the choke point for every switch path) now calls `reroot_jupyter` — background kill + respawn rooted in the new folder, serialized by a lifecycle lock; port/token are fixed in server meta so the MCP entry stays valid. Orphans self-heal on next start via the existing pkill. Existing stray notebooks remain in ~/Documents/OpenScience (by-design per-folder isolation). 31 Rust tests green; needs app rebuild + restart to take effect.

2026-07-05 04:35 · 3D viewer warns on unrenderable files (user: add a notice so people know the file is the problem). After framing, MeshView renders the model once offscreen over a magenta clear (backdrop off) and counts painted pixels; if ~none (<0.08% of the canvas) it overlays a "File problem — this 3D file has no visible geometry, the export looks incomplete or corrupt" banner. Catches the malformed sample (renders empty) and genuinely empty/corrupt exports; verified NOT to false-positive on a valid cube glTF or the torus-knot STL. 194 JS tests, tsc, eslint green; DMG rebuilt.
2026-07-05 04:20 · 3D viewer fidelity: glTF/GLB visibility + obj z-fighting (user: cube.gltf/glb showed nothing; cube.obj bottom face flickered at some angles in Shaded). ROOT CAUSES: (1) glTF meshes kept their own single-sided material and a default fully-metallic PBR material renders black without an environment map — added a PMREM RoomEnvironment so PBR renders correctly, and forced DoubleSide on all mesh materials so inconsistent winding never culls a face to invisible; (2) the shadow-catching ground sat coplanar with a flat model bottom (a cube) → z-fighting; dropped the ground by radius*0.01. Decisive check: a hand-built VALID cube glTF renders as a clean shaded cube — so the viewer is correct; the user's sample cube.gltf/glb are MALFORMED (only 2 of 6 faces, and a wrong indices bufferView byteLength), produced by the sample generator, which is also why the same-cube STL/OBJ/PLY render fine. Verified valid-gltf + obj by screenshot. 194 JS tests, tsc, eslint green; DMG rebuilt.
2026-07-05 03:15 · 3D mesh viewer for stl/obj/ply/gltf/glb (user: can 3D models / CAD show? then: add three.js, beautiful + workable). New MeshView (three.js + WebGL, self-contained, lazy-loaded loaders) added as its own preview kind: warm studio lighting (hemisphere + key/fill), soft contact shadow, warm gradient backdrop matching the app palette, auto-frames the model, OrbitControls (drag-orbit / scroll-zoom / right-pan), Shaded↔Wireframe toggle, triangle-count badge; theme-independent like the molecule/Office previews. Wiring: previewKind→"mesh" (+MESH_EXTS), FilePreviewInspector needsBytes, Rust mime_for returns these as binary→base64→ArrayBuffer, new "model" ArtifactKind (Box icon) so written models surface as artifacts. Three fidelity fixes found via screenshot: STL face normals are often zero → always computeVertexNormals (else unlit black); wireframe swaps to a dark high-contrast material (light clay drew near-invisible lines on the warm bg); material ref-swap toggles without rebuilding. Added `three`/`@types/three` deps. Verified with a torus-knot STL: shaded + wireframe both render cleanly. CAD note: DXF (mid) and STEP via occt-import-js WASM (heavier) possible later; DWG has no open reader. 192 JS tests (1 new: mesh mapping), tsc, eslint green; DMG rebuilt.
2026-07-05 02:35 · Office preview overflow fixes (user: wide xlsx changes background color when scrolled right; docx renders too large to see both page edges). (1) xlsx: the shadow `.page` was block-level (viewport-wide), so its white background stopped mid-table and cells without a fill turned a different color past that point when scrolled right — set `.page { width: max-content; min-width: 100% }` so the background covers the table's full width. (2) docx: a page has a fixed physical width (Letter/A4, portrait or landscape) usually wider than the pane, overflowing with both edges cut off — DocxView now scales the rendered page to fit the pane width via `zoom` (shrinks the layout box, no leftover scroll; never upscales) and re-fits on pane resize via ResizeObserver. Both verified by screenshot in a narrow pane. 191 JS tests, tsc, eslint green; DMG rebuilt.
2026-07-05 02:10 · xlsx preview now shows real formatting — fills, font size/color, bold, alignment, borders, column widths (user: docx/pptx/xlsx looked very different from WPS; xlsx had no background colors etc). ROOT CAUSE for xlsx: the open-source SheetJS build's `sheet_to_html` emits values ONLY (no styles), so every cell was an unstyled grid. Rewrote lib/xlsx.ts on ExcelJS (already a transitive dep, now explicit) — reads each cell's fill/font/alignment/border and emits inline styles + a `<colgroup>` of Excel column widths + merge spans; ARGB alpha ignored (files store `00` alpha that still means visible). workbookSheets is now async; XlsxView awaits it; SHEET_CSS reduced to a neutral gridline scaffold cells override. Verified with the user's real spec.xlsx: terracotta header fill + white bold text, and the Color-Palette sheet's hex cells render their actual swatch colors — matches WPS. docx (docx-preview) and pptx (pptx-preview, black bg already fixed) were checked and already apply document styles, so left as-is; remaining gaps there are font substitution + inherent browser-render approximation. 191 JS tests (xlsx suite rewritten, +1 style assertion), tsc, eslint green; DMG rebuilt.
2026-07-05 01:45 · File preview no longer bleeds the previous file's content (user: clicking a file's Open button sometimes shows crossed/mixed content). ROOT CAUSE: one FilePreviewInspector instance is reused across files (no key), and its load effect only called setText when `data.content === undefined` — so opening a second file that carries its own inline content never replaced the first (useState initializers don't re-run on prop change); url/bytes lingered too. FIX: the effect now resets text/url/bytes to the new file's baseline up front, before the async loads fill in only what the new file needs. Covers both the deterministic inline-content bleed and the fast-switch race (the existing `cancelled` guard drops the stale file's async result). 190 JS tests (1 new: reuse-across-files shows the new file), tsc, eslint green; DMG rebuilt.
2026-07-05 01:20 · CSV preview: dirty commas no longer explode the table (user: a design_tokens.csv row with `0 1px 3px rgba(0,0,0,0.04)` rendered as many extra columns). ROOT CAUSE: agents emit CSV with unquoted CSS/JSON values whose internal commas a strict RFC parser splits into extra fields; TablePreview renders one <td> per field, so that row spilled past the 4-col header and misaligned. FIX in parseDelimited: track bracket depth outside quotes — a delimiter inside ()/[]/{} is field content, not a separator (quoted fields still parse strictly, so well-formed CSV is unchanged; depth resets per line since brackets don't span rows). Rows are then normalized to header width (pad short, fold an over-long row's surplus into the last cell) so the table is always rectangular. Verified with the user's real file: 4 clean columns, shadow/clamp() values intact. 189 JS tests (2 new), tsc, eslint green; DMG rebuilt.
2026-07-05 00:45 · Markdown preview (editorial-blog paper) + pptx black-background fix (user: md should preview like a document; wanted a minimal blog feel with a touch of color; pptx slides sat on black). (1) `.md` gets its own preview kind with a Preview/Code toggle: MarkdownViewer grew a `document` variant styled as blog paper — a white card on the pane, warm ink (#2b2620), serif display headings (Iowan Old Style → Charter → Georgia; CJK → Songti), the app's terracotta brand (#c15f3c) as a layered accent (H2 accent bar, list markers, links, inline code), tuned tracking (CJK-safe near-zero on headings, +0.006em body), text-wrap balance/pretty, tabular-nums tables. Theme-independent by design (a document reads the same in dark mode). The shared component's element set (headings/ol/blockquote/pre/table/hr) also lifts chat markdown. (2) pptx black background ROOT CAUSE, reproduced in a standalone harness with the user's own deck + DOM inspection: pptx-preview hardcodes `background: black` on its `.pptx-preview-wrapper`, so every gap around slides showed black; fixed with a shadow-DOM override (`transparent !important` beats the inline style) — slides now float on the light canvas with shadows. Libraries stay: docx-preview / SheetJS / pptx-preview are the strongest pure-JS options (browser pptx rendering is inherently approximate). All three variants (EN report, CN blog, mixed elements) verified by screenshot. 187 JS tests (3 new), tsc, eslint green; DMG rebuilt.
2026-07-04 22:40 · HTML/PDF/image Preview fixed (user: index.html opened with code visible but Preview said "not found"). ROOT CAUSE, two layers in the preview file server: (1) artifact paths from write tools are ABSOLUTE (`/Users/…/2026-07-04-2204/index.html`) but preview_url pasted them into the URL as if workspace-relative — the server joined them under its root and 404'd every absolute-path artifact; (2) the server's root was captured once at first use and cached forever, so after the workspace-per-session change any session switch left it serving the wrong folder. FIXES: preview_url now relativizes absolute paths against the current workspace (sandbox-checked: outside-workspace paths are a clear error, not a silent 404); the server resolves each REQUEST against the workspace as it is now (serve(root_of) closure). User-verified in the app. 31 Rust tests (2 new), DMG rebuilt.
2026-07-04 22:30 · Live streaming text + file-path tool rows (user: a landing-page turn showed only "write / Working…" for ~50s — couldn't see what was being written or worked on). ROOT CAUSE, verified in opencode v1.17.13 source + the bundled binary: (1) OpenCode streams assistant text as `message.part.delta` events (full `message.part.updated` only fires at text-start with "" and at text-end with the whole passage) — the SDK client ignored deltas, so ALL agent prose popped in at once at the end; (2) during a write tool's argument streaming (the dominant wall time — tonight's 20KB index.html streamed 22:06:34→22:07:24) OpenCode 1.17.13 emits NOTHING (tool-input-delta is a no-op upstream; the part sits at `{status:"pending", input:{}}`), so no client can show live write content at this pinned version — upstream limitation, revisit on upgrade; (3) once input DID arrive, the tool row still ignored it — title fell back to `input.command` (bash) then the bare tool name. FIXES: SDK accumulates `message.part.delta` per partID into progressive `text.updated` events (cleared on session.idle; reasoning parts never seeded, so their deltas drop out); tool-row titles fall back to `input.filePath` so write/edit rows show the target file as soon as args parse; per-token events skip the debug-log IPC. VERIFIED LIVE against a real opencode server: text.updated arrived progressively 0→15→34→44 chars, final text identical to text-end. 184 JS tests (2 new; mock server made protocol-faithful: text streams as deltas), tsc, eslint green; DMG rebuilt.
2026-07-04 21:20 · Settings shows and changes the BASE workspace (user: Settings displayed a dated session folder like ".../2026-07-04-2103" instead of ~/Documents/OpenScience, and Reveal offered no way to pick a folder). Settings' Workspace card was reading the ACTIVE workspace (which follows each session into its dated subfolder); it now shows the BASE folder — the parent every new session's dated subfolder is created under — with two buttons: "Change…" (native picker → new `set_workspace_base`, persisted to base-workspace.txt; `base_workspace_dir` reads it first, so new sessions land under the chosen base; existing sessions keep their folders) and "Reveal" (new `open_workspace_base` — the sandboxed `open_path` can only resolve inside the active workspace). The per-session dated folder still shows in the conversation header chip, by design. 183 JS + 29 Rust tests, tsc, eslint, release build green; DMG rebuilt.
2026-07-04 20:55 · Session switching 9-20 ms, was 3-6 s (user: why is switching history so slow?). ROOT CAUSE: workspace-per-session + `set_workspace` killed and respawned the whole OpenCode sidecar on every cross-folder open (process boot ~2s + 1s-granularity reconnect polling + a 1.5s blind listSkills retry inside connect()). VERIFIED LIVE first: OpenCode serves many folders from ONE process via per-directory instances — a bare `/event` stream can't see other folders (the old comment was right) but `/event?directory=` CAN; `/session/:id/*` calls route by the session's own recorded folder (pwd proof); a directory-less createSession lands in the boot cwd (so it MUST carry the param). FIXES: SDK `/event` + `createSession` carry `?directory=`; Rust `set_workspace` no longer restarts the sidecar; store `connectRetry` backs off 250ms×8 then 1s; `loadCatalog` moved off connect()'s critical path with the 1.5s blind sleep replaced by 400ms polling. MEASURED with the real SDK against the live sidecar: 3-folder hop = 20/9/9 ms including getMessages; scoped SSE delivers a real turn's tool + idle events; the shell runs in the session's folder, not the boot cwd. 183 JS + 29 Rust tests, tsc, eslint, release build green; DMG rebuilt.
2026-07-04 20:25 · Skeleton while switching sessions (user: the right pane sat blank for a moment when opening a history session). LiveSessionPage shows pulsing placeholder shapes that mirror the real thread layout (user card / agent text lines / a quiet tool row) whenever a session id is set but its history hasn't loaded (`!thread.loaded`) — covers the getMessages fetch and the sidecar restart on cross-folder opens. 183 JS tests, tsc, eslint green; DMG rebuilt.
2026-07-04 20:15 · Pasting "/name args" now chips too (user: paste didn't activate the command chip). The commit rule generalized from "name + trailing space only" to "known name + whitespace + rest": one change event with the whole pasted text commits the chip and the remainder (multi-line included) becomes the arguments; unknown names ("/etc/hosts …") stay plain text. 183 JS tests (2 new), tsc, eslint green; DMG rebuilt.
2026-07-04 20:05 · Composer: command chip + terminal-style input history (user: "/xxx should be a distinct block, not degrade into string text; ↑ should recall past inputs like a terminal"). (1) Committing a slash command — palette pick, or typing the full known name + space — turns it into an accent chip on the left (mirrors the shell-mode chip); the input then holds only arguments (mono font, accent border, argless Enter allowed); Backspace on empty input or the chip's × dissolves it back to editable "/name [args]" text; palette/shell detection are disabled while chipped so args starting with "/" or "!" stay arguments. (2) Every sent input (prompt, "!cmd", "/name args" in typed form) is recorded to localStorage (global, cap 100, consecutive-dedupe); ↑ with the caret at position 0 enters history navigation, ↑/↓ walk older/newer, walking past the newest restores the unsent draft, any edit exits navigation, the open palette keeps ↑/↓ priority. 181 JS tests (7 new composer tests; 1 updated: palette pick now chips instead of inserting "/name "), tsc, eslint green; DMG rebuilt.
2026-07-04 19:50 · Slash commands in history show as typed, not expanded (user: "/growth-marketing …" reloaded as the skill's full 6.7k-char template text as if they had pasted it). OpenCode stores the EXPANDED template + appended arguments as the user message with NO marker; GET /command exposes each command's `template`, so historyToThread reverse-maps: a user text that starts with a known template (longest-first) renders as "/name args". Caveat found live: MCP prompts report template as an argument-schema OBJECT, not a string — SDK now passes template through only when it is a string (would have crashed `.trim()`). VERIFIED against the real session over the live sidecar: the message renders exactly as typed. 174 JS tests (2 new), tsc, eslint green; DMG rebuilt.
2026-07-04 19:35 · History rendering cleaned up (user screenshots: repeated "The following tool was executed by the user" bubbles with bare "bash" rows; a subagent session opened from the sidebar showing its raw task prompt and read/glob spinning forever). Three fixes: (1) historyToThread now renders a user-run "!" shell turn like the live path — OpenCode stores it as a synthetic user text (`synthetic:true`, new field on HistoryPart) + a bash tool part; we show "! cmd" + inline output and never the marker text; bash rows also fall back to `input.command` as their title (was showing just "bash"); (2) steps frozen mid-run (runtime restarted / turn killed — e.g. the 18:35 stuck explore turn whose pending permission asks died with the old sidecar process) render as quiet "pending" + ONE red "Interrupted — this turn did not finish" line instead of spinning forever; (3) subagent child sessions (parentId set) no longer get their own sidebar row — they are internals of the parent conversation. NOTE: also repaired 3 literal NUL bytes accidentally written into runtime.ts's permission-sig template (worked fine, but grep treated the file as binary). 172 JS tests (4 new history tests), tsc, eslint green; DMG rebuilt.
2026-07-04 19:05 · Fixed the stuck "Explore …" turn + "Send failed: Load failed" (user session ses_0d01a1073ffe…: a /growth-marketing turn froze forever and the send reported failure). ROOT CAUSE (one bug, two symptoms): a task-tool subagent runs in a CHILD session; its `external_directory` permission asks carried the child id, but the UI only rendered asks where `sessionId === currentId` — the approval card never appeared, the subagent's read/glob blocked forever, and the still-open synchronous `/command` POST was killed by WKWebView's ~60 s fetch timeout → WebKit's generic "Load failed" shown as a send failure. FIXES: (1) SDK task tool events now carry `childSessionId` (from part metadata) and `SessionMeta.parentId`; store keeps a `sessionParents` map (live task events + session list on recovery) and the page resolves asks through `rootSessionOf` — subagent asks surface in the parent conversation with an "Asked by <subagent>" label; (2) SDK permission mapping fixed for the V2 field names (`permission`/`patterns`, was reading nonexistent `action`/`resources` → cards would have shown "action" with no paths); (3) a sync shell/command POST that rejects AFTER SSE events arrived for the session is treated as "connection died, turn alive" — lock kept, no false failure line; session.idle ends the turn (SSE-sequence mark, no clock); (4) one reply answers all identical pending asks (same session+action+resources — the explore agent fired three at once); (5) working indicator says "Paused — the agent needs your answer below" while an ask is pending. VERIFIED: real SDK against the live stuck server session — all 3 pending asks now map to action `external_directory` with real paths and resolve to the parent session. 168 JS tests (4 new: child→parent mapping, dropped-POST-alive, genuine-failure line, batched reply), tsc, eslint, vite build green.

2026-07-04 10:45 · Composer command modes shipped (user: wanted a Claude-Code-style "!" command-line mode and "/" slash commands for skills/MCP/shortcuts). Mechanism — OpenCode-native, no fakery: "!" flips the input into shell mode (amber border, terminal chip, mono font) and Enter runs the line via `POST /session/:id/shell` — no model turn, executes directly in the session's workspace folder, recorded in session history as a bash tool part; "/" opens a command palette above the input fed by `GET /command` (OpenCode merges config commands + skills + MCP prompts into that one list) with filter-as-you-type, ↑/↓ + Tab/Enter autocomplete, Esc close, and `POST /session/:id/command` on send — an unknown "/name" stays a plain prompt (paths like /etc/hosts). Send lifecycle unified into one `performTurn` (prompt/shell/command). TWO RACES FOUND + FIXED during verification: (1) shell/command POSTs are SYNCHRONOUS (resolve after the turn; session.idle arrives first), so the running lock is set before the POST — set after, it would never clear; (2) new `shellTurns` flag (shows the bash OUTPUT inline for user-run commands only — agent bash steps stay quiet lines) must be cleared by the session.idle EVENT, not the POST settling, because SSE frames race the POST response on separate connections. VERIFIED live (Playwright against the real 1.17.13 sidecar + vite): palette filters "/ini"→/init, autocompletes; "!pwd && echo E2E-SHELL-OK" echoes, runs in the workspace, output shows inline in the thread, session lazily created, turn ends with no model configured. 164 JS tests (13 new: SDK endpoints, store lifecycle/races, palette + shell mode UI, inline output), tsc, lint, vite build green.

2026-07-04 10:15 · Proper send lifecycle, new → input → send → response (user: first message gave no feedback then the page suddenly switched; a second send was possible before any reply; errors — occasional "Load failed" — were invisible). Mechanism: (1) the message echoes into the thread INSTANTLY — a draft echoes under a local DRAFT_KEY thread whose blocks are grafted onto the real session id on create, so the page never visibly resets; (2) `sending` (click → POST accepted) plus `runningSessions` (until session.idle/error) lock the composer ("Waiting for the reply…") and show a bottom "Starting the session in its folder… / Working…" spinner row; (3) session-scoped errors fold into the conversation as red status lines (new "error" tone) and end the turn — no more silent failures; (4) the first POSTs after a sidecar restart retry once (kills most transient "Load failed"). 148 JS tests (7 new: instant echo/graft, second-send lock, idle/error turn end, create retry, hard-failure line), tsc, lint green.

2026-07-04 10:00 · Top-right status no longer churns on history/folder switches (user: badge flipped ready→connecting→ready and something flashed at the top-right; it should not change unless the runtime is really disconnected). The flash was the ConnBadge state flip plus the blue Connect button appearing while !connected. New store flag `switching`, set around the three deliberate sidecar-restart paths (cross-folder openSession, workspace pick, dated-folder first message); while set, the page treats the runtime as connected — badge stays "ready", no Connect button, no help card, composer stays enabled. Real failures still surface once the retry window is exhausted (switching clears in finally). 142 JS tests (1 new: switching lifecycle), tsc, lint green.

2026-07-04 09:55 · No more "New session"+workflow-starters flash while a history session loads (user: switching between history sessions showed the draft empty state before the real content). Cause: the empty state keyed off "thread has no blocks", which is also true for a session whose history hasn't loaded yet (seconds, on cross-folder opens). Fix: a URL with a session id is never a draft — the title stays blank instead of "New session" and WorkflowStarters render only on the real draft route (/live with no id). 141 JS tests, tsc, lint green.

2026-07-04 09:50 · Removed the "Connecting to the runtime…" in-content line (user: it appearing/disappearing made the page height shake). While connecting, nothing renders in the content flow — the header status badge is the feedback (its dot now pulses during connecting). The help card remains for real error/offline states. 141 JS tests, tsc, lint green.

2026-07-04 09:45 · No more error-card flash on folder switches (user: opening a history session in another folder — or starting a dated-default session — briefly showed "OpenCode runtime … Could not open OpenCode event stream" before the content). Cause: every cross-folder move restarts the sidecar on purpose, and (a) each failed reconnect attempt wrote its error into the store, (b) the big runtime help card rendered whenever status ≠ ready, including the deliberate "connecting" window. Fix: `connectRetry` now masks status AND error during the retry loop (the last error surfaces only if the whole window is exhausted), and LiveSessionPage shows a quiet "Connecting to the runtime…" one-liner while connecting, keeping the help card for real error/offline states. 141 JS tests (2 new: masking + exhausted-window), tsc, lint green.

2026-07-04 09:35 · History is now global across all workspace folders (user: switching folders changed the History list). Root cause: `GET /session` is scoped to the project the sidecar's cwd resolves to — non-git folders share one "global" project but a git folder is its own project, so picking one swapped the visible history. Fix in the SDK only: `listSessions` now uses `GET /experimental/session` (lists every project's sessions, newest first, each with its `directory` — verified against a live sidecar: 23 sessions across 2 projects) with a fallback to `/session` if the route is missing. Cross-project `DELETE /session/{id}` verified to resolve globally (200), and openSession already follows a session into its own folder, so no other changes. 139 JS tests, tsc, lint green.

2026-07-04 09:25 · Simplified the draft folder chip per user feedback ("a choose-folder icon is enough; dated is the default; keep-current not needed"): the dropdown menu is gone — a fresh draft's header now shows just a folder icon (tooltip names the dated folder the session will get); clicking it opens the native picker directly, and only after an actual pick does the chip show the chosen folder's name. Removed the `setWorkspacePinned` store action (pin = explicit pick via `switchWorkspace`; unpin = "New"/startDraft). 139 JS tests, tsc, lint, vite build green.

2026-07-04 09:20 · Moved the workspace-folder control out of the sidebar (user: the top-left switcher chip felt unnatural). The folder choice now lives where it matters — the session header: while a fresh draft is open, a small "New dated folder ▾" chip next to the "New session" title offers New dated folder (default, ✓-marked) / Keep current folder (…) / Choose folder…; once the session exists the chip becomes a quiet read-only indicator of the session's folder (tooltip = full path). New `WorkspaceChip` component (thread/), sidebar `WorkspaceSwitcher` deleted, store action generalized to `setWorkspacePinned(bool)`. 138 JS tests (3 new WorkspaceChip component tests), tsc, lint, vite build green.

2026-07-04 09:00 · New sessions now default to their own fresh dated folder (user: `pwd` in a new session answered the bare `~/Documents/OpenScience`; wanted `<base>/<date-time>` as default, or an explicit choice). A fresh draft's first message now runs `new_dated_workspace(<YYYY-MM-DD-HHMM>)` + kernel reset + reconnect BEFORE lazy session creation, so the session, kernel, Files and provenance all start inside the dated folder. Explicit choice = "pinning": picking a folder in the sidebar switcher ("New dated folder" / "Open folder…") pins the destination for the next session; a new "Keep current folder (…)" menu item pins without switching; "New" (startDraft) un-pins back to the dated default. While a draft is unpinned the switcher chip shows "New dated folder" (FolderPlus icon) so the destination is visible before sending. Frontend-only change (`runtime.ts` store + `Sidebar.tsx`; `datedWorkspaceName` moved to the store module); Rust untouched. 135 JS tests (5 new: dated-name format + store pin/unpin/lazy-create behavior via mocked sidecar), tsc, lint, vite build green.

2026-07-04 08:30 · Switchable workspace folders (user: sessions all dumped in one dir, no dating, no user choice). Sidebar now has a workspace switcher (folder chip + menu): "New dated folder" creates `~/Documents/OpenScience/<YYYY-MM-DD-HHMM>` and switches to it; "Open folder…" picks any existing folder. The choice persists (app-data `active-workspace.txt`) and `workspace_dir` returns it, so the kernel / Jupyter / Files explorer / provenance / previews all operate in the active folder; the agent's OpenCode sessions land there too. New Rust commands: `workspace_base`, `set_workspace`, `new_dated_workspace`, `pick_folder`; `workspace_dir` = active override else base (`base_workspace_dir` keeps the migrations). Store `switchWorkspace` + `openSession` (follows a session into its own folder). KEY MECHANISM FINDING (via API probing + a wrong first cut): OpenCode's `directory` is a per-request query param and its `/event` SSE stream is directory-scoped — passing `?directory=` on session/prompt routes the turn's events to a scope the app's global `/event` never sees, so turns silently produced NOTHING (empty assistant message). So scoping is done by the SIDECAR CWD (restart on switch, the same proven path as import-login), NOT a query param; reverted the `?directory=` additions on createSession/sendPrompt/event. VERIFIED (model-independent, since the free Mimo model is currently returning empty completions — confirmed via an empty assistant message, unrelated to this change): switched to a dated folder → chip updates, folder created on disk, sidecar restarts + reconnects, a Python notebook cell `os.getcwd()` returned exactly `/Users/asq/Documents/OpenScience/2026-07-04-0827` — proving kernel/Files/provenance follow the active folder. 130 JS + 29 Rust tests, tsc, lint, vite build green; installed to /Applications.

26-07-04 06:16 · Fixed "No module named matplotlib" in local notebook cells (user report). Root cause (systematic-debugging): the local kernel (kernel.rs) spawned bare `python3` with NO PATH set, so a Finder-launched app (minimal PATH) resolved it to a system Python (CommandLineTools / /usr/bin, no matplotlib) — while the agent/sidecar runs `python3` under the enriched PATH → the user's anaconda (WITH matplotlib). Kernel and agent used different Pythons. Not a regression from this session (kernel never set PATH); surfaced when running a matplotlib cell from Finder. Fix: the kernel now spawns with the SAME `crate::runtime::enriched_path()` the sidecar uses (made pub(crate)), and interpreter detection probes under that same PATH so detection and execution agree — so `python3` resolves to the agent's scientific Python. VERIFIED on the packaged app (launched from Finder): new Python notebook cell `import matplotlib, numpy, scipy; print(matplotlib.__version__)` → `matplotlib 3.10.1` (was ModuleNotFoundError). 130 JS + 29 Rust tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 05:15 · Usability pass (user asked to review comfort/naturalness of the UI) — verified on the packaged app. (1) Conversation was noisy: every mechanical step rendered as a heavy bordered card with raw absolute paths, reading like a shell trace. Fixed: ToolCallRow now renders successful/pending/running steps as quiet single-line log entries (muted monospace, no card); only waiting-approval/warning/failed keep a prominent card. `tidyToolTitle` shows workspace files by relative path (demo_analysis/analyze.py). `todo*` tool rows (opaque "N todos") are now dropped from the thread. Verified: the dose-response demo thread now reads as prose + artifacts, not command cards. (2) Sidebar had "Customize" (top) and "Settings" (bottom gear) both opening /settings — removed the redundant "Customize"; Settings is the single bottom entry. (3) DECISION (user): reverted the OS-keychain credential storage from earlier today back to the app-private mode-600 file — on unsigned/self-built copies a signature change makes macOS prompt for the login-keychain password every launch, a worse experience than the mode-600 file for a marginal gain; P2-3's no-leak Acceptance was already met. Removed keychain.rs + the `keyring` dep; KEPT the independent P2-4 exit-cleanup fix (Cmd+Q → RunEvent::Exit reaps the sidecar/kernel/Jupyter, which previously orphaned). Verified: reverted build launches with no keychain prompt, connects on free Zen, sidebar clean. 130 JS + 29 Rust tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 03:30 · Credentials at rest in the OS keychain (P2-3's open gap) + fixed a real orphaned-sidecar exit bug (P2-4). Provider credentials no longer sit in a plaintext file between runs: new `keychain.rs` (via the `keyring` crate — macOS Keychain / Windows Credential Manager) hydrates OpenCode's `auth.json` from the keychain before the sidecar starts and, on exit, writes it back and deletes the on-disk file. Never-lose invariant: the plaintext file is removed ONLY after a successful keychain write; any failure (or a keychain-less platform) keeps the file exactly as before — no regression (unit-tested via a SecretStore trait with an in-memory store: round-trip, save-failure-keeps-file, hydrate-never-clobbers, skip-empty). WHILE VERIFYING, found + fixed a genuine pre-existing bug: on macOS Cmd+Q/Quit the app terminates via `RunEvent::Exit`, NOT `ExitRequested`, so the exit cleanup NEVER ran — the OpenCode sidecar (and kernel/Jupyter) orphaned on every quit (confirmed: main app gone, sidecar PID still alive; 8 stale sidecars had accumulated), and my keychain persist wouldn't fire either. Cleanup now runs on BOTH events (idempotent). VERIFIED end to end on the packaged app: launch → Cmd+Q → sidecar reaped ✓ and auth.json moved into the keychain + removed from disk ✓ → relaunch → auth.json restored byte-identical (sha match) ✓ → OpenCode ready with the configured model (Mimo-V2.5-Free) ✓. (macOS keychain ACL trusts the app, so the item is readable by the app without a prompt but NOT by the `security` CLI — expected; verified via the app's own restore path.) Data-flow card copy updated (keychain, not "app-private file"). 127 JS + 33 Rust (4 new keychain, +1 ignored real-keychain) tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 02:55 · Workspace Files explorer shipped (P2-1's multi-file/"IDE for larger projects" gap). New sidebar "Files" page (`FilesPage`) browses the whole workspace tree — a breadcrumb for folder navigation, type-aware icons (folder/notebook/image/table/molecule/genome/text) + human sizes — and opens ANY file in the existing native viewers (reuses FilePreviewInspector + NotebookEditor, so figures/tables/PDF/molecule/genome/notebooks all work), not just files the agent mentioned. New Rust `list_dir(rel)` — non-recursive, workspace-sandboxed via resolve_under, skips dotfiles/node_modules/__pycache__, dirs-first sort (extracted to a testable `dir_entries` helper). VERIFIED end to end on the packaged app: Files → workspace listing with correct icons/sizes → clicked demo.pdb → opened in the 3D protein cartoon viewer (327 atoms) → navigated into canvas-project (breadcrumb "Workspace › canvas-project", subfolder contents listed). CAUGHT A REAL BUG only visible in the running app: DirEntry serialized `is_dir` (snake_case) while the frontend reads `isDir`, so folders rendered as files and clicking one hit "read failed: Is a directory" — fixed with `#[serde(rename_all = "camelCase")]` (the Rust unit test used the Rust field name so it passed regardless; the serde-boundary mismatch surfaced only in real-app verification). 127 JS (3 new) + 29 Rust (2 new) tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 02:45 · Native genome-track viewer shipped (P1-3's "then … genome tracks" — now BOTH a structure renderer and a track renderer ship, beyond the "at least one domain renderer" acceptance). New `lib/genome.ts`: pure, offline parser for BED / bedGraph / GFF3 / GTF / VCF → a uniform feature model, all coords normalized to 1-based inclusive (BED 0-based half-open, VCF POS+REF span, GFF/GTF as-is), attributes → name (GFF Name/gene_name/ID; GTF gene_name/gene_id), grouped by contig (busiest first), 50k-feature cap; plus greedy `packRows` so overlapping features stay visible. New `GenomeView.tsx`: an SVG track viewer — bp ruler with 1/2/5×10ⁿ "nice" ticks, features as row-packed rects colored by type (GFF/GTF) or strand via theme-aware `--series-*` tokens, contig `<select>`, hover tooltip (name/coords/strand/type/score), drag-to-pan + scroll-to-zoom (cursor-anchored, clamped to the contig), zoom/reset buttons, legend, MAX_ROWS/truncation notes. Wired the previewer registry: artifacts.ts previewKind "genome" for bed/bedgraph/bdg/gff/gff3/gtf/vcf (+REF_EXTS/MIME/EXT_KIND), FilePreviewInspector routes it with a Preview/Code toggle, Rust `mime_for` serves them as utf8 text so on-disk files reach the viewer. VERIFIED end to end on the packaged app: opened a real GFF3 (gene/mRNA/exon/CDS across chr1+chr2) → tracks rendered with the type legend, gene/mRNA on one row and exons/CDS packed below, contig selector "chr1 (10)", range readout; clicking zoom-in narrowed the view 720–15,280 → 5,379–10,621 and the ruler re-ticked (6k–9k) — interactive zoom confirmed. Debris removed. 124 JS (12 new: 8 parser + 4 view) + 27 Rust tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 02:35 · Windows robustness fix toward P1-4 (Windows parity). Audited the cross-platform paths since Windows is the one requirement whose acceptance can't be met/verified on this macOS host (needs a Windows runner + code signing — CI already builds NSIS/.msi via the build.yml matrix, and both sidecar fetch scripts correctly emit `*-x86_64-pc-windows-msvc.exe`). Found and fixed a real Windows-only defect: `start_jupyter`'s orphaned-jupyter-lab cleanup (a crash/force-quit leaves one holding the fixed port; a second instance then wedges — the exact bug the Unix path guards against) was `#[cfg(unix)]`-only, so Windows had NO cleanup. Extracted it into `kill_orphan_jupyter`: Unix keeps the proven, path-scoped `pkill -f <env>/bin/jupyter-lab` (zero behavior change → no macOS regression); Windows now `taskkill`s the recorded PID filtered to `IMAGENAME eq python.exe` (so a recycled PID on another process is spared). The managed jupyter-lab's PID is now written to `jupyter-env/jupyter.pid` on spawn for that precise, portable kill. VERIFIED: macOS unix path is a pure refactor (27 Rust tests green, no warnings); the Windows branch typechecks against `x86_64-pc-windows-msvc` (rustc --emit=metadata on the extracted std-only logic — llvm-rc for a full cross-build isn't on this host, but CI compiles the whole app on windows-latest). Remaining P1-4 gap is inherent to the host: producing + signing the installer and a real first-run on Windows can only happen on a Windows machine/CI.

2026-07-04 02:25 · Package-level environment capture shipped (closes P0-3's last gap — was pip-freeze/lockfile). Every provenance record now captures the installed Python packages, not just python/OS/app version. Design keeps the store small: `pip freeze` runs ONCE per app run (cached in a OnceLock, same as python_version — a per-write spawn would tax every agent edit), its output is written content-addressed to `.openscience/env/<hash>.txt` (DefaultHasher's fixed keys → deterministic addressing; identical environments dedupe to one lockfile, only written if absent), and the record's env carries just `{count, hash}`. New Rust `read_env_lockfile(hash)` (hash validated hex, can't escape the env dir) + `write_lockfile`/`content_hash`; capture_env now takes the workspace root. Frontend: ProvenanceEnv gains `packages`; ProvenancePanel shows an "N packages" chip that lazy-loads and reveals the full pip-freeze list in a scroll box; the Reproduce prompt now points the agent at the lockfile to reinstall matching versions if a result differs. VERIFIED end to end on the packaged app: a real agent turn wrote demo_pkg.py → its record carried `packages {count 664, hash b19b27e709614985}`, the 34 KB lockfile appeared at `.openscience/env/b19b27e709614985.txt` (real freeze: absl-py==2.1.0 … 664 pkgs), and the History panel rendered the "664 packages" chip → click revealed the scrollable list. Debris removed (file, provenance line, lockfile). 112 JS + 27 Rust tests, tsc, lint, vite build green; installed to /Applications.

2026-07-04 02:15 · Local R kernel shipped (closes P0-2's last gap — R was the only not-started P0 item). The notebook now runs Python OR R on a persistent local kernel, keyed by the notebook's kernelspec language. New base-R-only bridge `runtime/kernel/kernel_bridge.R` (no IRkernel/jsonlite/ZMQ — runs against any installed R, offline): holds one global env across cells, mirrors Jupyter semantics (final visible expression → result, intermediate visible values + cat/message → stdout, warnings inline via options(warn=1)), and emits the SAME JSON response shape as the Python bridge so Rust's read side is shared. kernel.rs generalized to a HashMap of kernels-by-language; request side branches (Python: inline JSON; R: host writes the cell to a code file, then pokes the id — zero escaping on the request). Rscript discovered via absolute-path candidates (incl. /opt/homebrew + /Library/Frameworks/R.framework) so the Finder-minimal-PATH GUI app finds it. Frontend: notebook-file.ts reads/writes the kernelspec (python↔R, code cells stay cell_type "code"); NotebookEditor shows a kernel badge, runs R cells, dispatches per-language; Notebooks page "New notebook" is now a Python/R menu. VERIFIED end to end on the packaged app (installed R 4.6.1 via brew to test): created an R notebook → cell ran `cat`+`summary`+`sd` → correct stdout ("mean is 4"), summary table, and last value `[1] 2`; a second cell used `x` from the first (`y <- x*10` → `[1] 20 40 60`, `sum(y)` → `[1] 120`), proving cross-cell shared state; a syntax error rendered R's `Error: <text>:1:5: unexpected symbol` with the caret. Rust `r_round_trips_and_keeps_state` drives real Rscript over the exact protocol. 111 JS + 26 Rust tests, tsc, lint, vite build green; installed to /Applications. (IME note: pinyin input mangled typed `<-`/quotes during UI automation — verified via clipboard paste instead.)

2026-07-04 00:55 · Molecule preview is now INTERACTIVE 3D, not a static 2D image (P1-3 redesign, per user + borrowing OpenClaudeScience's 3Dmol.js approach). Replaced the openchemlib→static-SVG renderer with a 3Dmol.js WebGL viewer: drag to rotate, wheel to zoom, stick/sphere/cartoon toggle, reset, live atom count. Expanded formats from 4 to 12 — cif/mcif/mmcif/pdb/mol/mol2/sdf/xyz/pqr/cube all render directly; macromolecules (proteins/crystals, detected via HELIX/SHEET or many Cα) open in cartoon, small molecules in stick. SMILES (.smi/.smiles) has no coordinates, so it is converted to a coordinate-bearing molblock via openchemlib and handed to the same viewer — one code path, no static fallback. molecule.ts is now pure helpers (format map, macromolecule heuristic, default style, smilesToMolblock), unit-tested; the WebGL component is tested with 3Dmol mocked (asserts model+format handed over, style re-applied on toggle). Rust mime_for serves all 12 as utf8 text so on-disk files reach the viewer. 3Dmol + openchemlib both lazy-loaded into their own chunks (out of the main bundle). NOT screenshot-verified on the packaged app (headless WebGL); covered by build + mocked-viewer tests + a real openchemlib coordinate-generation test. 108 JS + 24 Rust tests, tsc, lint, vite build green.

2026-07-04 00:15 · Pre-commit review pass (27-agent high-effort review of the pending work): 10 confirmed defects fixed before the commit. Worst three would have shipped broken features — SDF records with a BLANK title line parsed to 0 atoms (trim() shifted the fixed-position molfile header; delimiter regex now consumes the $$$$ newline instead), on-disk .mol/.sdf/.smi files never rendered (mime_for served them as base64 → viewer text stayed null; now text types), and Reproduce silently overwrote text the user was typing (draft now appends). Also: reproduce prompts fence-escape embedded ``` and flag truncated records; Slurm array/het job ids ('123_[0-15]', '123+0') can be canceled (charset + remote single-quoting); `squeue --me` → `squeue -u "$USER"` (works on Slurm < 20.02); ssh host-key policy accept-new → strict `yes` with a run-`ssh <host>`-once message (CLAUDE.md safety default: no silent first-contact trust); ssh-config aliases the connect path would reject are no longer suggested (and '+' allowed in hosts); corrupt hpc.json now surfaces a toast instead of a silent empty card + unhandled rejection; hpc-slurm skill's scp recipe used a $REMOTE that never survives across shell calls — replaced with the literal remote dir. 103 JS + 24 Rust tests, tsc, lint green.

2026-07-03 23:05 · Non-bio example project shipped (P1-1's last gap + P0-1's example gap). `examples/climate-trends/`: the REAL NASA GISTEMP v4 land-ocean global means CSV (12.9 KB, public domain, retrieved 2026-07-03, citation in its README) bundled into the installer as a resource. New Rust `install_example` (allowlisted names; recursive copy that NEVER overwrites — a re-install keeps user edits, unit-tested) + a 4th workflow starter "Explore an example: climate trends" whose `prepare` hook installs the files before sending the analysis prompt (install failure → toast, prompt NOT sent). VERIFIED on the packaged app: one click → README+CSV appear in the workspace → the agent (free mimo) loaded the real data (skiprows=1, `***`→NaN), ran OLS via scipy, and produced warming_trend.png (190 KB) + report.md with defensible numbers (+0.08 °C/decade full record, +0.21 °C/decade 1975–present, R²=0.90, full decadal table, dataset citation) — a genuinely non-bio, real-data showcase. Testing gotcha: vitest/tinyspy derives an extra promise from a REJECTING vi.fn (its result tracking), reported as an unhandled rejection — mock rejection paths with plain closures instead. E2E debris removed. 90 JS + 23 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 22:55 · Provenance env capture + Reproduce shipped (closes P0-3's two gaps). Every new provenance record now carries `env` {python version (detected interpreter, cached once per app run), os-arch, app version}; History panel shows it as a chip next to the model. New per-version **Reproduce** action: drafts a structured prompt (recorded code + env note + "compare regenerated vs current") into the composer of the originating session — prefilled, never auto-sent, human in the loop. Plumbing: `composerDraft` one-shot field in the ui store, consumed by the Composer on render. VERIFIED on the packaged app end to end: real agent turn wrote demo_env/mean_demo.py → record carried env {python 3.11.7, macos-aarch64, app 0.1.0}; History showed `py 3.11.7 · macos-aarch64 · app 0.1.0`; clicked Reproduce → composer prefilled → sent → agent re-ran the recorded code, diffed, replied "files match exactly — nothing changed". Old records without env render unchanged (field optional). UI automation note: a concurrent agent on this machine steals focus — clicks now retry until the app process is verifiably frontmost. Debris removed (demo session, files, records). 88 JS + 22 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 22:45 · HPC / Slurm over SSH shipped (P2-2, was the only not-started requirement). Shape: the app drives the user's cluster with the system ssh + their own keys — nothing installed remotely, no credentials of ours. Rust `hpc.rs`: parse ~/.ssh/config Host aliases (wildcards/comments skipped), probe host (`sbatch --version`, distinguishes unreachable vs no-Slurm), list (`squeue --me`, name last so pipes in job names survive) and cancel jobs; host/job-id validated against option/shell injection (unit-tested). Chosen host lives in `<workspace>/.openscience/hpc.json` — the single source shared with the agent. Settings "Cluster (HPC)" card: connect (datalist of real ssh-config hosts + free text; unreachable hosts are NOT saved), status line (green/amber dot + slurm version), live queue with per-job cancel, Remove. New bundled skill `hpc-slurm`: read hpc.json (never guess the host), write sbatch script INTO the workspace (provenance picks it up), scp+sbatch, track via squeue→sacct (quote the state, don't assume success), fetch outputs back. VERIFIED end to end on the packaged app against a localhost "cluster" (real sshd + fake sbatch/squeue/scancel/sacct shims): card connect→"slurm 23.11.4"→queue row→cancel (toast + state file emptied over real ssh); then a real agent turn followed the skill mechanically — wrote slurm/os-e2e.sbatch (provenance v1), submitted (job 7744), read the empty output, self-corrected the script (v2), resubmitted (3258), sacct COMPLETED, fetched slurm-3258.out (`sum=100`) into the workspace and reported the quoted state. All test scaffolding removed after (shims, .zshenv PATH line, authorized_keys entry, workspace/session debris). 87 JS + 22 Rust tests, tsc, lint green; installed to /Applications.

2026-07-04 03:00 · Fixed a session-deadlock bug the user hit (P2-4): the agent's `question` tool (pick-an-option) and OpenCode permission prompts rendered as blank/stuck rows with NO way to respond — the run just hung, unusable. Wired OpenCode's interactive-request system end to end. SDK: normalize `question.asked`/`question.v2.asked` (+ replied/rejected) and `permission.asked`/`.v2.` into typed events; new methods answerQuestion/rejectQuestion/replyPermission/listQuestions/listPermissions. IMPORTANT contract discoveries (verified against the live server, not assumed): these are DIRECTORY-scoped GLOBAL endpoints `GET/POST /question[/:id/reply|reject]` and `/permission[...]`, NOT the `/api/session/:id/...` variants (those returned empty); reply body is `{answers:[[label,…],…]}` / `{reply:"once"|"always"|"reject"}`; and the `workspace` query param is a `wrk_` id — passing a path 500s the server, so send only `?directory=<path>`. Store holds pending questions/permissions, updates from live events, and recovers them on session open (an ask can predate connect/reload). New InteractionPrompt card renders above the composer: single-select answers on click, multi-select/custom behind Submit, permission Allow-once/Always/Reject; blank interactive tool rows suppressed from the thread. VERIFIED on the packaged app's real runtime: triggered the question tool → answered via the corrected endpoint → question went running→completed and the agent PROCEEDED with the chosen file (read atlas.csv, began analysis). Live card render not screenshot-verified — a second autonomous agent on this machine kept moving/closing the app window across displays — but the UI is covered by component tests (quick-pick, multi-select, reject, permission) and the wiring is tsc-clean. 83 JS + 17 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 13:10 · Chart design system + command palette + empty-session redesign (P1-5). One validated palette (dataviz standard, checked against the app's real light #ffffff / dark #1e1d24 surfaces) is the single source of truth in three synced places: `@ai4s/shared` chartPalette (categorical/sequential/status, light+dark), `index.css --series-*` tokens, and `runtime/skills/core/publication-figures/openscience.mplstyle` — a first-party skill that makes every agent matplotlib figure publication-grade (2px lines, hairline y-grid, no top/right spines, left title, frameless legend) in the SAME 8 categorical hues as native UI. VERIFIED: rendered a 4-series figure with the style → exact palette (#2a78d6/#1baf7a/#eda100/#008300), clean chrome. Command palette rewritten so all 7 items are real actions (was 3 dead ones that just closed): new session, analyze-data + audit-report workflows (send the starter prompts), notebooks, skills, settings, theme — verified all visible in the packaged app. Course-correction on user feedback: my first cut put a Sessions/Notebooks/Skills stat-tile row on the empty session — vanity metrics that dominated the view; removed it (and the speculative StatTile/WorkspaceGlance) and redesigned the empty session as a quiet, centered welcome (serif headline, one refined 3-row starter card) — screenshot-confirmed. Dark mode is token-only (no hardcoded color), structurally assured. NOTE: the app got moved to a second display mid-verification; combined with a second autonomous agent on this machine, UI automation is unreliable here — a blind keystroke once landed in another app's browser form (no submit; harmless but noted) — so I verify via window-scoped capture when possible and structure otherwise. 78 JS + 17 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 12:15 · Curated open-source science connectors, one-click (P1-2). Rather than reimplement literature/DB access, we one-click provision EXISTING open-source MCP servers into a shared isolated env via the bundled uv, then register them — same shape as the Jupyter integration. New Rust `science_mcp` (setup_science_mcp installs one pip package into runtime/science-mcp-env and returns the managed python; science_mcp_python reports it; package name validated against arg/shell injection, unit-tested). Curated catalog `lib/scienceConnectors.ts`: paper-search-mcp (arXiv/PubMed/Crossref/Semantic Scholar/bioRxiv) and biomcp (PubMed/ClinicalTrials/variants), launched as `python -m <module>`. Settings MCP card shows them as "OPEN SOURCE" rows with source repo + Enable. New `docs/CONNECT_YOUR_TOOLS.md` (BYO MCP local/remote + minimal fastmcp example + skill install + safety). VERIFIED on the packaged app's real runtime: provisioned paper-search-mcp through the bundled uv into the app env → registered via the live config API → OpenCode reports it `connected` → drove a real agent turn: `paper-search_search_arxiv` returned 3 papers with real arXiv ids (incl. 2207.07410 AlphaFold-knots), exactly the auditable identifiers traceability-review consumes. (UI Enable button is the same enable path as Jupyter, tsc-checked; verified the underlying provision+register+connect chain directly because a second autonomous agent sharing this machine made window screenshots unreliable.) Verification debris removed (config entry, env, test sessions). 77 JS + 17 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 11:55 · First-boot reliability fix (P2-4): connectRetry window raised 30×0.5s → 120×1s. Root cause observed twice today: on a fresh install (or any re-signed rebuild) macOS TCC's "access Documents" consent blocks the sidecar inside getcwd until the user answers, so the webview's ~15–30 s retry budget expired and stranded users on an error screen a single manual Connect would fix. Verified on the packaged app: relaunch → TCC allowed → auto-connected in ~9 s with no manual Connect. 77 JS + 15 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 11:50 · One-click full workflows shipped (P0-1). The empty live session now shows three starter cards (components/thread/WorkflowStarters): "Demo: analysis end to end" (simulate → fit → figure → report, numbers traced to code), "Analyze my data", "Audit a report" (routes into traceability-review). One click sends the complete-workflow prompt. VERIFIED on the packaged app: single click → 65 s → run_analysis.py + figure1.png (real 4-param logistic fit with 95% CI band, R²=0.985) + report.md (numbers from code output) + summary_stats.json, all as clickable artifact chips with provenance records; figure renders natively in the right pane. Also re-observed the boot race (P2-4): webview's 30×connectRetry gives up while a TCC-delayed sidecar is still booting — one manual Connect fixes it; a longer/smarter retry is the natural next reliability fix. 77 JS + 15 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 11:40 · Plain-language data-flow disclosure shipped (closes P0-2's statement gap + P2-3). New "Privacy & data flow" card in Settings (components/settings/DataFlowCard): two columns — stays local (workspace files with real path, local kernel/Jupyter execution, session history + provenance, credentials in an app-private mode-600 file) vs. sent to the provider (messages + task-relevant file/command content, only during a turn, provider policy governs retention) + a skills/MCP third-party note; model chip is live (verified both "no model configured" and opencode/mimo-v2.5-free states on the packaged app). Copy promises scope, not guarantees — unit test asserts no "no errors / zero hallucination" phrasing. IMPORTANT correction while verifying: provider credentials are NOT in the OS keychain — OpenCode stores them in app-private auth.json (mode 600); REQUIREMENTS P2-3 status fixed, keychain migration recorded as open. 75 JS + 15 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 11:35 · Reviewer's three traceable checks shipped (P0-4). New first-party skill `runtime/skills/core/traceability-review` (bundled as the `skills-core/` resource; deploy_bundled_skills now syncs both packs and skips SKILL.md-less placeholder dirs): citation audit via Crossref/arXiv/PubMed public APIs, untraceable-number flagging, and figure↔code staleness read from `provenance.jsonl` — output is the existing ```review contract, now with a `check` field (citation|number|figure). UI: ReviewerCard findings carry a check-type tag and are dismissible one by one (session-local; "N dismissed" in the header, "All findings dismissed." when empty). VERIFIED end to end on the packaged app with a fixture report (fake DOI + unsourced 37.4% + figure older than its code): the free mimo model followed the skill mechanically (Loaded skill → curl Crossref 404 → stat mtimes) and the card rendered exactly the three acceptance findings with correct levels/tags; dismiss click → "2 findings · 1 dismissed". Observed: the model used the mtime fallback because it claimed the hidden `.openscience/provenance.jsonl` didn't exist — skill now tells it to cat the file directly. Note copy never claims "no errors". 73 JS + 15 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 11:20 · Artifact provenance shipped (P0-3, the #1 gap): every successful agent write now appends a version record to `<workspace>/.openscience/provenance.jsonl` (workspace-relative path, version, timestamp, tool, sessionId, model, written content capped at 100 KB, log) via new Rust `record_provenance`/`list_provenance` (append-only, mutex-serialized, corrupt lines skipped, path-escape rejected); recording happens on live tool.updated events (deduped per callId; jupyter reads excluded, mutations included) — note it is live-only: turns that stream while the webview is still connecting are not backfilled, and failures now go to debug.log instead of vanishing. UI: FilePreviewInspector AND NotebookEditor gained a History toggle → ProvenancePanel (newest first, latest expanded: code, model chip, log, "Open conversation" → /live/:sessionId); artifact cards are now fully row-clickable. VERIFIED end-to-end on the packaged app: three API-driven real agent turns produced v1/v2 (two writes, one turn) and v3 (edit from a second session, correct cross-session increment); History panel screenshot-confirmed on the final build. Found along the way: rebuilt (re-signed) apps re-trigger macOS TCC "Documents" consent, and the pending dialog wedges the sidecar in getcwd — looks exactly like the app hanging; also `open -a "Open Science"` can launch the repo-target bundle instead of /Applications (use the explicit path). 72 JS + 15 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 09:40 · Live notebook UX fixes from the user's first real Jupyter-MCP run (4 real bugs). (1) Tool rows went blank on completion: finished MCP parts report title "" and `?? tool` doesn't catch empty strings — now `title || tool`, unit-tested. (2) No live panel while the agent drives a notebook: jupyter_* tools now derive a notebook artifact from their notebook_path, and the session auto-opens that notebook in the right pane (once per path, manual close respected); NotebookEditor polls the file every 2 s while idle so the agent's cell inserts/executions appear live (own saves excluded via raw-text compare). (3) Figures were invisible: image/png outputs now parse into NotebookCell.image, render as <img>, and round-trip through serialize. (4) Duplicate jupyter-lab after restart: app quit left an orphan; the fixed port then wedged and all MCP calls timed out — start_jupyter now pkills the env's stale jupyter-lab first. Verified on the packaged app via an API-driven real agent turn + window-scoped screenshot (no focus stealing): tool names persist, right pane auto-opens, scatter figure renders. 65 JS + 12 Rust tests, tsc, lint green.

2026-07-03 08:50 · Jupyter MCP integrated, one-click, in the installer (user ask). Best-practice shape: bundle `uv` as a second sidecar (fetch-uv.sh pinned 0.11.26, +~20 MB dmg → 67 MB) instead of bundling Python; "Set up & enable" in the MCP card provisions an ISOLATED env in app data (uv venv + pinned jupyterlab 4.4.1 / jupyter-collaboration 4.0.2 / jupyter-mcp-server / ipykernel — ~361 MB, user's system untouched), the app manages a headless jupyter-lab (new jupyter.rs: stable port+token persisted in server.json so the MCP entry survives restarts; root_dir = workspace so agent and Notebooks page share files; killed on exit; auto-restarts on launch via ensureJupyter), and the MCP entry (JUPYTER_URL/TOKEN/ALLOW_IMG_OUTPUT) is written into OpenCode's config. VERIFIED end-to-end on the packaged app: clicked the real button → env provisioned → jupyter-lab up → GET /mcp reports jupyter "connected" — the agent now has notebook tools. Placement note: MCP in Settings matches Claude Desktop (Connectors), Cursor (Settings→MCP), Goose (Settings→Extensions). 62 JS + 12 Rust tests, tsc, lint green.

2026-07-03 08:20 · MCP server management (user ask: skills + MCP support). New "MCP servers" card in Settings: rows show name, local/remote type, LIVE status (connected/failed/disabled dot from GET /mcp) and the command/URL; add form (name, type, command-or-URL) writes via PATCH /global/config (applies live, verified); Remove goes through the generalized `remove_config_entry(section, key)` Rust command (replaces remove_custom_provider; allowlist provider|mcp; pure JSON transform tested) + sidecar restart — full add/remove loop E2E-verified on the packaged app (clicked Remove in the real UI, config file confirmed cleaned). SDK: listMcpServers (status ⋈ config), addMcpServer, McpConfig/McpServer types. Skills were already manageable (live list + install-with-agent on the Skills page); fixed its stale `.opencode/skill/` prose to `skills/`. 62 JS + 12 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 08:00 · Settings visual redesign (user: elements looked bad). Rebuilt the page as a consistent card system in the app's paper aesthetic: serif-titled cards with hairline-divided headers (Agent runtime / Model / Workspace / Appearance); one shared control kit (inputCls/btnGhost/btnAccent — every input and button h-9, 13px); providers as a single bordered list container (Zen gets an uppercase ring badge, connect-search row with inline icon on a tinted band, custom endpoint as a chevron disclosure row); dashed borders eliminated; select got a custom chevron; Appearance became a segmented control; runtime status line now shows the active model inline; import-CLI-login is an icon'd tertiary action. No logic changes. 62 JS + 12 Rust tests, tsc, lint green; installed + screenshot-verified.

2026-07-03 07:45 · Custom endpoints + provider-row semantics. (1) New "Custom endpoint" form in Settings (name, OpenAI-/Anthropic-compatible style, base URL, optional key, model ids) for self-hosted gateways and LOCAL Ollama (catalog only has ollama-cloud); writes provider config via PATCH /global/config (verified live: models selectable immediately, no restart); removal via new `remove_custom_provider` Rust command (edits opencode.jsonc + restarts sidecar; pure JSON transform unit-tested). Noted: Kimi coding plan (kimi-for-coding), DeepSeek, zhipu/minimax coding plans, LM Studio were ALREADY in the 150-provider catalog — searchable directly. (2) Fixed provider-row action semantics (user report): OpenCode Zen is built-in with no stored credentials — its dead "Disconnect" replaced with a "Built-in · free" label; other providers' action renamed Remove (credentials/config) to stop colliding with the runtime's Disconnect. 62 JS + 12 Rust tests, tsc, lint green; installed + screenshot-verified.

2026-07-03 07:25 · Settings provider fixes + workspace rename (3 user questions). (1) The connect list was wrongly sourced from GET /provider/auth (only 10 special-flow providers — Anthropic wasn't connectable); now a searchable box over the FULL catalog GET /provider (~150 providers, datalist autocomplete). Every provider gets a generic API-key row (PUT /auth works universally, placeholder shows its env var); special OAuth flows (incl. Copilot-style extra prompts — select/text inputs now supported and passed as authorize `inputs`) stack on top. (2) Clarified: the "MiMo V2.5 Free" default model was set by ME while verifying PATCH /global/config — not a prior user config; left in place as a working out-of-box default. (3) Workspace renamed ~/Documents/"Open Science" → ~/Documents/OpenScience (agent shell commands break on unquoted spaces); migration chain: OpenScience > rename "Open Science" > rename old app-data dir; verified live — files moved, old dir gone, sidecar cwd = new path. 62 JS + 11 Rust tests, tsc, lint green; installed to /Applications, search box verified by screenshot.

2026-07-03 07:05 · Removed the Review button from the artifact inspector (user: unnecessary). The ```review → ReviewerCard rendering stays — reviews surface when the agent produces them (skills / chat), not via a dedicated button; unused reviewPrompt deleted. 62 JS tests, tsc, lint green; rebuilt + installed.

2026-07-03 06:50 · Unified model configuration to ONE OpenCode-native path (user: configure OpenCode itself, not raw model keys). Settings rewritten around the sidecar's own API: Default model = grouped dropdown from GET /config/providers (includes OpenCode Zen free models — works out of the box), saved via PATCH /global/config (verified live: applies without restart); Providers = connected list with Disconnect (DELETE /auth) + connect flow per provider from GET /provider/auth (API key → PUT /auth/{id}; OAuth → authorize → system browser → paste code → callback); silent seed_auth copy REMOVED from sidecar spawn — now an explicit "Import OpenCode CLI login" button (new import_opencode_login command, restarts sidecar). Removed the old provider/key/baseURL form and two fake controls (workspace path input → real path + Reveal; Docker backend select). Sidebar Model pill is now real (shows the configured model name from /config). SDK: 8 new client methods + mock routes. Old configure_opencode Rust command kept but no longer wired to UI (candidate for an Advanced custom-endpoint flow or deletion). 62 JS + 11 Rust tests, tsc, lint green; installed to /Applications, verified by screenshot (model dropdown, Zen provider row, workspace path all live).

2026-07-03 06:35 · Conversation-first notebook UX (user's design: chat is the main surface, the agent drives notebooks, session ↔ notebook mapping visible). NotebookEditor extracted into a shared component (components/notebook/) with a new Reload button to pick up the agent's file edits. (1) .ipynb artifacts now open in the RUNNABLE notebook editor in the right pane next to the conversation (new "notebook-file" inspector variant) instead of raw-JSON preview. (2) Session header shows chips for every notebook the agent touched in that session (deduped artifact blocks) — click opens it beside the chat; active one highlighted. Notebooks page stays as the global library. Kernel-level agent control still pends the jupyter-server + jupyter-mcp route (PROGRESS 05:00 note). 62 JS + 11 Rust tests, tsc, lint green; installed to /Applications, editor re-verified (previous run's output loads back from file).

2026-07-03 05:50 · Closed two Claude-Science gaps: Notebooks entry + real Reviewer cards. (1) New sidebar "Notebooks" page: lists workspace .ipynb (new Rust `list_notebooks`), create/open/edit/delete cells, Shift/⌘+Enter runs on the real local Python kernel, debounced autosave writes valid nbformat back (new `write_workspace_file`, sandboxed) — the agent works on the same files. Verified END-TO-END by UI automation on the packaged app: clicked through sidebar → notebook → ran a cell → output "42" rendered AND persisted into the .ipynb. Two real bugs found+fixed by that E2E: .ipynb missed the text-MIME map (read came back base64 → "could not read"), and save-in-handler raced setState (persisted "running…" instead of the result) — now a debounced autosave effect. (2) Reviewer: "Review" button on the live file inspector sends a structured review prompt (uses integrity-auditor skill); a ```review fenced JSON block in the agent's reply is parsed (splitReview) into the existing ReviewerCard — streaming-safe, malformed JSON left as text. Not done (next): versioned artifact tabs with real per-version data (#1 of the gap table). 61 JS + 11 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 05:00 · Three fixes from user feedback. (1) History delete now confirms via a new in-app ConfirmDialog (window.confirm is unreliable in the webview); examples confirm as "Hide". (2) Workspace moved from the hidden app-data dir to the OS documents folder (`~/Documents/Open Science`, Windows/Linux via Tauri document_dir with $HOME/Documents fallback) — user-facing files belong in user-visible places; one-time migration renames the old dir (falls back to keeping the old location if rename fails), verified live: files moved, skills still listed, sessions intact. (3) Root-caused "no Python environment": Finder-launched apps get a minimal PATH so the agent couldn't see conda/Homebrew — sidecar now gets an enriched PATH (existing anaconda/miniconda/pyenv/homebrew/.local dirs prepended); verified by launching the packaged app with env -i and confirming /opt/anaconda3/bin in the sidecar's PATH. Bundling a managed Python/R runtime (micromamba) is a pending product decision. 56 JS + 11 Rust tests, tsc, lint green; installed to /Applications.

2026-07-03 04:30 · Composer attachment UX rework (3 user items). (1) Adding files is now silent on success — no toast. (2) Attached files render as removable chips (paperclip + name + X, surface-2 background) above the input instead of plain text in it — can't be mangled while editing; on send they become a "Files added to the workspace: …" note appended to the prompt, chips clear after. (3) Input is now an auto-growing textarea (caps at 160px then scrolls; Shift+Enter for newline), and a paste >2000 chars or >25 lines is auto-saved to the workspace as `pasted.txt` (new `add_text_to_workspace` Rust command, collision-safe) and becomes a chip instead of flooding the box. 56 JS (3 new mocked-bridge attach tests) + 11 Rust tests, tsc, lint green; rebuilt + installed to /Applications.

2026-07-03 03:50 · Composer fixes from user feedback (3 items). (1) Removed the divider line above the composer (live + example pages). (2) Fixed pinyin IME: Enter during composition was sending the message instead of picking the candidate — keydown now ignores `isComposing` / legacy keyCode 229 (WebKit), covered by a new Composer test. (3) Added a real "Add files" paperclip button (desktop only): native open dialog → files copied into the agent workspace (collision-safe naming `name-1.ext`, unit-tested) → names inserted into the prompt + success toast. New Rust command `add_files_to_workspace`. 53 JS + 11 Rust tests, tsc, lint green; rebuilt + installed to /Applications, verified by screenshot.

2026-07-03 03:40 · UI refinement pass from user feedback (5 items). Sidebar 272→232px; logo/brand shrunk to one compact 17px line with inline BETA; session header slimmed (18px→13px title, py-4→py-2.5) and its New/Disconnect buttons removed (New lives in the sidebar; users never need Disconnect — Connect still appears when offline); composer reduced to a single input+send row; Settings/Skills titles 2xl→xl. New: external http(s) links now open in the system browser (global click interceptor in AppShell + `open_url` Rust command) — previously a clicked link navigated the webview away with no way back. 51 JS + 10 Rust tests, tsc, lint green; rebuilt aarch64 .app/.dmg, installed to /Applications, verified by screenshot.

2026-07-03 03:20 · Fixed three UI polish bugs from user testing. (1) Downloads now use a native "Save As" dialog (new tauri-plugin-dialog + async `save_text_file` Rust command; browser dev falls back to Blob) with a new bottom-center toast for saved/failed feedback (silent on cancel) — wired into ArtifactInspector + FigureBlock; removed PdfInspector's dead Download button. (2) Removed the composer's three fake buttons (Plus/Tools-grid/Mic — all were no-ops). (3) Fixed the mismatched titlebar strip: macOS window now uses titleBarStyle Overlay + hiddenTitle, sidebar reserves a draggable 32px strip under the traffic lights — window background is now one continuous surface (verified by screenshot on the packaged app). Installed the new build to /Applications (old instance quit; session history intact). 51 JS + 10 Rust tests, tsc, lint, aarch64 .app/.dmg green.

2026-07-03 03:10 · Bundled the ai4s-skills pack (7 scientific skills: research-explorer/literature-survey/experiment-suite/paper-writer/integrity-auditor/mindmap-render/ai4s-agent) into the one-click installer. `scripts/dev/fetch-skills.sh` pins ai4s-research/ai4s-skills@8fa2ab0 into git-ignored `runtime/skills/external/` (wired into CI); tauri.conf bundles it as a resource; on every sidecar start `runtime.rs::deploy_bundled_skills` syncs it into the app-private profile's global skills dir (`xdg-config/opencode/skills/` — the workspace `.opencode/skills/` stays for user installs). Found+fixed along the way: OpenCode skill instances are lazy per-directory, so bare `GET /api/skill` can return [] — SDK now sends `?directory=<workspace>` (new `workspace_path` Tauri command) and loadCatalog retries once; fixed the install-skill prompt's wrong `.opencode/skill/` (singular) path. Verified end-to-end on the built .app: 7 skills deployed to the private profile and listed by the sidecar's /api/skill. 49 JS + 10 Rust tests (2 new for skill sync), tsc, lint, aarch64 .app/.dmg (46 MB) green. Known issue (pre-existing): sidecars orphan when the app is killed instead of quit — found 6 stale `opencode serve` processes from earlier runs (cleaned up).

2026-07-03 02:20 · Published to GitHub: https://github.com/ai4s-research/open-science (public, master). Two commits: accumulated feature work (previews/artifact resolution/kernel) + the Open Science rebrand. Secret scan on pending changes came back clean; sidecar binaries and installers stay git-ignored.

2026-07-03 02:10 · Rebranded to "Open Science" (slogan: "An open AI workbench for scientists — Your research partner for rigorous science"). README now opens with the banner (docs/assets/banner.webp) + new title/slogan; sidebar shows the cloud logo (src/assets/logo.webp) left of "Open Science"; productName/window title/index.html title renamed — bundles are now "Open Science.app" / "Open Science_0.1.0_aarch64.dmg". Deliberately unchanged: bundle identifier com.ai4s.workbench (renaming it would orphan the existing Application Support workspace/data) and internal @ai4s/* package names. AGENTS.md records the branding rule. 49 JS tests, tsc, packaged build green.

2026-07-03 01:55 · Set the app icon from the user's cloud/terminal artwork (webp, 1254px, transparent corners verified, content plate ~82% of canvas — matches macOS icon proportions). Converted to 1024px PNG and ran `tauri icon`: regenerated icon.icns (Dock), icon.ico (Windows), and all png/Android/Windows-store sizes in src-tauri/icons/; repacked aarch64 .app/.dmg with the new icns embedded. Note: Finder/Dock may show a cached old icon until the app is moved/relaunched.

2026-07-03 01:35 · Fixed messy Office preview styling (user: WPS looks far better). Root cause was mostly OUR CSS, not the renderers: Tailwind preflight resets (list-style, margins, img display/height) hit elements the renderers leave to browser defaults, and the app theme's inherited font/color (light text in dark mode) bled into document content expecting black-on-white. Fix: all three previews now render inside a Shadow DOM (document stylesheets can't cross it) with an explicit base reset — black text, CJK-aware font stack, neutral backdrop; docx-preview's own page chrome (white sheet on gray) now applies cleanly, pptx slides get centered cards with shadows. xlsx switched from our first-row-header grid to SheetJS `sheet_to_html` (merged cells → rowspan/colspan, gridline look like WPS; 500×50 cap preserved, escaping verified by test). Found+fixed along the way: useShadowPage used useRef+useEffect so a late-mounted host (XlsxView after data load) never got its shadow root — now a callback ref. Verified by mounting the real components with the user's actual docx/xlsx/pptx in jsdom (content lands inside shadow root, tables/merges present); 49 JS + 8 Rust tests, tsc, build green; repacked aarch64 .app/.dmg. Remaining gap vs WPS is inherent: font substitution (no embedded-font rendering) and renderer approximations.

2026-07-03 00:30 · Added inline docx/xlsx/pptx preview (was: "Open in the default app" only; user expects Codex-desktop-like inline viewing). All local, no conversion service, each renderer dynamic-imported into its own lazy chunk: docx → docx-preview 0.3.7 (HTML render); xlsx → SheetJS 0.20.3 from the official CDN tarball (npm's 0.18.5 is stale + ReDoS advisory) with sheet tabs + capped grid (500×50, first row as header, "charts not rendered" note); pptx → pptx-preview 1.0.7 (slide list, bundles echarts so pptx charts render). Chose SheetJS over exceljs after exceljs crashed on the real user file: workbooks containing charts hit "Cannot read properties of undefined (reading 'anchors')" in reconcile — SheetJS ignores drawings. New `previewKind` values docx/xlsx/pptx replace last night's "external"; FilePreviewInspector reads binary artifacts as base64 → ArrayBuffer; TablePreview extracted for csv/xlsx reuse. Verified against the user's actual canvas-project files in jsdom: docx renders DOM, pptx reports 5 slides, xlsx grids 3 sheets (chart file included); 48 JS + 8 Rust tests, tsc, vite build green; repacked aarch64 .app/.dmg. Caveat: jsdom needed same-realm bytes for jszip instanceof checks — webview unaffected. Pending: visual check in the packaged app.

2026-07-03 00:10 · Fixed file chips: only 2 of 5 generated files were clickable and index.html previewed as "not found" while canvas.pdf worked. Root causes: (a) `REF_EXTS` lacked docx/xlsx/pptx so Office files never became chips; (b) refs extracted from prose (bare filenames like `index.html`) were resolved verbatim against the workspace root — the file actually lives in `canvas-project/`; the pdf only worked because a stale copy sat at the root. Fixes: new Rust `resolve_artifact` (literal path if it exists, else bounded basename search — skips hidden/node_modules/__pycache__, ≤10k entries/depth 8, newest mtime wins on duplicates); AgentMessage now resolves every mention and only renders chips for files that exist, carrying the resolved path; docx/xlsx/pptx added to ref/MIME maps on both sides with a new "external" preview kind (note + "Open in the default app" button — no inline viewer for Office formats). Verified: repro test with the exact agent message extracts all 5 files; 47 JS + 8 Rust tests green (new: locate literal/bare/missing/duplicate); tsc + vite build clean. Pending: restart the app to pick up the new Rust command. · Fixed PDF preview not rendering in the packaged app (WKWebView): silent blank, no error, while "Open externally" worked. Two macOS WKWebView gaps: (a) pdf.js 6 calls `Promise.withResolvers()` (Safari 17.4+) — added a polyfill in main.tsx; (b) loading the worker as a `.mjs` module over `tauri://` via `workerSrc` failed silently — switched to Vite's `?worker` import + `GlobalWorkerOptions.workerPort`, so Vite bundles/instantiates it as a CLASSIC worker (`new Worker(url)`, no type:module) that WKWebView runs reliably. Re-verified render in Chrome (no regression); rebuilt aarch64 .app/.dmg. Pending user confirmation it now renders in the WKWebView build.

2026-07-02 23:20 · Made execution-produced files first-class previewable artifacts (fixes two test findings: HTML showed code but no render; a PDF produced by running python was un-clickable prose only). Root cause: `deriveArtifact` only caught write/edit tool calls, so files created as a side effect of `bash`/code runs never surfaced. Added: (1) `extractArtifactRefs()` — pulls workspace file paths out of agent prose (strips backticks/quotes, ignores URLs) so `canvas-project/canvas.pdf` becomes a clickable chip under the message; (2) Rust `read_artifact` (workspace-sandboxed: canonicalize + starts_with check, 25 MB cap, text→utf8 / binary→base64 via a std-only encoder) and `open_path` (OS opener, per-OS); (3) new `FilePreviewInspector` that renders by type — HTML → live `<iframe srcdoc sandbox>` with a Preview/Code toggle, PDF → pdf.js (`pdfjs-dist`, worker bundled as an asset; packaged app has csp:null so no restriction), image → `<img>` (svg/png/jpg/gif/webp), text → CodeViewer — loading bytes lazily from disk or using inline write-tool content. Also fixed the kernel to run in the agent's workspace (`current_dir`) so notebook code sees the same files. Verified in Chrome with a real 39 KB matplotlib month-night PDF: pdf.js rendered all layers; HTML srcdoc executed canvas JS live. JS typecheck/lint/40 tests (new: extractArtifactRefs/previewKind/refToArtifactBlock) + 5 Rust tests (new: base64 vectors) green; rebuilt aarch64 .app/.dmg (45 MB), confirmed pdf.worker asset + read_artifact/open_path embedded.

2026-07-02 22:15 · Made the notebook a REAL local Python kernel (was: dispatch-to-agent stub) — the honest local-first answer to "can't this run locally". Added `runtime/kernel/kernel_bridge.py`: a stdlib-only persistent process speaking line-delimited JSON ({id,code}→{ok,stdout,result,error}), one shared namespace across cells, Jupyter-style last-expression value, tracebacks on error. Rust `kernel.rs` embeds the bridge (`include_str!`), spawns it (cross-platform Python detection: `py`/`python` + `%USERPROFILE%` dirs on Windows, `python3` + `/opt/anaconda3` etc. on mac/Linux — GUI apps launched from Finder/Explorer have a minimal PATH), and does synchronous write-line/read-line over piped stdio; commands `kernel_execute`/`kernel_reset` registered + killed on exit. Frontend `lib/kernel.ts` + NotebookInspector run cells on the real kernel in the desktop app (async, "running…" → real output), falling back to the agent/hint in browser dev. Verified: bridge driven directly (2+2→4, state persists across cells `x*10`, 1/0→traceback); a Rust integration test drives the real bridge over the same protocol (4 Rust tests green); JS typecheck/lint/35 tests/build green. Note: remote-cluster timers, real PDF (pdf.js + local pdflatex/xelatex — both present), and figure-byte rendering (Tauri file read) are all equally local-capable and remain as follow-ups.

2026-07-02 21:50 · Closed the gap between the three Claude-Science reference shots (live workbench) and our build (static mock + bare live chat) by making the workbench interactive and wiring the live agent to it. (1) Live artifact surfacing: the SDK was dropping tool `state.input/output`; now it captures them, a pure `deriveArtifact` classifies file-writing tool calls (figure/script/report/table/notebook by extension), and `foldEvent`/`historyToThread` surface them as deduped `ArtifactCard` blocks openable in the live inspector — a real agent session now shows the files it produces instead of only text+tool rows. (2) Figure annotation authoring (shot 1): click a figure to drop a numbered pin, write a note, Send → forwarded to the agent as a follow-up prompt (coords clamped to bounds); multiple pins. (3) Artifact version switching + downloads (shot 1): per-version code/log/review; prev/next chevrons + pill switch content; Download writes a real Blob. (4) Notebook expression input (shot 2): the "Type an expression and press Enter" line is now a working input that appends a cell and dispatches to the agent's kernel. Verified in a real browser: v1↔v2 switches code and the Review check, annotation Send box appears, notebook input renders. typecheck/lint/35 JS tests (new: artifacts + foldEvent-artifact + figure-authoring)/build all green.

2026-07-02 11:10 · Bundled OpenCode as a Tauri sidecar (one-click install) with full isolation from any user-installed OpenCode. Rust `runtime` module spawns the bundled binary on a dedicated free port, with an app-private XDG config/data dir (`~/Library/Application Support/com.ai4s.workbench/runtime/`), `--cors "*"`, and kills it on exit; `configure_opencode` writes the key into that private config and restarts the sidecar. Frontend auto-starts + connects on launch (browser dev still uses a manual server). Sidecar binary is git-ignored + fetched by `scripts/dev/fetch-opencode.sh` (wired into CI). Verified end-to-end on the built .app: bundled opencode runs from inside the bundle on a random port (e.g. 54229) while a user's own `opencode serve` on 4096 keeps running untouched; global `~/.config/opencode` unchanged; app auto-creates sessions on its bundled runtime. dmg 44 MB. Rust 3 tests + JS 24 tests + typecheck/lint/build green.

2026-07-02 10:05 · Wired the in-app API key into OpenCode (closes the "usable" gap): Tauri Rust command `configure_opencode` merges provider key + model + baseURL into `~/.config/opencode/opencode.json` (3 Rust unit tests). Settings "Save" calls it (browser falls back with guidance). Verified end-to-end at the OpenCode level: writing that config flipped OpenCode's error from "Model not found" to "invalid x-api-key" — i.e. it now reads the config, resolves the model, and uses the key; a valid BYOK key completes a real turn. typecheck/lint/24 JS tests/3 Rust tests/build all green.

2026-07-02 09:35 · Switched agent runtime Hermes → OpenCode (pinned v1.17.13). Rewrote packages/sdk to the real OpenCode HTTP+SSE protocol (`OpenCodeClient`: POST /session, POST /session/:id/prompt_async, GET /event SSE; normalizes message.part.updated text/tool + session.idle/error). Installed the real OpenCode binary, ran `opencode serve`, and drove the actual app against it: verified GET /event 200, POST /session 200, live "OpenCode · ready", prompt sent (204), streamed events rendered. Found+fixed two real bugs while driving it: (a) unbound `fetch` → "Illegal invocation" in browser; (b) session.error message nested at error.data.message. Labeled the three mock sessions honestly as "Examples"; live agent work is the OpenCode-backed "New" session. Full agent turn still needs a configured model (`opencode auth login` or a provider key) — currently surfaces the real "Model not found" error. typecheck/lint/24 tests/build all green.

2026-07-02 09:00 · Added the Tauri 2 desktop shell (chosen architecture: bundle Hermes as a sidecar for one-click install). Installed Rust 1.96; scaffolded `apps/desktop/src-tauri` (Cargo/tauri.conf/main/lib + shell plugin), generated app icons, and built a real macOS bundle: `AI4S Workbench.app` + `AI4S Workbench_0.1.0_aarch64.dmg` (5.0 MB, shell only — Hermes not yet bundled). Verified the .app launches and runs the UI. Added GitHub Actions matrix (macOS aarch64/x86_64 + Windows x86_64) to produce .dmg and NSIS/.msi installers — Windows must be built in CI. Confirmed real Hermes uses an OpenAI-compatible API server (`hermes gateway`, 127.0.0.1:8642, Bearer API_SERVER_KEY, streams chat.completion.chunk + hermes.tool.progress) and owns provider keys; SDK protocol still to be re-pointed from the placeholder gateway to this.

2026-07-02 08:20 · Slice #2 (Hermes integration layer): `packages/sdk` `HermesClient` speaks the Hermes TUI Gateway JSON-RPC contract over WebSocket (session.create/prompt, streamed message.delta/tool.start/tool.complete/approval.request/session.done), transport injectable. Wired into the desktop: live `/live` session folds streamed events into thread blocks, real runtime status in the sidebar + Settings Gateway URL + Connect. Proven against a protocol-compatible mock gateway both in an integration test and live in the browser UI (connect → prompt → streamed tool + text + done). 24 vitest tests, typecheck/lint/build green. Note: a real agent turn still needs the actual Hermes binary + a model API key (kept empty per requirement).

2026-07-02 08:05 · Slice #1 (UI shell + static workspace) built browser-first: pnpm workspace + Vite/React/TS/Tailwind/Radix. Three-column Claude-Science-style UI (sessions sidebar / thread / contextual inspector) with warm paper theme, reproducing all three reference screenshots (figure+artifact, table+notebook, literature+PDF) from mock data. Settings has fillable, empty API-key fields with Hermes as runtime. Verified on Mac: typecheck ✓, eslint ✓, 19 vitest tests ✓, vite build ✓; screenshots confirmed against references.

2026-07-02 07:04 · Initialized project skeleton: monorepo directory tree (apps / packages / runtime / docs / examples / scripts), AGENTS.md + CLAUDE.md symlink, README, MIT LICENSE, .gitignore, and English PRD + TECHNICAL_DESIGN docs. No build tooling yet.
2026-07-14 07:07 · feat(ai4heor): shipped the first-party `heor-model-validation` alpha capability and converted the previously nominal independent-validation gate into a fail-closed native admission boundary. A local `heor/model-validation.json` now binds the exact analysis-plan, conceptual-model, uncertainty-plan, and budget-impact-plan bytes plus bounded hash-matched evidence under `heor/validation-evidence/`; the adapted China 2020, ISPOR-SMDM, AdViSHE, and TECH-VER contract requires 18 intended-use validation coverage items without creating a misleading quality score. Agent and developer evidence cannot be relabeled as independent review, the declared reviewer must differ from the developer and exactly match the app-owned approval actor, stale bytes or invalid evidence fail closed, and open blocker/major issues prevent approval. The natural-language-first review pane exposes validation status and a `$heor-model-validation` preparation task while keeping approval human-only; the unimplemented release gate now rejects explicit attempts instead of silently accepting them. 517/517 frontend, 127/127 Rust, 22/22 HEOR engine, and 20/20 artifact-contract tests, all eight HEOR skill validations, typecheck, lint, production build, strict clippy with the three pre-existing unrelated lint allowances, browser workflow QA (0 console errors), packaged-resource execution, and an 85 MB AI4HEOR x86_64 macOS debug DMG pass. The configured macOS/Windows/Linux CI matrix remains unrun remotely, and local reviewer identity/independence remains an assertion rather than cryptographic proof.
2026-07-14 08:50 · feat(ai4heor): implemented the industrialization rule for valuable but non-reusable assets by keeping HEORAgent MCP revision `19f5f0eea5764d7a2695c372f3ec8f3aa0f53dd8` quarantined and independently rewriting only its bounded multi-source search value as first-party `heor-evidence-search`. The Agent now drafts and portably validates exact `heor/evidence-search-request.json` bytes through natural language; the native app alone can execute a human-reviewed SHA-256 against fixed PubMed and ClinicalTrials.gov HTTPS endpoints. Unknown fields, arbitrary URLs/headers/credentials/paths, sensitive egress, redirects, oversized/non-JSON responses, changed requests, symlink output paths, and overwrites fail closed. Runs preserve request URLs, response hashes, counts, limitations, immutable candidate metadata, and an app-owned hash-linked network-authorization event; candidates never become included or appraised evidence automatically. A fresh upstream audit still found 12 dependency vulnerabilities including 4 high and the same remote HEAD, so no upstream Node code is bundled. Verification: 517 frontend tests, 144 Rust tests plus an explicit live two-source API pilot, 28 artifact-contract tests, 7 asset-admission tests, valid new Skill metadata, typecheck, lint, production build, strict clippy with three pre-existing unrelated lint allowances, browser QA with 0 console errors, and an 81 MB x86_64 macOS debug DMG whose resources include the first-party Skill and exclude HEORAgent. The configured macOS/Windows/Linux CI matrix remains unrun remotely; OpenAlex remains pending a separate API-key and consent boundary.
2026-07-14 15:10 · feat(ai4heor): closed the bounded event-rate uncertainty gap by upgrading the uncertainty contract to schema 0.3.0 and the local HEOR engine to 0.4.0. DSA/PSA can now target only exact positive `rate_per_year` fields inside an admitted analysis-schema-0.5.0 `constant_competing_rates` derivation, with the exact event extraction/assumption ID as its sole evidence basis; gamma, lognormal, and positive-uniform sampling are supported, while beta/Dirichlet, nonpositive values, generic derived-row targets, unmatched provenance, and out-of-scope transforms fail closed. Every draw first replaces all sampled event rates, then recomputes each affected complete matrix or schedule exactly once before normal Markov validation, so multiple competing rates in one row preserve event mass and allocation instead of independently applying probability conversion. The natural-language starters, first-party transition-rate/uncertainty/cohort/workbench Skills, portable validator, native audit, UI copy, and product/technical/ecosystem contracts now expose the same boundary. The adaptation is grounded in current NICE evidence-based distribution/correlation and comprehensive-PSA expectations, ISPOR-SMDM parameter-uncertainty guidance, and direct rate-to-probability propagation literature, without claiming general CTMCs, time-unit conversion, HR/RR/OR transforms, correlated sampling, or structural transformation scenarios. Verification: 525/525 frontend tests across 82 files, 178 Rust tests plus 1 explicit live-network test ignored, 40/40 Python engine tests, 53/53 artifact-contract tests, 7/7 asset-admission tests, all 5 changed Skills passed the official Skill Creator validator and capability metadata regeneration, typecheck, lint, production build, strict clippy with exactly three pre-existing unrelated lint allowances, rustfmt, browser workflow QA (0 console errors; 1 existing dev warning), and `git diff --check` pass. The x86_64 macOS debug DMG is 86,838,536 bytes with SHA-256 `70f50576d787cde4aa9c1f8c6b5caad92de2ff5a2636fe30bdf4ea621dbfe0ce`; a read-only mount audit found 13 changed packaged Python/Skill assets byte-identical, zero packaged `__pycache__`, and all 40 Python tests passed from the mounted app resources. Windows, Linux, remote macOS CI, signing, and real first-run packaging remain unrun in this local iteration and are not inferred from the configured matrices.

2026-07-31 · test(ai4heor): added a macOS-only native Tauri/WKWebView smoke path behind the explicit `desktop-e2e` Cargo feature. The isolated standard-library WebDriver client proves the Tauri bridge and clicks the real New task and Plugins & skills controls; three consecutive native runs passed. The exact-pinned MIT driver is absent from ordinary/release dependency trees, registered in the all-features source license inventory, and never shipped. Verification: contract 5/5, frontend 770/770, Rust 377 pass + 1 existing ignore, HEOR 188/188, developer contracts 385/385, release contracts 46/46, typecheck, lint, rustfmt, production build, and 41-source/445-file preflight. Task execution, HTML preview, permissions, import/export, and installed-DMG interaction remain separate P1-TEST-001b loops.

2026-07-31 · test(ai4heor): extended the macOS native Tauri/WKWebView E2E to the shared natural-language message queue without changing product behavior. A one-shot, bounded local-provider gate holds the first main reply while the real UI queues three prompts, moves the third up, removes the first, and then proves that only the retained prompts reach the model in the adjusted order before the queue clears. The fixture remains loopback-only, contains no real credential, and always releases the gate during cleanup. Verification: focused desktop contract 11/11 and fixture contract 2/2, direct native runs 3/3, root `test:e2e:desktop`, frontend 770/770, HEOR 188/188, Rust 377 pass + 1 existing network ignore and staging 2/2, developer contracts 391/391, release contracts 47/47, typecheck, lint, rustfmt, diff check, and 41-source/445-file preflight. Installed-DMG evidence, queued Skill/attachment payloads, Human-in-the-loop resume, and provider-error retry remain separate loops.

2026-07-31 · test(ai4heor): extended the native macOS flow through a real OpenCode `question` lifecycle and queue resume without changing product behavior. The pinned local provider emits one standard Anthropic `tool_use` response; AI4HEOR renders the Human question card, keeps a newly queued message out of provider traffic while the answer is pending, submits the selected answer into the original turn, and drains the queued message only after that turn completes. Default fixture replies remain unchanged, all traffic is loopback-only, and no real model or key is used. Verification: fixture contract 3/3, desktop contract 11/11, root native E2E plus two direct reruns (3/3 total), frontend 770/770, HEOR 188/188, Rust 377 pass + 1 existing network ignore and staging 2/2, developer contracts 391/391, release contracts 48/48, typecheck, lint, rustfmt, diff check, and 41-source/445-file preflight. Installed-DMG evidence, question rejection/multi-select, command permission, subagent requests, and provider-error recovery remain separate loops.

2026-07-31 · test(ai4heor): extended the native macOS OpenCode/Tauri/WKWebView flow through the default approval-mode boundary without changing product behavior. A one-shot local provider tool call requests the fixed `rm -f ai4heor-e2e-permission-sentinel` command only inside the disposable standalone-task workspace. The real UI shows the exact resource, keeps a newly queued prompt out of provider traffic while permission is pending, and after the researcher chooses Allow once proves a matching tool result reaches the resumed original turn before the queue drains. The initial native run exposed only a missing prompt candidate in the new test observer after the real permission card had rendered; the observer was corrected without weakening sequence or permission assertions, after which native execution passed 3/3 including the root rebuild. Verification: fixture contract 4/4, desktop contract 11/11, frontend 770/770, HEOR 188/188, Rust 377 pass + 1 existing network ignore and staging 2/2, developer contracts 391/391, release contracts 49/49, typecheck, lint, rustfmt, production build, diff check, and 41-source/445-file preflight. Permission rejection, durable Always allow scope, multiple/subagent asks, provider-error recovery, and installed-DMG full-task evidence remain separate loops.

2026-07-31 · test(ai4heor): completed the real macOS command-permission rejection branch without changing product behavior. A distinct loopback-only provider tool call asks to delete a sentinel created inside the disposable standalone-task workspace; the WebDriver verifies the exact command is shown, a follow-up remains queued while permission is pending, Reject leaves the sentinel bytes unchanged, and the next provider request is the queued user message with the rejected tool result retained only as prior conversation history. The first native run disproved the test's initial two-request assumption: pinned OpenCode rejection ends the tool turn, so the one subsequent model request is correctly the queued turn. The observer was corrected to assert that real protocol shape rather than altering queue or permission code. Verification: fixture contract 5/5, desktop contract green, native execution 3/3 including the root rebuild, frontend 770/770, HEOR 188/188, Rust 377 pass + 1 existing network ignore and staging 2/2, developer contracts 391/391, release contracts 50/50, typecheck, lint, rustfmt, production build, and 41-source/445-file preflight. Durable Always allow scope, multiple/subagent asks, provider-error recovery, import/export, and installed-DMG full-task evidence remain separate loops; no installer was generated.
